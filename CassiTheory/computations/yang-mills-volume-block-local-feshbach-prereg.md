# Anchored Block-Local Feshbach Family

## Status: Pre-registered—September 2026

## Abstract

This screen retains one fixed local plaquette mode rather than increasing the
retained source space with spatial volume. The vacuum and the first plaquette
in the frozen source ordering are retained on both the seven-link graph and the
open $3\times2\times2$ graph. The construction is anchored at the common
corner of the two source geometries, with no translated copies, fitted
weights, or volume-dependent enlargement. The test asks whether a genuinely
block-local retained family preserves a finite Feshbach bridge as the outer
source dimension grows from $4$ to $868$.

## 1. Frozen source spaces and anchored block

Use the exact source and recovered large-volume Hamiltonian fixed by
`computations/verify_yang_mills_exact_block_spectrum.py` and
`computations/verify_yang_mills_su2_larger_volume_hamiltonian.py`. The outer
source dimensions are $4$ and $868$. With $S$ the diagonal Haar overlap,
$e_0$ the normalized all-trivial vacuum coordinate and

$$
 u_p=S^{-1/2}W_pS^{-1/2}e_0,
$$

retain exactly

$$
 L_R=\operatorname{span}\{e_0,u_0\},
 \tag{VBL1}
$$

where $u_0$ is generated by `PLAQUETTES[0]` on each graph. On the large graph
this is the named `xy_z0_0` plaquette; the source ordering is bound to the
large-volume source. The small-graph first plaquette is retained by the same
ordinal rule. The arithmetic source columns are the vacuum and this one
plaquette vector. Orthonormalization uses singular-value threshold $10^{-10}
relative to the largest singular value. A rank other than $2$ is a frozen
source-family failure and classifies the run `INCONCLUSIVE`.

## 2. Projected Feshbach blocks

For each graph and coupling, form the normalized Hamiltonian
$\widehat H_{G,x}$, its lowest eigenpair $(E_0,\Omega)$ and measured outer
gap $\Delta_{G,x}$. Project (VBL1) into $\Omega^\perp$ and orthonormalize it
to obtain $V_P$. Define $V_Q$ as the orthonormal complement of
$\{\Omega\}\cup\operatorname{ran}V_P$. The blocks are

$$
 A=V_P^*(\widehat H_{G,x}-E_0)V_P,
 \quad
 B=V_P^*(\widehat H_{G,x}-E_0)V_Q,
 \quad
 D=V_Q^*(\widehat H_{G,x}-E_0)V_Q.
$$

For nonempty $\mathcal Q$, record

$$
 \alpha=\lambda_{\min}(A),
 \qquad
 \delta_Q=\lambda_{\min}(D),
 \qquad
 \beta=\|B\|_2,
$$

and evaluate the resolvent and self-energy bounds at
$\lambda_{\rm test}=\frac12\min(\Delta_{G,x},\delta_Q)$:

$$
 \|(D-\lambda I)^{-1}\|_2\le\frac1{\delta_Q-\lambda},
 \qquad
 \|B(D-\lambda I)^{-1}B^*\|_2
 \le\frac{\beta^2}{\delta_Q-\lambda}.
 \tag{VBL2}
$$

Use

$$
 \Phi(\lambda)=\alpha-\lambda-\frac{\beta^2}{\delta_Q-\lambda}
 \tag{VBL3}
$$

and, when $\Phi(0)>0$,

$$
 \gamma_{\rm Fesh}=\frac{\alpha+\delta_Q-
 \sqrt{(\alpha-\delta_Q)^2+4\beta^2}}2.
 \tag{VBL4}
$$

Require a positive direct Schur matrix at the test energy and a measured gap
at least (VBL4) within $10^{-10}$ absolute tolerance. Record the tail-floor
domain as undefined when the smallest discarded eigenvalue is the floor.

## 3. Statistic and decision tree

The primary statistic is the eight-row inventory of source and projected ranks,
$\alpha$, $\delta_Q$, $\beta$, resolvent and self-energy norms, positive-root
count, root-to-gap ratios, $\beta^2/(\alpha\delta_Q)$ and the anchored
large-to-small ratios of $\alpha$, $\delta_Q$ and $\beta$.

Any source, rank, finite-matrix, residual, inequality, Schur, root, domain or
summary failure is `INCONCLUSIVE`. If all controls pass and all eight rows
have a positive (VBL4) root, classify
`SUPPORTS_FINITE_VOLUME_ANCHORED_BLOCK_FAMILY`. If controls pass but one or
more roots are absent, classify `NO_POSITIVE_ANCHORED_BLOCK_FAMILY`.

The schedule is fixed at
$x\in\{1/64,1/16,1/4,1\}$. No graph, coupling, plaquette ordinal, rank
threshold or tolerance is added after inspection. The independent checker
reconstructs scalar bounds, roots, ranks, row schedule, grouped ratios and
source bindings without assembling either graph matrix.

A positive result concerns one anchored local block only. It does not establish
uniformity over translations, block shapes, spatial volume, lattice spacing,
continuum recovery or the Yang–Mills mass gap. Those are separate proof
obligations.

## 4. Evidence boundary and receipt

Write the primary receipt once to
`runs/yang_mills_volume_block_local_feshbach/verification.json` and the
independent receipt once to
`runs/yang_mills_volume_block_local_feshbach/verification-independent.json`.
Refuse to overwrite either receipt. Bind both receipts to this protocol, the
primary and independent sources, the adapted-family source and receipts, the
constant-sector volume-bridge source, the exact seven-link source, the
large-volume source, both large-volume protocols and the recovered
large-volume receipt.

## References

- `foundations/loop-to-bubble-projection-theorem.md` §9.38—the collective finite-family boundary.
- `computations/verify_yang_mills_volume_adapted_feshbach.py`—source assembly and Feshbach arithmetic.
- `computations/yang-mills-volume-feshbach-bridge-prereg.md`—constant-sector finite-volume bridge.
- `computations/verify_yang_mills_volume_feshbach_bridge.py`—exact finite-volume matrix helpers.
- `computations/verify_yang_mills_exact_block_spectrum.py`—seven-link exact source and plaquette ordering.
- `computations/verify_yang_mills_su2_larger_volume_hamiltonian.py`—open $3\times2\times2$ recovered source and plaquette ordering.
