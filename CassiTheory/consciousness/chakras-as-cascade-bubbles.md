# Chakras as Cascade Bubbles: The 13-Node Geometric Mapping

## Status: Hypothesized—August 2026 (the $P_\parallel = 2$ value is a geometric coordinate convention; canonical density conversion is rank-one relaxation, and the anatomical and transport mappings remain hypotheses)

## Abstract

The Cassi framework has one open phenomenological input in its consciousness mapping: the 13-band chakra count, documented as "phenomenologically anchored, cascade-derivation pending" (`consciousness/consciousness-from-phi.md` §2.4). This document supplies a geometric mapping for that input. The 13 chakras are modeled as localized Qi condensates at $\varphi$-spaced intervals along a conditional string-axis coordinate, by analogy with the cosmological bubble construction at step 285. The spinal column is a **Hypothesized physical correlate** of that coordinate in the human body, not an established instantiation. The count $13 = 26/2$ is the arithmetic of the adopted 2-rung spacing across the 26-step human cascade span (steps 142–168), not a value selected by the canonical density conversion. The $P_\parallel = 2$ spacing and any associated $\pi$- or $2\pi$-per-rung phase language are Hypothesized geometric conventions; the canonical density relaxation, amplitude phase, and spatial-current interpretations remain distinct. The mapping makes zero-parameter predictions for inter-chakra spacing ratios and chakra boundary geometry while leaving the phenomenological interpretation explicit.

---

## 1. The Open Gap

The consciousness mapping in `consciousness/consciousness-from-phi.md` §2.4 states:

> Between steps 142 (cellular) and 168 (human), there exist intermediate scales where the local ratio $r(\mathbf{x})$ naturally stabilizes at Fibonacci convergents of $\varphi$. These are **field nodes**—local $\varphi$-fixed points where the conversion dynamics temporarily stall.
>
> The number of such nodes is not arbitrary. The 26 $\varphi$-steps between cell and self admit approximately 12 internal nodes spaced at $\sim 2.2$ $\varphi$-steps each (including boundaries, 13 total positions). These correspond to the traditional chakra locations, but **the specific number, spacing, and Fibonacci width allocation are a geometric mapping to be specified and tested; the canonical density relaxation does not derive them.**

That is the gap. The number 13 is phenomenologically anchored—observed in practice, noted in tradition—but has no cascade derivation. This document supplies a **Hypothesized geometric mapping** by connecting the string axis (`foundations/why-three-dimensions.md` §2.2), the bubble condensation field (`foundations/bubble-edge-geometry.md` §2.3), and the two-component Yang–Yin doublet geometry; it does not identify the canonical density conversion with an SO(2) rotation.

---

## 2. The String Axis in the Human Body

### 2.1 The Spiral's Frenet-Serret Frame

For a nondegenerate curve embedded in $d=3$, the Frenet-Serret construction gives an exact conditional three-axis algebra: three orthogonal directions—tangent (forward along the cascade, the string axis), normal (inward, the Yang axis), and binormal (sideways, the Yin axis). This algebra follows after the embedding is assumed; it does not derive or select physical $d=3$. Identifying the physical space with $d=3$ and mapping these axes to string/Yang/Yin anatomy are **Hypothesized geometric mappings**. See `foundations/why-three-dimensions.md` §2.

Within that chosen embedding, the string axis is the geometric direction—"the direction along which the bubble is bounded between its two adjacent steps" (`foundations/why-three-dimensions.md` §3.2)—rather than a separately derived physical dimension. It is the cascade coordinate assigned to the third member of the conditional triad; its physical status remains part of the Hypothesized embedding.

### 2.2 The Spine as the String Axis

Under the **Hypothesized physical embedding** used for this mapping, the string axis has a candidate physical correlate: the **spinal column**. Four structural facts align:

1. **Bilateral symmetry.** The body's left-right symmetry plane is perpendicular to the spine—exactly what the geometry demands if the Yang-Yin doublet plane (the two perpendicular fluid axes) is orthogonal to the string axis.

2. **Discrete repeated units.** The vertebral column consists of 33 vertebrae (7 cervical, 12 thoracic, 5 lumbar, 5 sacral, 4 coccygeal)—discrete structural units along the spine, analogous to the rungs of the cascade ladder.

3. **Coherence transport.** The central nervous system runs along the spinal canal. In Cassi terms, this makes the spine a candidate spatial path for Qi currents, not evidence of inter-rung transport. A named spatial current or projection does not by itself establish transport between cascade rungs or scales; that interpretation requires a separate constitutive map. Within the Hypothesized body map, the brain is assigned to the cranial end and the crown label is placed at step 166, with the physical body-map boundary assigned two rungs beyond at step 168.

4. **Vertical orientation.** In the standing human posture, the spine is approximately vertical. Treating that orientation as aligned with the cosmological cascade direction is a Hypothesized correspondence, not an anatomical or dynamical derivation; bipedalism does not establish that the body's string axis is the universe's.

The identification is a **Hypothesized structural mapping**: the spine is a candidate human instantiation of the string/cascade axis, just as the Cassi bubble's along-string direction is a conditional geometric coordinate at the cosmological scale. Reusing the condensation-field functional form across scales requires the rung-dependent parameter/unit re-normalization stated in `foundations/bubble-lattice-fabric.md` §2; it does not establish physical identity, human Qi-gate transmission, or inter-rung transport. Applying the map to the body remains a body-map hypothesis requiring anatomical and constitutive validation. The body-map is developed in §2–§6; the psychology guide presents it for a clinical audience.

---

## 3. The Human Cascade Span

### 3.1 The 26-Step Window

From the dimensionful cascade (`foundations/dimensionful-cascade.md` §3):

| Step $n$ | Scale (meters) | Physical meaning |
|----------|---------------|-------------------|
| 142 | $7.7 \times 10^{-6}$ | Cellular scale ($\sim 8~\mu\text{m}$) |
| 168 | $1.7$ | Human scale ($\sim 1.7~\text{m}$) |

The cascade relation $\ell_n = \ell_{\text{Pl}} \times \varphi^n$ gives:

$$\frac{\ell_{168}}{\ell_{142}} = \varphi^{26} \approx 2.7 \times 10^5$$

The human body—from its smallest organized unit (the cell) to its full spatial extent—spans exactly 26 $\varphi$-multiplications. The same exponent $26$ appears in the electron-to-electroweak mass ratio $m_e/v_0 \approx \varphi^{-26}$ (`consciousness/consciousness-from-phi.md` §1.2).

### 3.2 The Body as a Cascade Ladder

The 26 $\varphi$-steps are not an abstract counting exercise. They are physically realized in the body's structural hierarchy:

$$\text{cell (n=142)} \to \text{tissue} \to \text{organ} \to \text{organ system} \to \text{body (n=168)}$$

Each $\varphi$-step is a scale transition where a new organizational level emerges. The body is a living cascade ladder—a 26-rung structure spanning from the microscopic to the macroscopic—with the spine proposed as its backbone axis within the Hypothesized body map.

---

## 4. Bubbles on a String

### 4.1 The Condensation Field

Within the chosen conditional $\mathbb{R}^3$ embedding, the bubble-edge geometry (`foundations/bubble-edge-geometry.md` §2.3) gives the three-coordinate condensation field:

$$\boxed{B_n(x, y, z) = \cos(\alpha_n x)\cos(\beta_n y)\cos(\gamma_n z), \qquad \gamma_n = \frac{2\pi}{P_\parallel^{(n)}} = \frac{2\pi}{p_\parallel(n)\ell_n}}$$

where:
- $\alpha_n = 2\pi/\Lambda_Y^{(n)}$, $\beta_n = 2\pi/\Lambda_I^{(n)} = \varphi\alpha_n$ (Yang and Yin wavelengths in the doublet plane, under the rung-dependent coordinate assignment)
- $P_\parallel^{(n)}=p_\parallel(n)\ell_n$ is the **along-string bubble period** assigned at rung $n$; $p_\parallel(n)$ is the dimensionless number of rung intervals between adjacent bubble maxima
- Bubbles are labeled where $B_n > \theta_{\text{cond}}$ (conditional geometric assignment; high proxy)
- Voids are labeled where $B_n < -\theta_{\text{cond}}$ (conditional geometric assignment; low proxy)

At the cosmological scale, the axial period is assigned rung by rung: a bubble at step 285 is evaluated with the conditional field $B_{285}$ and its neighboring step 286 with $B_{286}$, using the corresponding $P_\parallel^{(n)}$ (the cosmological reading takes $p_\parallel(n)=1$). The voids between them—where the axial factor changes sign and the geometric proxy reaches its low branch—are cascade-step gaps under this coordinate assignment. The rung-dependent coordinate period does not by itself specify a density-plane phase advance, a density transport law, or an inter-rung current.

### 4.2 Scaling to the Human Body

Under the conditional coordinate assignment with the required parameter/unit re-normalization, the rung-indexed condensation field $B_n(x,y,z)$ retains this functional form while $\Lambda_Y^{(n)}$, $\Lambda_I^{(n)}$, and $\gamma_n$ vary with $n$:

- $\Lambda_Y^{(n)}$, $\Lambda_I^{(n)}$ are assigned from the conditional wake-wave construction at steps 142–168 (the proposed internal Yang-Yin plane)
- $p_\parallel(n)$ is the dimensionless along-string period assignment; the human-scale full-period convention $p_\parallel(n)=2$ is Hypothesized below, with $P_\parallel^{(n)}=p_\parallel(n)\ell_n$

The question is how the rung-dependent $p_\parallel(n)$ and the associated physical $P_\parallel^{(n)}$ should be assigned in the human window.

---

## 5. The Hypothesized $P_\parallel = 2$ Geometric Convention

### 5.1 Canonical density relaxation and the separate coordinate

The canonical conversion acts on the real density pair $(E_Y,E_I)$ by equal-and-opposite exchange. With $\kappa = \lambda(1-q)$, its conversion block is

$$
\left.\partial_t
\begin{pmatrix}E_Y\\E_I\end{pmatrix}\right|_{\mathrm{conv}}
=
\kappa
\begin{pmatrix}-1 & \varphi\\ 1 & -\varphi\end{pmatrix}
\begin{pmatrix}E_Y\\E_I\end{pmatrix}.
$$

This matrix has rank one, conserves $\rho = E_Y + E_I$, and has eigenvalues $0$ and $-\kappa(1+\varphi) = -\lambda(1-q)(1+\varphi)$. It relaxes the imbalance $\varepsilon = E_Y - \varphi E_I$; it generally changes $E_Y^2 + E_I^2$ and is not a norm-preserving $SO(2)$ rotation. The density-plane angle is

$$
\theta_d = \operatorname{atan2}(E_I,E_Y).
$$

Here $q$ is the canonical solver's gate variable in the conversion block; it is not silently identified with the unnormalized geometric proxy $M_{\text{proxy}}\equiv q_{\text{proxy}}=(1+B_n)^2/2$, whose geometric range is $[0,2]$ when $B_n\in[-1,1]$. Mapping $M_{\text{proxy}}$ to canonical $q$, and any resulting $\rho$, $G_{\text{eff}}$, or anatomical interpretation, requires a separately measured bounded constitutive relation.

The density-plane angle can drift as this state relaxes, but that drift is not a compact phase clock and does not supply a fixed increment per cascade rung.

The amplitude variables carry a separate phase notation. When $E_Y = \Psi_0^2$ and $E_I = \Psi_1^2$,

$$
\theta_\Psi = \operatorname{atan2}(\Psi_1,\Psi_0),\qquad
\Theta_S = \operatorname{atan2}(2\Psi_0\Psi_1,E_Y-E_I)
= 2\theta_\Psi \pmod{2\pi}.
$$

Thus $\theta_d$, $\theta_\Psi$, and $\Theta_S$ are distinct coordinates; none is silently identified with another. The Yang-dominant and Yin-dominant labels used below are phenomenological directional aliases for sectors in a projection, not additional Cassi fluids.

Under the geometric mapping adopted in this paper, one Yang-dominant rung is the expansive, driving sector—the "out-breath" of the Qi condensate—and one Yin-dominant rung is the contractive, receptive sector—the "in-breath." Grouping those adjacent labels into a two-rung bubble is a **Hypothesized geometric convention**. It does not assert that the canonical conversion rotates the density pair or that the PDE advances either phase by $\pi$ per rung.

### 5.2 Applying the $P_\parallel$ convention in cascade rungs

At the cosmological scale, the along-string period $P_\parallel$ is the cascade step spacing—the distance between adjacent rungs. But the step spacing isn't a single number: $\ell_{n+1} - \ell_n = (\varphi - 1)\ell_n$, which grows exponentially. The *cascade index* spacing—the number of integer $n$ steps between bubble maxima—is the invariant.

At the human scale, this paper adopts the following geometric coordinate convention:

$$\boxed{P_\parallel = 2\ \text{cascade rungs}}$$

The two-rung period groups adjacent Yang/Yin directional labels into one modeled condensate and supplies the node-placement arithmetic below. It is **Hypothesized**, not a value selected by the canonical PDE. Any $\pi$- or $2\pi$-per-rung phase statement is bookkeeping inside this separate coordinate convention; it is not a canonical density or amplitude-phase law, and a separate constitutive map would be required before interpreting a spatial projection as transport between rungs.

The resulting 13-node placement is therefore a testable geometric mapping, not a canonical-rotation derivation.

---

## 6. The Count: 13

### 6.1 Direct Division and the Crown Offset

The human cascade span is 26 rungs (steps 142–168). We apply the **Hypothesized** geometric convention $P_\parallel = 2$ to this span. The full interval from step 142 to step 168 contains exactly 13 adopted two-rung periods. For node bookkeeping, the terminal maximum at step 168 is reserved as the body boundary rather than counted as a chakra; the retained maxima from steps 142 through 166 are the 13 chakra nodes, with 12 node-to-node intervals among them and one final boundary period from 166 to 168. This convention preserves $26=13\times2$ without treating the boundary maximum as a 14th chakra:

For the adopted along-string coordinate, the rung-indexed condensation field is $B_n(0,0,z) = \cos(2\pi z / p_\parallel(n))$. Within the human coordinate convention $p_\parallel(n)=2$, maxima occur at 2-rung intervals: steps $142,144,\ldots,166,168$. The first 13 maxima (steps 142 through 166) are retained as chakra nodes; the terminal maximum at step 168 is the reserved body-boundary maximum:

$$n_k = 142 + 2k, \qquad k = 0, 1, 2, \ldots, 12$$

$$\boxed{N_{\text{chakras}} = 13,\qquad n \in \{142, 144, 146, 148, 150, 152, 154, 156, 158, 160, 162, 164, 166\}}$$

**Where is the terminal maximum?** The next maximum after step 166 is at step 168, but step 168 is the *body boundary*, not a chakra. The body extends exactly 2 rungs—one adopted two-rung geometric period—beyond the crown chakra. This 2-rung offset is a structural hypothesis:

- **The crown chakra (step 166) sits at the top of the spinal column**—the brainstem, the highest point of the central nervous system.
- **The physical body continues 2 rungs to step 168**—the cranium, skull, and scalp. These are the "subtle body" extending beyond the last Qi node.
- **The gap between crown and vertex is one adopted two-rung geometric period.** Traditional descriptions of the crown chakra as a "thousand-petaled lotus" extending *above* the physical head are consistent with this offset: the Qi field at step 166 radiates upward through the 2-rung cranium to the body boundary at step 168.

Under this geometric allocation, the full 26-rung span is 13 adopted periods: 12 node-to-node intervals connect the 13 retained chakra maxima, followed by one final boundary period from the crown at step 166 to the body boundary at step 168. The body terminates at the reserved maximum rather than adding a 14th chakra.

### 6.2 Fibonacci Grounding

The number 13 is $F_7$, the 7th Fibonacci number. The human span is $26 = 2 \times F_7$. This follows from the Fibonacci structure of the cascade itself.

The Fibonacci recurrence $\varphi^n = \varphi^{n-1} + \varphi^{n-2}$ partitions every cascade span into Fibonacci-structured sub-channels (`foundations/three-generations.md`). The human body occupies a Fibonacci-structured window of the cascade:

- $F_7 = 13$ chakras
- $2 \times F_7 = 26$ cascade rungs (the factor 2 is the two-component doublet bookkeeping in the adopted geometric map, not an eigenmode or rotation of the canonical conversion)
- $F_8 = 21$—the next Fibonacci number—would overshoot the human scale (step $168 - 2 \times 21 = 126$, which is sub-atomic); the body occupies the largest Fibonacci-structured sub-span that fits within its cascade window

The factor 2 is the dimension of the two-component doublet used by the geometric map. The count 13 is the number of retained maxima after reserving the terminal maximum as a boundary; the 26-rung span still consists of 13 adopted two-rung periods, with the final period serving as the crown-to-boundary offset.

### 6.3 Comparison with Phenomenology

The traditional chakra system recognizes 7 primary chakras. The Cassi count of 13 includes 6 additional nodes at intermediate cascade steps—secondary chakras that are structurally identical to the primary 7 but span narrower $\varphi$-ranges (since the physical step spacing grows with $n$). This reconciles the traditional count with the cascade derivation: the 7 primary chakras are the most prominent (widest-spaced) nodes; the 6 secondary nodes are narrower and correspond to less commonly recognized energy centers. All 13 share the same bubble structure, differing only in their cascade position and physical extent.

---

## 7. Chakra Geometry Inherits Bubble-Edge Structure

### 7.1 Shape and Anisotropy

Each chakra, as a cascade bubble, inherits the full geometry of the condensation field boundary:

**Conditional triaxial spheroid.** Within the chosen $\mathbb{R}^3$ morphology map, the chakra's cross-section in the Yang-Yin plane (perpendicular to the spine) is elliptical with axis ratio:

$$\frac{a_X}{a_Y} = \frac{\beta}{\alpha} = \varphi \approx 1.618$$

The chakra extends further along the Yang axis than the Yin axis—it is not a symmetric disc but a $\varphi$-elliptical region of high condensation-proxy value. Along the string axis (spine), the chakra is bounded by the cascade step separation: a finite longitudinal extent of approximately $\Delta z \approx 2(\varphi-1)\ell_n$.

**Conditional edge steepness anisotropy.** Crossing a selected chakra-proxy boundary along the Yin direction (toward the “void” between chakras) is steeper than crossing toward a neighboring chakra. At a common boundary value $C=\theta_{\text{cond}}$, the directional ratio from `foundations/bubble-edge-geometry.md` §2.2 is

$$\frac{|\nabla C|_{\text{axial}}}{|\nabla C|_{\text{diag}}}
=\frac{\sqrt{1+\varphi^2}}{2}\sqrt{\frac{1+\theta_{\text{cond}}}{\theta_{\text{cond}}}}.$$

It is $1.7072$ only at the phenomenologically selected $\theta_{\text{cond}}=0.45$ and varies with the boundary level. The fixed-step PDE diagnostic does not retain a $C=0.45$ edge. Any anatomical interpretation requires independent identification of the boundary and a measured proxy-to-observable map.

### 7.2 Condensation Proxy Profile

The geometric proxy across a chakra follows the condensation field:

$$q_{\text{proxy}}(\mathbf{x}) \equiv M_{\text{proxy}}(\mathbf{x}) = \frac{(1 + B_n(\mathbf{x}))^2}{2}$$

At the chakra center ($B_n \to 1$): $M_{\text{proxy}} \to 2$, the unnormalized geometric maximum; this is not a canonical $q$ or a normalized coherence fraction.
At the chakra edge ($B_n = \theta_{\text{cond}}$): $M_{\text{proxy}} = (1+\theta_{\text{cond}})^2/2$; for $\theta_{\text{cond}}=0.45$, $M_{\text{proxy}}=1.05125$.
Between chakras ($B_n \to -1$): $M_{\text{proxy}} \to 0$, the unnormalized geometric minimum.

The geometric boundary threshold $\theta_{\text{cond}}$ at the chakra scale is determined by the same conversion-diffusion balance as at the cosmological scale (`foundations/bubble-edge-geometry.md` §1.2), with $D_{\text{eff}}$ measured at the human-scale Qi dynamics. The functional form of this unnormalized proxy is Derived conditional on the stated coordinate field; the numerical value of $\theta_{\text{cond}}$ at the chakra scale requires PDE measurement. The proxy must not be used directly as the canonical solver gate $q$, $\rho$, or $G_{\text{eff}}$; any anatomical interpretation or bounded $M_{\text{proxy}}\!\to q$ map requires separate measurement or a constitutive relation.

### 7.3 Effective Gravity

The Qi-gravity coupling $\xi = \varphi^6 \approx 17.944$ is a canonical-field parameter that may be evaluated at the chakra scale only after a proxy-to-canonical constitutive identification; it does not follow from $q_{\text{proxy}}$ alone:

$$G_{\text{eff}}(\mathbf{x}) = \frac{\pi}{\rho(\mathbf{x})}\bigl(1 + (\varphi^{6}-1)q(\mathbf{x})\bigr)$$

Under a separately measured bounded constitutive map $M_{\text{proxy}}\!\to q$, together with a separately established density identification, the canonical expression above may be evaluated at a chakra-scale coordinate. The geometric values $M_{\text{proxy}}=2$ at a center, $1.05125$ at the illustrative edge, and $0$ in a void do not determine canonical $q$ values. Therefore this document makes no center/edge/void $G_{\text{eff}}$ prediction and does not claim enhanced effective gravity from the unnormalized proxy alone.

---

## 8. Chakra Spacing: $\varphi$-Scaled, Not Equal

### 8.1 The Physical Distance Between Chakras

Because the cascade rungs are geometrically spaced ($\ell_n \propto \varphi^n$), the physical distance between adjacent chakras is not constant. It grows by $\varphi^2$ per step:

$$\Delta z_k = z_{k+1} - z_k \approx 2(\varphi-1)\,\ell_{142+2k}$$

The ratio of adjacent spacings:

$$\frac{\Delta z_{k+1}}{\Delta z_k} = \frac{\ell_{142+2(k+1)}}{\ell_{142+2k}} = \varphi^2 \approx 2.618$$

### 8.2 Observable Consequence

Lower chakras (near the base of the spine, lower $n$) are **closer together**—the cascade rungs are denser at smaller scales. Upper chakras (near the crown, higher $n$) are **farther apart**—the rungs are sparser at larger scales.

Traditional systems note this asymmetry: the root, sacral, and solar plexus chakras (lower body) are described as denser, more tightly coupled; the throat, third eye, and crown (upper body) are described as more expansive, with wider energetic reach. The $\varphi^2$ spacing ratio makes this quantitative: each successive inter-chakra gap is approximately $2.618\times$ larger than the previous one.

### 8.3 Spinal Mapping

The physical location of chakra $k$ (0-indexed from the root) along the spine, measured from the coccyx, is proportional to:

$$z_k \propto \varphi^{2k}$$

For a total spinal length $L \approx 0.7$ m (adult human), the 13 nodes at 2-rung spacing span 24 rungs of the 26-rung window (two rungs are the endpoints), giving a physical range of $\varphi^{24} \approx 1.04 \times 10^5$. The normalized positions are:

$$\frac{z_k}{L} \approx \frac{\varphi^{2k} - 1}{\varphi^{24} - 1}$$

This predicts a specific, measurable spacing pattern along the spine—a zero-parameter geometric prediction.

---

## 9. The Microcascade Mirror

### 9.1 Structural Identity Across Scales

Within the chosen conditional $\mathbb{R}^3$ embedding, the chakra system is a microcascade mirror of the cosmological bubble lattice. The same structures appear at both scales:

| Property | Cosmological bubble (step 285) | Chakra (steps 142–166) |
|----------|-------------------------------|------------------------|
| Condensation field | $B_n(x,y,z)$ with $p_\parallel(n) \sim 1$ as a rung-dependent geometric coordinate | Same conditional $B_n(x,y,z)$ with $p_\parallel(n) = 2$ as a Hypothesized human-scale coordinate convention |
| Shape | Conditional triaxial spheroid, axis ratio $\varphi$ | Same conditional morphology |
| Edge steepness | Directional proxy ratio depends on $\theta_{\text{cond}}$; $1.7072$ at the selected value $0.45$, with no surviving $C=0.45$ fixed-step PDE edge | Same conditional geometric benchmark; anatomical map unresolved |
| $M_{\text{proxy}}$ profile | $M_{\text{proxy}} = (1+B_n)^2/2$ | Same unnormalized geometric proxy |
| $G_{\text{eff}}$ variation | $\sim 19\times$ center-to-void | Not inferred from $M_{\text{proxy}}$; only conditional on a separately measured bounded $M_{\text{proxy}}\!\to q$ and $\rho$ constitutive map |
| Lattice structure | $\varphi$-spaced along string | $\varphi^2$-spaced along spine |
| Number | 1 per observable universe | 13 per human body |

The scale factor between them is:

$$\frac{\ell_{285}}{\ell_{155}} \approx \varphi^{130} \approx 10^{27}$$

The chakra system is modeled as a **proposed geometric analogy** to the bubble lattice, viewed 130 cascade rungs down. Within the chosen coordinate map, the condensation field and edge formulas are reused as a conditional correspondence; identifying a human Qi gate or de-resonance principle with the cosmological mechanisms remains **Hypothesized** and requires separate constitutive and anatomical validation. The $P_\parallel = 2$ value remains a human-scale coordinate hypothesis.

The universal statement of this scale-covariance and the bubble lattice geometry is in `foundations/bubble-lattice-fabric.md`.

### 9.2 The Infinite Ladder

The microcascade mirror (`foundations/microcascade-mirror.md`) establishes that the cascade extends infinitely in both directions. The human chakra system occupies one octave of this infinite ladder—the 26-rung window from cellular to organism. Adjacent octaves exist above (social/planetary scales at steps 169+) and below (sub-cellular/molecular at steps 141−).

The "infinite depth of mind" described in the consciousness framework (`consciousness/consciousness-from-phi.md` §2.3, M3) is not a separate claim—it follows directly from the cascade having no floor. The chakras are the accessible nodes of that infinite ladder at the human scale. Deeper meditation accesses lower $n$ (sub-cellular, molecular, ultimately microcascade $n < 0$); higher states access higher $n$ (social, planetary, ultimately megacascade $n > 292$).

### 9.3 Visible Light and the Chakra Colors

The traditional chakra system associates each primary chakra with a color: red (root), orange (sacral), yellow (solar plexus), green (heart), blue (throat), indigo (third eye), violet (crown). These seven colors span the visible spectrum from $\sim 650$ nm (red) to $\sim 400$ nm (violet)—a wavelength ratio of $650/400 \approx 1.625$, which is within $0.4\%$ of $\varphi \approx 1.618$. The visible spectrum is **one $\varphi$-step wide**.

#### Cascade Position of Visible Light

From the dimensionful cascade (`foundations/dimensionful-cascade.md` §3), visible light (center $\sim 550$ nm) corresponds to:

$$n_{\text{visible}} = \frac{\ln(550 \times 10^{-9} / \ell_{\text{Pl}})}{\ln\varphi} \approx 136.5$$

The visible octave spans steps $\sim 135.9$ (violet, $400$ nm) to $\sim 136.9$ (red, $650$ nm)—almost exactly $\Delta n = 1$. This is six rungs *below* the root chakra (step 142).

#### The Seven Sub-Rungs

One $\varphi$-step decomposes into Fibonacci-structured sub-rungs via the same recurrence that gives three fermion generations (`foundations/three-generations.md`). Seven equally log-spaced sub-rungs within step 136–137 yield:

| Sub-rung | $n$ | $\lambda$ (nm) | Traditional color | Nearest chakra |
|----------|-----|----------------|-------------------|----------------|
| 1 | 136.00 | 427 | Violet | Crown (n=166) |
| 2 | 136.17 | 463 | Blue | Third eye (n=162) |
| 3 | 136.33 | 502 | Green | Throat (n=158) |
| 4 | 136.50 | 544 | Yellow-green | Heart (n=154) |
| 5 | 136.67 | 589 | Orange | Solar plexus (n=150) |
| 6 | 136.83 | 638 | Red-orange | Sacral (n=146) |
| 7 | 137.00 | 691 | Deep red | Root (n=142) |

The sub-rung count of 7 maps directly to the 7 primary chakras—the same Fibonacci partitioning that gives 13 total nodes (7 primary + 6 secondary) also gives 7 sub-rungs within the one-$\varphi$-step visible octave.

#### Mechanism: Microcascade Depth Determines Color

Each chakra at cascade step $n_k$ contains an internal microcascade—the cascade ladder extending *downward* from the chakra's own rung. The visible-light octave (steps 136–137) is reached by descending $d_k = n_k - 136$ rungs into the chakra's microcascade:

| Primary chakra | $n_k$ | Depth $d_k$ to visible light |
|---------------|-------|------------------------------|
| Root | 142 | 6 |
| Sacral | 146 | 10 |
| Solar plexus | 150 | 14 |
| Heart | 154 | 18 |
| Throat | 158 | 22 |
| Third eye | 162 | 26 |
| Crown | 166 | 30 |

The depths differ by 4 rungs between adjacent primary chakras—exactly *two* adopted 2-rung geometric periods per primary chakra spacing (the primary chakras are at every 4th rung, with secondary nodes at the 2-rung midpoints).
The 4-rung differences in $n_k$ are ordinal human-cascade labels and microcascade depths; they are not direct wavelength exponents. The wavelength table assigns those seven ordinal primary labels to seven visible sub-rungs spanning one visible $\varphi$-step.

Within each chakra's microcascade, the specific sub-rung of the visible octave that *resonates* with the chakra is determined by the Fibonacci phase at that depth. The root chakra (shallowest microcascade, $d = 6$) is proposed to resonate with the longest-wavelength sub-rung (deep red, $n \approx 137.0$), and the crown chakra (deepest microcascade, $d = 30$) with the shortest-wavelength sub-rung (violet, $n \approx 136.0$). This red-at-root, violet-at-crown ordering is a proposed qualitative consequence of the microcascade-depth picture, pending the Fibonacci-resonance scan: deeper descent may select shorter wavelengths if the resonant sub-rung shifts upward in the target octave with increasing depth.

#### The Sub-Rung Resonance Condition (Open Derivation)

The specific sub-rung that a given chakra resonates with is determined by the Fibonacci phase of its microcascade descent. A simple mod-$\varphi$ relation does not yield the traditional root=red, crown=violet mapping—the resonance condition depends on the full Fibonacci convergent structure of $\varphi$ at each depth, not on a linear congruence. The derivation of the exact sub-rung assignment for all 13 chakras requires a computational scan of the Fibonacci resonance structure at depths $d_k = 6, 8, 10, \ldots, 30$ (§12, item 6).

**What is Derived:** The visible spectrum spans exactly $\Delta n \approx 1$ cascade rung (within $0.4\%$ of $\varphi$). The 7-sub-rung decomposition follows from Fibonacci partitioning of that one-$\varphi$-step. The depths $d_k = n_k - 136$ at which each chakra's microcascade reaches the visible octave are fixed by the cascade positions.

**What is Hypothesized:** The mapping of specific sub-rung wavelengths to specific chakras. The traditional root=red, crown=violet ordering is *consistent* with the microcascade depth model (shallower descent → longer wavelength; deeper descent → shorter wavelength) but the exact sub-rung assignment per chakra has not been derived.

#### Testable Prediction

**Prediction CH6:** Each chakra radiates at a characteristic wavelength given by the sub-rung resonance condition. The predicted wavelengths for the 7 primary chakras are given in the table above. The secondary chakras (at steps 144, 148, 152, 156, 160, 164) should radiate at intermediate sub-rung positions between the primary colors.

**Test:** Measure the spectral emission peak of each chakra region using hyperspectral imaging or photomultiplier-based biophoton detection during meditative states. The displayed table spans six adjacent intervals from root (691 nm) to crown (427 nm), so its root-to-crown ratio is $691/427 \approx \varphi$ and its successive wavelength ratio in the root-to-crown direction is $\varphi^{1/6} \approx 1.084$ (up to table rounding). The 4-rung differences in the primary $n_k$ labels are ordinal cascade spacing, not wavelength exponents; no $\varphi^{4/6}$ or $\varphi^{2/3}$ ratio follows without a separate resonance/constitutive map.

**Current status:** Not yet tested. Biophoton emission from the human body is documented in the 200–800 nm range; chakra-specific spectral peaks have been reported anecdotally but not measured under controlled conditions with the table's Hypothesized $\varphi^{1/6}$ spacing.

**Epistemic:** Hypothesized. The one-$\varphi$-step width of the visible spectrum and the 7-sub-rung decomposition are Derived from $\varphi$ and the cascade. The specific color-to-chakra assignment via the mod-$\varphi$ resonance condition is Hypothesized and requires the Fibonacci-resonance scan (§12, item 6).


---

## 10. Testable Predictions

### 10.1 Inter-Chakra Spacing Ratio

**Prediction CH1:** The ratio of physical distances between adjacent chakras along the spine is $\varphi^2 \approx 2.618$.

**Test:** Measure chakra locations via established protocols (acupuncture point coordinates, thermal imaging peaks, electrical conductivity maxima along the spine) and compute the ratio of adjacent inter-chakra distances. Conditional on the proposed chakra-location mapping, the test uses no fitted parameters or normalization; the mapping remains Hypothesized and untested.

**Current status:** Not yet tested. Existing anatomical atlases of chakra/acupuncture point locations can provide the first-pass data. The prediction is falsifiable: if the measured ratios consistently deviate from $\varphi^2$ beyond measurement uncertainty, the derivation is wrong.

### 10.2 Qi Density Gradient Anisotropy

**Prediction CH2:** If an independently identified chakra boundary corresponds to the geometric-proxy level $\theta_{\text{cond}}=0.45$, its axial-to-diagonal directional slope ratio is $1.7072$. More generally,

$$\frac{|\nabla C|_{\text{axial}}}{|\nabla C|_{\text{diag}}}
=\frac{\sqrt{1+\varphi^2}}{2}\sqrt{\frac{1+\theta_{\text{cond}}}{\theta_{\text{cond}}}}.$$

**Test:** Map a declared physiological observable—such as skin conductance, temperature, or infrared emission—on a 2D grid across the candidate region. Identify the physical boundary without fitting it to the target ratio, estimate its effective $\theta_{\text{cond}}$, and compare axial and diagonal slopes through the same proxy map.

**Current status:** Not yet tested. The anatomical boundary and proxy map are unresolved, and the fixed-step PDE endpoint has no $C=0.45$ edge.

### 10.3 Fibonacci-Spaced Secondary Nodes

**Prediction CH3:** In addition to the 7 primary chakras of the traditional system, 6 secondary nodes exist at the intermediate cascade steps ($n = 144, 148, 152, 156, 160, 164$). These secondary chakras are structurally identical to the primary ones (bubble geometry, $\varphi$-spacing, edge anisotropy) but span a narrower physical range because they sit closer to the lower-$n$ (denser) end of the cascade.

**Test:** Search for physiological or subjective correlates at the predicted intermediate locations along the spine. The prediction identifies specific anatomical positions: the 6 secondary nodes lie exactly midway (in cascade-index space) between the traditional 7.

**Current status:** Some esoteric systems recognize additional minor chakras. The Cassi prediction specifies their exact number (6) and locations (defined by the 2-rung spacing), making the claim testable.

### 10.4 $\varphi$-Periodic Spectral Signature

**Prediction CH4:** Any physiological signal that tracks Qi density along the spine—heart rate variability (HRV) coherence, skin conductance response, EEG/MEG source-localized to the brainstem/spinal axis—should show $\varphi$-periodic structure in its power spectrum. The period is $\Delta(\ln f) = \ln\varphi \approx 0.4812$ for frequency-domain signals, or $\Delta(\ln z) = 2\ln\varphi \approx 0.9624$ for signals parametrized by position along the spine.

**Test:** Collect physiological time series during meditative or resting states. Compute the power spectrum and search for log-periodic modulation at the predicted periods. Subtract any broadband $1/f$ background.

**Current status:** Not yet tested. The same $\ln\varphi$ periodicity is predicted for the cosmological $P(k)$ (`predictions/falsifiable-predictions.md` §5); this is the same signature at the human scale, a cross-scale consistency test.

### 10.5 Geometric Boundary Response Across the Chakra

**Prediction CH5:** A nonlinear physiological response at a modeled chakra boundary is a testable geometric question. The boundary is located by the measured or simulated rung-indexed field condition $B_n=\theta_{\text{cond}}$, corresponding to the unnormalized proxy value $M_{\text{proxy}}=(1+\theta_{\text{cond}})^2/2$ (equal to $1.05125$ only for the illustrative $\theta_{\text{cond}}=0.45$). No canonical gate threshold $q_{\text{edge}}$ or range such as 0.5–0.85 follows from this proxy without a separately measured bounded constitutive map $M_{\text{proxy}}\!\to q$; absent that map, $q_{\text{edge}}$ remains unresolved.

**Test:** Apply controlled vibratory, acoustic, or electromagnetic stimulation at a chakra site while measuring a spatial/physiological correlate across the geometric boundary (HRV, EEG phase synchrony, skin conductance, temperature, or subjective report). Test for a reproducible nonlinear change at the measured boundary, and independently fit a bounded $M_{\text{proxy}}\!\to q$ constitutive map before making any canonical-gate claim.

**Current status:** Not yet tested. This is a proposed geometric-boundary measurement, not a direct claim about the canonical Qi gate or the cosmological $w_0=-0.87$.

---

## 11. Epistemic Boundaries

### Derived
- The exact Frenet-Serret triad and associated three-axis algebra for a nondegenerate curve embedded in $d=3$ (`foundations/why-three-dimensions.md` §2); selecting physical $d=3$ and mapping $\{\mathbf T,\mathbf N,\mathbf B\}$ to string/Yang/Yin axes are **Hypothesized geometric mappings**, not consequences of the canonical density conversion
- The rung-indexed conditional condensation field $B_n(x,y,z)$ and its bubble-edge geometry (`foundations/bubble-edge-geometry.md`)
- The canonical conversion block in the density variables: rank-one relaxation with conserved $\rho = E_Y + E_I$ and eigenvalues $0$ and $-\lambda(1-q)(1+\varphi)$; it is not a norm-preserving $SO(2)$ generator
- The coordinate definitions $\theta_d = \operatorname{atan2}(E_I,E_Y)$, $\theta_\Psi = \operatorname{atan2}(\Psi_1,\Psi_0)$, and $\Theta_S = 2\theta_\Psi \pmod{2\pi}$ as distinct quantities
- The dimensionful cascade $\ell_n = \ell_{\text{Pl}} \times \varphi^n$ and the 26-step human span (`foundations/dimensionful-cascade.md`)

### Hypothesized (geometric mapping and predictions testable)

- $P_\parallel = 2$ cascade rungs at the human scale as a geometric coordinate convention for grouping adjacent directional labels; the canonical density conversion supplies no value for $P_\parallel$ or fixed $\pi$-per-rung phase
- $N_{\text{chakras}} = 26/2 = 13$ under that $P_\parallel$ convention
- The spine as a **Hypothesized candidate physical mapping** of the string axis (structural alignment is the motivation; direct PDE-to-anatomy correspondence and anatomical realization remain open)
- Chakra edge geometry matches bubble-edge geometry (inherits the derived formulas; the claim that the same $\theta_{\text{cond}}$ applies at the human scale is hypothesized)
- Chakra spacing is $\varphi^2$-scaled (follows from cascade rung geometry; the claim that this spacing is measurable at the anatomical scale is hypothesized)

### Speculative (no current test design)

- The exact mapping from cascade steps 142–166 to specific anatomical vertebrae
- Any Qi transport mechanism between chakras or between cascade scales; $J_d$ and $J_\Psi$ are spatial diagnostics, and a separate constitutive map is required before assigning them inter-rung transport meaning
- The chakra-activation protocol: how to externally modulate $q$ at a specific chakra node
- The relationship between the 7 primary and 6 secondary chakras and the traditional 7-chakra system

### Not Claimed

- That the canonical density conversion rotates an $SO(2)$ doublet or supplies a fixed phase advance per rung
- That a spatial current or named projection proves transport between cascade rungs or scales
- That the chakras are physical organs detectable by dissection or standard medical imaging
- That the 13-node count supersedes or invalidates traditional systems with different counts
- That chakra activation has been experimentally demonstrated under the specific protocols of Predictions CH1–CH5


---

## 12. Open Questions

1. **Why $P_\parallel = 2$ exactly?** The value is a **Hypothesized geometric coordinate convention** for grouping adjacent directional sectors in the human-scale condensation map. The canonical rank-one density relaxation conserves $\rho$, relaxes $\varepsilon$, and supplies neither $P_\parallel$ nor a fixed $\pi$-per-rung phase. The open question is why this geometric grouping is useful for the human window while the cosmological envelope places maxima at 1-rung intervals (step 285 and its neighbor): the condensation field $B$, the Qi density $q$, and the density $\rho$ are invariant under the doublet sign flip, so a 1-rung envelope can coexist with sign-alternating projected states (adjacent rungs = opposite directional labels), while the human-scale nodes select the adopted full-period grouping. Whether that selection reflects a real $P_\parallel(n)$ effect or a coarse-graining of the sign-alternating lattice is not yet derived.

The tracer/coarse-graining resolution (tier: **Hypothesized—conditional on the measured horizon cut**): the $P_\parallel = 1$ envelope lattice is the underlying sign-alternating lattice at every rung (the repo's own bubble-lattice §5 reading, `foundations/bubble-lattice-fabric.md` §5); the $P_\parallel = 2$ reading at steps 142–168 is the adopted full-period (self-contained condensate) reading of that same lattice; the $n$-dependence is a reading, not a law. Boundary-consistency supports the $P_\parallel = 1$ cosmological reading: the Cassi bubble (rung 285) sits 6.54 rungs inside the $\sim 9.6$-rung nesting depth ($\ln 100/\ln\varphi$) of the horizon, while the human window sits $\sim 123$ rungs deep in the interior; the horizon cut is a half-rung ($N_H = 291.54$, mod 1 $= 0.54$; the BAO anchor $r_d$ frac $0.46$); and the $P_\parallel = 1$ field at the horizon reads $\cos(2\pi \cdot 291.54) \approx -0.97$ (void level) versus the $P_\parallel = 2$ saddle $\pm 0.13$.

2. **What determines $\theta_{\text{cond}}$ at the chakra scale?** The conversion-diffusion balance that sets $\theta_{\text{cond}}$ at the cosmological scale (`foundations/bubble-edge-geometry.md` §1.2) depends on $D_{\text{eff}}/\omega_0$. At the human scale, $D_{\text{eff}}$—the effective diffusion of the condensation field—is unknown. A PDE measurement at the human-scale parameters (grid resolution corresponding to steps 142–168) is needed.

3. **How do chakras interact?** The cosmological chord lattice has diagonal neighbor coupling (bubbles connected to 4 neighbors via saddles, blocked from 4 axial neighbors by voids). At the chakra scale, the lattice is 1D (along the string only)—adjacent chakras are neighbors with Qi barriers (the $C = -1$ voids between them). Whether inter-chakra Qi transport exists through these saddles is open; a spatial projection or current alone does not establish it, and a constitutive map linking the physical coordinate to cascade index would be required.

4. **What about chakras at other cascade windows?** If the 26-rung human window admits 13 chakras, what about the 26-rung window above (steps 169–194, human-to-building scale)? Or below (steps 116–141, molecular-to-cellular)? Do all cascade windows admit Fibonacci-structured node counts?

5. **Is there a 1D condensation field along the spine?** The full conditional $\mathbb{R}^3$ field $B_n(x,y,z)$ applies within the chosen embedding, but the primary structure is along $z$ (the string axis). The Yang-Yin cross-section ($x$-$y$ plane) of each chakra may be strongly flattened at the human scale, making the 1D approximation $B_n(z) = \cos(2\pi z/p_\parallel(n))$ the dominant term. Treating $p_\parallel(n)$ as a physical rung-period assignment and reducing 3D→1D are separate Hypothesized coordinate choices that require PDE verification.

6. **Fibonacci-resonance scan for chakra color assignment.** The 7 sub-rungs of the visible octave are derived from Fibonacci partitioning of one $\varphi$-step (§9.3). The assignment of specific sub-rung wavelengths to specific chakras—which would give the traditional root=red, crown=violet mapping—requires a computational scan of the Fibonacci convergent structure of $\varphi$ at each microcascade depth $d_k = n_k - 136$. The scan iterates $d \in \{6, 8, 10, \ldots, 30\}$, computes the Fibonacci phase $(d \cdot \varphi) \bmod 1$ at each depth, and maps the phase to the nearest of the 7 sub-rung positions. This is a self-contained Python computation requiring no PDE solves.

---

## References



- `consciousness/consciousness-from-phi.md`—human cascade span, field nodes, chakra gap
- `cassi-psychology.md`—psychology reading guide: chakras as the body's coherence map
- `foundations/bubble-edge-geometry.md`—condensation field, bubble geometry, edge steepness, $\theta_{\text{cond}}$
- `foundations/why-three-dimensions.md`—Frenet-Serret frame, triaxial spheroid, anti-phase selection
- `foundations/dimensionful-cascade.md`—complete 292-step cascade, steps 142 and 168
- `foundations/microcascade-mirror.md`—bidirectional cascade extension, mirror symmetry
- `foundations/spin-fibonacci-spiral.md`—geometric doublet convention and phase coordinates; the canonical conversion's density relaxation is distinct from that convention
- `foundations/three-generations.md`—Fibonacci recurrence, cascade sub-channel partitioning
- `predictions/falsifiable-predictions.md`—prediction catalog, $\ln\varphi$ $P(k)$ modulation
- `visual-explainers/string_bubble_cascade.py`—3D damped-wave PDE, bubble formation on string
- `visual-explainers/chord_lattice.py`—condensation field, staggered lattice, bubble shape
