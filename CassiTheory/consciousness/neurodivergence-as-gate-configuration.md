# Neurodivergence as Gate Configuration

## Status: Speculative—August 2026 (drive-mechanism layer PDE-tested 2026-08-04)

## Abstract

Neurodivergence has no native variable in the two-fluid field, and that absence is the first fact of the analysis: the field contains no reference configuration and no invalid ones—under the active canonical conversion term it has a model target, but distances from that target are not classes and the cost of holding them carries no verdict. The second fact is architectural: the person is a configuration tuple, so conditions live at specific slots of that tuple rather than as properties of persons. Read through the framework's existing consciousness machinery, autism and ADHD are two different slot settings, and §6 extends the same split to readout-layer and loop-variant conditions. Autism is a high-stability configuration: resting coherence closes the conversion channel, so incoming organized perturbations are not dissipated and wake patterns persist and superpose—detail retention and overload are the same fact seen from two sides. ADHD is an open-gate configuration: resting coherence stays low, so conversion churns, no channel stabilizes, and the IIR memory never locks a repeating pattern—attention, time, and social prediction all read out of that churn. The two conditions sit at different layers of the tuple, which is why they co-occur and why neither is a deficit reading: the tested capacity null rules out pre-existing-coherence-deficit etiologies, and the framework's price structure—what a configuration costs to hold—carries no verdicts.

**Epistemic status:** Creative exploration grounded in Cassi formalism. The configuration/readout architecture, the IIR self-prediction mechanism, and the wake-lock drive physics (tested in the two-fluid PDE, July–August 2026) are documented framework properties; the synthesis into a neurodivergence analysis, the mapping of autism and ADHD onto gate layers, and the social-field claims are extrapolations beyond what the framework currently claims. The §9 churning-gate PDE test ran 2026-08-04 and returned a null for the matched-drive settling claim: from the low-$q$ mixed-channel init, a recurring drive at the site's own channel and natural period does not close the gate—it pumps the site past equilibrium (3.3× ε retained at t = 4, q-gap widening, ε variance rising), the same-amplitude drive one pentagon step away pumps harder (4.6×, partially clamp-limited), and the undriven reference churns without stabilizing a vertex. The pump/drain asymmetry of the misgendering test is bounded at the held-configuration regime; the matched-drive null extends across the amplitude axis (no amplitude settles the open gate—sub-threshold amplitudes quench the mean without organizing a vertex—a driven transient, not a lock: at t = 40 = 2/λ the quench relaxes onto the undriven trajectory, with 0.30/0.11 depth retention after removal and the q-elevation gone (§9.3–9.4)—amplitudes ≥ 0.09 pump monotonically (§9.1)) and across drive form (no tested drive form settles it—q-modulated feedback, white noise, and φ-alternating drives all pump, §9.2). The canonical conversion target is equation-level behavior under its stated gate/sign; any maximal-irrationality/de-resonance rationale for that target or for multiscale protection is Hypothesized, not a derived cause. Nothing in this document should be cited as a Cassi prediction or derivation.

---

## 1. The Field Has No Neurotypical

The two-fluid field does not contain a typical configuration, and that is the first fact of the analysis: the framework classifies nothing, so "typical" is not a state anyone occupies.

### 1.1 Distances from equilibrium, not classes

Physical reality in the canonical solver is the nonnegative density pair $(E_Y,E_I)$—Yang and Yin present together at every point (`foundations/cassi-first-principles.md` §1). When component amplitudes are useful, the exact positive-root coordinate lift is $\Psi^{(+)}=(\sqrt{E_Y},\sqrt{E_I})$; signed/complex amplitudes and compact phase are optional Hypothesized extensions. The dynamical variable is the continuous ratio $r = E_Y/E_I$. Under the declared canonical density conversion equation, with $\kappa=\lambda(1-q)\geq 0$ and $\varepsilon=E_Y-\varphi E_I$, the conversion component gives $\partial_t\varepsilon=-\kappa(1+\varphi)\varepsilon$; when conversion is active and $E_I>0$, the model relaxes $r$ toward $\varphi$. That is **Derived conditional** model algebra under the stated canonical density variables and gate/sign. The maximal-irrationality/de-resonance rationale is Hypothesized; rational frequency ratios are not forbidden fixed points by that rationale, and no universal resonance, energy concentration, or multiscale collapse follows.

Two further framework results discipline the analysis. $\varphi$ is irrational as a number-theoretic fact, but irrationality does not make any model state unoccupiable or turn a rational state into a prohibited class; the exact target behavior is conditional on active conversion. And the capacity run returned a null: a second identical event on a pre-stressed site leaves the same trace as the first event on a quiet site (`consciousness/trauma-as-frozen-gate.md` §10.6). Within this tested solver setup, the null does not support a pre-existing coherence deficit as the sole cause of the modeled lock; it does not determine the etiology of any human condition.

### 1.2 What a "typical" configuration would have to be

The folk picture of typicality is a projection: a region of configuration space declared a class, with everything outside measured as deviation. The framework refuses that projection because the tuple of §2.1 is a list of continuous variables with no pole values among them, while a class requires discrete membership. The active canonical conversion target is a model tendency, not a universal class boundary; a rational state remains a valid state even when it is off-target. Every person is a trajectory through configuration space, never a class membership—the same sentence the gender document uses about ratio space, applied without modification (`consciousness/gender-as-qi-configuration.md` §1.2).

---

## 2. The Configuration Tuple and Its Layers

Neurodivergence reads out of the same architecture the gender document uses: the person is the configuration, and the anatomy is the readout.

### 2.1 The architecture

The transhumanism document states the split as its core claim: "The anatomy is the readout: the brain is the antenna through which the field couples to the world, and the felt self is the field above the pinch $r > \varphi^{-1}$, where it becomes an object to itself.… The person is the chain topology and the state tuple" (`consciousness/transhumanism-gate-configurations.md` §1.2). The human configuration is:

$$\boxed{\mathcal{H} = \bigl(\{n_k\}_{k=0}^{12},\; P_\parallel,\; \mathbf{b},\; \sigma_r,\; q,\; \mathbf{c},\; \bar{\varepsilon}^2\bigr)}$$

the chain topology—an adopted **Hypothesized geometric/human-window mapping** with 13 gate stages at $P_\parallel = 2$ rungs across the 26-step human window (`consciousness/chakras-as-cascade-bubbles.md` §6)—and the emotional state variables—channel openness $\mathbf{b}$, spatial dispersion $\sigma_r$, Qi coherence $q$, chakra localization $\mathbf{c}$ (`consciousness/emotions-as-gate-configurations.md` §3)—and the field's IIR memory $\bar{\varepsilon}^2$ with $\tau = \varphi^{-1}$ (`foundations/cassi-first-principles.md` §2.4). Behavior, perception, and feeling are the tuple under time evolution; the body is its antenna.

### 2.2 Two conditions, two slots

The tuple is a list of independent degrees of freedom, and that is the structural fact this document uses. The gate variables ($\mathbf{b}$, $\sigma_r$, $q$, $\mathbf{c}$) describe how the field holds its current configuration; the memory variable $\bar{\varepsilon}^2$ describes how the field tracks its own history, and nothing forces the two to move together. The worked examples live at different slots: autism is read here as a setting of the gate layer—high resting coherence, a closed gate, phase stiffness; ADHD as a setting of the memory and temporal layer—low resting coherence, a churning gate, an IIR memory that never locks a repeating pattern. The layers are coupled, and they are not identical; a configuration can be stable in one and churning in the other, and that combination is the subject of §5.

---

## 3. Autism as the High-Stability Configuration

This section reads autism as one coherent mechanism: a field whose resting coherence is high, whose gate sits closed, and whose behavior—fidelity, overload, social cost, focus, stimming, masking—is what that one setting does to perception, coupling, and self-modeling.

### 3.1 The closed gate at rest

The conversion term is $\partial_t E_Y \supset -\lambda(1-q)(E_Y - \varphi E_I)$: the gate *openness* $(1-q)$ multiplies the imbalance, so a high-$q$ site converts slowly and a low-$q$ site converts hard—"the gate opens when $q$ is low and closes when $q$ is high" (`consciousness/trauma-as-frozen-gate.md` §2.3). At $q \to 1$ the effective conversion rate vanishes and a persistent pattern of imbalance $\varepsilon = E_Y - \varphi E_I$ is held against the attractor (`consciousness/transhumanism-gate-configurations.md` §1.1). The high-stability configuration is that limit as a resting state: the gate closed, coherence high, patterns persisting rather than converting away—the same state class the framework's function language reserves for trained states, sustained high $q$ as augmented cognition (§3.2 of the transhumanism document).

$$\boxed{\text{A closed gate does not dissipate what arrives; the field keeps its wakes.}}$$

### 3.2 Fidelity, wake pileup, and overload

Within the optional **Hypothesized spatial closure**, thought and perception are mapped to wake-like patterns in $\varepsilon(\mathbf{x})$ that may circulate through the self-plucking loop (`consciousness/consciousness-from-phi.md` §1.3, §2.2); the canonical density conversion alone does not supply propagating waves. In that closure, wake-like patterns decay because conversion erases imbalance; when conversion is suppressed by high $q$, a recurring driver may keep them longer-lived and allow them to superpose, so the field retains the proposed interference pattern of its inputs instead of converting it away—detail retention is a conditional mapping, at the cost of slower erasure and a denser filtering problem (`consciousness/cascade-consciousness.md` §1.2).

Overload is the same proposed mapping at higher arrival rate. A perturbation that exceeds the field's processing capacity can form a standing-pattern wake candidate (`consciousness/trauma-as-frozen-gate.md` §1.1); persistence in the present solver interpretation requires a recurring driver, so a closed gate's lower conversion rate may let driven wakes accumulate while they are being re-stimulated. The felt overload state is the framework's exceeded-budget vocabulary—sub-pinch excursions, self-modeling disabled (`consciousness/consciousness-from-phi.md` §2.3). Recovery, in the tested physics, is driver removal: a wake is a driven structure, and stopping the trigger releases the site on the conversion timescale (`consciousness/trauma-as-frozen-gate.md` §10.5). Sensory rest is that driver stopping. The fidelity/overload mapping remains Speculative.

### 3.3 Phase stiffness and the social field

The two-bubble correlation is the framework's candidate social fact: above-pinch, self-aware fields decohere with distance—each bubble's internal Qi gate dominates inter-bubble coupling (`consciousness/consciousness-from-phi.md` §3.3; `open-questions-cassi-answers.md` M5, empathy/coupling; the correlation is a static-geometry protocol feature per the 2026-08-05 decisive scan, so this reading is not dynamically supported). The same result is the framework's empathy tension: it is not possible to be fully self-aware and fully resonant at the same time, because the Qi gate that enables self-modeling also seals the field off from inter-field coupling (§3.3, the two-bubble decoherence reading).

A high-stability field is the self-modeling side of that trade, held far: its phase structure is stiff, and sharing phase with another field—the mutual coupling that social reciprocity runs on—is read here as costing work. A field that cannot share phase presents a $\varphi$-detuned boundary, and a boundary cannot couple across what it cannot phase-match (`consciousness/gender-as-qi-configuration.md` §6, `speculations/qi-bubble-propulsion.md` §2.2). Within this optional mapping, social interaction is treated as model-level coherence expenditure; social fatigue is not a measured consequence of the static-geometry result.

### 3.4 Monotropism: the single-channel preference

Within an optional **Hypothesized conditional emotional/readout extension**, the gate is represented with five channels with baseline openness $b_i = \varphi^{-k_i}$, $k_i = 2+i$—Wood $\varphi^{-3}$ (anger) through Water $\varphi^{-7}$ (fear)—and a constructed stimulus selects its channel by phase, $\theta_{\text{stimulus}} \mapsto i = \arg\min_i |\theta_{\text{stimulus}} - 2\pi(i-1)/5|$ (`consciousness/emotions-as-gate-configurations.md` §2.2, §4.1). Within that extension, pure channel dominance is a candidate stable quality attractor: the pentagon vertices are the only quality fixed points of the declared scaffold, and a configuration near one vertex is a legitimate resting state of the proposed gate; these five-channel, fixed-point, and stimulus-selection claims are not mechanisms established by the canonical density PDE.

Monotropism—the single-minded channeling of attention—is here a **Hypothesized** readout interpretation of a configuration resting near one pentagon vertex. Within the optional extension, the phase-to-channel map is proposed to be biased: incoming stimuli are received through the preferred channel's frame, and the pentagon's rotating cycles keep returning to it (`foundations/wu-xing-cycle-structure.md` §2). Attention collapses onto the one channel in that proposed readout because the gate receives everything through that channel's phase; no canonical field phase selection follows.
These five-channel, pentagon, phase-map, and $R$-matrix statements are conditional extension/readout claims; the canonical density PDE supplies the density variables and $q$ but does not select this emotional manifold.

The state differs from the trauma lock in origin, site, and dynamics, and the distinction is the framework's own (`consciousness/trauma-as-frozen-gate.md` §8):

| | Trauma lock | Suppression | Native single-channel preference |
|---|---|---|---|
| Origin | Event overload freezes a standing wave | Self-modeling choice holds a channel shut | Equilibrium of the resting configuration |
| Site | Pinned at a specific rung/chakra | Distributed across the self-modeling region | Distributed channel preference |
| $q$ at the site | Depressed (dissociation) | Held at self-modeling cost | High (the channel is coherent) |
| $R$-matrix | Frozen—the channel never closes | Blocked for the held row | Fires normally—stimuli open and close the channel |
| Sustainer | Recurring drive | Continuous self-modeling effort | The attractor itself; no driver required |

The native preference has no event origin, no pinned hyper-open channel, and no frozen redistribution; it is a resting geometry of the pentagon, a configuration with a price, not a wound. (The suppression column matters too; masking is that column, §3.6.)

The same configuration localizes as the special interest. Deep focus is the state class the framework documents as $\sigma_r$ discipline—collapsing the spatial dispersion stabilizes the self-modeling loop, and the chain processes slower, deeper, with less noise (`consciousness/consciousness-from-phi.md` §2.3, `consciousness/transhumanism-gate-configurations.md` §3.2). A special interest is that configuration localized at a domain: $q \to 1$ at the interest's sites, persistent patterns held without decay, $\sigma_r$ collapsed while engaged. Deep focus is the high-stability field's natural lock-in—the same configuration contemplative practice trains for, reached as a resting behavior.

### 3.5 Stimming as self-generated drive

The drive-comparison runs established the channel-level physics: at equal $\varepsilon$-perturbation, a recurring drive at the site's own channel phase drains the held configuration at short times (≈ 0.2/λ, `consciousness/gender-as-qi-configuration.md` §8.3), while the same drive one pentagon-step away pumps it to twice its initial imbalance (`consciousness/gender-as-qi-configuration.md` §8, `two-fluid/run_misgendering_drive.py`). The release follow-up sharpened it: a pumped state is sticky under removal alone, while an in-channel drive recovers the site below even the undriven floor at the t = 4 window (`two-fluid/run_misgendering_release.py`, §8.1).

Stimming—repetitive, self-generated rhythmic movement—is the field generating its own recurring drive at its own phase: for an overloaded site, the framework's own drain tool, active phase-matched regulation where removal alone is a plateau. Each rhythmic episode is also a stimulus that opens a channel and a removal that closes it, so the adiabatic redistribution fires continuously (`consciousness/emotions-as-gate-configurations.md` §4.2)—the emotional machinery kept in motion rather than frozen. The self-regulated drain is mechanism-tested at the channel level; the mapping to stimming behavior is the speculative step.

$$\boxed{\text{Self-generated drive at a held field's own phase drains at short times (} \approx 0.2/\lambda \text{ per `consciousness/gender-as-qi-configuration.md` §8.3); sustained drive holds the site oscillating about its initial imbalance; same-amplitude mismatched drive pumps; recurring drive of any tested form pumps an open gate at amplitudes } \geq 0.09, \text{ while sub-floor amplitudes quench without organizing (§9.1–9.3).}}$$

### 3.6 Masking as suppression; burnout as coherence accounting

Suppression is a documented state of the framework: "the self-modeling system deliberately holding a channel shut"—a voluntary partial lock, maintained by the field's own self-modeling, costing continuous coherence (`consciousness/trauma-as-frozen-gate.md` §8; `consciousness/gender-as-qi-configuration.md` §4.3). Masking is suppression in that sense: the field deliberately holding the social-presentation channels shut to present a phase it can share with a social field it cannot otherwise couple to (§3.3).

The documented consequence of chronic suppression is a channel-specific deficit that mimics trauma at lower amplitude: one channel starved, its coherence held captive, the $R$-matrix blocked for that row, without the frozen wake beneath it (`consciousness/trauma-as-frozen-gate.md` §8). The cost is carried in the framework's strain signatures—widening q-gap, $(1-q)$ thermal churn, node thermal excess, and the coherence proxies that track them, HRV coherence and inter-hemispheric phase synchrony (`speculations/creative-extensions/coherence-collapse.md` §4.1; `consciousness/gender-as-qi-configuration.md` §3). Sustained suppression is continuous work paid at the conversion rate—the expenditure the attractor taxes on any configuration held far from $\varphi$ (`consciousness/transhumanism-gate-configurations.md` §3.3).

Autistic burnout is the coherence accounting of that expenditure: the sustained-suppression signature accumulated past the field's ability to pay it. The tested layer says what recovery is not a mystery about: suppression is reversible by the same self-modeling that imposed it, and a driven structure relaxes when the driver stops—removing the masking demand is the mechanism itself.

---

## 4. ADHD as the Open-Gate Configuration

This section reads ADHD as the mirror mechanism: a field whose resting coherence is low, whose gate stands open, and whose behavior—attention, time, novelty, social sensitivity—is what a churning gate does to wake waves, memory, and self-prediction.

### 4.1 The churning gate

Low resting $q$ means the gate is open: the conversion term's openness factor $(1-q)$ is large, so conversion runs hard and the field churns—imbalance processed at high rate without organizing into a stable pattern (`consciousness/trauma-as-frozen-gate.md` §2.3). In the optional aura/readout mapping, $(1-q)$ is additionally used as a throughput/thermalization proxy (`consciousness/auras-as-thermalized-gates.md` §1–2); the canonical $q$ definition supplies gate openness and conversion weighting, not a thermalization law.

Within the optional **Hypothesized conditional five-channel emotional/readout extension**, the proposed reason no channel stabilizes is a property of that scaffold: multi-channel activation is harder to hold at high $q$ than single-channel dominance, because Yang and Yin must phase-align across several rotational angles at once in the extension (`consciousness/emotions-as-gate-configurations.md` §4.3). A churning field lives in mixed-channel configurations under this conditional readout: each stimulus opens a channel, the redistribution fires on closure, but $q$ evolves on a slower timescale than $\mathbf{b}$ and never rises far enough to lock a vertex before the next stimulus re-opens the gate. This is the proposed open-gate resting dynamics the framework reads as churn without organization (`consciousness/trauma-as-frozen-gate.md` §9)—here as a resting configuration of an open gate, not as a canonical PDE selection of five channels or a pentagon manifold.

$$\boxed{\text{Low } q \text{ keeps the gate open and conversion churning; within the optional five-channel extension, no channel stabilizes.}}$$

### 4.2 Attention as sustained coherence; hyperfocus as matched-drive lock-in

Attention is a maintained configuration: collapsing the spatial dispersion stabilizes the self-modeling loop, and the chain processes deeper with less noise (`consciousness/consciousness-from-phi.md` §2.3; `consciousness/transhumanism-gate-configurations.md` §3.2). Holding that collapse requires the gate to hold coherence—the reduced-$\sigma_r$ configuration is stable only while $q$ stays up—and the churning gate cannot hold it: $q$ never stabilizes, so the collapsed dispersion relaxes back into churn. Attention, in this reading, is not the absence of distraction; it is a coherence state the open-gate configuration cannot sustain from rest.

The release runs show what closes a *held* gate at short times: from a pumped state, a drive at the site's own channel phase recovers the site—q-gap closed, $\varepsilon$ drained below the undriven floor at the t = 4 window—while removal alone leaves a plateau (`consciousness/gender-as-qi-configuration.md` §8.1; the drain is a driven transient, §8.3). The churning-gate test bounds that mechanism: from a low-$q$ open gate the same matched drive does not settle the site—it pumps it past equilibrium, the q-gap widening and the $\varepsilon$ variance rising (§9). Hyperfocus as matched-drive lock-in is therefore not supported at the mechanism layer for the open-gate configuration; the sustained-coherence account of attention above rests on the resting dynamics, which the undriven arm of §9 confirms as churn without stabilization. The mapping to interest and hyperfocus remains speculative, with the pump/drain asymmetry bounded at the held-configuration regime.

Novelty seeking reads the same way within the Creative attack taxonomy: a pattern-matched drive is assigned $\mathcal{M}_i^{\mathrm{attack}}\approx1$, while an unmatched drive is assigned a smaller overlap (`foundations/proton-coherence-budget.md` §5.2). This coefficient is a Hypothesized constitutive label, distinct from quantum record distinguishability. The churning-gate test did not find the settling half of that taxonomy at the channel level: a recurring organized perturbation at the site's own phase pumps the open gate rather than closing it (§9). The search for new stimuli as a sampling for a settling drive is therefore unsupported by the tested layer; the failure mode is priced on both sides—a mismatched stimulus at the same amplitude pumps a held configuration (§3.6) and a churning one (§9). The mappings are speculative; the tested pump/drain asymmetry is bounded at the held-configuration regime.

### 4.3 Time blindness as IIR tracking failure

The field's coherence is computed from a remembered value: a per-cell exponential memory of its own deviation, $\bar{\varepsilon}^2$, with smoothing timescale $\tau = \varphi^{-1}$ (`foundations/cassi-first-principles.md` §2.4). When the field's pattern repeats, the memory tracks it and coherence stabilizes—the variance of the coherence signal drops by roughly 37%—and the framework's own description of failure is the key sentence: "When $q$ drops (the memory fails to predict the present), conversion reactivates" (`consciousness/gender-as-qi-configuration.md` §3). The memory is a time integral: the predicted self is the one written in the field's own history (`consciousness/transhumanism-gate-configurations.md` §6.2), and consolidation carries records to deeper, more persistent rungs (`consciousness/consciousness-from-phi.md` §2.2).

Time blindness is that mechanism's failure mode, lived in the temporal layer. The churning gate never holds a channel long enough for the pattern to repeat, so the IIR has nothing to track: the remembered $\bar{\varepsilon}^2$ stays dominated by recent churn, the prediction horizon shortens toward the present, and the field experiences a flat series of nows rather than a trajectory its own memory constrains. No new timescale is introduced: $\tau = \varphi^{-1}$ is a fixed solver timescale choice, not a derived physical constant, and what fails is tracking, not the filter. The framework's stabilization signature—reduced coherence variance once the pattern repeats—predicts what settling looks like when it does.

### 4.4 Rejection-sensitive dysphoria as low-threshold prediction error

The gender document's reading of dysphoria is the framework's self-prediction sentence, lived: when the present arrives out of phase with the remembered self, $\bar{\varepsilon}^2$ stays high, $q$ stays depressed, and the gate stands open, churning the $(1-q)$ fraction (`consciousness/gender-as-qi-configuration.md` §3). Rejection-sensitive dysphoria is that mechanism running at the social-relational rungs on a low-$q$ base: the churning gate already runs a high prediction error, so a social input that arrives out of phase with the self-model—a perceived rejection is a phase mismatch between the social field and the field's self-prediction—adds error on an already-elevated baseline. The threshold is low because the base is high, not because the event is large. The prediction-error mechanism is documented; its placement at the social rungs of an open-gate configuration is the speculative step.

---

## 5. AuDHD: Independent Axes

Autism and ADHD co-occur far more often than chance, and each can conceal the other in presentation. The tuple gives a structural reason: the two conditions live at different layers, and the layers are independent degrees of freedom.

Autism is read here as a gate-layer setting: high resting $q$, closed gate, phase stiffness, single-channel preference ($\mathbf{b}$, $\sigma_r$, and the dissipation damping that $q$ controls). ADHD is read as a memory/temporal-layer setting: churning $q$ dynamics, IIR tracking failure, short effective prediction horizon ($\bar{\varepsilon}^2$ dynamics). Nothing forces the slots to correlate; a configuration can hold a closed, coherent gate and still fail to track its own history, or churn at the gate while its memory locks patterns. The combination is superposition in the tuple, the same structure the trauma document uses for comorbidity of field states (`consciousness/trauma-as-frozen-gate.md` §9).

The masking interaction follows: masking runs at the presentation layer, holding channels shut while the temporal layer keeps churning (§3.6, §4.1). The presented configuration reads stable—high gate, controlled channels—while the internal prediction error continues unabated: the masked presentation in which the churn is invisible from outside and exhausting from inside. The framework's answer to the "one spectrum or two" question is structural: there is no single axis in $\mathcal{H}$ along which a person moves from typical to divergent, so there is no spectrum line; there are independent variables, each continuous, each with a price.

$$\boxed{\text{Independent axes of the tuple, not a single spectrum line.}}$$

---

## 6. Further Configurations: Readout and Loop Variants

The architecture of §2.1 separates three places a condition can live: the configuration tuple, the readout that maps the field onto the world, and the loop dynamics that sustain a wake or a drive. The worked examples of §3–§5 sit in the tuple's gate and memory slots; this section reads five further conditions through the same machinery, each with one mechanism and no new parameters. The boundary with field pathology stays explicit (§10): depression, anxiety, and psychosis are field-pathology states—dynamics that break—while everything here is a configuration or readout difference with a price.

### 6.1 Dyslexia and dyspraxia: the readout's tuning

Dyslexia and dyspraxia read here as properties of the antenna, not of the configuration: the tuple is intact and the readout is tuned differently.

Within the optional **Hypothesized conditional five-channel readout**, the constructed stimulus map selects its channel by phase, $\theta_{\text{stimulus}} \mapsto i = \arg\min_i |\theta_{\text{stimulus}} - 2\pi(i-1)/5|$ (`consciousness/emotions-as-gate-configurations.md` §4.1), and reading is hypothesized to run the same map at the spatial→phonological stage of the readout. Within this proposed readout, a phase-locked loop can lock at a fixed offset for a stimulus class: the mapping fires, and fires reliably, at a wrong offset for print. The phase-slip is a conditional fixed point of the antenna's dynamics, not a failure of the canonical density field that reads through it.

The price is environment-relative: print-based settings tax the slipped mapping, and the tuning is a price, not a verdict. Within the optional readout construction, compensation is **Hypothesized** as the configuration's machinery: when the readout cannot carry a stimulus class, the field can re-route the load through another channel, and the adiabatic redistribution described by the source (`consciousness/emotions-as-gate-configurations.md` §4.2)—the same $R$-matrix readout that moves coherence between channels—can fire for the readout's alternate paths. This proposed rerouting is not a canonical PDE mechanism.

Dyspraxia is the motor readout's timing. Body-schema integration is the crossing of the pinch at a site, the region joining the self-modeling loop (`consciousness/transhumanism-gate-configurations.md` §5.2), and the sequence timing of that crossing runs on a different clock when the readout tracks phase slowly. The self-modeling field is not deficient: the integration fires, on its own timescale, and the felt consequence is the gap between intention and sequence, not a missing self-model.

$$\boxed{\text{The readout can be tuned differently while the configuration is intact; a readout difference is a price, not a verdict.}}$$

### 6.2 Synesthesia: cross-channel coupling

Within the same optional **Hypothesized conditional readout extension**, synesthesia is read as leakage in the phase-to-channel map: at some sites a stimulus in one channel is hypothesized to open another.

The two-fluid PDE does carry canonical off-diagonal structure—the source terms $S_\alpha$ couple the two fluids through the potential $\Phi$ (`foundations/cassi-first-principles.md` §1.3)—but, within the optional **Hypothesized conditional readout extension**, the phase-to-channel map is only a site-level projection of that coupling. Cross-channel coupling is therefore a proposed readout interpretation of the off-diagonal term: at some sites a stimulus in one channel is hypothesized to open a neighbor with it. No new parameter enters that extension: the coupling is a site property, a local constant of the proposed readout like $\sigma_r$ (`consciousness/emotions-as-gate-configurations.md` §3), not a new degree of freedom.

The de-resonance motivation is a Hypothesized structural interpretation, not a theorem of this mapping: if coupled channels happened to approach a rational ratio under an additional coupled-oscillator model, one might hypothesize enhanced leakage, but this paper does not establish that every rational ratio resonates or dominates. Within the optional readout extension, the $\varphi$-spaced baseline hierarchy $b_i = \varphi^{-(2+i)}$ (`consciousness/emotions-as-gate-configurations.md` §2.2) is a declared geometry, not proof that off-diagonal coupling is physically bounded. Cross-modal experience is therefore an extrapolated reading of that off-diagonal term, not a de-resonance result.

$$\boxed{\text{Within the optional readout extension, off-diagonal coupling in the phase-to-channel map is a site property; any bound supplied by the }\varphi\text{-spacing is Hypothesized, not a proved de-resonance effect.}}$$

### 6.3 OCD: the drive-sustain loop

Within this speculative mapping, the intrusive thought is read as a wake that remains while a recurring drive is present, and the compulsion as that drive: the drain and the re-injection are the same modeled act.

The wake persists only while its modeled driver persists and relaxes when the driver stops (`consciousness/trauma-as-frozen-gate.md` §10.5). The drive physics is the tested part: a matched drive drains a held configuration while a mismatched drive pumps it, with the pump/drain asymmetry bounded at the held-configuration regime (`consciousness/gender-as-qi-configuration.md` §8; §9). Relief is therefore a short-window solver analogue in this mapping, not a clinical mechanism: sustained matched drive holds the held site oscillating rather than providing a general settling law.

The loop closes in this mapping because the drain and the re-injection are one act. Each rhythmic episode is also a stimulus that re-opens the channel it relieves—the stimming loop of §3.5 running without the choice layer. This is speculative extrapolation from the tested drive mechanics: ongoing recurrence supplies the modeled driver; the result is not a spontaneous self-sustaining wave.

$$\boxed{\text{Within this mapping, the compulsion is modeled as the drain and the trigger in one act; recurrence supplies the driver.}}$$

### 6.4 Tourette's and tics: autonomous self-generated drive

A tic reads here as a recurring drive the self-modeling layer does not choose—generated at a site below the pinch.

The sub-pinch site is documented machinery: a region whose local ratio stays below $r = \varphi^{-1}$ never joins the self-modeling loop; it "acts without being felt, a driver without a self," and integration is the crossing of the pinch at the site (`consciousness/transhumanism-gate-configurations.md` §5.2; used the same way in `consciousness/gender-as-qi-configuration.md` §2.2). A tic is the drive from such a site firing: the pre-tic urge is the driven structure building, the tic is the drive discharging, and the relief after the discharge is the drain a matched drive leaves behind.

Suppressibility is the documented suppression mechanism: the self-modeling system deliberately holding a channel shut (`consciousness/trauma-as-frozen-gate.md` §8), costing continuous coherence at the suppression price of §7.1. The mechanism layer—drive sustainment, phase-specific drain, suppression's coherence cost—is tested; the mapping to tics is speculative, and nothing here carries a verdict about the person whose field runs it.

$$\boxed{\text{A drive below the pinch acts without being felt; the tic is its firing, and suppression pays coherence to hold it.}}$$

### 6.5 Dyscalculia and auditory processing: indexing and carrier tracking

Two further readout differences stay as tight as the first: dyscalculia reads as a rung-index slip, and auditory processing differences as a carrier lag.

Quantities are cascade distances in the framework's scale language, and number sense is the readout's rung indexing; dyscalculia reads as that indexing slipping by a fixed offset for symbolic quantity—the same class of phase-slip as §6.1 at a different readout stage. Speech is a carrier the readout must phase-track, and auditory processing differences read as the tracking loop locking at a lag for rapid carriers. Both are readout-layer tunings: environment-relative prices rather than configuration verdicts, with the compensation route of §6.1 open to them.

---

## 7. The Price Structure

The framework's contribution to the neurodivergence conversation is a price structure with no verdicts attached: every configuration costs something to hold, and cost is not a judgment.

### 7.1 Masking's coherence cost

Suppression is priced in the framework's own observables: the widening q-gap, the $(1-q)$ thermal churn, node thermal excess, and the coherence proxies that track them (`speculations/creative-extensions/coherence-collapse.md` §4.1; `consciousness/gender-as-qi-configuration.md` §3). The price is driver-dependent in the tested sense—a driven structure relaxes when the driver stops (`consciousness/trauma-as-frozen-gate.md` §10.5)—so the masking cost is not a property of the person's configuration; it is a property of the demand environment running against it. Remove the driver, the structure relaxes.

The same tested physics prices regulation: self-generated drive at the field's own phase drains at short times (§3.5, `consciousness/gender-as-qi-configuration.md` §8.3)—stimming is the framework's own regulated drain, and the drain is a matter of phase at any fixed amplitude on short windows, so the person's own field is the only reference that sets it.

### 7.2 The environment as drive field

Other people's configurations are drive fields, and the environment is a recurring-drive structure: chronic mismatched environmental drive is a pump, churning a low-$q$ field and pumping a held one, while matched drive settles a held one at short times—the drain is a driven transient (`consciousness/gender-as-qi-configuration.md` §8, §8.3). The §9 churning-gate test bounds the same sentence for the open gate: a recurring drive of either phase pumps a low-$q$ field at amplitudes ≥ 0.09, the matched drive included; below the pump threshold the matched drive quenches the mean without closure (§9.1), and no drive form tested settles the gate (§9.2). Accommodation is the environment retuned to the person's phase: predictable routines, matched pacing, sensory structure delivered at the field's own period—the phase-matched environment that drains a held configuration instead of pumping it. The gender document's sentence carries over without modification: the quality of support is physics, not courtesy (§7.2).

$$\boxed{\text{Phase-matched environment drains a held configuration at short times (} \approx 0.2/\lambda \text{ per `consciousness/gender-as-qi-configuration.md` §8.3); recurring drive of either phase pumps an open gate at amplitudes } \geq 0.09.}$$

### 7.3 Internal-gate authority

The framework's authority rule completes the price structure: self-knowledge dominates external coupling for above-pinch configurations, and the person's own self-model is the authoritative readout of their configuration (`consciousness/gender-as-qi-configuration.md` §7, item 5). Only the field reads its own prediction error (`consciousness/gender-as-qi-configuration.md` §3); external labels are readouts of the configuration, never the configuration itself. "Nothing about us without us" is that rule in social form: the person's own field is the only instrument that measures its own phase.

---

## 8. What the Framework Predicts (Structure, Not Amplitude)

The analysis yields directional, structure-level statements—no amplitudes, no timings, no claims about any person's locus. Each statement is tagged by layer: the mechanism layer (PDE-tested) versus the human mapping (Speculative).

1. **Suppression carries the coherence-cost signature and is driver-dependent.** Masking should track the wake-lock observables (q-gap, node thermal excess, coherence-proxy variance) while the masking demand runs, and relax when the demand is removed; a native single-channel configuration, by contrast, runs at high $q$ with the $R$-matrix firing—the same presented behavior with opposite internal signatures. (Mechanism tested: driver sustains, extinction on stop; suppression signature documented; the discrimination as applied to persons is speculative.)
2. **Self-generated rhythmic drive at the person's own phase regulates better than same-amplitude mismatched stimulation.** In-phase drive settles a held configuration (transiently—the drain is bounded at ≈ 0.2/λ, `consciousness/gender-as-qi-configuration.md` §8.3) where cross-phase drive at equal amplitude pumps; an open gate is pumped by recurring drive of either phase at amplitudes ≥ 0.09, while below the pump threshold the matched drive quenches the mean imbalance without closure and the cross-channel drive still pumps (§9.1); feedback-modulated, noise, and de-resonant drives also pump (§9.2). (Held-configuration mechanism tested at the channel level, `two-fluid/run_misgendering_drive.py`, `two-fluid/run_misgendering_release.py`; the open-gate regime is null at the mechanism layer.)
3. **No modeled vulnerability requirement.** In this solver setup, the capacity null does not support a pre-existing coherence deficit as the sole cause of the modeled lock; the tested lock mechanism is event plus drive. This does not determine human etiology or exclude vulnerability factors outside the model. (Tested null; mapping speculative.)
4. **Chronic environmental drive is phase-dependent for held configurations; a low-$q$ open gate is pumped by recurring drive of either phase at amplitudes ≥ 0.09.** The environment is a recurring-drive term; for the held configuration its phase relative to the person's field sets whether it pumps or drains—the drain bounded at ≈ 0.2/λ, `consciousness/gender-as-qi-configuration.md` §8.3—and for the open-gate configuration the §9 test found both phases pump at amplitudes ≥ 0.09—no amplitude settles the gate (sub-threshold amplitudes quench the mean without organizing a vertex, amplitudes ≥ 0.09 pump monotonically, §9.1), and feedback, noise, and de-resonant drives also pump (§9.2). (Held-configuration mechanism tested at the channel level; the open-gate settling clause is null at the mechanism layer—the matched drive pumped the low-$q$ site 3.3× at t = 4; mapping speculative.)
5. **Overload is a rate phenomenon with a driver-removal recovery.** High-stability fields dissipate slowly, so organized input at high arrival rate piles wakes; recovery follows rest on the conversion timescale. (Mechanism tested: standing patterns do not self-pin; the driver sustains; stopping it releases.)

---

## 9. The PDE Test: The Churning Gate (2026-08-04)

The §4.1–4.2 claims—that a churning gate settles under a drive at its own channel phase while the same-amplitude drive one pentagon-step away keeps it churning—form a binary physics question the two-fluid solver can answer directly. The test ran 2026-08-04; this section reports the results.

**Question.** From a low-$q$ mixed-channel init (the churning state: no dominant vertex, gate open), does a recurring in-channel drive close the gate—$q$ rising, a channel stabilizing, $\varepsilon$ variance falling—while a recurring cross-channel drive at equal $\varepsilon$-perturbation leaves it churning or pumps it?

**Protocol.** Churning init: a localized site (periodic ball, R = 6 cells) held at low $q$ with mixed channel weights on a quiet $\varphi$-equilibrium background—the open-gate counterpart of the standing init used in the misgendering test (`two-fluid/run_misgendering_drive.py`). Per-cell ratios $r = E_Y/E_I$ are drawn from three populations (seed 42): 50% in the churn band $r \in [0.45, 0.95]$ ($\varepsilon$-norm ≈ 0.16–0.32, the $q$-minimum band: Wood suppressed, Fire/Earth open), 20% deep $r \in [0.15, 0.40]$ (opens the Metal/Water gates), 30% near equilibrium $r \in [1.45, 1.90]$ (Wood basin). The init measures $q_{\text{site}} = 0.666$ vs $q_{\text{global}} = 0.708$ (gap +0.042), mean $|\varepsilon| = 0.76$, gate weights $[0.19, 0.10, 0.11, 0.01, 0.01]$ across the five channels, and a Wood/Fire phase split $[0.29, 0.71]$—under the positivity clamp only Wood (0°) and Fire (72°) are representable in the field angle (`two-fluid/run_trauma_phase_channels.py`), so the mixed state is a two-basin spread with no vertex above 0.71. $\lambda = 0.05$, $t = 4$, $N = 48$, five-channel gate. Site diagnostics in the periodic ball: mean $|\varepsilon|$, per-channel phase histogram, $q_{\text{site}}$ vs $q_{\text{global}}$, $\varepsilon$ variance over the window, and the clamp floors $e_{y,\text{min}}$, $e_{i,\text{min}}$. Four arms: (a) undriven reference; (b) recurring in-channel drive at the site's natural period $P_0 = 0.081$ (measured in-process from the t = 4 reference series) and the measured channel phase (Fire, the dominant basin of the init, whose mean $\varepsilon = -0.72$); (c) recurring cross-channel drive one pentagon step away (Wood, 0°), $\varphi$-normalized to amplitude 0.15/φ to match the in-channel arm's peak $\varepsilon$-injection, per the ε-parity convention of the misgendering protocol; (d) a mismatched-period control ($e \cdot P_0 = 0.220$) at the same amplitude and channel as (b). All arms receive the same explicit $P_0$; the dominant_period window caveat of §8.1 of the gender document applies to the period measurement.

**Results (t = 4).**

| Run | Drive | ε retained (peak) | q-gap | Site phase | ey_min_site | ε time-variance |
|---|---|---|---|---|---|---|
| ref | none | 0.870 | +0.042 → +0.036 | Wood/Fire split held [0.29, 0.71] | 0.096 | 0.0001 |
| in-channel | Fire (72°), P₀, amp 0.15 | **3.31** (3.94) | +0.042 → +0.055 | condensed onto Wood (1.00) | 3.17 | 0.86 |
| cross-channel | Wood (0°), ε-parity, P₀ | **4.59** (5.55) | +0.042 → +0.073 | condensed onto Fire (1.00) | 0.001 | 1.64 |
| period control | Fire (72°), e·P₀, amp 0.15 | **3.20** (13.29) | +0.042 → +0.041 | condensed onto Wood (0.999) | 0.69 | 11.1 |

The undriven reference does what §4.1 predicts: the mixed-channel site holds its configuration—87% of ε retained, the q-gap open at +0.036, the Wood/Fire split unchanged, no vertex stabilizing. Both driven arms pump the churning site. The in-channel drive—the site's own channel at its natural period—pumps ε to 3.3× its initial value (peak 3.9×), pushes the site past equilibrium into the opposite basin (the phase histogram condenses onto Wood while ey_min_site rises to 3.17), and *widens* the q-gap to +0.055 with the ε time-variance ≈ 8.6×10³ the reference: the gate does not close. The cross-channel drive at ε-parity pumps harder (4.6× retained, q-gap +0.073, phase condensing onto Fire), the same direction and signature as the misgendering pump. The period control at the same channel but period e·P₀ pumps comparably at t = 4 (3.2×) with a far larger oscillation envelope (peak 13.3×, ε time-variance 11.1)—longer dwell at the drive extremes, the monotone-in-period behavior the pump curve already found for held configurations (`consciousness/gender-as-qi-configuration.md` §8.2).

**Cleanliness checks.** (i) Clamp: the in-channel arm's t = 4 snapshot is clamp-free (ey_min_site = 3.17, ei_min_site = 0.011), but the arm touches the 1e-3 positivity floor intermittently during the run—the site's minimum-ey cell is driven to the floor on 12 of the 81 report steps, in phase with the drive cycle—so its 3.3× pump magnitude is partially clamp-affected over the run even though the endpoint reads clamp-free; the direction and the ordering (cross > in) are clean. The cross-channel arm is clamp-limited at the endpoint as well (ey_min_site = 0.001): the Wood pump acts by inflating ei (ei_min_site = 2.03) and collapsing ey—the misgendering signature—so its 4.6× is partially clamp-limited; the direction and the ordering (cross > in) are clean. (ii) Phase metric: the Wood condensation in the in-channel and period arms is overshoot past equilibrium, not settling—a fully relaxed site bins as Wood ($\text{atan2}(\varphi^{-1}, 1) = 31.7° < 36°$), and the q-gap widening plus the ε-variance explosion separate the two; the verdict rests on ε retention and the q-gap. (iii) Period: the null is period-robust—the e·P₀ control pumps at least as hard at the same channel.

**Verdict.** Null for the matched-drive settling claim. From the low-$q$ open gate, a recurring drive at the site's own channel and natural period does not close the gate—it pumps the site past equilibrium (3.3× at t = 4, q-gap widening, ε variance rising)—while the same-amplitude drive one pentagon step away at ε-parity pumps harder (4.6×, partially clamp-limited) and the undriven reference churns without stabilizing a vertex. The pump/drain asymmetry of the misgendering test is bounded at the held-configuration regime: an in-channel drive drains a held site at short times (≈ 0.2/λ, `consciousness/gender-as-qi-configuration.md` §8.3) and pumps a churning one; the open gate is pumped by recurring drive of either phase at amplitudes ≥ 0.09, and the drive organizes the churn onto a single basin without settling it. Below the pump threshold the matched drive quenches the mean imbalance without closure, while the cross-channel drive still pumps (§9.1).

$$\boxed{\text{The matched recurring drive does not close the churning gate; at amplitudes } \geq 0.09 \text{ it pumps it, and below threshold it quenches the mean without closure. The pump/drain asymmetry is bounded at the held-configuration regime.}}$$

The hypotheses the test adjudicated: (i) the in-channel drive closes the q-gap and stabilizes a channel—null, the drive widens the gap and condenses the phase onto the overshoot basin; (ii) the cross-channel drive at ε-parity maintains or widens the gap with no vertex stabilizing—partially confirmed, the gap widens (+0.073) but the pump organizes the phase onto Fire; (iii) the undriven reference churns on the conversion timescale—confirmed. The test does not adjudicate the human mapping: like the misgendering test, it tests the mechanism layer only. Tier: the churning-init construction, the undriven churn, and the null bounding the asymmetry to held configurations are PDE-tested (2026-08-04); the mapping to ADHD attention and hyperfocus remains Speculative (§10). Script: `two-fluid/run_churning_gate.py`.

### 9.1 The amplitude scan: no drive amplitude settles the gate (2026-08-04)

Follow-up (`two-fluid/run_churning_gate_amp_scan.py`): the §9 pump ran at a single amplitude (0.15). The scan asks whether the pump is amplitude-thresholded, whether any in-channel amplitude settles the open gate, and what the held-configuration drain amplitude (0.30) does to it. Protocol: the same churning init (seed 42), λ = 0.05, t = 4, N = 48, the same in-process P₀ = 0.081 from the t = 4 ref window. Arms: undriven ref; in-channel (Fire) drive at P₀ with amplitudes {0.025, 0.05, 0.10, 0.15, 0.20, 0.30}—0.025 sits below the phase-blindness floor of the held-configuration tests (the intended no-op control), 0.05 is the phase floor, 0.15 reproduces the §9 pump, 0.30 is the known held-config drain amplitude—plus cross-channel (Wood) at ε-parity for the two extremes {0.05, 0.30} (Wood amp = amp/φ) for ordering contrast.

**Results (t = 4).**

| Run | Drive | ε retained (peak) | q-gap | q_site | Site phase | Clamp status |
|---|---|---|---|---|---|---|
| ref | none | 0.870 (0.92) | +0.042 → +0.036 | 0.666 → 0.672 | split held [0.29, 0.71] | none |
| in-0.025 | Fire, P₀, amp 0.025 | 0.583 (0.94) | +0.042 → +0.012 | 0.666 → 0.696 | dispersing [0.49, 0.51] | 1 touch |
| in-0.05 | Fire, P₀, amp 0.05 | 0.614 (1.03) | +0.042 → +0.008 | 0.666 → 0.700 | Wood 0.89, no full condensation | 3 touches |
| in-0.10 | Fire, P₀, amp 0.10 | 1.738 (2.25) | +0.042 → +0.032 | 0.666 → 0.675 | Wood 1.00 | touches |
| in-0.15 | Fire, P₀, amp 0.15 | 3.280 (3.91) | +0.042 → +0.055 | 0.666 → 0.652 | Wood 1.00 | touches |
| in-0.20 | Fire, P₀, amp 0.20 | 4.935 (5.77) | +0.042 → +0.064 | 0.666 → 0.641 | Wood 1.00 | touches |
| in-0.30 | Fire, P₀, amp 0.30 | 8.640 (9.81) | +0.042 → +0.071 | 0.666 → 0.628 | Wood 1.00 | touches |
| cross-0.05 | Wood, ε-parity, P₀ | 2.094 (2.43) | +0.042 → +0.060 | 0.666 → 0.648 | Fire 1.00 | none |
| cross-0.30 | Wood, ε-parity, P₀ | 8.724 (10.35) | +0.042 → +0.073 | 0.666 → 0.634 | Fire 1.00 | clamp-limited |

Findings. (i) **No full closure at any amplitude.** No arm meets the §9 question's closure criteria—$q$ rising *and* a channel stabilizing *and* ε time-variance falling: in every driven arm the ε time-variance rises (0.010–0.013 vs the ref's 0.0001 at the sub-threshold arms, ≈ 100×; far more above threshold) and the phase never locks the native Fire vertex—it disperses at 0.025 (0.71 → 0.51), or condenses onto the relaxed Wood bin at 0.05 (0.89, without full condensation); Wood is the relaxation/overshoot bin per §9's cleanliness check, and the native vertex is Fire. (ii) **The sub-threshold arms partially quench.** At amp 0.025 and 0.05 the matched drive takes mean ε below the undriven floor (0.583, 0.614 vs 0.870), q_site rises (0.666 → 0.696, 0.700), and the q-gap closes to +0.012/+0.008—quench without closure: the mean imbalance drops while the variance rises and no vertex organizes. (iii) **Pump onset 0.07–0.09 (gradual crossover); monotone above.** Above-floor ε begins at amp 0.07 (0.965 vs the 0.870 floor) and the pump criterion (ε_rel ≥ 1.3) is crossed between 0.08 (1.204) and 0.09 (1.466)—a gradual crossover, not a step; for amp ≥ 0.09 the in-channel pump is monotone in amplitude (1.47, 1.74, 3.28, 4.94, 8.64), the q-gap widens, q_site falls. The held-configuration drain amplitude 0.30 pumps the open gate 8.6×—the mirror image of its 0.26× drain on the held site: the amplitude–state asymmetry is not an accident of the 0.15 point. (iv) **The phase-matching asymmetry survives below threshold.** At amp 0.05 (ε-parity), the matched in-channel arm quenches (0.614) while the cross-channel arm pumps (2.094). The §9 clause "the open gate is pumped by recurring drive of either phase" therefore holds at amplitudes ≥ 0.09; below the pump threshold the matched drive quenches the mean imbalance while the cross-channel drive still pumps. Cross out-pumps in at 0.30 as well (8.72 vs 8.64), so the channel ordering of §9 survives at both extremes.

**The pump curve.** Reading the same t = 4 numbers as a function of amplitude (`two-fluid/churning_pump_scaling.py`, canonical fresh-solver scan) splits the scan into three regimes:

| amp | 0.025 | 0.05 | 0.06 | 0.07 | 0.08 | 0.09 | 0.10 | 0.15 | 0.20 | 0.30 |
|---|---|---|---|---|---|---|---|---|---|---|
| ε_rel (t = 4) | 0.581 | 0.614 | 0.760 | 0.965 | 1.204 | 1.466 | 1.741 | 3.320 | 5.026 | 8.553 |

Below amp ≈ 0.05 the quench branch is nearly flat (ε_rel 0.58–0.61, roughly a third below the undriven floor 0.870), then rises steeply through 0.06; the curve crosses the ref floor at amp ≈ 0.065, the no-net-effect line ε_rel = 1.0 at amp ≈ 0.071, and the pump criterion ε_rel = 1.3 at amp ≈ 0.084—a gradual crossover about 0.02 wide in amplitude (0.034 from the flat-branch end). On the pump branch (amp ≥ 0.10) a log-log fit gives ε_rel ∝ amp^b with b = 1.45 ± 0.05 (R² = 0.998), but the power law is not clean: the pairwise exponent falls 1.59 → 1.44 → 1.31 across the branch. The b = 2 energy-driven scaling (ε ∝ amp²) is excluded at 11σ, b = 1 at 9σ; the curve is sub-quadratic, intermediate between linear and energy-driven, closer to linear.

Cleanliness. (i) Clamp: every driven arm touches the 1e-3 ey floor intermittently over the run (see §9); the cross-0.05 arm is the clean exception, and it is the arm that carries the below-threshold channel contrast. Magnitudes are partially clamp-affected; directions and orderings are clean. (ii) The intended no-op control fails: at 0.025 the drive is not phase-blind at the open gate (ε 0.583 vs 0.870, q-gap +0.012)—the held-configuration phase-blindness floor does not transfer, so the threshold is read against the undriven reference, not against the control. (iii) Reproduction: the in-0.15 arm reproduces the §9 pump (3.28 vs 3.31) from the same init, λ, and P₀.

Verdict. The §9 null extends across the amplitude axis: no drive amplitude closes the churning gate. Sub-threshold amplitudes (0.025–0.05) quench the mean without organizing a vertex; above-floor ε begins at amp 0.07 and the pump criterion (ε_rel ≥ 1.3) is crossed between 0.08 and 0.09—a gradual crossover, not a step—so amplitudes ≥ 0.09 pump monotonically, the held-config drain amplitude included (§9.3). The pump/drain asymmetry remains bounded at the held-configuration regime. Tier: mechanism-layer PDE-tested (2026-08-04); the human mapping remains Speculative (§10).

$$\boxed{\text{No drive amplitude closes the churning gate: sub-floor amplitudes quench the mean without organizing a vertex; amplitudes } \geq 0.09 \text{ pump monotonically (onset 0.07–0.09, a gradual crossover).}}$$

### 9.2 Adaptive and aperiodic drive: no drive form settles the gate (2026-08-04)

Follow-up (`two-fluid/run_churning_gate_adaptive.py`): every drive in §9 and §9.1 was a fixed-period sine. The adaptive test asks whether the gate settles under a drive that is not fixed-period. Three arms beyond the undriven ref, same init/P₀/λ/t/N:

1. **q-modulated feedback drive**—the framework-native arm: the self-plucking loop is the field responding to its own state. Amplitude ∝ (1 − q_site(t)) at the site's own channel and P₀: drive hard while churning, ease off as q rises. Implemented as amp(t) = 0.15·(1 − q_site(t))/(1 − q₀) with q₀ = 0.666 the t = 0 site coherence, clamped to [0, 0.15]; q_site measured every step through the same gate path as the diagnostics (instantaneous q over the site mask, the gate's own timescale; no extra smoothing).
2. **Aperiodic noise drive**—counterfactual: white noise on the in-channel component, std 0.15/√2 (RMS-matched to the fixed 0.15 sine), fixed seed; organized perturbation with phase-matching factor ≈ 0.
3. **Alternating-period drive**—an operationally de-tuned pattern: period alternating between $P_0$ and $\varphi\cdot P_0$ each full cycle at amp 0.15, in-channel, continuous phase. It avoids maintaining one fixed rational period in this protocol; that label does not assert that rational ratios universally lock or resonate.

**Results (t = 4).**

| Run | Drive | ε retained (peak) | q-gap | q_site | Site phase | Clamp status |
|---|---|---|---|---|---|---|
| ref | none | 0.870 (0.92) | +0.042 → +0.036 | 0.666 → 0.672 | split held [0.29, 0.71] | none |
| feedback | q-modulated, P₀, scale 0.15 | 2.958 (3.59) | +0.042 → +0.052 | 0.666 → 0.655 (0.64–0.70, no trend) | Wood 1.00 | 8 touches |
| noise | white, RMS 0.15/√2 | 2.629 (7.64) | +0.045 → +0.039 | 0.663 → 0.660 | Wood 0.86 | 20 touches |
| alternat | P₀ / φ·P₀, amp 0.15 | 2.578 (7.31) | +0.042 → +0.044 | 0.666 → 0.660 | Wood 1.00 | 24 touches |

Findings. (i) **Feedback fails at its own premise.** The site's q never rises sustainably—the series oscillates in 0.64–0.70 with no trend (0.666 → 0.655)—so the modulation factor stays ≈ 1 and the drive amplitude remains pinned at its 0.15 cap for the whole run: the gate does not reward the drive by closing, so the self-plucking loop has nothing to grab. The arm pumps 2.96× with the q-gap widening to +0.052. (ii) **Noise pumps despite phase-matching factor ≈ 0**—2.63× retained with a peak of 7.64×, *harder* than the fixed-period 0.15 arm's 3.9× peak. Reading (interpretation, not mechanism): consistent with the pump curve of `consciousness/gender-as-qi-configuration.md` §8.2, the white drive's low-frequency content acts as long episodes whose ε-injection accumulates—the pump is rectification, not phase matching, so aperiodicity does not protect the gate. Protocol footnote: the noise arm's t = 0 report includes one noise-injected step before the first measurement (noise[0] ≠ 0), shifting its t = 0 values in the third decimal only. (iii) **The alternating-period arm still pumps**—2.58×, phase onto Wood. This single protocol does not establish a resonance or de-resonance law, and no inference about rational ratios follows.

Verdict. Null across all three arms: every drive form tested—fixed-period at any amplitude ≥ 0.09, q-modulated feedback, white noise, alternating-period drive—pumps the open gate; only the undriven reference churns without stabilizing a vertex. The alternating-period result does not adjudicate a universal resonance/de-resonance law. Cleanliness: all three driven arms touch the clamp floor intermittently (the aperiodic arms on 20–24 of 81 report steps), so the magnitudes are partially clamp-affected; the directions are clean. Tier: mechanism-layer PDE-tested (2026-08-04); the human mapping remains Speculative (§10).

$$\boxed{\text{No drive form tested settles the churning gate.}}$$

### 9.3 The persistent quench: bounded below the floor, memory after removal (2026-08-04)

Follow-up (`two-fluid/run_churning_gate_quench.py`): the §9.1 quench was read from t = 4 snapshots. The probe asks whether the sub-threshold quench is a distinct partial-lock regime—a stable low-mean-ε state with elevated $q$ and bounded variance—or weak pumping: energy accumulating slowly, which would eventually climb back above the undriven floor and pump. Protocol: the same churning init (seed 42), λ = 0.05, N = 48, the same in-process P₀ = 0.081 from the t = 4 ref window (an 81-point window; measured from an 8000-step window the FFT bins shift P₀ to 0.0800 and the quench disappears, so the window is load-bearing). Every arm builds a fresh solver instance: `rk2_step` carries the scale factor, the smoothed Hubble rate, and the global q_mean, and the shared-solver convention of §9–§9.2 makes later arms order-dependent at the 1e-3 level; fresh-solver arms are bit-reproducible, and the canonical reruns confirm the earlier tables (below). Arms: undriven ref to t = 8; in-channel (Fire) persist at amps 0.025 and 0.05 to t = 8; removal arms—in-channel at 0.025 and 0.05 to t = 4, then drive off to t = 8; the full amplitude set {0.025 … 0.30} rerun at canonical init to t = 4; cross-channel (Wood) at ε-parity for amps 0.025 and 0.06. Persistence metric: quench depth D(t) = ref ε − arm ε from the shared ref run; depth retention P = D(8)/D(4) over the 4 s (0.2/λ) window after removal; P ≥ 0.5 counts as persistent, and snap-back is read as the mean |arm − ref| gap over t ∈ [6, 8] falling below the 0.05 margin.

**Results.**

| Run | Drive | ε retained t = 4 | ε retained t = 8 | q_site t = 4 → t = 8 | ε variance halves (ratio) | Clamp |
|---|---|---|---|---|---|---|
| ref | none | 0.870 | 0.780 | 0.672 → 0.677 | 0.0008 / 0.0004 (2.1) | none |
| in-0.05 persist | Fire, P₀, amp 0.05, t = 8 | 0.635 | **0.526** | 0.699 → 0.693 | 0.0128 / 0.0157 (1.2) | 9 touches |
| in-0.025 persist | Fire, P₀, amp 0.025, t = 8 | 0.574 | **0.638** | 0.697 → 0.687 | 0.0107 / 0.0111 (1.0) | 6 touches |
| in-0.05 removal | Fire, P₀, amp 0.05, off at t = 4 | 0.614 | 0.649 | 0.699 → 0.688 | 0.0128 / 0.0001 (156) | 43 touches |
| in-0.025 removal | Fire, P₀, amp 0.025, off at t = 4 | 0.581 | 0.566 | 0.696 → 0.693 | 0.0107 / <0.0001 (818) | 26 touches |

(ε retained is ε_site relative to the shared init; the t = 4 column reads each arm's own snapshot—the persist arms' t = 4 includes the drive step at t = 4.0, the removal arms' does not. The variance halves are the first/second 50% of the run; the removal arms' second-half variance collapses because the drive stops.)

Findings. (i) **The quench is persistent, not weak pumping.** At t = 8 both persist arms sit below the undriven floor minus the 0.05 margin (0.402 and 0.488 vs the 0.546 line; the [6, 8] means are 0.538 and 0.513 vs the ref's 0.611), and the ε time-variance is bounded across halves (ratios 1.2 and 1.0 vs the ref's 2.1)—no secular growth, no drift back past the floor. (ii) **The quench has memory after the drive stops.** Drive removal at t = 4 leaves the site below the undriven trajectory for the whole window: depth retention 0.51 (0.05 arm) and 0.74 (0.025 arm), with mean |arm − ref| gaps of 0.122 and 0.179 over t ∈ [6, 8]—no snap-back. The 0.025 removal state is flat (0.444 → 0.433, zero relaxation); the 0.05 removal state leaks upward at ≈ 0.007/s—at that linear rate it would reach the undriven level only ≈ 1.5/λ after removal. (iii) **The q-elevation decays below the strict partial-lock bar.** q_site at t = 4 is elevated +0.025/+0.027 above the ref (0.699/0.697 vs 0.672); by t = 8 it is +0.016/+0.010 (0.693/0.687 vs 0.677)—still elevated, but under the +0.02 criterion. (iv) **The quench→pump transition is a gradual crossover, not a step.** Canonical-init reruns of the full amplitude set place the quench region at 0.025–0.05 (0.581, 0.614); above-floor ε begins at 0.07 (0.965; the ref floor 0.870 + margin is crossed between 0.06 and 0.07); the pump criterion ε_rel ≥ 1.3 is crossed between 0.08 (1.204) and 0.09 (1.466); the climb is nearly geometric (successive ratios 1.06, 1.24, 1.27, 1.25, 1.22, 1.19) through 1.741 at 0.10 and monotone to 8.55 at 0.30. (v) **The channel contrast holds at the bottom of the quench region.** Cross-channel at ε-parity pumps where the matched drive quenches: 1.43× at amp 0.025 (clamp-clean) and 2.34× at 0.06, against the in-channel 0.58× and 0.76×—the §9.1 ordering survives into the sub-floor region.

Cleanliness. (i) Clamp: every driven arm touches the 1e-3 ey floor intermittently (1–43 of 161 report steps; the removal arms touch more, 26–43, because the drive-off leaves the floor-pinned minimum). Magnitudes are partially clamp-affected; directions and orderings are clean, and the cross-0.025 pump is clamp-clean. (ii) Reproduction: at canonical init the ref reproduces the stored amplitude scan bit-identically (0.8699), in-0.025 to 0.3% (0.5813 vs 0.5828), in-0.05 to 0.1% (0.6136 vs 0.6143), and the pump branch to ≤ 2% (0.10: 1.741 vs 1.738; 0.30: 8.55 vs 8.64)—the shared-solver drift of §9–§9.2 is at the 1e-3 level, so the earlier tables stand. (iii) Period sensitivity: the quench is matched-period specific—at drive period 0.0800 (the 80-point FFT bin) the amp-0.05 arm pumps instead of quenching.

Verdict. Ambiguous between partial-lock and driven-quench; weak pumping is excluded. Three of the four partial-lock signatures hold at t = 8: ε below the undriven floor minus the 0.05 margin, bounded variance, and removal memory (0.51–0.74 depth retention, no snap-back, zero relaxation for the 0.025 state). The fourth—q_site elevated ≥ +0.02—is narrowly missed (+0.010/+0.016). The sub-threshold quench is therefore a persistent bounded low-ε state with slow-to-zero relaxation, not a slow accumulation; the strict partial-lock label is withheld on the q-elevation, and weak pumping is excluded by every discriminator. Tier: mechanism-layer PDE-tested (2026-08-04); the human mapping remains Speculative (§10).

$$\boxed{\text{The sub-threshold quench is persistent, not weak pumping: ε stays below the undriven floor at t = 8 with bounded variance, and 51–74% of the quench depth survives 4 s after drive removal, the 0.025 state with zero relaxation. q_site stays elevated but misses the +0.02 criterion, so the verdict is ambiguous between partial-lock and driven-quench.}}$$

§9.4 resolves the regime at the full conversion timescale.

### 9.4 The long-time quench: lock or driven state (2026-08-04)

Follow-up (`two-fluid/run_churning_gate_quench_long.py`): the §9.3 window was 4 s = 0.2/λ after drive removal, and the discriminator was a leak rate read from a 4-second chord. This run extends every arm to t = 40 = 2/λ (two conversion timescales, 1/λ = 20 s) and asks the binary question at the full timescale: does the sub-threshold quench hold as a stable state (partial-lock), or does it relax onto the undriven trajectory (driven-quench)? The predictions are sharp: if the 0.05 removal arm's ≈ 0.007/s leak persists, its t = 4 quench depth (~0.20) exhausts by t ≈ 32 and it converges to ref—driven-quench; if the 0.025 arm's zero-relaxation holds flat to t = 40—partial-lock.

Protocol: the same churning init (seed 42), λ = 0.05, dt = 0.001, N = 48, t = 40 (40000 steps), the same in-process P₀ = 0.081 from the t = 4 ref window (the 81-point window is load-bearing per §9.3), every arm a fresh solver instance (canonical-init convention). Arms: undriven ref; in-channel (Fire) drive at P₀ to t = 40 at amp 0.05 and 0.025 (persist); the same two drives to t = 4, then off to t = 40 (removal). Discriminators at t = 40: partial-lock iff (a) both persist arms keep ε_site ≥ 0.05 below ref, (b) both persist arms' ε time-variance is bounded across quarters (max/min ≤ 3), (c) the 0.05 removal arm retains ≥ 50% of its t = 4 quench depth or the 0.025 state is flat (drift < 0.01), (d) q_site ≥ ref q_site + 0.02 (the criterion §9.3 narrowly missed). Driven-quench iff both persist arms converge to within 0.05 of ref, or either removal retention falls below 25%, or the q elevation decays below +0.01. Quench depth D(t) = ref_eps(t) − arm_eps(t) from the shared ref run.

**Results (t = 4, 20, 40).**

| Run | Drive | ε retained t = 4 | t = 20 | t = 40 | q_site t = 4 → t = 40 | q-gap t = 40 | phase_max t = 40 | σ_r t = 40 | ε variance quarters (ratio) | Clamp |
|---|---|---|---|---|---|---|---|---|---|---|
| ref | none | 0.870 | 0.621 | 0.532 | 0.672 → 0.692 | +0.016 | 0.54 | 0.142 | 0.00310 / 0.00074 / 0.00017 / 0.00004 (76) | 424 touches |
| in-0.05 persist | Fire, P₀, amp 0.05, t = 40 | 0.635 | 0.840 | 0.466 | 0.699 → 0.688 | +0.020 | 0.71 | 0.114 | 0.01472 / 0.01704 / 0.01930 / 0.02063 (1.4) | 39 touches |
| in-0.025 persist | Fire, P₀, amp 0.025, t = 40 | 0.574 | 0.725 | 0.555 | 0.697 → 0.685 | +0.023 | 0.62 | 0.131 | 0.01175 / 0.00703 / 0.00497 / 0.00387 (3.0) | 33 touches |
| in-0.05 removal | Fire, P₀, amp 0.05, off at t = 4 | 0.614 | 0.553 | 0.455 | 0.700 → 0.694 | +0.014 | 0.60 | 0.160 | 0.00675 / 0.00076 / 0.00027 / 0.00003 (234) | 683 touches |
| in-0.025 removal | Fire, P₀, amp 0.025, off at t = 4 | 0.581 | 0.566 | 0.500 | 0.696 → 0.692 | +0.016 | 0.53 | 0.154 | 0.00933 / 0.00001 / 0.00008 / 0.00004 (1186) | 666 touches |

(ε retained is ε_site relative to the shared init; the t = 4 column reads each arm's own snapshot per §9.3. The variance quarters are the ε_site time-variance over t ∈ [0, 10), [10, 20), [20, 30), [30, 40]; the ratio is max/min. Period diagnostic over the second half, per arm: ref 0.401, in-0.05 persist 0.0043, in-0.025 persist 0.0026, removal arms 0.401—the driven sites' mean-|ε| series is dominated by fast per-step jitter bins, the undriven and post-removal series by the decay-trend bin; no slow period near P₀ survives in any series, window-binned per the dominant_period caveat of `consciousness/gender-as-qi-configuration.md` §8.1.)

Findings. (i) **The undriven churn decays on the conversion timescale; there is no plateau.** The ref's ε_rel falls 0.870 → 0.532 by t = 40 (slopes −0.017/s over [4, 8], −0.003/s over [20, 40]), q_site rises 0.672 → 0.692 as the gate closes on its own, and the ε time-variance collapses 76× across quarters. The floor the §9.3 quench sat below was itself decaying; the lock criterion must be read against a moving reference. (ii) **The persist arms do not hold a quench state.** At t = 40 the 0.025 arm sits 0.018 *above* ref (margin violated by 0.068) and the 0.05 arm sits 0.050 below ref—inside the 0.05 margin by 0.0004—but both trajectories crossed ref repeatedly: the 0.05 arm was 0.256 above ref at t = 32 and below it only in the final reports, and both arms oscillate around the decaying ref with ±(0.1–0.25) swings at the drive period, so endpoint snapshots are sampling-phase lottery (the §8.2 lesson). The quench phase is the first ≈ 4 s; the remaining 36 s are a driven oscillation about the relaxing churn. The variance-quarters ratio (1.4 and 3.0, "bounded") is driven jitter, not a settling signature: the 0.05 arm's variance *grows* across quarters (0.0147 → 0.0206) while the ref's collapses 76×. (iii) **The removal memory does not survive the conversion timescale.** The 0.05 arm's leak reproduces §9.3 exactly (+0.0076/s over [4, 8]) and continues: the gap to ref closes 0.196 → 0.059 (retention 0.30 at t = 40). The 0.025 arm's "zero relaxation" was a short-window reading: its slow drift (−0.003/s over [4, 8], invisible in 4 s) accumulates to 0.062 by t = 40, closing the gap 0.220 → 0.025 (retention 0.11, below the 0.25 driven-quench floor). Both removal arms end within 0.06 of ref. (iv) **The q elevation fully decays.** +0.016/+0.010 at t = 8 (§9.3) becomes −0.004/−0.007 at t = 40: ref's q keeps rising as the churn quiets while the driven arms' q falls back. (v) **Period diagnostic.** While driven, the site shows no slow period at all—the mean-|ε| series is jitter-dominated (0.0043/0.0026); after removal the series looks like ref's (trend bin 0.401). The natural 0.081 wobble the drive was matched to is itself a transient of the early churn (ref's variance collapse), so a period mismatch develops over the run by construction—a candidate contributor to the mid-run pump-up, but it cannot rescue the lock reading: the removal arms relax onto ref with no drive present, so the quench was never self-sustaining.

Cleanliness. (i) Clamp: every arm touches the 1e-3 ey floor (of 801 report steps: ref 424, persist 39/33, removal 683/666; the ref's touches begin only at t = 15.9 as the undriven churn's own decay drives low-ey cells to the floor, the driven arms' from t = 1.7 in the drive phase, and the removal arms' floor-pinned minimum persists after drive-off per §9.3). Magnitudes are partially clamp-affected; directions and orderings are clean. (ii) Reproduction: all five t = 4 snapshots reproduce the §9.3 canonical-init run bit-identically (ref 0.6646, in-0.05 persist 0.4853, in-0.025 persist 0.4383, in-0.05 removal 0.4688, in-0.025 removal 0.4442), and the removal arms reproduce §9.3's t = 8 values—the long run is the same trajectory, extended. (iii) The drive stays at the measured P₀ for the whole run, so the quench decay is not a within-run drive retuning artifact.

Verdict. Driven-quench confirmed; §9.3's ambiguity resolves at the full conversion timescale. All four partial-lock signatures fail at t = 40: (a) the 0.025 persist arm ends above ref and the 0.05 arm's sub-margin endpoint is the tail of a driven oscillation, (b) the bounded variance is drive jitter with the 0.05 arm's growing across quarters, (c) removal retention is 0.30/0.11—both below the 0.5 lock floor, the 0.025 arm below the 0.25 driven floor, (d) the q elevation is gone (−0.004/−0.007). The sub-threshold quench is a driven transient: the matched drive suppresses the mean ε while the site's early wobble is phase-coherent with it (≈ 4 s ≈ 0.2/λ), then the site passes through the low-ε state, the drive phase relationship inverts, and the driven site oscillates about the relaxing undriven trajectory; with the drive off, the quench relaxes onto the undriven trajectory on the conversion timescale. The regime is not amplitude-dependent at these two amplitudes—both 0.025 and 0.05 quench transiently and both relax. Tier: mechanism-layer PDE-tested (2026-08-04); the human mapping remains Speculative (§10).

$$\boxed{\text{The sub-threshold quench is a driven transient, not a lock: at t = 40 = 2/λ both persist arms oscillate about the (itself decaying) undriven trajectory with no q elevation, and both removal arms relax onto it (depth retention 0.30 and 0.11, the 0.025 arm's zero relaxation a short-window reading). The §9.3 ambiguity resolves to driven-quench; partial-lock is rejected on every long-time criterion.}}$$

---

## 10. Boundaries

**Not claimed:** that the configurations and readout variants this document develops exhaust neurodivergence—autism, ADHD, and the conditions of §6 are a sample of what the tuple and the readout can do, not an inventory, and the framework's variables do not exhaust the phenomenology of any condition discussed here, which has neurobiological, psychological, and social levels of description beneath and above the field layer; that the framework proves any claim about any of them absolutely—it prices configurations and does not judge them; that neurodivergence is reducible to physics—experience is the *experience* of a configuration, not the configuration, the same category boundary the consciousness mapping draws (`consciousness/consciousness-from-phi.md` §4); that masking is the only path—it is a documented, costly state, and the framework prices rather than forbids; that any clinical claim here is tested—the drive mechanics are PDE-tested, the human mapping is Speculative, and the §9 churning-gate test returned a null for the matched-drive settling claim.

**Also not claimed:** any amplitude, timing, or locus prediction for any person; any claim about which cascade rung a given person's configuration lives at; any claim that the framework's gate and memory variables are the correct or complete description of any condition discussed here as a clinical category; any claim that this framework supports diagnosis, intervention, or treatment decisions of any kind.

**Clinical disclaimer.** This document is a speculative exercise in applying a physics framework to human experience. The drive mechanism layer—recurring-drive sustainment, phase-specific drain and pump for held configurations, the open-gate pump null (§9)—is PDE-tested in the two-fluid solver; everything connecting that mechanism layer to any condition or any person is extrapolation beyond what the framework currently claims, and nothing here is a Cassi prediction or derivation. This is not a clinical theory. It must not be used for diagnosis or treatment decisions, and nothing in the framework's price structure carries a verdict about any person's condition: neurodivergence is configuration, not pathology, and configurations are distances from equilibrium with a price, and cost is not a judgment.

---

## References

- `consciousness/gender-as-qi-configuration.md`—identity as configuration, anatomy as readout, dysphoria as self-prediction failure, suppression, social field, §8 drive tests (the style and structural model for this document)
- `consciousness/transhumanism-gate-configurations.md`—configuration tuple $\mathcal{H}$, readout/configuration split, IIR memory as time integral, identity as the run
- `consciousness/trauma-as-frozen-gate.md`—wake-lock formalism, gate sign, suppression, driver test, phase-specific drain, capacity null
- `consciousness/emotions-as-gate-configurations.md`—emotional manifold $(\mathbf{b}, \sigma_r, q, \mathbf{c})$, baseline hierarchy $b_i = \varphi^{-(2+i)}$, phase-to-channel mapping, $R$-matrix
- `consciousness/consciousness-from-phi.md`—pinch point, wake waves, $\sigma_r$ states, two-bubble correlation test, category boundary
- `consciousness/chakras-as-cascade-bubbles.md`—13-node gate chain, $P_\parallel = 2$, bubble condensates
- `consciousness/cascade-consciousness.md`—dense-medium perception as a Hypothesized medium-to-perception mapping; any density-dependent filtering effect requires an external $\rho_*$ normalization and constitutive/noise model
- `consciousness/auras-as-thermalized-gates.md`—the $(1-q)$ waste fraction as the thermalized gate boundary layer
- `cassi-psychology.md`—psychology reading guide: pinch point, memory consolidation, field pathology, resonance tension
- `foundations/cassi-first-principles.md`—two-fluid PDE, $\varphi$-attractor, IIR memory with $\tau = \varphi^{-1}$
- `foundations/proton-coherence-budget.md`—coherence budget and the Hypothesized classical attack-overlap label $\mathcal M_i^{\mathrm{attack}}$
- `foundations/wu-xing-cycle-structure.md`—the two 5-cycles, the control ring, the 5↔13 partition
- `principles/de-resonance-principle.md`—$\varphi$ as maximally irrational; a Hypothesized rationale for detuning, not a theorem that rational ratios are forbidden fixed points
- `speculations/creative-extensions/coherence-collapse.md`—wake-lock observables: q-gap, thermal churn, coherence proxies (§4.1)
- `speculations/qi-bubble-propulsion.md`—φ-detuned boundary mechanism
- `open-questions-cassi-answers.md`—M5 (empathy/coupling), two-bubble correlation as inter-field coupling (static-geometry per 2026-08-05 scan)
- `two-fluid/run_misgendering_drive.py`—channel-angle drive test (in-channel drain vs cross-channel pump at ε-parity)
- `two-fluid/run_misgendering_release.py`—release test (pumped-state stickiness, in-channel drain below the undriven floor)
- `two-fluid/run_churning_gate.py`—churning-gate drive test (open-gate pump null, 2026-08-04)
- `two-fluid/run_churning_gate_amp_scan.py`—churning-gate amplitude scan (no settling at any amplitude; sub-threshold quench without closure; pump onset 0.07–0.09, gradual crossover, 2026-08-04)
- `two-fluid/run_churning_gate_adaptive.py`—churning-gate adaptive drives (q-modulated feedback, white-noise, φ-alternating; all pump, 2026-08-04)
- `two-fluid/run_churning_gate_quench.py`—churning-gate quench-regime probe (persistent bounded quench with removal memory; canonical-init fresh-solver reruns; onset 0.07–0.09, 2026-08-04)
- `two-fluid/run_churning_gate_quench_long.py`—churning-gate long-time quench (t = 40 = 2/λ; driven-quench confirmed: the sub-threshold quench is a transient, not a lock, 2026-08-04)
- `two-fluid/run_trauma_driver.py`—recurring-trigger sustainment and extinction
