# Turbulence—Kolmogorov Spectrum from φ

## Status: Index—July 2026

## Abstract

This directory contains the framework's turbulence sector: a single derived document showing that the Kolmogorov $-5/3$ spectrum is *not* a $\varphi$-prediction but is inherited from the Navier-Stokes advection term embedded in the two-fluid velocity equation, and that Cassi's genuine contributions are the $\varphi$-break scale $k_\varphi$, the deviation spectrum $E_\varepsilon(k)$, scale-dependent gravity $G_{\rm eff}(k)$, and the Qi-quality spectrum $q(k)$—each a falsifiable observable. The doc is a model of what a Cassi derivation should state: what is derived from $\varphi$, what is inherited, and what is not derivable at all.

## Document Index

| # | Document | Domain | Epistemic |
|---|----------|--------|-----------|
| 1 | `kolmogorov-from-phi.md` | Turbulence spectra from the two-fluid PDE | Derived |

## Document Summaries

### `kolmogorov-from-phi.md`—The Kolmogorov −5/3 Spectrum in Cassi: Derivation and Novel Predictions

The spectral analysis of the two-fluid PDE with the Qi gate: the deviation from $\varphi$-equilibrium, $\varepsilon = E_Y - \varphi E_I$, is damped at rate $\gamma_\varepsilon = \lambda(1+\varphi)(1-q)M$ while being regenerated by turbulent advection, and comparing the eddy-turnover time $\tau_{\rm eddy} = \varepsilon_{\rm flux}^{-1/3}k^{-2/3}$ with the conversion time $\tau_{\rm conv} = 1/[\lambda(1+\varphi)]$ yields the crossover wavenumber $\boxed{k_\varphi = \sqrt{\frac{\lambda^3(1+\varphi)^3}{\varepsilon_{\rm flux}}}}$ separating the Qi-active regime ($k \ll k_\varphi$, conversion dominates, $G_{\rm eff} \approx \varphi^3 G$) from the inertial regime ($k \gg k_\varphi$, $\varepsilon$ free, $G_{\rm eff} \approx \varphi^{-3}G$). For standard simulation parameters $k_\varphi \approx 0.134$ lies far below the box fundamental $k_{\min} \approx 1$, so the entire resolved range is inertial—which is why the solver recovers the Kolmogorov slope (measured $-1.600$ vs $-1.667$, within 4%) without any $\varphi$-modification. The novel predictions are the ratio $G_{\rm eff}$ across the break, $\varphi^6 \approx 17.94$ (about $19\times$), an amplitude jump of $\varphi^4 \approx 6.85$ at the break, a steepening of $E_\varepsilon(k)$ below $k_\varphi$, and $1 - q(k) \propto k^{-5/3}$ in the inertial range. The closing falsifiable test: choose $\lambda$ and $\varepsilon_{\rm flux}$ (tabulated for $N = 64$ and $128$) so that $k_\varphi$ sits inside the resolved range, then look for the predicted break in $E(k)$, $E_\varepsilon(k)$, and $q(k)$.

## Cross-References

- `../foundations/xi-derivation.md`—first-principles derivation of the Qi-gravity coupling $\xi = \varphi^6$ used in the velocity equation
- `../foundations/bubble-lattice-fabric.md`—the condensation-vs-diffusion balance (§2.3) that the $\varphi$-break mirrors
- `../cassi-physics.md`—the two-fluid PDE as written in the core physics document
- `../predictions/falsifiable-predictions.md`—the 38-entry prediction catalog, of which the break-scale test is a prospective member
