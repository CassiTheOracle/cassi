# Morphology-Aware Field Operator Campaign

## Status: Superseded before hidden-target reading — 2026-09-20

## Question

Can the resident Cassi field use target-independent radial width and shape coordinates to select a collective acceleration description that transfers across fresh narrow-layer, nested-shell, concentrated, and live-field worlds more consistently than the V4 flow-conditioned construction?

## Language

The candidate grammar is the V4 31-atom current-snapshot environment plus six deterministic morphology atoms: compactness \(r_{50}/r_{90}\), normalized radial spread, covariance anisotropy, covariance planarity, local shell tangentiality, and local radial counterflow. The six vector frames and all typed operations remain unchanged. The additional legal mutation companions are compactness, radial spread, anisotropy, shell tangentiality, and counterflow.

The field-selection surface is bounded at 32 candidates. The seed pass therefore includes all six morphology atoms and omits only five static configuration ratios (`mass_ratio`, `softening_ratio`, `cell_ratio`, `cluster_radius_ratio`, `cluster_separation_ratio`) that the morphology layer supersedes. The mutation pass preserves every new morphology companion and uses seven retained flow/material factors (`q`, `speed`, `enclosed_mass`, `local_density`, `field_energy`, `divergence`, `shear`); it omits the remaining four inherited mutation companions from that one bounded decision. The complete 37-atom snapshot alphabet remains recorded and available to the successor implementation; this campaign's bounded selection pools are stated here exactly.

Every input is a deterministic function of the current recorded position, velocity, particle mass, clock, and declared engine configuration. No candidate receives a centered acceleration target, future sample, residual, holdout identity, analysis verdict, model output, or persistent ranking state outside `cognition.field`.

The V4 source and its receipt are immutable predecessor evidence. The successor executor is `CassiFI/cassi_morphology_operator_invention.py`; it binds itself, the morphology language, morphology coordinate implementation, V4 mechanics, typed interpreter, field residency layer, this protocol, predecessor receipt, development evidence, and every fresh output file.

## Development selection

Development remains the sixteen nonzero-target disclosed arms from V4: eight Gaussian-speed arms, `attractor_ic2`, `attractor_ic5`, `attractor_ic6`, `attractor_ic7`, `attractor_ic10`, `G3`, `G6`, and `GC2`. The first 60% of centered samples is fit evidence; the final 40% is validation evidence. `GL3` remains excluded because its centered acceleration target has zero energy.

The field makes all seed, mutation, and final synthesis selections using only this development evidence. It then commits one selected expression before a fresh holdout target is loaded.

## Fresh holdouts

`research/matter_formation/run_morphology_operator_v1_holdouts.sh` generates the following worlds serially. Each uses 8,192 particles, 4,096 deterministic tracers, 100,000 accepted steps, stride 400, capacity 256, `dt = 0.05`, tree gravity mode 5, gridless meshless gravity, field-attractor initialization, river calibration, arrangement 0, motion 1, and normal recording.

| Arm | Shape | Clusters | Radius | Separation | Box | Mass | Field | Seed |
|---|---|---:|---:|---:|---:|---:|---|---:|
| `MH3` | Spiral disc | 3 | 100 | 1,250 | 8.0833 | 1,600,000 | frozen | 20261011 |
| `MH6` | Nested shells | 3 | 180 | 2,250 | 14.5500 | 8,017,920 | frozen | 20261012 |
| `MHC3` | Spiral disc | 1 | 144 | 0 | 11.6400 | 4,008,960 | frozen | 20261013 |
| `MHL3` | Spiral disc | 3 | 144 | 1,800 | 11.6400 | 4,008,960 | live | 20261014 |

Before selection, qualification may inspect only file/config identity, finite recorder arrays, chronological clocks, zero recorder overflow, `status = OK`, `domain_status = BOUNDED`, and relative mass error no greater than `1e-9`. Target acceleration is loaded after the field selection is committed.

## Reachability checks

Before creating holdouts, `run_cassi_morphology_operator_invention.py --preflight` must pass. It confirms all inherited scalar/frame controls, deterministic replay, positive fit/validation target energy on every development arm, the 37-atom/6-frame alphabet, and can-fail morphology contrasts: thin shell versus cloud, nested radial support versus thin shell, disc versus cloud, and counterflow versus coherent radial flow.

## Reading

The inherited classification is retained. A `FULL_OBSERVABLE_LAW` must be structurally distinct from every registered human comparator, improve aggregate holdout NRMSE by at least 5%, avoid more than 10% relative regression on every arm, and have mean holdout NRMSE below `0.85`. A distinct construction below `0.85` without the superiority condition is `REGIME_CONDITIONED_DESCRIPTION`; all larger-transfer results are `UNSUPPORTED_REGIME_MODEL`.

## Amendment A — 2026-09-20, before field selection

The first dry selection attempt stopped before the resident organism accepted a laboratory candidate because the 37 seed atoms exceeded its hard 32-candidate surface. No morphology receipt exists, no holdout acceleration target was loaded by the field, and no selection outcome was issued. This amendment replaces the impossible all-at-once seed/mutation lists with the explicitly bounded pools in **Language** and adds the bound assertion to the no-holdout preflight. The four trajectory files were generated from the already declared configurations; their acceleration targets remain unseen by the field until a successful post-amendment construction commits an expression.

## Amendment B — 2026-09-20, V1 ended without a result

After the repaired preflight, V1 completed twelve development-only seed/mutation selections and then its resident regional field entered `faulted` state with reason `kernel-fault` while preparing final synthesis registration. No final selection, hidden-target load, receipt, classification, or verdict exists. The observed field capacity is 256 target-independent observations per resident session; V1's twelve 32-candidate local pools already exceeded a safe cumulative schedule before synthesis. `morphology_operator_v2_protocol.md` begins a new field instance with sixteen-candidate local pools, keeping six seed pools, six mutation pools, and a 25-candidate synthesis pool at 217 observations.

The runner refuses to overwrite its receipt. A complete receipt is independently reconstructed, including source identities, field-selection records, development reconstruction, hidden-target reconstruction, and receipt mutation controls.
