# Field Operator Invention Report

## Status: HUMAN_EQUIVALENT — 2026-09-19

Pre-registration: `research/equation_discovery/field_operator_invention_prereg.md`

## Verdict

The resident Cassi field constructed and selected recursive typed operators, but it did not select an alien effective law. Before hidden-holdout disclosure, the field chose the registered prior radial support

\[
\frac{\mathbf a}{A_0}
=
-1.01770392177\,q^{0.75}\hat{\mathbf r}
+0.72563238034\,q\hat{\mathbf r}.
\]

Its development-validation NRMSE was `0.4845986084`. Because the selected support is exactly a registered human comparator, the frozen classification is `HUMAN_EQUIVALENT`.

This result separates operator construction from law discovery: the field demonstrably generated recursive non-human candidates, yet its own development evidence preferred the known radial reduction.

## Constructed operator lineage

The field made ten blinded development-only choices: one seed and one mutation in each of four vector frames, followed by one final synthesis choice. Its evolved terms were:

- radial: a constant coefficient along `r_hat`;
- flow: `signed_sqrt(tanh(radial_speed))` along `v/V0`;
- transverse: signed radial speed along the transverse-velocity frame;
- normal: a constant coefficient along the normal frame.

The strongest invented final candidate was the constant radial term. It reached development-validation NRMSE `0.5138421014` and penalized selection score `0.5208421014`, compared with `0.4845986084` and `0.4985986084` for the selected prior radial support. The best two-frame invention, constant radial plus transverse radial speed, reached NRMSE `0.5115172744` but lost after its second-term complexity penalty.

Thus the field's recursive flow operator was structurally novel, while the development evidence did not support promoting it over the human comparator.

## Hidden structural holdout

The final selection was frozen before the four holdouts were loaded. They changed geometry, mass/length regime, cluster count, and frozen-versus-live field state.

| Holdout | Selected prior-radial NRMSE | Inverse-square NRMSE |
|---|---:|---:|
| `G3` | `6344.5343` | `0.7545` |
| `G6` | `6280.2630` | `0.7559` |
| `GC2` | `31082455.5183` | `56003.0637` |
| `GL3` | `224.6706` | `1.0000` |
| Mean | `7773826.2465` | `14001.3935` |

The selected law's relative improvement over the best human comparator was `-554.2180`; its predictions were exactly cosine `1.0` with the registered prior-radial comparator. The reduced global-coefficient law therefore does not transfer across these scene-calibrated scales. The holdout exposes a regime-normalization boundary rather than a microscopic force law.

Only the selected program was exposed to hidden targets. The receipt consequently makes no hidden-transfer claim for the unselected alien candidates.

## Field ownership and integrity

The live runtime made zero model calls and retained no learned ranking sidecar. Candidate identities were content hashes of canonical typed trees; the field received measured development evidence and selected through its resident `semantic.autonomous-agenda` path. The canonical field retains every selection and the revealed outcome.

The independent verifier reconstructed the receipt and reported:

- `PASS`;
- `10` field selection records verified;
- `87` source hashes verified;
- all `6` mutation controls fired;
- selected candidate `program-e773629b2d017505e8c3`;
- result digest `5e3d3a9dea75643ffe05970e38be882b1a5a1c13d3868deaa1f8588f5da8299e`.

The affected CassiFI verification set completed with `12 passed`.

## Invocation recovery

The first invocation stopped before receipt assembly when the C drive exhausted its free space. No hidden holdout was loaded and no scientific classification was issued. Three completed field selections had already been retained. Generated-data roots were relocated to `E:/CassiData/outputs` behind path-compatible NTFS junctions, and the generic organism replay path was repaired to return an already-retained selection instead of attempting to select again from resolved obligations. The resumed invocation reused the same campaign identity and retained choices; it did not change the evidence split, grammar, scores, thresholds, or holdouts.

Receipt: `CassiFI/_diag/alien-equation-discovery/receipt.json`.
