# Full-Observable Field Operator Invention Report

## Status: UNSUPPORTED_REGIME_MODEL — 2026-09-20

Pre-registration: `research/equation_discovery/full_observable_operator_v4_prereg.md`.

## Verdict

The resident Cassi field constructed a structurally distinct, flow-conditioned collective operator, then failed the frozen transfer threshold across the four newly generated worlds. The result is a measured regime boundary, not a promoted physical law.

Before hidden targets were loaded, the field selected

\[
\frac{\mathbf a}{A_0}
=
5.1716573291\times10^{-5}
\left(\frac{s_{\rm cluster}}{R_0}\right)q\,\hat{\mathbf u},
\]

where \(q\) is the recorded local field-energy atom and \(\hat{\mathbf u}\) is the instantaneous flow frame. Its 16-arm development-validation NRMSE was `0.8854508821`.

This is a meaningful construction: it asks whether collective acceleration follows the combination of environmental separation, local field energy, and actual flow direction. It is not radial, and its prediction cosine to the closest registered comparator, harmonic radial support, was only `0.65573`.

## Blind transfer

All four post-freeze worlds completed 100,000 accepted steps, remained bounded, and conserved mass exactly within the declared threshold. The field committed its selection before their centered-acceleration targets were loaded.

| Holdout | Shape / regime | Selected NRMSE |
|---|---|---:|
| `DH3` | three-cluster spiral, frozen field | `0.80001` |
| `DH6` | nested shells, frozen field | `10.11935` |
| `DHC3` | concentrated one-cluster spiral, frozen field | `1.00000` |
| `DHL3` | live-field spiral | `0.99041` |
| Mean | four-regime transfer | `3.22744` |

The constructed operator beat every registered human comparator on the aggregate mean: its relative improvement over the best comparator was `0.9999585`. That apparent advantage is not enough to claim a law, because the predeclared absolute transfer requirement was mean NRMSE below `0.85`, and the shell regime alone reached `10.11935`. The per-arm regression gate also failed.

The result locates the next mathematical target precisely: the local complete-observable grammar can construct a flow-conditioned collective term, but it lacks the regime-normalization structure required to transport that term between shell, concentrated-cluster, and live-field geometries. The unresolved object is therefore not a new force coefficient; it is a scale and morphology coordinate that remains stable across those worlds.

## Field ownership and integrity

A dedicated persistent Cassi field made all 14 development-only selection decisions. The runtime made `0` model calls and retained no learned ranking sidecar. The independent reconstruction verified:

- receipt status `PASS`;
- `31` target-independent atoms and `6` vector frames;
- `102` SHA-256-bound source files;
- `14` retained field-selection records;
- all `9` receipt-mutation controls;
- receipt digest `958f5e0edf2ce4e1bf8b6103f0fb709ba1994170b17c37dbc265cdef0e367d27`.

Receipt: `CassiFI/_diag/full-observable-operator-v4/receipt.json`.
