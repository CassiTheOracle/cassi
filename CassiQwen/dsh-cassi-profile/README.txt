Side-by-side DeepSeek Harness profile

Set DSH_HOME to a separate home, install/link this profile's dependencies, and launch the installed dsh binary with --profile cassi. The profile expects the already-running authenticated Cassi entity at 127.0.0.1:8090 and reads its bearer from CASSI_ENTITY_TOKEN_FILE or the bundle default path.
