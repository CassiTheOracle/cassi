# Cassi Universal Latent Reasoning System

## Status and authority

**Implemented integration with specified reasoning extensions.** Field-resident invocation, one-child reasoning episodes, self-development, fixed model instruments, bounded observations, paired publication, field-owned native emission, and the bounded twelve-step universal-interpreter demonstration now have production source paths. The numerical helical packet basis and the packet-aware multi-operation episode are exercised by `run_cassi_universal_interpreter_program.py`; the full lifecycle is exercised by `run_cassi_universal_interpreter_full_program.py` with independent verification and a same-task native Qwen receipt. These measurements establish scoped mechanics and ownership, not broad acquisition, productive open-domain language, semantic latent alignment, practical efficiency, or general intelligence.

The [CassiFI computer design](../CassiFI/FIELD-INTELLIGENCE-DESIGN.md#32-one-universal-regional-field-computer) remains authoritative for regional state and execution. Its [world-model design](../CassiFI/FIELD-INTELLIGENCE-DESIGN.md#33-field-native-world-modeling-and-lifelong-general-intelligence) governs shared cognitive semantics, and its [learning program](../CassiFI/FIELD-INTELLIGENCE-DESIGN.md#34-cumulative-training-and-evaluation-program) records acquisition mechanisms and their evaluation. This document defines latent-reasoning composition and LLM integration using those same schemas and owners, rather than creating another computer, ontology, learning service, or competing checkpoint.

[Cassi: A Persistent Field–Brain Entity](../CASSI-ENTITY-DESIGN.md) supplies the integrated autonomous-researcher design. Its explicit `field-brain` profile uses the pretrained model throughout a recurrent cognitive life, combining shared semantic work and supported native capabilities with field-owned continuity and learning. The entity service implements the current communication and brain boundary; complete multi-program autonomy remains integration work. This document supplies model-specific mechanisms and their actual capability boundaries. Native displacement is a separately selected experimental objective, not the researcher's success condition. Existing field-only profiles remain explicitly separate.

**Current development direction:** Cassi's learning is an established foundation. Build and operate complete research programs using it. The learning comparisons, teacher-withdrawal exercises, transfer batteries, and native-ownership campaigns retained here describe particular experiments; do not initiate or repeat them as a prerequisite to autonomous-research development. They require an explicit user request. Operational checks and verification of the actual scientific work remain part of implementation.

Statements labeled **Implemented basis** refer to cited source or retained execution evidence. **Specified mechanism** denotes a concrete design obligation not yet present on every profile. **Research question** denotes a capability whose useful acquisition, scalability, or generalization remains unresolved. Unmarked requirements remain normative behavior. These categories do not introduce preregistration, approval-stage, or frozen-verdict documents. Development proceeds through implementation, focused behavioral checks, and actual scenarios.

## Reading map

- [Sections 1–5](#1-destination-and-maximum-ambition): destination, universality, implemented basis, ownership, and operating profiles.
- [Sections 6–9](#6-shared-semantic-substrate): common meanings, reasoning episodes, bubbles, timescales, and mathematical scope.
- [Packet-aware reasoning](#packet-aware-reasoning-scope-and-status): assemblies, resident state handoff, root budgets, selective work, refinement, correction, acquisition, and direct comparisons.
- [Executable capability apprenticeship](#executable-capability-apprenticeship): the next designed increment, learning a small SQLite capability from documentation, model proposals, and actual execution before using it without the teacher.
- [Sections 10–16](#10-adapter-capability-and-identity-surface): model capabilities, alignment, native ownership, calls, transactions, and scheduling.
- [Sections 17–23](#17-world-modeling-inquiry-and-multimodal-grounding): world modeling, acquisition, self-improvement, continuity, transfer, language, and authority.
- [Self-development skills](#first-class-self-development-skills): meditation, study, practice, reflection, teaching, consolidation, and learning better methods.
- [Sections 24–27](#24-performance-storage-and-execution-cost): physical cost, failure behavior, the integrated demonstration, and discriminating evaluation.
- [Sections 28–30](#28-source-ownership-and-integration-boundaries): source responsibilities, implementation dependencies, and unresolved research.
- [Section 31](#31-recursive-self-improvement-campaign): industry RSI landscape, field-native apprenticeships, recursive succession, and model displacement.

## 1. Destination and maximum ambition

Build a continuously learning reasoning system whose understanding survives a context-window change, an interrupted computation, a different task, a new modality, and replacement of its language-model interface. It should acquire useful distinctions from experience, construct and revise executable world models, discover transferable procedures, reason without narrating every intermediate step, and decide which computation or observation would resolve an important uncertainty.

An LLM is an explicit computational instrument: a source of candidate interpretations, measurements, demonstrations, static operations, or language realization. It is not the permanent owner of the system's memory or the mandatory executor of every reasoning step. The field may eventually own interpretation, deliberation, emission, recurrent state, and useful portions of computation. At the maximal endpoint, a model artifact is an offline teacher, tokenizer description, or selectively accessed static resource; a full native autoregressive model need not remain running.

This endpoint includes:

- productive, grounded language and multimodal understanding;
- shared relational, numerical, temporal, causal, and procedural knowledge;
- open-ended construction from a bounded executable language;
- persistent multi-hypothesis reasoning and hierarchical planning;
- model-specific latent observation and intervention;
- acquisition of reusable representations and methods from model-assisted and direct experience;
- cross-model transfer of acquired knowledge without replaying a lifetime into existence;
- selective use, compression, replacement, and retirement of native model state and computation;
- resource-aware learning and inquiry with explicit limits and recoverable unfinished work;
- deliberate self-development through internal practice, authorized study, and acquired methods for improving future computation;
- explanations tied to actual evidence and computation;
- user-owned goals, privacy boundaries, and effect authority.

Within a separately requested native experiment, field ownership and native displacement are explicit objectives and are reported independently of output quality or throughput. They are not the default objectives of the autonomous researcher. Researcher development prioritises useful task execution, continuity, practical cost, and authority behavior; it does not require a fresh displacement or learning campaign.

The design does not promise efficient solutions to every computable problem, unrestricted intelligence, universally identifiable latent causes, or an architecture-independent hidden vector with the same meaning everywhere. Those are different claims.

## 2. Meanings of universal and latent

| Term | Design meaning | Excluded inference |
|---|---|---|
| Computational universality | Stored programs can express general computation in the standard expandable-storage model. Every actual run has finite limits. | Tractability, a solution to P versus NP, or practical efficiency follows from universality. |
| Interface universality | One cognitive lifecycle can serve different model/runtime capability profiles. | A text API exposes hidden activations or every architecture supports the same hook. |
| Knowledge portability | Shared meanings, evidence, mechanisms, and procedures can survive an adapter change. | Residual vectors, token IDs, or KV pages are portable across models. |
| Task generality | Useful representations and procedures transfer to unfamiliar problems and compositions. | A finite task census establishes unrestricted general intelligence. |
| Latent reasoning | Intermediate computation uses nonlinguistic field state, bindings, programs, and hypotheses. | Hiding a textual chain of thought makes its computation field-owned. |
| Native latent integration | A declared adapter observes or changes an actual model execution boundary. | A prompt insertion or unused sidecar is a native intervention. |

The persistent cognitive representation is the portable layer. Model coordinates are identified measurement and actuation frames. The same mathematical relation can be shared between models while its activation correspondences remain different, uncertain, or unavailable.

## 3. Implemented integration and remaining capability boundary

**Implemented basis.** The [regional implementation](../CassiFI/README.md#one-computer-specialized-regions) uses a `RegionalMachineImage`, `LearningComputer`, the owner publication path, bounded kernels, and the shared semantic record vocabulary. The current `cognition.field` operation surface includes observation, query, prediction, planning, explanation, representation and procedure learning, correction, attributed perspectives, reasoning episodes, and bounded self-development. These are bounded capabilities, not evidence of open-domain mastery.

`LearningComputer` now owns a protected `frames` region. `call()` stores the exact caller task, session, arguments, outcome, result, typed dependencies, declared return binding, and callee state in that region. `advance()` restores a completed child through a bounded typed return; `cancel_call()` publishes a cancelled return. The owner exposes replay-safe `call` and `cancel-call` operations. An interrupted child was reopened from the same owner checkpoint and completed with its parent binding and unrelated memory unchanged.

The cognition kernel now implements `begin-reasoning`/`finish-reasoning` and `start-development`/`run-development`/`finish-development`/`reopen-development`. Reasoning and the named meditate, study, practice, investigate, reflect, teach, and consolidate methods use the same resident frames and six semantic record families. Their information, model-call, write-class, storage, work, assessment, stopping, and reopening boundaries are explicit. The initial method selector is the disclosed fixed `supplied-balanced-v1` strategy; useful acquired improvements to that selector remain an empirical objective.

The [resonant packet implementation](../CassiFI/cassi_resonant_field.py) supplies reversible multiresolution views and energy-bounded momentum writes through the regional `packet-impulse` operation. Its packet views are source-bound and disposable. Task-directed assembly, multi-operation `advance-reasoning`, typed numerical continuation within the exercised resident episode, packet-based work selection, readout refinement, correction-local repair, and source-span provenance now run through the ordinary owner path. The continuing-world composition receipt links a nonzero retained packet successor to the fixed interpreter boundary; it does not claim native numerical lowering or semantic alignment.

The [Qwen workbench](cassi_field_qwen_workbench.py) exposes ordinary owner-operated `semantic()`, `reason()`, `develop()`, identified model-observation admission, and paired native execution. [The fixed model-instrument boundary](cassi_model_instrument.py) validates model/runtime/hook/arithmetic identity, closed capability descriptions, bounded little-endian float32 envelopes, payload products and hashes, finite-value policy, routing, state dependencies, work/disposition, and access scope. Unsupported sites, partial acceptance, or exact continuation produce explicit unsupported or unavailable results rather than another profile.

`LatentQwenInstrument` drives the existing Qwen35 residual harness at declared block-input sites. It returns identified activation envelopes and stages the existing Qi intervention with a measured state successor. It explicitly reports `native-continuation-unavailable` because the harness does not serialize a resumable KV/model context. `QwenNativeInstrument` drives the existing `cassi-qwen` executable in coupled or field-owned mode. Field mode retains the declared GGUF vocabulary dependency while reporting zero Qwen forwards, zero model-logit reads, and zero model-tensor bytes loaded.

`CoupledTransactionJournal` is nonlearned execution control rather than adaptive state. It durably records reservations, measured or bounded-unknown work, native trial lineage, sealed publication intent, the field/native successor pair, delivery identity, rejection/cancellation, and crash-recovery disposition. The CassiFI owner transition remains the learned-state publication. A retained field-emission smoke reopened as an exact committed pair and recovered its exact admitted receipt.

The current continuing-world evidence remains bounded. A one-seed, one-block run of `run_general_intelligence_program.py` produced 72 evaluation rows. Its final frozen checkpoint scored 4/4 in each of measurement, temporal, inventory, and software families, while the full cold/control/intervention census retained 52 nonfinal or control failures and the shared-belief challenge passed 7/9 checks. The independent verifier accepted the receipt. The 2026-09-17 full universal-interpreter program now exercises all twelve Section 26 steps in a bounded workshop: prospective relationship acquisition, a new participant, fixed language composition, field-owned calculation and emission, correction-local repair, pause/reopen, replacement-model transfer, selective relation loss, a held-out field emission, a genuine cap-regime gap, and matched fixed-versus-acquired method transfer. These results establish the exercised scoped mechanisms and controls, not general intelligence or unrestricted language.

The retained [workcase analysis](_diag/upgrade-workcases-final-v2-20260914/analysis.json) records 3/17 exact baseline completions and 8/17 field-memory completions, five gains, and zero regressions. The retained [theory preparation](_diag/upgrade-theory-final-v2-20260914/prepare.json) records 442 admitted chunks with exact restart. The retained [persistent latent experiment](_diag/latent-reasoning/campaign-27b-persistent/verification.json) and [phase-control experiment](_diag/latent-reasoning/campaign-27b-phase-control/verification.json) demonstrate model influence and a narrow ordered-composition completion. These are dated measurements of their exact source identities; none establishes broad reasoning quality.
The 2026-09-17 native interpreter run
(`CassiQwen/_diag/universal-interpreter-native-program-v2-20260917/`) closes
the declared native boundary on the exact Qwen3.5-0.8B target. A Vulkan graph
captured the complete declared site bundle with capture-off/on logits
byte-identical, and the selected layer-input trace entered one persistent
`QiFieldState.field`, survived exact restart, and produced field-owned
emission with zero Qwen forwards and model-logit reads. Graph-native
displacement 6 measured field-owned LM-head logits with a zero-state donor
control. Displacement 3 measured a field-owned write to the leading 1,024
channels of the selected recurrent row while the model retained the remaining
5,120 channels; matched field/model and suppressed-model controls were causal
at the graph readout. The independent verifier rehashed the linked float32
artifacts and recomputed the ownership pairs. This is native causal ownership
and partial displacement, not broad semantic alignment or model-independent
replacement of all transformer and KV computation.
The 2026-09-17 continuing-world composition
([`run_cassi_universal_interpreter_program.py`](run_cassi_universal_interpreter_program.py))
exercises the bounded packet-aware loop and the fixed interpreter in one
task/evidence path. The episode changes its workshop premise from `2.0` to
`5.0`, reopens retained work, compares six frontier selectors, forces a
coarse-to-fine refinement, and applies source-admission controls. A nonzero
successor row is hash-bound into the interpreter, which learns one corrected
meaning, reads it on a held-out trace and a distinct model identity through the
same fixed adapter coordinate, and recovers it after restart. The packet
regional workspace and interpreter `QiFieldState.field` are separate explicit
adaptive owners; the bridge carries only task, source, and bounded trace
metadata. Receipt
`_diag/universal-interpreter-program-20260917.json`
(`3c7c696bac8e75eed694ab6bf087a69801927d041fc5f323d88230731d2727cc`)
and [`verify_cassi_universal_interpreter_program.py`](verify_cassi_universal_interpreter_program.py)
both pass. This closes the composition increment.

The complete bounded Section 26 demonstration is exercised by
[`run_cassi_universal_interpreter_full_program.py`](run_cassi_universal_interpreter_full_program.py)
and independently checked by
[`verify_cassi_universal_interpreter_full_program.py`](verify_cassi_universal_interpreter_full_program.py).
The shared-task receipt
`_diag/universal-interpreter-full-program-native-shared-strict-20260917.json`
(`dfa05a3505d7effec22a5bd15a6055ea813be7890e1a157751c6c9dc175afe52`)
records all twelve steps: prospective affine-relation acquisition, a new
participant, fixed bounded language composition, field-owned arithmetic and
emission, correction-local plan/explanation repair, actual unfinished-work
pause/reopen with one effect, replacement-model transfer, isolated selective
relation loss, held-out field emission, a genuine cap-regime gap, and a
matched fixed-versus-acquired method comparison on a second family. The fixed
method returns `17` against held-out truth `9`; the acquired guard returns `9`
after interruption/reopen while preserving the corrected `11` result. The
structured path makes zero model calls. Its strict native sibling
`_diag/universal-interpreter-native-shared-task-strict-20260917.json`
(`f8f63ec7399e1c8fab20fcf20106b4f7658101545d4cfc9e9850a563ab6b9e5a`)
uses the same task ID, question, and source revision with
`--without-answer-bearing-target`: the captured native logits are retained
for measurement but are not supplied as the field's semantic target. This
closes the bounded integrated demonstration; it does not establish open-domain
language, architecture-wide semantic alignment, or unrestricted general
intelligence.

**Remaining boundary.** The measured program uses a fixed bounded language parser, evaluator-owned workshop outcomes, explicit field arithmetic, and a small second-family method transfer. Useful open-domain language acquisition, high-dimensional semantic alignment, task-directed latent readout, broad cross-model semantic transfer, scaling beyond the exercised field capacity, practical native lowering of the acquired formal route, and general intelligence remain research work. The native same-task receipt proves shared task/evidence identity and native observation/ownership measurements; it does not turn the structured fixture's meanings into native semantic understanding. Fixed adapters transport and attribute measurements without hiding a learned projection. Exact field restart does not imply exact native continuation. Other model architectures still require inspected capability implementations rather than a generic decoder.

**Current designed increment.** The [executable capability apprenticeship](#executable-capability-apprenticeship) now has measured typed-input transfer over new values and unseen operation compositions, field-owned support aggregation/final routing, bounded field-directed question choice, one coupled native-model observation admitted into a field-owned binding and predictive evidence record, and one learned language-to-program route after persistence reopen. Five separately acquired native-rooted components now compose eight held-out typed ASTs exactly, including recursive depth transfer and a native-taught parametric recipe whose repeat-count role generalizes to depth five without an arrangement-specific execution branch. The field persisted and routed those frontiers, selected separating study questions over coarse decoys, admitted the actual native response as an identified binding, and retained selective component and recipe loss/recovery across reopen. This establishes bounded parametric recipe generalization, field-owned candidate routing, field-directed question choice, a native observation boundary, and a first bounded native evidence update; autonomous curriculum, native semantic benefit, and teacher-model transfer remain open.

The linked `_diag/` evidence is machine-local and ignored in source control. Its absence in another checkout is an unavailable historical artifact, not permission to reconstruct or silently regenerate it as the original measurement.

## 4. System boundary and invariant ownership

For one principal's continuing intelligence, retain the Cassi decomposition:

\[
\mathcal C_k=(F_k,E_k,A_k,K).
\]

- `F`: the sole authoritative adaptive field image and complete Cassi continuation.
- `E`: authorized exact evidence and immutable completed receipts.
- `A`: nonlearned access, effect authority, revocation, and external-operation control.
- `K`: fixed codecs, validated instruction semantics, bounded numerical kernels, and compiler/checker rules.

An explicit model-instrument session additionally uses identified frozen model resources `W_m` and model execution state `N_m`. These remain outside the cognitive ownership claim until actually replaced. A saved native context is an execution snapshot of the external instrument, not another learned Cassi memory. Its identity is bound to the coupled commit described in Section 15.

| Invariant | Consequence |
|---|---|
| One cognitive publisher | All learned changes, semantic invalidations, reasoning branches, and Cassi continuations publish through the same owner. |
| One semantic identity | Prediction, planning, retrieval, language, and explanation use the same versioned meanings. |
| No hidden adaptation | Learned adapter mappings, priorities, procedural frontiers, cost observations, and consolidation state live in `F`. |
| Exact evidence is distinct | Source bytes can remain in `E`; their relevance, interpretation, and dependencies remain in `F`. |
| Explicit model dependence | Every model call and weight access is attributed; no failure silently invokes an LLM. |
| Hypotheses retain their type | Simulations, teacher assertions, inferred latent states, and proposed interpretations do not become observations by commitment or repetition. |
| Authority is external | A learned priority, coherence diagnostic, model instruction, or successful prediction cannot grant permission. |
| Finite work is conserved | Failed proposals, retries, rejected speculation, validation, learning, transfers, and persistence consume accounted resources. |
| One root does not imply one allocation | GPU pages, read-only replicas, and lossless paging can represent `F`, but cannot have independent semantic clocks or commit authority. |


```text
User / authorized evidence / environment
                  |
        fixed admission boundary
                  |
       one CassiFI field owner
       | meaning and beliefs |
       | hypotheses and work | <--> explicit model instrument
       | acquired procedures |      fixed adapter + declared native state
                  |
        supported result / proposal
          |                   |
     output boundary     current authority
                              |
                         external effect
```
Deleting a derived field cache must preserve the next logical Cassi transition. Deleting a native model context may require model recomputation or make exact native continuation unavailable; it is not covered by that cache claim. Descriptors disclose this difference.

## 5. Operating profiles and field-owned endpoint

Profiles declare ownership, permitted model use, and supported behavior. They are explicit session choices, not an automatic fallback chain.

| Profile | Model role | Field responsibility |
|---|---|---|
| Standalone field | No model execution or model-state dependency. | Interpretation available through acquired/fixed boundaries, reasoning, learning, and deterministic field-owned emission. Unsupported language remains unsupported. |
| Structured instrument | Named text/tool-capable model service. | Shared problem state, selected reasoning operations, evidence use, learning, and assessment. Model proposals and realization remain attributed. |
| Latent instrument | Named native model with verified observation/write capabilities. | The structured responsibilities plus model-native observations and declared interventions. Native state and outputs remain attributed by actual ownership. |
| Selective substrate | Named static matrix operations, tokenizer metadata, or selected native computations. | Routing, reasoning, retained state, and all acquired operations that replace native work; only requested model resources are accessed. |
| Offline acquisition | Frozen teacher or comparator invoked in an acquisition environment. | Evidence admission and field-owned learning; no teacher is implicitly retained in standalone inference. |

A maximal field-owned inference implementation may use a fixed byte/event transducer without loading a GGUF or native model. A tokenizer-assisted or matrix-assisted implementation is separately identified. Copying model weights into the field and calling them acquired knowledge does not meet the learning objective.

An unavailable required capability produces an explicit unsupported result. Continuing in a different profile requires an explicit caller decision and a visible new operation boundary. Experimental native builds may deliberately replace or break ordinary Qwen behavior; recoverable source and accurate ownership evidence remain required.

## 6. Shared semantic substrate

Use the existing six record families rather than introducing `WorldModel`, `Bubble`, `Thought`, or `AdapterMemory` persistence roots.

| Record family | Integration uses |
|---|---|
| `Value` | Typed measurements, referents, alternatives, goals, numerical coefficients, model-feature hypotheses, or uncertainty sets. |
| `Binding` | Current identity-to-value/program resolution under a scope, including portable concepts and model-specific correspondences. |
| `Event` | Source admission, model observation, token acceptance, correction, action acknowledgment, or externally measured outcome. |
| `Program` | Representation, mechanism, reasoning procedure, interpretation, readout, emission construction, or migration. |
| `Assessment` | Pre-outcome prediction, causal comparison, procedure adequacy, model-intervention result, or measured computation cost. |
| `Obligation` | Unresolved inference, missing observation, pending effect, invalidation, migration, learning assessment, or interrupted integration duty. |

Every semantic use resolves versioned references and carries scope, evidence roots, applicability, epistemic kind, observation masks, valid time, and units/frame where meaningful. A native activation observation adds model, layer/site, token-position, sequence, arithmetic, and capture identities. The schema remains extensible through bounded typed payloads and fixed interpreter rules; arbitrary executable host objects are not payloads.

Model features have a model-specific frame. Portable entities and mechanisms have an identified shared frame. A correspondence is an acquired hypothesis/program linking these frames, with its evidence and domain. It is not established by coordinate equality or a suggestive feature name.

A document stating that a valve is closed, a sensor reporting a closed valve, and an LLM predicting a closed valve are different events. Their consequences may interact through one belief, but their support and reliability remain distinct.

## 7. Reasoning episode and result semantics

A reasoning episode is a bounded stored computation over the shared state. It may span many model tokens, model calls, observations, and restarts, or none of them.

Its logical request contains:

| Field | Meaning |
|---|---|
| Identity | Principal, session, episode, idempotent delivery/operation identity, and parent episode if any. |
| Question | Requested proposition, prediction, construction, decision, explanation, or acquired capability. |
| Grounding | Exact source references, current bindings, unresolved interpretation alternatives, and their versions. |
| Objective | User-owned desired consequences, output requirements, permitted approximation, and priorities. |
| Constraints | Assumptions, invariants, represented action domain, and nonlearned authority references. |
| Allocation | Work, memory, external-call, time, branch, and acquisition limits. |
| Dependencies | Field/model/adapter/catalog/profile identities required for continuation and result validity. |
| Learning mode | Frozen acquired knowledge, bounded adaptation, or continuing online acquisition. |

The request is not an answer-bearing fixture. A caller may supply a formal problem, but must disclose supplied roles, decomposition, or representations when claiming acquisition.

The episode proceeds through admission, interpretation, binding, eligible-work selection, bounded execution, assessment, and publication. The work can retrieve exact evidence, derive a relation, propagate constraints, execute a procedure, simulate a supported mechanism, branch, seek information, or declare a limitation. It does not automatically ask the LLM to do an operation the field failed to complete.

The result carries the original question, answer/alternatives if any, semantic status, assumptions, evidence and program dependencies, applicability, unresolved obligations, cost, and continuation identity. The status must distinguish:

- an exact conclusion relative to checked premises;
- a supported numerical estimate with declared error/domain;
- a supported set of alternatives;
- missing or revoked evidence;
- source conflict or contradiction under the stated premises;
- representation insufficiency;
- unresolved identification, with the basis for any stronger non-identifiability claim;
- pending external observation or effect reconciliation;
- resource exhaustion or numerical failure;
- denied authority or unsupported adapter capability.

A wrapper must not manufacture a more specific status than the underlying computation justifies. A numerical solver settling, an LLM sounding certain, and an answer being eligible for publication are three different events. Goals influence inquiry and plan choice; they cannot change evidence or a goal-independent prediction to make the desired outcome appear likely.

## 8. Bubbles, dual perspectives, and timescales

A reasoning bubble is a descriptive name for a scoped dependency-closed working assembly. Its durable representation is an ordinary set of the records above, rooted in an episode and identified scope. It contains a question, relevant meanings, alternatives, work sites, result bindings, and expansion boundaries. No separate packet store or new core record family is required.

A bubble supports two operational views:

1. **Generative/consequence view:** propose interpretations, apply mechanisms, construct candidate programs, and predict consequences under explicit assumptions.
2. **Constraining/evidence view:** resolve sources, propagate requirements, seek counterexamples, inspect violated invariants, and bound what the first view can claim.

Both views use the same referents and dependencies. They can disagree without averaging incompatible worlds into one vector. A backward constraint operation must name its valid inverse, preimage, or solver semantics; running a forward mechanism backward without such a basis is invalid. These are computational perspectives, not truth labels assigned to Yang and Yin, and not a replacement for attributed beliefs of real speakers or agents.

The two-phase helix is an optional numerical organization of these coupled views. Phase can encode order or reciprocal interaction only through declared operators and readouts. A drawing of a helix, a phase rotation, or two channels alone establishes no reasoning benefit.

| Timescale | Persistent meaning | Permitted evolution |
|---|---|---|
| Fast | Current excitations, residual responses, numerical scratch, eligible work. | Bounded numerical/scheduling updates; no automatic creation of facts. |
| Episode | Active bindings, hypotheses, partial proofs, plans, and prediction obligations. | Dependency-aware revision, continuation, rollback, and explicit publication. |
| Lifetime | Supported mechanisms, representations, procedures, exceptions, and assessed applicability. | Identified acquisition, correction, migration, and consolidation. |

Frequency banks may represent several response horizons within owned numeric regions. Their values, readout, delay, and update meaning must be specified. Frequencies are not automatically calibrated timescales, probabilities, or semantic categories. A clock or inactivity can reduce priority without erasing a rare valid fact or unresolved external effect.

Bubble composition shares bindings and scoped program interfaces, checks frames and assumptions, and accounts for shared work once. Coarse bubbles retain a finer expansion or an explicit information-loss boundary. Splitting work into bubbles must not hide independent adaptive solvers.

### Acquiring predictive interfaces and temporal organization

The bubble boundary can itself be a learning target. Starting from a supported assembly, propose which ports, historical distinctions, and conditions are sufficient for a declared family of future questions. Challenge the proposed interface by varying context it treats as irrelevant, testing delayed consequences, and composing it with another supported assembly. Store the candidate as ordinary representation and mechanism programs, with tested coverage and known separating cases.

A separating outcome expands the interface, splits its regime, or narrows its claims. A finite set of successful boundary challenges does not prove global causal independence. Reduction never invents discarded detail: expansion uses retained fine state or authorized evidence when available, and otherwise retains the resulting information-loss limitation. Explicitly selected ports remain a useful supplied-structure condition, distinct from discovering the decomposition.

Temporal structure is also assessed through the consequences it preserves. For a linear mode proportional to `exp((-gamma + i omega)t)`, positive `gamma` controls decay while `omega` controls oscillation. Memory lifetime and oscillation period are different. A temporal representation must distinguish immediate responses, delayed effects, recurring conditions, and long-lived unresolved state without assigning truth or lifetime solely from frequency.

Candidate delays, retained predictive state, and supported numerical modes live in the same field. Compare their held-out temporal consequences, interference, stability, and actual cost with simpler delay buffers and nonoscillatory realizations. Meditation or consolidation may propose a changed organization; neither can rewrite a fixed arithmetic profile or discard relevant history merely to obtain a quieter signal.

### Packet-aware reasoning: scope and status

**Specified mechanism.** Extend the resident reasoning episode into a resumable, packet-aware work loop. It selects a relevant operation and resolution, executes through the existing regional computer, checks the returned consequence, and retains useful interfaces and procedures through ordinary acquisition. All adaptive decisions, unfinished work, and numerical amplitudes remain in the one field image. This extension does not add a reasoning service, independent packet store, model fallback, or a seventh semantic record family.

The intended improvement is task adequacy per total resource allocation: better completion under a fixed allowance, less work for the same justified result, or cheaper repair and reuse. Numerical reconstruction alone establishes none of these improvements.

| Surface | Current basis | Required extension |
|---|---|---|
| Numerical packets | [`cassi_resonant_field.py`](../CassiFI/cassi_resonant_field.py) implements `balanced-contiguous-haar-phase-space-v1`, source-bound analysis/split/composition, and bounded packet impulses. | Bind useful work and readouts to these supports; measure whether their use changes a consumer-visible result or cost. |
| Resident episode | `begin-reasoning` admits one supplied semantic child request; `finish-reasoning` records its returned result. | Execute a field-resident work frontier over multiple operations, with a root allowance, typed conclusions, and an explicit stopping rule. |
| Nested execution | `LearningComputer.call()` retains the caller and executes a child in the same image. Its current return contains outputs, diagnostics, and identity hashes. | Export the selected numerical successor and any retained partial continuation; a hash or energy receipt cannot restore wave coordinates. |
| Acquisition | Representation, procedure, assessment, correction, and self-development operations use the shared semantic state. | Acquire and challenge packet interfaces and work-selection methods through those operations. Useful acquisition remains unmeasured. |

**Implemented.** The packet work loop runs inside the resident semantic state in [`cassi_field_cognition.py`](../CassiFI/cassi_field_cognition.py). A reasoning episode holds a bounded work frontier with one root allowance, publishes a reservation before dispatch, consumes each child return exactly once against the published request identity, and keeps host-executed readout, branch, and refinement items under reservations derived from their own contracts. Completed items with one content identity reuse their result instead of dispatching again. Corrections publish an invalidation barrier with a retained repair frontier, refuse reads of dependency-bearing records until each known cause is checked, run in bounded quanta through `advance-invalidation`, and repair a reopened episode's premise, assembly, and hierarchy references while releasing the reservation that the changed dependency made ineligible. Episode and item statuses map through the declared result vocabulary, including `resource-exhausted`, `non-identifiable`, and `alternatives`. A work item that declares no reservation holds the remaining allowance rather than the whole limit, so one unknown-cost item no longer strands the rest of its frontier once anything has been charged.

Evidence: ten transition regressions in [`test_cassi_packet_reasoning.py`](../CassiFI/test_cassi_packet_reasoning.py) (adapter dispatch with corrupted-return refusal and single charging, enforced-allocation refusal, decisive and refinement-required readouts with an uncertified-norm refusal, correction with bounded repair and re-dispatch, reuse without a second dispatch, two incompatible hypotheses published as distinct hypothetical values, an acquired selector program that orders the frontier and refuses an unresolvable or fractional score, and a two-item frontier whose undeclared reservations hold only the remaining allowance), the continuing-world run in [`run_cassi_reasoning_scenario.py`](../CassiFI/run_cassi_reasoning_scenario.py), and the workbench path in [`CassiQwen/test_cassi_field_qwen_workbench.py`](test_cassi_field_qwen_workbench.py). The scenario interrupts once after a child return and before consumption with the reservation outstanding and nothing charged, corrects the shared premise, repairs and reopens through the ordinary owner with the stale return left unconsumed, finishes `supported` under an unchanged allowance, reads a coarse bound that stays unresolved, splits that support into `RL`/`RR`, and then reads the refined child with a decisive interval.

Three organizations remain distinct. The **numerical hierarchy** partitions ordered ports into balanced contiguous `L`/`R` supports. The **semantic dependency graph** connects meanings, programs, evidence, and obligations; related records need not occupy adjacent ports. The **hypothesis tree** separates incompatible assumptions and futures. A numerical split does not discover a semantic decomposition, and composing numerical siblings does not reconcile hypothetical branches.

The four common/counterflow position/momentum channels retain their numerical meaning. Generative and constraining views are scoped semantic roles, each permitted to use both transport strands. Packet depth, oscillation frequency, retention lifetime, confidence, and source authority are separate quantities.

### Packet assemblies and versioned state

**Specified mechanism.** A packet assembly is a bounded typed payload in existing records, rooted in the reasoning episode. Its `Program` declares the interface and allowed operations; `Binding` records connect semantic roles to exact current values and numerical regions. The canonical numerical value is the owned resonant workspace, including `qY,qI,pY,pI`, its profile, bindings, phases, clocks, and ledger. Packet coefficients are reconstructible views of that workspace, never a second adaptive copy.

| Owned content | Required meaning |
|---|---|
| Episode identity | Original question, information boundary, objective, learning mode, scope, program version, return binding, and predecessor episode if explicitly reopened. |
| Assembly interface | Input/output roles, typed semantic references, units/frame, masks, scoped assumptions, accessible support roots, and unresolved external boundaries. |
| Port binding | Numerical workspace reference/version, ordered port identity, coordinate role, fixed codec/readout version, serialized channel units/frame and positive finite normalization scales, and declared missing-value behavior. These new codec fields are not already supplied by `ResonantProfile`. |
| Work frontier | Bounded eligible/deferred work items, discovery cursor, prerequisites, read/write footprints, scope, required checks, and result references. |
| Resolution frontier | An antichain of active numerical supports, retained finer-state reference, refinement reasons, and any applicable readout bounds. Ancestor and descendant supports cannot count the same coefficient twice. |
| Continuation | Episode phase, selected operation identity, outstanding call/return slot, child continuation reference, consumed-return identities, and scheduler fairness position. |
| Assessments and allowance | Actual outcomes, applicability and failure boundaries, resource reservations/charges, and separate numerical-energy accounting. |

Work items and frontiers live in the existing episode/`Obligation` payloads and protected invocation state. Programs remain bounded typed data interpreted by fixed catalog semantics. Native Python objects, executable callbacks, host queues, and learned adapter dictionaries cannot supply missing persistent state.

A versioned assembly can reference several disjoint numerical supports or share read-only semantic premises with another assembly. The initial write rule gives each active numerical port one owning workspace binding; overlapping proposals serialize through that owner. Repacking ports is an explicit layout migration of coordinates, operator/boundary data, bindings, and readouts. It is not a metadata rename that leaves dynamics attached to different meanings.

Closure is query-relative and explicitly bounded. Starting from the question's declared output roles, traverse known program inputs, guards, constraints, and current evidence dependencies. Retain a cursor if traversal exceeds its quantum. Unknown dependencies become obligations, not assumed irrelevance. Cyclic constraints require a declared solver/fixed-point operation or remain unresolved; they do not become an endlessly waiting prerequisite cycle.

The initial binding/decomposition can be supplied and is reported as supplied structure. An acquired decomposition is a separately assessed `Program`. A complete scan of known dependencies establishes closure only under those declared programs, not that the world has no omitted causes.

Use separate version checks for semantic references, assembly/interface programs, and the complete numerical source workspace. Every numerical mutation invalidates packet views of that source under the existing packet contract; reanalyze before another split/composition. Unrelated semantic publication need not invalidate a numerical view when its exact source workspace and binding dependencies remain unchanged. Do not relabel a stale packet with a new hash or weaken whole-source composition identity to a partial-slice comparison.

### Resident execution and numeric handoff

**Specified mechanism.** Add `advance-reasoning` to the existing cognition operation surface. Keep `begin-reasoning` and `finish-reasoning` as the episode boundaries. These names describe the intended implementation; `advance-reasoning` is not currently an available operation.

`begin-reasoning` accepts exactly one of a bounded direct operation or a versioned reasoning-program reference, together with Section 7's question, dependencies, and allocation. Boundary normalization turns a direct operation into a one-item program; both forms then use the same work loop. A caller-supplied solution-specific sequence remains assisted execution and is identified as such. The initial packet program is a fixed, disclosed method; acquired replacements are ordinary assessed field programs.

Admission order is normalize, validate typed semantic content, canonicalize, hash, then resolve the operation ledger. The canonical content includes the normalized program and its versions, question, allocation, input/dependency references, scope, and assistance/authority boundary. Transport form and optional diagnostic metadata do not change its meaning. Equivalent direct and explicit one-item forms share content identity only when all those semantics match; changed content under an existing caller operation identity is a conflict. Bind begin, selected child, advancement, return consumption, and finish to the same episode lineage plus their distinct stable transition identities. The current semantic kernel hashes the supplied mapping before such normalization, so this admission rule requires a real boundary/schema change.

Each `advance-reasoning` transition performs one bounded phase:

1. Resolve pending returns or invalidations against their dependency versions.
2. Extend the known work frontier within the discovery allowance.
3. Determine eligible work and whether the requested result already has sufficient justification.
4. Reserve a bounded quantum and publish its selected operation and expected return footprint.
5. Either perform a local cognition step, emit one catalog invocation, or retain a waiting/terminal disposition.

The fixed host driver transports only the emitted invocation and advances the retained call. It does not choose a packet, solver, hypothesis, evidence source, or answer. The ordinary CLI, owner, and Qwen workbench must all consume this same episode transition. The initial executor uses one active invocation chain; logical alternative scopes can be interleaved without introducing independent adaptive workers.

| Episode phase | Meaning and next transition |
|---|---|
| `ready` | No outstanding child; bounded discovery, checking, or selection may run. |
| `reserved` | Selected operation, allocation, predecessor versions, and return target are published; dispatch may be retried only with that identity. |
| `running-child` | The existing protected frame owns the callee and suspended parent. Resume that frame, not a newly constructed child. |
| `checking-return` | Validate and consume the exact returned output once, then update eligible dependent work. |
| `waiting` | An identified observation, authority, or external-effect reconciliation is missing. Preserve the wake condition. |
| `terminal` | Publish the justified result or typed limitation with remaining obligations and retained continuation where applicable. |


Version the closed invocation-frame and child-return schemas to retain the selected operation identity, canonical request digest, source/predecessor versions, reservation identity, expected read/write/return footprint, phase, and consumed-return disposition. The current `call()` combines frame creation, child scheduling, and advancement and has no persisted `reserved` phase; the packet path must split durable selection/reservation from dispatch. Existing owner publication remains the atomic boundary. A resumed operation validates its recorded reservation and frame instead of manufacturing a fresh call.
Episode phase is execution control, not an epistemic verdict. A child `halted`/`done` status says that an operation ended. The enclosing result preserves Section 7's exact/estimated/alternatives/conflict/missing-evidence/exhaustion distinctions and the child's actual limitation. `finish-reasoning` must not turn all completed children into “supported,” or every failure into a generic support gap. It publishes an already assessed episode and performs no hidden additional search.

For numerical children, the invocation declares the source workspace version, permitted output roles, maximum output size, arithmetic profile, and return binding. The initial return exports the complete selected successor as a bounded owned value that remains reachable after caller restoration. A later immutable typed region reference may replace that copy only with the same lifetime and validation guarantees. For the resonant workspace the export includes all phase-space words and necessary profile, objective/binding, clock, phase, integration, and ledger data. Readout metrics or `task_sha256` alone are insufficient.

The parent validates that export and its predecessor, then publishes the successor workspace binding and dependent semantic results once. A paused parent retains an immutable predecessor; the child owns the sole active successor proposal. A correctness-first implementation may copy the selected state inside the same image, with bytes and work charged. It cannot keep a live host `ResonantWorkspace` between calls or reconstruct a successor from diagnostics.

Return acceptance is one owner transition containing successor bindings, any new semantic records/assessments, reservation reconciliation, and the consumed-return marker. Validate required typed export fields, bounded dimensions/size, layout/codec/arithmetic identity, and predecessor dependencies before that transition. An opaque return dictionary with a valid outer status is insufficient. A repeated delivery observes the consumed marker; it cannot append another observation or apply the same numerical successor again.

An unfinished child retains its actual task, session/program position, numerical substep, trial state, and remaining allowance in the image. Exhaustion must not unwind and discard the only partial state. Return a bounded continuation export or retain the frame under an explicit resumable disposition. A terminal numerical fault preserves the last valid predecessor; an invalid trial is not a resumable valid workspace. Resume checks exact program, arithmetic, input, and authority dependencies.

The current one-child payloads and result wrappers need an explicit versioned migration when this path is implemented. Migrate ordinary callers and retained episodes into the unified path, or reject an incompatible continuation before mutation. Do not retain a hidden legacy executor alongside packet episodes.

### One episode allowance and bounded progress

**Specified mechanism.** The root episode owns lifetime limits for logical work, storage words, evidence reads, model calls, branch count, frontier size, and discovery/refinement depth. A child receives a reservation from the remaining root allowance, never a new copy of the original budget. `steps` bounds a scheduling quantum; it is not itself a proof that numerical work, copying, or nested work fits the lifetime allowance.

There is one authoritative reservation record for the episode in protected machine/invocation state, referenced by parent and descendants. Restoring a caller snapshot cannot restore an older allowance. The existing machine resource ledger records executed work; the episode record adds the bounded reservation/consumption partition and checks that descendants cannot bypass it. Semantic assessments may project these values but cannot become another mutable budget owner.

For each resource, admissibility requires `charged + outstanding reservations + requested reservation <= root limit`. Child charges reconcile the reservation once. Definitively unused reservation can be released; spent work remains charged after cancellation, failed speculation, correction, or a rejected result. Storage is bounded by live occupancy and reserved output capacity, with transfer/copy work charged separately. Refine operations reserve metadata and both child frontiers before replacing a parent frontier.

The root reserves enough control capacity to record a return, cancellation, or limitation. Work that cannot fit its worst-case indivisible quantum and result does not start. Internal publication checks and failed admission work are charged as work, although malformed requests cannot grant themselves access or enlarge an allowance.

`reserved` publication precedes dispatch. Recovery checks the retained selected operation, frame, return slot, and consumed-operation identity before advancing. A crash between return publication and parent consumption cannot apply the numerical delta, evidence, or charge twice. For external native work whose exact cost is lost, use Section 15's conservatively charged bounded-unknown reservation; reopening does not refund a lost measurement.

Exhaustion publishes an explicit limit and the remaining frontier. A larger allowance requires a new caller-authorized episode revision with lineage; repeated `advance-reasoning`, nested calls, or automatic reopening cannot reset it. Reopening after a new observation, revised premise, or new allocation states which condition changed.

### Baseline and packet-driven work selection

**Specified mechanism.** Eligibility is exact before priority is numerical. An eligible item has satisfied current prerequisites, compatible scope, permitted effects/write classes, a valid operation, and a quantum that fits the allowance. Invalidated results cannot be read or published while their repair is merely waiting in a priority queue. Waiting for new evidence is distinct from having available internal work.

Reuse [CassiFI §32.8–32.9](../CassiFI/FIELD-INTELLIGENCE-DESIGN.md#328-automaton-native-coordination-and-meaningful-topology): the existing field-owned work sites, exact eligibility/reason precedence, synchronous automaton cycle, versioned eligible/priority snapshot, integer priority key, queue-sequence tie-break, and every-`B`-th-dispatch oldest-ready service. Packet features are declared inputs to that same bounded lane/priority computation. They cannot form a replacement scheduler or bypass the automaton through a host callback.

The baseline uses the ordinary dependency-local frontier, discovery order, eligibility, and automaton profile without packet cues. Hierarchy-only supplies a fixed bounded depth cue for the same sites: `d_max - d(P)`, where `d(P)` is the shallowest support in a nonempty cover and `d_max` is the admitted layout's maximum depth. The packet-driven profile instead supplies the activation cue below. Both profiles specify how this input enters the existing lane law; the eventual priority is the law's output, not a host-side sort of raw features. Work without a numerical support remains eligible with the profile's ordinary input, not a fabricated numerical measurement.

The packet-driven method adds a dimensionless activation cue. Let `P` be a validated family of disjoint supports, `n_A` the port count of support `A`, `U` their unique port union, and `c^(A)` its complete coefficient array. With positive finite channel scales `s`, define

\[
a(P)=\frac{1}{|U|}\sum_{A\in P}
\sum_{\alpha\in\{q_+,q_-,p_+,p_-\}}
\sum_{j=1}^{n_A}\left(\frac{c^{(A)}_{\alpha j}}{s_\alpha}\right)^2.
\]

`P` must be a nonempty cover from one numerical workspace/source and codec, validated as an antichain of disjoint balanced-tree supports in canonical increasing-start order. Reject duplicate, ancestor/descendant, or overlapping support declarations before scoring; do not add parent and child norms. The denominator is `|U| = sum(n_A)`. Persist the canonical cover/index-domain identity with the score. Multi-workspace work splits into explicit numeric items or remains unscored. Serialized positive channel scales, units/frame, and codec version belong to the dispatch dependencies. The norm identity holds separately for each channel, and for the sum of their fixed-scale normalized squares, not for an arbitrary physical-energy metric.

Convert the activation cue to the existing bounded integer input with `floor(S * min(a(P) / a_clip, 1))`, where finite positive `a_clip`, positive integer `S`, arithmetic/rounding, and saturation rules are fixed in the versioned scheduling profile. Use bounded norm accumulation and saturate before overflow; nonfinite source coefficients still fault. The input and its source/codec/profile versions are included in the frozen priority snapshot. No probability or semantic-error interpretation is assigned to it.

Fairness, ready-age changes on blocking/requeue, stable ID ordering, and quantum size are exactly the canonical event-queue rules, with no second burst counter. A priority-only change cannot reset ready age. Discovery scans are themselves charged, resumable, fairly serviced work sites; a partial scan cannot declare the frontier empty. Dispatch rechecks snapshot versions and reservations. This is bounded service for continuously ready work, not a claim that every growing computation terminates.

The cue is consumed only from the exact source and codec versions named by the selection. Missing or unavailable cues retain their missing status and supply the disclosed hierarchy-only input to the same lane law; this is part of the selected policy, not a model fallback. A corrupted/nonfinite numerical result faults its numerical operation and records the limitation rather than silently posing as an ordinary missing cue.

Compute the full score only when its cost is covered. A bound, incremental aggregate, or cached ranking may replace it only if it is a reconstructible, version-valid view with the same declared selection semantics. The current packet helpers can reconstruct arrays and perform whole-source checks; selective execution and cheap incremental maintenance are additional work, not existing performance claims.

An acquired selection `Program` can supply bounded priority inputs for the same eligible sites using supported conditions, prior costs, and observed outcomes. Its proposal and assessment costs are charged. It cannot bypass the automaton or alter eligibility, fairness, premise checking, resource limits, result correctness, or authority. The initial implementation does not invent expected-information-gain probabilities or a trained external ranker.

### Cue admission and numerical accounting

**Specified mechanism.** A cue is a typed `Event` payload plus an `Obligation` continuation, not a new record family. It names the actual source event or completed operation, source epistemic kind, semantic dependency versions, codec/scales, destination workspace and ordered mode list, total numerical allowance, per-mode reservation, cursor, and consumed operation identities. Numerical source hashes and `event_kind` do not replace this identity. The existing impulse API has no consumed-event ledger; the new cognition/owner path must perform this admission and bind its cue identity to the invocation/return before dispatch.

The initial fixed codec has two inputs: consequence-work demand and constraint-work demand. For symbolic work they are exact Boolean features: an available prerequisite with a required consequence still uncomputed, and an outstanding required check or represented conflict. Each numerical replacement declares a finite residual `r`, matching units, and a positive finite scale `s`; normalize to `sign(r)` when `abs(r) >= s` and to `r/s` otherwise. This yields `u` in `[-1,1]` without overflowing the ratio. Boolean features use `0` or `1`. Both required inputs must be present; a missing input retains its mask and produces no impulse. Mixed-unit or incompatible-frame input is rejected before conversion.

The bounded vector `u = (u_consequence, u_constraint)` supplies `flow_signal`. This declares operational cue roles, not truth labels for Yang and Yin. Because `_packet_momentum_direction` normalizes its input, the codec separately maps magnitude to requested work: `rho_squared = (u_consequence^2 + u_constraint^2) / 2` and `W_cue = W_max * rho_squared`, with fixed serialized `0 <= W_max <= 1` in resonant energy units. The zero vector performs no numerical write. All work is reserved before deposition; a short allowance returns a resource limitation rather than silently changing the codec.

The initial mode list is one scale-mode deposit per disjoint active support, ordered by increasing start. Divide `W_cue` in proportion to each support's port count, using a deterministic remainder rule so allocated work does not exceed the total. An explicitly supplied/acquired program may select detail modes under the same declared budget and leaf rules. Preserve the original bounded vector, normalization scales, requested work, accepted work, and unit direction in the cue/return records; the current unit-vector-only receipt is not the full cue.

Requested shares sum to at most `W_cue`. Accepted kinetic work is checked with the explicitly accumulated energy-roundoff allowance; report that total rather than hiding repeated per-deposit tolerances. If normalized magnitude/work underflows to zero in the selected arithmetic, record the original nonzero demand and zero accepted work without attempting an unstable normalization. Its semantic obligation remains eligible.

Cue admission consumes each source-operation contribution and destination allocation once, with the cursor and accepted deposits committed together. Shared evidence references or parent/child views do not create extra inputs. An unchanged reread, replay, or duplicate return does not inject again; the low-level nondecreasing evidence clock cannot enforce that rule. Codec/layout migration transforms state or explicitly rebuilds a new numerical lineage rather than accumulating old history again as fresh evidence. Scheduling profiles count repeated internally generated work and cannot reinterpret it as additional observed support.

`packet-impulse` retains its existing restrictions: scale/detail mode, leaf-detail rejection, compatible profile, finite bounded work, nondecreasing evidence clock, and no insertion inside an initialized integration substep. Stage impulses at a valid integration boundary. The current per-call numerical `work_budget` in `[0,1]` is an energy-input bound under the resonant metric, not the episode's logical-computation allowance.

The implementation must add an explicitly internal `reasoning-work` event kind before using internally derived demands. That input retains the external evidence clock and records internal numerical work separately from observation work; a repeated derivation cannot manufacture corroborating observations. External observations advance evidence only through ordinary identified admission. Logical computation, numerical evolution, and world/event time remain separate clocks.

Energy balance includes stored Hamiltonian energy, applied impulse work, parameter/boundary changes, dissipation, and numerical residual/roundoff terms under the actual operator. Euclidean packet norm is not generally that Hamiltonian because mass and coupling metrics are nontrivial. Lower activation, lower energy, or phase agreement never settles a semantic obligation.

### Safe coarse-to-fine readout

**Specified mechanism.** Initial refinement changes what the reasoner inspects and executes; it retains the complete fine numerical state. The operation expands a parent support into children and exposes their declared work interfaces. It does not drop detail coefficients, zero inactive coordinates, or claim that Haar scale coefficients summarize a symbolic proposition.

A stopped refinement must justify the requested output, not merely report a small detail norm. For a declared affine readout on one fixed source, `y = b + w^T c`, first use the codec's invertible positive diagonal scaling `c = S_c x` and `v = S_c^T w`. Thus `x` is dimensionless and `v` has the output units. Partition inspected coordinates `R` and unresolved coordinates `D`. With valid upper bounds on the unresolved norms,

\[
\hat y=b+v_R^T x_R,\qquad
|y-\hat y|\leq B_D=
\|v_D\|_2\|x_D\|_2+\epsilon_{\mathrm{arith}}.
\]

The coefficient/readout frames and units must agree. `epsilon_arith` covers bounded codec, transform, dot-product, and bound-evaluation errors; use conservative outward enclosures or an explicit proved arithmetic allowance. For a threshold `theta`, decide only when the entire interval `[hat y - B_D, hat y + B_D]` is on the permitted side, with equality/tie semantics fixed by the output program. An estimate requires the caller's stated error tolerance. Ranking alternatives requires bounds on their differences; a single candidate's small norm is insufficient.

The bound provider is a fixed or certified field `Program` whose result identifies the complete source hash, basis/codec/readout versions, exact `D` index-set identity, norm upper enclosures, arithmetic allowance, and construction cost. The initial provider can compute those enclosures by full bounded scans with conservative rounding; every read and transform is charged. It makes no sublinear-work claim. Reuse is permitted only for matching source and index-set identities. Removing a reconstructible enclosure forces recomputation, never a different justified result.

A future incremental provider must preserve the same enclosure guarantee and invalidate/update it at each relevant mutation. The standalone packet validator has no live-workspace argument, so the consumer must also compare the packet's source to the current workspace before use. An infinite, unavailable, stale, or decision-crossing bound triggers refinement, recomputation, or a typed unresolved result.

For nonlinear readouts, use a justified interval/Lipschitz/domain enclosure or execute the finer operation. For symbolic outputs, use exact dependency, proof, guard, and constraint semantics. A low-amplitude exception such as a forbidden action remains decisive; a large irrelevant signal does not gain logical importance.

This instantaneous readout bound is not a bound on future evolution. Freezing unselected ports changes a coupled operator. The initial packet loop therefore runs an invoked numerical evolution with its existing full declared coupling and reports that cost. Selective numerical updates require a valid boundary treatment and temporal error bound, or a supported reduced operator preserving the declared responses. Static elimination can lose delay and resonance as described in [CassiFI §26.18](../CassiFI/FIELD-INTELLIGENCE-DESIGN.md#2618-scaling-without-losing-the-wave). Those optimizations are separate from selective semantic work.

### Alternatives, corrections, and shared work

**Specified mechanism.** Alternative assumptions own distinct semantic scopes and result dependencies. Share immutable current premises by reference; retain only branch-specific changes and work where the storage surface permits. A copy-based first implementation must report its actual cost. No hypothetical branch dispatches an external effect.

Reuse a completed operation across branches only when program, inputs, scope-dependent assumptions, authority/information boundary, arithmetic, and applicability are compatible. Share the result reference rather than count the same evidence or execution as independent support. Branch-specific numerical successors remain distinct proposals inside the image. `compose_helical_packets` applies only to ordered siblings of one exact numerical source; it cannot combine these different successors.

Parent conclusion rules state how children participate: conjunction requires every necessary checked child; a supported set retains its surviving alternatives; a conditional conclusion carries its branch assumptions. A branch cannot be pruned merely for low numerical activity. A precondition refutation, valid bound, or an explicitly disclosed incomplete search can exclude work under its respective semantics.

A correction or revocation invalidates affected uses before further publication. Traverse reverse dependencies in bounded quanta, retain the repair frontier, and reject stale reads even if full repair is unfinished. A returned child depending on an old binding becomes ineligible; its charged work and historical receipt remain. Unaffected version-compatible results can be reused. A wave mutation can require fresh numerical packet views while leaving unrelated semantic conclusions valid.

The current `_semantic_invalidate` walks records and dependencies in one call and raises at its frontier bound; calling it repeatedly is not resumable invalidation. Replace that use with a retained changed-root queue, seen identities, scan/reverse-index cursor, and repair frontier. The revised premise and invalidation barrier publish together. Until a relevant dependency closure is checked against that barrier, an old result is unavailable even if the breadth-first repair has not yet updated its immediate reference. This prevents stale transitive dependents from being read between quanta. A correctness-first bounded scan is allowed and charged; a reverse index is a versioned optimization, not an assumed existing capability.

With one active invocation chain, new observations wait in the existing owner queue. An observation or revocation relevant to suspended work triggers a bounded yield/cancellation boundary: retain the child's valid continuation, restore the parent, admit the queued revision, and invalidate before consuming any affected return. Reopening cannot overwrite the revised parent with its older frame snapshot. Fixed access revocation is rechecked at every protected read/effect boundary even before semantic repair completes. This specifies the interrupt path that the current snapshot-based caller restoration must implement.

### Acquiring interfaces, procedures, and selection methods

**Specified mechanism.** Acquisition extends the existing representation/procedure and self-development paths. It does not mutate the Haar transform or install new trusted checker semantics.

The initial bounded proposal vocabulary is: select/drop an interface role; add a required historical distinction; split a guard/regime; expose a finer support; compose two typed supported programs; parameterize a repeated computation; or propose a work-ordering rule within the fixed eligibility boundary. A candidate carries its parent versions, derivation, allowed output questions, retained expansion, costs, and unresolved applicability.

Assess an interface on future observable consequences before admitting their outcomes. Vary roles, parameter values, delays, distractors, omitted context, and compositions. An exact reduction requires a valid rule or certificate under its declared semantics. Empirical adequacy remains scoped evidence, including exceptions and uncertainty. A finite successful challenge set does not prove causal sufficiency. A separating case expands the interface, splits its regime, or narrows its domain without rewriting old predictions.

Store accepted meanings in `Program`/`Binding` records and assessment/failure lineage in the existing families. A compiled shortcut is a disposable executable view; removing it must preserve the logical transition and may increase measured cost. Reuse instantiates fresh arguments and checks current guards and dependencies. A memorized input/output waveform is not a parameterized procedure.

The existing skills have specific roles here: meditate proposes a working reformulation from admitted evidence; practice challenges an interface before feedback; reflect localizes a failed binding, missing distinction, or costly work order; consolidate retains supported shared structure and exceptions; study/investigate obtains authorized separating evidence. A candidate generated internally is not independent empirical corroboration.

Compare methods on equivalent targets, assistance, information, and total allocation, including proposal construction, checks, persistence, unsuccessful attempts, and maintenance. Acquired selection must help unfamiliar eligible tasks and preserve earlier required competence relative to the fixed method. Frozen-knowledge evaluation permits working state and packet scheduling while prohibiting acquisition of parameters, interfaces, procedures, or policy.

### Temporal organization and model-native use

**Specified mechanism.** Packet scheduling first serves the standalone semantic computer. It does not require a model, a new frequency bank, or one wave tick per language token. Existing exact bindings/events remain authoritative for known current facts and timestamps. Dynamic cues can prioritize ambiguous contexts or delayed obligations but cannot replace source identity or information-access history.

A proposed temporal interface declares the relevant delay/horizon, input/output ports, clock, mode operator, decay behavior, and aliasing limits. Compare its consequences and total cost with exact temporal records, delay buffers, and nonoscillatory traces. Period and decay rate remain independent; an unresolved obligation survives fading activity. Changing a temporal organization is an assessed representation/operator change, not an unrecorded parameter adjustment.

Native use follows Sections 11–15 after a portable operation is specified. Bind identified model measurements to a field-owned representation program, predict their task consequences, and assess a model-specific readout/intervention separately. Model layer, position, residual dimension, quantization/arithmetic, codec, and hook identities remain required. Contiguous coordinates of a model residual have no presumed semantic adjacency.

A packet transform can organize those measurements but does not establish semantic alignment, an inverse native map, a safe recurrent input, or native continuation. Preserve norm/direction distinctions where required. Compare any claimed dynamic benefit with static, no-evolution, reset, and matched-strength controls, including collateral failures. Base-weight edits and automatic fallback are outside this extension.

### Packet reasoning demonstration and comparison

**Specified mechanism.** Extend the continuing-world workshop/software scenario in Section 26. One episode interleaves several jobs, distinguishes requested/acknowledged/completed effects, receives delayed or stale reports, and answers a goal requiring connected temporal and resource consequences. Supply only authorized observations, legal actions, and the declared question; evaluator-only world state and future outcomes remain outside the learner.

Use a correction that changes one decision dependency, an independent completed subproblem that should remain reusable, and a low-activation decisive exception. Interrupt once with real unfinished numerical work and once after child return but before consumption. Reopen through the ordinary owner and finish without a host-restored workspace, duplicate cue, repeated observation, or renewed lifetime allowance.

| Comparison | Held fixed | Question answered |
|---|---|---|
| Existing dependency-driven reasoning versus hierarchy-only | Same ordinary frontier/discovery/eligibility contract, program and cue-source access, output semantics, and root resources; each arm pays for what it actually uses. | Does packet organization improve work locality beyond the existing local baseline? |
| Hierarchy-only versus live packet priority | Same hierarchy, cue inputs, eligible operations, fairness, and available evidence. | Does consuming evolving numerical state improve task completion or cost? |
| Live priority versus no-evolution/static priority | Same packet allocation, cue admission, and declared total resource cap; include all actual costs. | Does evolution help beyond an accumulated static working signal? |
| Live priority versus shuffled ranking cues | Exact semantics, bindings, scopes, and numerical norm/distribution preserved; shuffle only the cue-to-priority association. | Is useful work selected because of task-aligned cues rather than arbitrary ordering? |
| Fixed method versus acquired interface/procedure/selector | Comparable tasks, assistance, information, and total acquisition-plus-use accounting. | Does learned structure transfer and repay its cost? |

Each comparison is an isolated evaluator branch from an identified predecessor, not cooperating copies in production. Report unmatched resource usage rather than secretly equalizing by giving one arm free computation. The hierarchy-only arm need not pay for unused wave evolution; a matched-overhead diagnostic arm can be added and labeled separately. Do not weaken the baseline by forcing it to scan unrelated records.

Separate a scheduling diagnostic from the continuing-world comparison. The diagnostic replays an identified exogenous observation tape and codec with equal information access; fixed-profile wave ticks occur at declared committed dispatch boundaries, not wall time. Internal computation cues remain consequences of each arm's actual operations and are not replaced by another arm's results. The static arm admits the same kinds of available cues but performs no wave evolution. If numerical evolution also supplies task predictions, use a separate matched task solver in every arm so this diagnostic changes scheduling alone.

The continuing-world comparison instead couples the same accessible initial conditions and exogenous world process while permitting different actions and resulting observations. Do not force one arm's action-dependent feedback into another arm or call their divergent information histories identical. Attribute this result to the complete policy and report acquired information and all costs. Temporal-interface comparisons additionally vary held-out delays/horizons and include nonoscillatory traces or delay buffers with coverage, abstention, and collateral-error measurements.

**Model-native schedule input.** The native latent harness accepts a per-token coupling schedule (`--schedule PATH`), so a packet or temporal consumer can spend a fixed injection budget at chosen decodes instead of uniformly: holds run at `alpha 0`, which the identity control leaves byte-identical, and a zero-step entry is refused. Every arm reports its applied `coupling_budget`, and the comparison refuses a run whose scheduled arms do not match the constant reference budget. The first matched-budget placement measurement, on six 0.8B cases, responds only for `delayed_cue`: the constant arm, a single coupled decode at position 0, and a front-loaded window all answer correctly with the same eleven changed tokens, while the same budget spent from position 1 onwards changes nothing. Placement decides that case and the constant arm spends eight times the coupling it needs, which is the headroom a packet scheduler would have to capture.

Report correct results, incorrect results, justified alternatives, waiting/abstention, exhaustion, prior-skill regressions, actual logical work, operator applications, bytes touched/copied, peak live words, persistence overhead, and latency. Show how many operations and outputs actually consumed packet readouts. A changed packet hash, fewer tokens, or a zero-mismatch report with no attempted comparisons does not establish useful reasoning.

Completion requires actual ordinary-path execution of state handoff, finite root budgeting, return-once recovery, dependency-local correction, nonvacuous coarse-bound decisions and forced refinement, source-bound composition refusal, preserved hypotheses, and prior-skill behavior. Prove refusal/check paths with plausible corrupted or stale real artifacts where practical. Use focused transition regressions and the real scenario, not a new large suite of wiring assertions.

Useful packet scheduling requires either less measured total work at matched adequacy or better adequacy under matched allocation, with per-family and worst-family outcomes retained. Useful dynamics additionally require an advantage over the static/hierarchy comparisons. If only hierarchy helps, retain the simpler method. Interface learning, temporal benefit, and native semantic control remain separate measured claims; none is implied by mechanical completion. Implementation dependencies are specified in [Section 29](#29-implementation-sequence-and-completion-evidence).

## 9. Mathematical and computational foundation

Cassi's fixed numerical kernels and exact program machinery have complementary roles. Numerical activity can represent relationships, estimate admissible values, and allocate work; exact logic governs identities, dependency validity, instruction behavior, evidence checks, and authority.

For a declared linear relation family with observations `H z = o`, choose `H z0 = o` and let the columns of `N` span the permitted nullspace directions, so `H N = 0`. A local workspace preserves those observations through `z = z0 + N y` and has energy

\[
\mathcal E(y)=\tfrac12(A(z_0+Ny)-b)^T W(A(z_0+Ny)-b),\qquad W\succeq0.
\]

With constant symmetric positive-semidefinite `Gamma` and `dy/dt = -Gamma grad E`,

\[
\frac{d\mathcal E}{dt}=-(\nabla\mathcal E)^T\Gamma(\nabla\mathcal E)\leq0.
\]

This is a local descent statement for fixed premises and the stated family. It does not show that the premises are true, the minimum is unique, a discrete algorithm converges under any step size, or semantic structure discovery is solved. Rank deficiency preserves uncertainty. Contradictory constraints remain contradictions even when an approximate residual is small.

Wave or resonant operations may add a distinct working mechanism under their own arithmetic and stability conditions. Exact constraints do not become soft because an oscillatory state crosses them. Any combination uses the same constrained coordinates and a specified energy/accounting rule; no global convergence theorem is assumed for arbitrary symbolic, hybrid, or open-ended reasoning.

The fixed computational vocabulary must support bounded resolve/bind, scoped branch, relation evaluation, factor/constraint propagation, numerical refinement, checked program execution, evidence resolution, and assessment. Larger methods are stored programs with field-owned frontiers. The automaton prioritizes eligible work but cannot change what constitutes a valid proof, observed fact, or authorized action.

## 10. Adapter capability and identity surface

The adapter is a fixed boundary implementation, with its complete capability description bound into each session. It is not a learned router. Different adapters may expose different subsets:

| Capability | Required declaration |
|---|---|
| Structured request/result | Message schema, encoding, accepted tools, output ownership, streaming and cancellation semantics. |
| Activation observation | Exact graph site, pre/post operation meaning, layer, position, shape, dtype, normalization, and synchronization. |
| Latent intervention | Exact write site and allowable shape, bounds, scheduling point, affected downstream state, and trial/commit behavior. |
| Trial state | Whether native fork, rollback, accepted-prefix recovery, or exact snapshot is available. |
| Selective computation | Named static/model operation, loaded/touched resources, output semantics, and the ordinary work it bypasses. |
| Emission boundary | Who selects and commits bytes/tokens, tokenizer identity if used, and any remaining native head/sampler. |
| Persistence | Exact native restore, deterministic replay, or neither; numerical determinism class and backend dependence. |

Identity includes the model artifact, architecture/tensor inventory, tokenizer and chat framing, quantization, runtime and build, hook implementation, arithmetic profile, context/sequence policy, and adapter version. A family name is not sufficient. A changed quantization or backend can invalidate an acquired intervention mapping even when dimensions match.

The abstract adapter operations are describe capabilities, open a scoped instrument session, execute a named request, expose identified measurements, stage a supported intervention, accept/discard a trial, checkpoint/restore when supported, and close. These are design roles to implement in the existing host/native surfaces, not assertions that new public endpoints already exist.

Text-only APIs can participate in nonverbal Cassi reasoning through structured results. They do not acquire read/write activation support. A final pooled embedding is not silently substituted for a requested layer-input residual. Unsupported model architectures remain unsupported for that native capability; they may be used only through a separately selected supported profile.

Model measurements and intervention requests require bounded, versioned envelopes:

| Envelope part | Required content |
|---|---|
| Routing | Principal, session, episode, operation, sequence, trial ancestry, accepted prefix, and producer identity. |
| Meaning | Observation or actuation kind, exact site/frame, semantic question and representation references, and epistemic attribution. |
| Numeric payload | Shape, strides/order, dtype/endianness, finite-value policy, masks, normalization and retained magnitude, payload byte length/hash, and owned buffer lifetime. |
| State dependency | Field predecessor, native predecessor if applicable, adapter/model/profile/catalog identities, and affected read/write versions. |
| Work and completion | Maximum work/allocation, result or continuation identity, explicit status, and consumed/cancelled/committed disposition. |
| Trust | Evidence/access scope and required capability; no payload-supplied authority upgrade. |

Envelopes are wire or regional payload schemas, not additional adaptive record families. Validate lengths, products, bounds, identity, and access before allocation or field mutation. Inline bytes and referenced device buffers have the same logical meaning; a buffer cannot be reused before its declared consumer or copy completes. Nonfinite data may be retained as exact fault evidence but cannot enter a numerical evolution as an ordinary valid measurement.

## 11. Observation and semantic alignment

For model `m` at site `l`, the boundary observes

\[
o_{m,l,t}=C_{m,l}(h_{m,l,t},\iota_{m,l,t}),
\]

where `C` is an identified fixed codec and `iota` supplies model, sequence, position, arithmetic, and source context. A codec may normalize, partition, or transform measurements only with declared information loss and the metadata needed for its stated inverse. Normalized direction and magnitude remain distinct observations where magnitude is retained.

A field-owned representation program `R` interprets these measurements for a stated question:

\[
(F_{k'},r,s)=\operatorname{Run}_K(F_k,o_{m,l,t},q,R,b).
\]

The learned content of `R`, its applicable frame, evidence, and candidate frontier live in `F`. Fixed boundary code does not quietly contain a learned encoder, projection head, semantic classifier, or answer table. The result `r` need not be a dense vector: it may be a relation, typed program result, admissible set, or unresolved obligation.

**Research question.** Acquiring high-dimensional, behaviorally meaningful activation correspondences without a parallel neural learner is not solved by a lossless codec. Initial representational families can be sparse affine maps, guarded local relations, finite alternatives, and typed compositions already compatible with field execution. Their dimension, rank, support, numerical conditioning, and unresolved distinctions must be exposed. Extending them is a field-native algorithmic requirement, not permission to introduce a conventional trained sidecar.

Alignment acquisition follows an operational procedure:

1. Collect identified model measurements on tasks with independently available observable consequences.
2. Propose a bounded correspondence from a recurring unresolved distinction, not from a hidden answer label or test filename.
3. Store its program, frame, assumptions, and construction evidence as a candidate in the field.
4. Predict an observable consequence on new cases before admitting their outcomes.
5. Where intervention access exists, test a declared change against static, no-evolution, reset, and matched-strength controls.
6. Retain the mapping only for its measured domain; preserve exceptions, alternatives, and uncertainty outside that domain.
7. Use the acquired mapping in ordinary reasoning and demonstrate that removing it changes the expected downstream capability.

Transport reconstruction, semantic prediction, causal model influence, and useful task transfer are separate measurements. A map that merely reconstructs its inputs has not acquired a concept. Activation amplitude, coherence, proximity, or a teacher's confidence is not a truth score.

### Acquiring operations as well as readings

Acquire supported model operations alongside observation mappings. A portable operation states the intended semantic transformation, its preconditions, and what should remain unchanged. Role renaming may preserve corresponding relations; reversing two noncommuting instructions should change their composed consequence. These provide meaning-preserving and meaning-changing challenges over shared programs, rather than an assumption that nearby vectors have nearby meanings.

A model-specific intervention program is a candidate implementation of such an operation at an identified native site. Assess its predicted consequence, preserved distinctions, collateral behavior, and unfamiliar compositions through the ordinary paired execution and comparison rules. A round trip or one successful patch is insufficient. The portable operation can survive a model swap while its native implementation is suspended and reacquired.

The generative bubble view proposes operations or compositions; the constraining view constructs separating cases and checks their claimed invariants. The field owns these programs and their evidence. Success establishes scoped control over a computational instrument; any corresponding external-world claim still requires its own grounding.

## 12. Reasoning inside and around model inference

A supported result can return through several interfaces, with ownership kept separate:

- structured evidence, a computed answer, an executable plan, or a request for missing information;
- field-conditioned selection among model proposals, with the candidate-set restriction disclosed;
- a declared residual/state intervention;
- additional bounded field deliberation before native continuation;
- field-owned emission through a fixed boundary;
- selective replacement of a native computation by a field-owned result.

For a residual interface, let `P` be a field-owned readout program and `D` a fixed model-frame codec:

\[
\delta h=D_{m,l}(P(F_{k'},r)),\qquad h'=h+\alpha\delta h.
\]

`P` is not assumed to be the inverse of `R`. Its correctness question is the consequence of consuming this readout in the named model/site/domain. The intervention binds its applicability assessment, numerical limits, model dependencies, and exact result source. Ineligible output causes an explicit no-intervention result or episode limitation according to the declared session policy; it never silently substitutes model reasoning for a field-owned computation.

A norm or amplitude bound protects a stated numerical region; it does not prove semantic correctness or safety. Model-internal recurrence, soft-token continuation, KV edits, or re-entry into layers require a supported execution rule and separate measurement. An arbitrary hidden state cannot be fed back as though it were a trained token position.

Depth, token, and field clocks are independent. Additional field work may improve a decision without producing a language token, but it still costs computation. An LLM may also contribute genuine pretrained reasoning. Receipts attribute each operation to its actual executor rather than relabeling the entire answer as field reasoning.

## 13. Field-owned emission and native displacement

The design must support the complete ownership progression, not stop at additive steering:

| Ownership capability | What becomes field-owned | Required displacement evidence |
|---|---|---|
| Useful causal steering | A consumed correction or state/route decision. | Zero displacement is permitted and reported as zero. |
| Selection | Choice among identified alternatives. | Whether any candidates, output rows, or native selection work were actually avoided. |
| Emission | Committed output through acquired field constructions and a fixed deterministic boundary. | Remaining native head, sampler, tokenizer, and model dependencies are named. |
| Retained state | Memory and recurrence used for owned work. | Native KV/recurrent/context bytes actually removed, compressed, or avoided. |
| Procedure/layer computation | A field program produces an owned intermediate or replaces a native operation. | Executed versus bypassed graph work and actual resource access. |
| Inference ownership | The field chooses work and emits results without a full mandatory model forward. | Which model resources remain loaded, called, touched, or unnecessary. |

No blended native logits count as field-only emission. A deterministic field transducer can operate over bytes or declared event codes; acquired language constructions live in field records. General productive language through this boundary is an acquisition objective, not an implication of exact replay.

When the field owns a route, idle native paths should be bypassed or unloaded rather than retained as a silent warm fallback. Static matrix access is an explicit operation with dimensions, resource identity, and cost. Skipping a transformer block while executing an equivalent amount of hidden host computation is not a demonstrated efficiency gain.

Persistent edits to base model weights are not required by this design. The base artifact remains recoverable; any separately authorized derived-model experiment has a new identity and does not replace the field's knowledge owner. Learned Cassi program/state change is the primary persistent adaptation path.

The [whole-machine performance design](../../CASSI-PERFORMANCE-DESIGN.md#field-acquisition-of-qwen-graph-work) specifies the shared graph-site invocation, four initial specialist working regions, native state and field-owner publication boundary, compatible candidate batching, and measured displacement path for acquired Qwen computations. The combined routed/shared expert contribution now has an owner-held method and a resident Qwen bypass with a coherent next hidden state; that site has been exercised against a real model with its native expert work counted. The same contract extends to complete recurrent branches and other supported graph work without creating a separate adaptive owner.

## 14. Native regional execution and lowering

The current float64 regional image and historical float32 Qi tensor have different semantics. Integration requires explicit numeric-region codecs and bounded executors; it cannot reinterpret exact identities or instruction words as native wave amplitudes.

The source of truth is a field-resident typed program and its operands. Native execution may use CPU or GPU pages, compiled kernels, and disposable address maps. Each lowered operation declares:

- input region references and versions;
- writable regions and maximum allocation;
- arithmetic profile, rounding, tolerance, and determinism class;
- maximum indivisible work and resumable continuation;
- readout/result layout and meaning;
- evidence, effect, and publication behavior;
- required backend capability and synchronization point.

Exact structural fields remain exact. Float32 numerical work can be represented through a declared codec in an otherwise float64 image; round-trip and arithmetic semantics must be explicit. CPU/GPU agreement within a bound is not byte-identical cross-backend replay. A profile change requires explicit migration or a new execution branch.

Only a missing irreducible bounded primitive justifies a catalog extension. A native entry point that secretly runs an entire unbounded solver, planner, or learned model reinstates a second interpreter. Numeric recurrence that is genuinely useful can become a kernel over owned regions; the semantic graph must actually consume its result.

Procedure compilation derives an executable cache from the field program, catalog, target, arithmetic, and dependency versions. Its guard checks are part of execution. Changing any semantic dependency invalidates or revalidates that cache. Compilation itself cannot promote an empirical approximation to exact equivalence or change fixed authority/checker rules.

### Resident cognition and nested computation

`submit()` is admission of a new resident task, not a generic subroutine call. A reasoning episode must not overwrite the resident cognition task to run an exact solver, a numerical kernel, or an LLM-facing operation. Serializing cognition into a private host dictionary and restoring it afterward would create an unowned continuation and fail interruption safety.

Extend the existing field instruction/program path with field-owned invocation frames. Each frame binds the caller program and return continuation; callee catalog/program identity; typed argument and state references; argument mode (versioned reference or explicit immutable snapshot); result binding; scope; read/write/effect footprint; reserved allocation; and lifetime work allowance. Parent beliefs and knowledge remain resident. A callee owns only its declared workspace and cannot mutate its caller through an undeclared alias.

Scheduling may pause a caller while advancing the callee in bounded quanta. The same field image retains both frontiers. Completion validates dependencies and publishes the result binding before waking the caller; failure publishes a typed result or retained obligation. Cancellation and rollback invalidate descendant work without reclaiming its charged budget. External model requests yield obligations and return through admission; they are not recursive unmetered callbacks.

The initial implementation can keep one active invocation chain and bounded depth. Concurrent independent episodes require explicit frame/region allocation and owner scheduling rather than one `LearningComputer` per cognitive function. Existing persistent memory cannot be displaced by admitting a temporary task. A generic goal-directed episode driver that selects these calls through acquired/fixed field programs is an implementation requirement; a host-supplied answer-specific call script remains assisted execution.

## 15. Coupled transaction, speculation, and crash recovery

There is no atomic hardware operation covering GPU memory, a file checkpoint, a model service, and network output. The integration uses staged local work, one durable publication decision, and explicit effect reconciliation.

**Implemented basis.** The native apprenticeship API has token acceptance and rollback primitives, but its field checkpoint does not itself contain a general resumable native model/KV state. Those local mechanisms can be reused where their contracts apply. They do not already implement the regional/native paired publication specified here. Deterministic sampling settings alone also do not prove reproducible native continuation.

A coupled operation identifies its predecessor field root, native execution root when present, principal/session/sequence, token prefix, graph site, trial ancestry, and operation ID. A measurement identity additionally distinguishes the capture definition and all positions it represents. Re-evaluation of the same committed event is replay, not fresh learning.

### Local coupled operation

1. Resolve capabilities, dependencies, authority-sensitive premises, and resource reservations against the predecessor roots.
2. Stage field changes in a scoped continuation and native changes in an isolated trial/snapshot where the adapter supports them.
3. Execute bounded work. No speculative token, action, or learned update is externally published as accepted.
4. Synchronize all required native results and validate the complete affected closure, including numerical and sequence checks.
5. Persist the immutable field successor and any native execution snapshot required by the declared continuation class. Seal their hashes, accepted prefix, and publication intent.
6. Publish one durable coupled commit record through the owner-controlled operation journal. It references the complete predecessor/successor pair; it is the acceptance decision.
7. Expose committed tokens or effect proposals with stable delivery identity. Acknowledgment is a separate event.

The journal contains nonlearned execution identity and commit control, not another learned state. If a process dies before the commit decision, staged work is unaccepted. If it dies after commitment, restore the referenced pair and reconcile delivery; do not execute or learn the same committed event again.

Work reservations must be durable before launching a quantum whose cost must survive a crash. On successful completion, reconcile the reservation with measured work. If a crash prevents an exact measurement, retain a disclosed consumed/unknown interval bounded by the reservation, conservatively charge the admitted allowance, and never report an invented exact counter or refund it as unspent. Batch publication may amortize persistence only when it preserves the declared acceptance and externally visible prefix.

If the native adapter cannot snapshot or reproduce its state under the declared determinism class, exact paired resume is unavailable. The field remains recoverable, but continuing model inference requires an explicit new instrument branch with a new identity. Reconstructing a prompt from memory is not an exact native restart. A remote text service normally supplies only its stated message/delivery semantics, not local activation transaction guarantees.

### Speculative decoding and partial acceptance

A speculative suffix has a scoped field overlay and corresponding native trial lineage. Rejecting the suffix removes its semantic eligibility; spent work remains charged. Accepting a prefix publishes only effects justified by that prefix and retains or discards the suffix explicitly. A prefix that cannot be isolated by the current native state API cannot be claimed as independently committable; use non-speculative execution until a valid mechanism exists.

Each accepted logical event is admitted once, including when prefill is split across batches or decoding retries a batch. Rejected activations can be retained only as explicitly scoped computational trial evidence if the experiment permits their later use; they are not accepted conversation/world observations. Such retention must not alter an identity-control path accidentally.

An intervention must precede the dependent native state update or force explicit recomputation from a suitable predecessor. Editing a residual while keeping downstream KV/recurrent values from another trajectory produces an invalid mixed state. Already delivered tokens are immutable observations of what was sent; later revision is a new message, not rollback of the user's history.

## 16. Scheduling, clocks, concurrency, and liveness

The computer distinguishes world/event time, Cassi logical transitions, and native model positions/depth. Wall-clock time is a measured cost and an external deadline condition, not an undeclared semantic input. If elapsed time affects a model, it enters as an identified observation.

The existing event queue and automaton schedule eligible work over field-owned dependencies. Every running task has a retained frontier, bounded quantum, allocation, and completion/waiting reason. A numerical oscillation, repeated proposal, or inability to settle cannot monopolize the computer indefinitely. Fairness and lifetime limits remain fixed rules even when priorities are learned.

One principal's field can host multiple scoped tasks. Concurrent native workers use immutable predecessor views and propose bounded deltas; the owner serializes publication or applies explicitly verified nonconflicting updates. Computed results never commit against stale dependencies merely because the backend completed successfully. Independent user principals have independent fields and access boundaries.

Batching must preserve per-sequence identity, clocks, hypothesis scopes, and commit order. Reusing a slot cannot expose the prior sequence's numeric or semantic state. Eviction records whether model state is cached, reconstructible, or unavailable. Replica reconciliation is publication of checked deltas to one root, not averaging independently learned fields.

A paused model callback must not recursively request work from the same unavailable model execution lane. Inquiry can yield a retained obligation, request a separately declared instrument, or continue internal work. The design requires an explicit re-entrancy and wait-dependency policy rather than a hidden nested model call.

External effects cannot occur from a hypothetical branch. Pending dispatch and acknowledgment use the owner lifecycle. When an effect may have occurred before a crash, reconcile it through its stable operation identity; never retry a non-idempotent action simply because an acknowledgment is missing.

## 17. World modeling, inquiry, and multimodal grounding

The shared field maintains learned mechanisms, current beliefs, hypothetical alternatives, and assessments as linked semantic views. It must distinguish the environment from the instrument observing it, and prediction from intervention and counterfactual questions.

A model activation is an observation of a computational instrument. Pixels, text, audio samples, tool results, and numeric tensors are observations through their own fixed codecs and provenance. An LLM-proposed object identity or causal explanation is an attributed hypothesis. It can help search, but cannot replace direct correspondence evidence or an observed action consequence.

Mechanisms may combine categorical modes, continuous parameters, delays, relational state, and observation processes. Unobserved alternatives remain represented when they affect the question. Identical current observations can correspond to different histories; predictive-state compression must preserve distinctions required by future questions and reopen when a new action or outcome separates them.

Planning uses the same mechanisms and beliefs as prediction. It composes guarded procedures, explores reversible scopes, accumulates model uncertainty, and selects actions or observations whose consequences matter to the user goal. It repairs affected unexecuted segments after revision while preserving acknowledged effects and unrelated knowledge.

Inquiry chooses among retrieving, calculating, refining a hypothesis, obtaining a model measurement, making an authorized world observation, waiting, or reporting a limitation. Initially these choices can use supported-set decision relevance and measured costs rather than invented probabilities. Learning the inquiry policy is itself field-owned and assessed on actual consequences.

CassiCosmos can supply one world adapter, but a successful socket or tensor transfer is not grounding. A cognitive result requires pre-outcome prediction, authorized interaction, actual outcome, shared revision, and transfer on unfamiliar situations. The universal reasoner has no mandatory Godot dependency or requirement to simulate physical PDEs for every language token.

## 18. Acquisition from experience and model instruments

All learning follows identified evidence and the existing owner path. It can acquire parameters, useful state distinctions, representation programs, causal/mechanistic hypotheses, grounded constructions, reusable procedures, and computation preferences. Candidate priorities and search frontiers are learned state in the field; fixed kernel semantics are not a learned sidecar.

The learning episode binds available information, candidate versions, chosen work, pre-outcome prediction, actual result, attribution, cost, and update identity. The observation is admitted once. Replay and additional transforms may consume work but do not multiply independent support. Hypothetical outcomes stay hypothetical.

Teacher acquisition has separate possible claims:

| Evidence | Permitted claim |
|---|---|
| Exact teacher token/activation trace | What that identified model did on that identified input. |
| Exact learned trajectory replay | The field retained and reproduced a demonstrated route. |
| Independently checked solution | Correctness relative to the checker and admitted premises. |
| New parameters/roles/compositions | Transfer within the independently exercised domain. |
| Successful world prediction/action | Scoped empirical competence under accessible information. |

A teacher can propose a candidate program or alignment. That proposal still passes fixed parsing/type/effect checks and prospective assessment. Teacher agreement, repeated self-critique, or many samples from the same model does not become independent world evidence. Outcome labels unavailable to the learner cannot be used as hidden alignment features.

The maximal acquisition objective is to extract reusable computational structure, not just outputs: relations, representations, guarded procedures, recurrent sufficient state, and emission constructions that work on new inputs. Static model operations may provide observations during this process, but remain attributed external computation. Whether such field-native acquisition can capture broad LLM competence at practical cost is a central research question.

### Counterexample-directed acquisition

When several candidate relations or programs fit the admitted evidence, construct an accessible input or permitted observation on which they predict different consequences. The question is selected before its outcome is obtained. The owner then admits the result, preserves its provenance, and revises the surviving candidates. A teacher can propose a separating case, but does not become an independent correctness oracle merely by also supplying its answer.

Use exact execution or an independently observable consequence where available. Keep unknown outcomes and missing alternatives explicit: one survivor in a bounded candidate set is not a proof that the world belongs to that set. Inability to find a separating case under a finite allocation is unresolved search, not established equivalence or non-identifiability.

Retain useful counterexamples as support for guards and future challenges. Compare the acquired method with equal-cost passive examples and source exposure on unfamiliar cases. More rejected candidates alone is not competence; the acquired distinction must improve an ordinary supported use.

### Complete-capability extraction

A computational capability can be acquired end to end even when its native realization spans many layers. Bind its typed arguments, supported domain, evidence, checks, and output meaning; acquire a field program that performs it on new instances. This is a complementary route to operation/layer replacement, not a requirement to imitate every hidden coordinate before gaining independence.

Separate supplied formalization, model-assisted interpretation, owned computation, and realization. Exercise the owned computation with its native route unavailable under the declared profile, then intervene on the acquired field program to test the predicted selective loss. Include acquisition and maintenance cost. Exact teacher replay, a copied model, and a field program with new-input competence remain different outcomes.

### Executable capability apprenticeship

**Specified mechanism.** Give the existing regional field computer one useful software capability to acquire, check, retain, revise, and execute on unfamiliar inputs. The first subject is a restricted part of SQLite over small nullable-integer tables. SQLite is unfamiliar to the initial field; the teacher may already know SQL. Extracting and checking that pretrained knowledge is part of the objective.

The learned capability is: given a supported query or acquired language request and a new input table, predict the exact result through a retained field program. During acquisition, a model proposes interpretations and SQLite supplies observed consequences. During teacher-disconnected evaluation, neither the model nor SQLite can solve the task for the learner. This design extends Sections 11, 18, 19, and 22; it does not add another cognitive owner or a preregistration process.

#### Subject and observable meaning

Use the system Python `sqlite3` binding with a fresh in-memory database for each observation. The implementation profile starts with one nullable integer column, zero through eight rows, integer values and query constants in `[-32, 32]`, and at most four nested SELECT scopes. These are development bounds, not claims about the limits of the field.

The query subset contains:

- projection of the column, an integer/NULL literal, or `COALESCE(column, integer)`;
- predicates `=`, `<>`, `<`, `>`, and `IS NULL` over the supported expressions;
- optional `DISTINCT`;
- ascending or descending ordering of the result column, with default or explicit NULL placement;
- nonnegative `LIMIT` from zero through eight;
- nesting to express changes in operation order.

Exclude joins, aggregates, multiple data columns, strings, floating-point affinity/coercion, user-defined functions, external databases, and arbitrary SQL. Result-column aliases and table names are syntax identities, not semantic features. WHERE operands resolve within the input scope; ORDER BY refers explicitly to the output column. Every sequence-scored query has explicit ordering, including an inner query before LIMIT. Equal sort keys have identical one-column outputs, so ties cannot change the result bytes. Any unordered extension must compare bags with multiplicity rather than trusting insertion order.

The official [SELECT processing](https://www.sqlite.org/lang_select.html), [NULL behavior](https://www.sqlite.org/nulls.html), and [COALESCE definition](https://www.sqlite.org/lang_corefunc.html#coalesce) are identified teaching sources. Preserve their actual source bytes and revisions at acquisition; their text is evidence about SQLite, not an executable definition supplied by the harness. Record the SQLite version, source ID, binding/library identities, and relevant configuration alongside each observed result.

A separating example uses input `[NULL, 0, 3, NULL, 3]`:

| Request | SQLite result, ascending |
|---|---|
| Replace missing values with zero, then remove duplicates. | `[0, 3]` |
| Remove duplicates, then replace missing values with zero. | `[0, 0, 3]` |
| Keep values for which `x <> 0` is true. | `[3, 3]` |
| Keep values for which `x = NULL` is true. | `[]` |
| Keep values for which `x IS NULL` is true. | `[NULL, NULL]` |

These distinctions require context-dependent equality, missing-value handling, preserved multiplicity, and composition order. An empty result is a successful computed value; it is distinct from unsupported interpretation or an unavailable observation.

#### Bootstrap, acquired content, and fixed boundaries

The initial field has the existing owner/lifecycle and generic computational primitives. It has no SQL operation-to-program bindings, query/result lookup table, domain-specific language constructions, or acquired model-feature mapping. The initial image and supplied library are recorded so any preinstalled competence is visible.

Represent a cell as an explicit integer or null tag; absent columns and malformed values are errors, not nulls. Represent a table as a bounded sequence of typed rows with an explicit length. The input/output codecs only validate and transport these values. A syntax-only parser may expose the restricted SQL syntax tree, preserving scopes, operators, and arguments; it must not lower SQL clauses to their correct field execution order.

The smallest required execution extension is bounded typed-sequence computation in the existing program machinery: indexing, length, construction, iteration, branching, integer operations, and comparisons on declared primitive types. Reusable map/filter/fold/sort/slice algorithms may be supplied as generic procedures with explicit predicate, equivalence, and ordering arguments. SQL null propagation, WHERE acceptance, duplicate equivalence, null ordering, clause bindings, and composition order must be acquired programs or acquired parameters of those procedures. No fixed `sqlite-select`, `sql-equality`, or whole-query answer kernel is allowed.

This is acquisition of semantic rules and their composition using supplied computational building blocks. It does not claim discovery of sorting algorithms or a new programming language from no prior machinery. A generic comparison may inspect a null tag; it must not silently choose SQLite's different equality rules for WHERE and DISTINCT.

The current `table` mechanism in [program execution](../CassiFI/cassi_field_program.py) matches exact `when`/`set` transitions. It is not a relational executor. Existing automatic mechanism induction can memorize observed transitions but cannot supply this new-input capability. Bounded typed-sequence execution and a compositional candidate generator are real implementation work. They belong in the current program/cognition paths, not a Python predictor around the field.

#### One adaptive owner and explicit evidence

All acquired SQL rules, language constructions, model mappings, candidate programs, search priorities, unfinished computation, and learning assessments live in the existing regional image under [the owner](../CassiFI/cassi_field_owner.py), [cognition](../CassiFI/cassi_field_cognition.py), and [LearningComputer](../CassiFI/cassi_learning_computer.py). Use the existing Value, Binding, Event, Program, Assessment, and Obligation records and resident invocation frames.

Raw source documents, teacher transcripts, capture arrays, and oracle responses are immutable evidence. They are not a parallel adaptive store. Host buffers may stage an observation but cannot own the candidate frontier or reconstruct unfinished reasoning after restart. This apprenticeship does not instantiate a second adaptive `RawEventStore`/`UniversalLLMInterpreter` beside regional cognition. Reuse immutable model identities and observation envelopes from the interpreter; retain historical runners as separately scoped evidence.

Each teaching exchange binds the actual input-table/query or utterance bytes, accessible source revisions, teacher/model/runtime/tokenizer identities, prompt bytes and token IDs, proposal bytes, capture identity if present, field predecessor, pre-outcome predictions, oracle operation identity, observed result, and accepted successor. Identities route and attribute events; filenames, case IDs, source digests, and withheld answer labels are excluded from predictive features.

#### Learning and deliberate study

The same resident episode performs the following cycle:

1. **Attempt.** Interpret the accessible question, execute current applicable candidates, and retain the result, alternatives, or named gap before seeing the outcome.
2. **Propose.** Admit a teacher's typed program or semantic edit as an attributed candidate. Reject malformed, truncated, out-of-language, or unauthorized proposals explicitly. Teacher confidence and self-critique cannot promote a candidate.
3. **Construct.** The field generates bounded well-typed variants: alter a tag guard, comparison, role binding, parameter, or operation order; compose supported procedures. Enumerate by structural cost with a persisted cursor. Constants come from the generic library, request, or admitted teaching evidence, not evaluator-only cases. Candidate programs cannot inspect case IDs or embed a table of example answers.
4. **Choose a study example.** Execute competing candidates on feasible small proposed tables without calling SQLite yet. Prefer an input whose predicted results separate the current candidates. The selected input and candidate predictions are published before the real observation is requested.
5. **Observe.** Execute the authorized SQLite query once, admit its typed result with provenance, and update support and counterexamples. An execution error or timeout is an unavailable observation, not a fabricated empty table or a negative training label.
6. **Retain.** Assess a candidate on separate learner-visible validation cases. Publish a scoped supported program through the existing promotion/dependency path; retain unresolved alternatives and known exceptions. Validation used for selection is not final evaluation.
7. **Use and revisit.** Apply retained rules to new arguments and compositions. Correct local support when new observations disagree, or stop with a specific limitation when no available candidate or affordable study action resolves the problem.

For table-valued candidate outputs, partition by exact canonical result equality; do not turn result hashes into numeric distances. Reuse the existing `_semantic_inquire` structured-alternative, affordance, and action-proposal lifecycle: minimize the largest surviving group, then risk, cost, and deterministic question identity. The missing work is executing candidate predictions and constructing their partitions inside the field. Only fully evaluated predictions enter that exact-partition API. A query with unknown candidate outputs remains unscored until those computations finish; an unknown candidate cannot be discarded or counted as eliminated. This is a supplied initial inquiry policy, compared against passive examples; learning a better policy is a later measured increment. The separate numeric interval-based `choose_inquiry` is not a table evaluator.

Begin with 32 teacher exchanges and 64 oracle observations per learning lifetime, at most 64 live candidates, 256 nodes per proposed program, and one explicit root allowance of 1,000,000 logical primitive operations and 64 MiB of persisted field state. Nested work shares those allowances. A scheduling quantum does not replenish them. Native proposal generation initially respects the existing adapter's verified 256-token ceiling; truncation is reported rather than completed by the harness. These are configurable engineering starting values. A revised allocation is a separately identified comparison; capacity exhaustion is not a finding that acquisition is impossible.

#### Three separately assessed request routes

| Route | Learner receives | Claim assessed |
|---|---|---|
| Typed query | Input table plus syntax-only SQL tree. | Acquired SQL semantics and computation, with parsing supplied. |
| Learned language | Input table plus an utterance; the evaluator's intended query is hidden. | Acquired request constructions and their composition. |
| Model-assisted language | Input table plus an attributed model interpretation of the utterance. | Useful instrument-assisted interpretation with field-owned subsequent computation. |

Train language constructions through existing `learn-construction`/`interpret` operations using identified utterance/role/meaning examples. Role annotation is teaching assistance and is recorded. Extend composition through acquired components; do not replace the workshop regex with a new hand-coded SQLite phrase parser.

Language assessment separates new role values, taught surface variants, new combinations of learned constructions, and genuinely unseen wording. Unsupported new wording remains unsupported. An evaluator-owned typed intent and an independently reviewed utterance establish language correctness; executing a model's proposed SQL only establishes what that SQL does, not whether it means the user's utterance. Model-generated paraphrases with the same model's approval are development material. Unreviewed natural-language items cannot support an independently grounded language score.

Teacher-disconnected claims are measured on the first two routes. The third remains visibly model-assisted. A typed-query success cannot be relabeled as autonomous natural-language understanding.

#### Actual teacher and native observation path

The model must receive the actual apprenticeship question, input data, permitted source excerpts, and declared proposal language. The native capture must come from that same prompt, before the teacher's first answer token and before the oracle outcome is available. Capture-on and capture-off instrumentation must preserve the teacher computation within its measured determinism class.

The current native universal-interpreter runner uses a fixed unrelated sentence and attaches the task question afterward. Its `FieldCoordinateCodec` hashes either an adapter key or an exact trace digest. A shared adapter key discards trace-content distinctions; removing the key still supplies an identity hash, not a numeric semantic representation. Neither path is a substitute for the acquired numerical mapping required here.

Reuse [LatentQwenInstrument](cassi_model_instrument.py) and [native capture](cassi_llama_capture.py). The latent instrument exposes residual envelopes and generated token IDs from one native execution; extend its result boundary to retain/decode the actual proposal bytes with the same tokenizer and exact generation identity. Verify the native copy point, token position, fresh-context policy, and capture definition from the executing source. A separately repeated capture and generation can be linked observations, but cannot be reported as one execution.

The first adapter target is the existing Qwen3.5-0.8B artifact and its supported native build. Use observation-only execution: no field substitution, latent injection, or learned recurrent native state in the teacher. Native scratch is reset between independent examples and is not continuing Cassi knowledge. If the small teacher cannot supply usable proposals, record that assistance limitation and assess a separately identified stronger-teacher profile; do not tune the teaching prompt on withheld failures.

For the native arm, lift finite captured numeric values into typed numeric regions without replacing them by hashes or packet identities. Lossless float32-to-float64 transport is permissible under a declared arithmetic profile. Any normalization or dimensional reduction is explicit, and retained magnitude remains available when claimed. Frame identity is a dependency; it is not a semantic label.

Start with bounded field-resident sparse coordinate comparisons and small affine/guarded readout programs. They predict a declared distinction between candidate interpretations or prioritize a separating study action. Their coordinates, coefficients, support, search history, and applicability are learned in the field. Independent executed outcomes may supervise acquisition after the prediction; withheld outcomes and model next-token targets may not. A useful readout must change an ordinary candidate choice, inquiry, or supported result. Its amplitude or input reconstruction alone earns no semantic claim.

The first readout target is concrete: whether the executed query result contains a NULL value. Derive that target from admitted SQLite output only after the prediction; include both outcomes for the same input table under different queries so input-null presence cannot solve the task by itself. Use the estimate to order candidate checks, never to promote a rule or discard a semantic alternative without execution evidence. The field owns the resulting scheduling state and learned readout. Full ordered-row equality remains the task score: predicting this one bit is a channel diagnostic, and native usefulness still requires better acquisition or lower total work on the complete capability.

Site selection and representational bounds use development examples only. Report candidate rank, conditioning, unresolved distinctions, and truncation. Failure of this small readout family is an expressibility result for that family, not evidence that model activations contain no usable meaning.

#### Comparisons that answer different questions

| Condition | Information and execution | Question |
|---|---|---|
| Cold field | Generic bootstrap; no acquired SQLite rules. | What already works without apprenticeship? |
| Text apprenticeship | Documents, teacher proposal text, and observed SQLite outcomes. | Does externally checked teaching produce reusable field competence? |
| Text plus native | Exactly the same teaching exposure plus identified before-answer activations. | Does native access improve acquisition or supported interpretation? |
| Shuffled native | Same text and numeric payload population, with trace-to-task pairing permuted across distinct semantic examples. | Is any native benefit carried by the trace's appropriate content? |
| Passive study | Text apprenticeship with a fixed example order instead of candidate-directed inquiry. | Does choosing informative examples improve learning? |
| Source-only model | The same model sees matched permitted sources/examples and answers new cases without learned Cassi state. | What capability and cost does the teacher already provide? |

First compare text/native/shuffled conditions on a common immutable teaching tape with identical text, source access, and oracle exposures. Native arms cannot receive additional teacher answers. A deterministic replay schedule aligns example order across conditions. A separate adaptive-study comparison permits different requested examples under equal oracle/model allowances; the changed experience distribution is the mechanism being measured, not an unnoticed confound.

Equal exposure and equal total cost are separate readings. Record teacher generation, activation capture/transfer, proposal checking, candidate search, oracle execution, field work, persistence, and held-out inference. Charge native capture to the native condition even if artifacts are shared for convenient replay. Also measure what text-only teaching achieves when allowed to spend the saved work. Reuse is beneficial only where measured inference savings eventually repay acquisition and maintenance; otherwise report the capability gain and its cost separately.

Content controls keep text fixed while changing only the trace-to-task association. Preserve appropriate shapes and model frames; avoid confusing gross norm or token-length changes with semantic pairing. Duplicate a numeric trace under changed nonsemantic IDs and require identical numerical readout, not necessarily an unchanged event ledger. A constant-trace control must expose loss of distinctions. Inspect pairwise output differences and singular values over genuinely distinct tasks; large trace rank does not by itself establish semantic benefit. Wrong teacher candidates must be rejected by actual execution, and a deliberately wrong oracle-result artifact must be detectable independently.

Do not add new output injections or graph-replacement experiments to this apprenticeship. The first native question is whether observation helps acquisition. Field-only output and later native actuation have different correctness and cost obligations.

#### Transfer, correction, and continuing use

Disconnect the teacher and oracle from the learner's executable capability surface before final inference. The evaluator runs SQLite in a separate process only after the learner has committed its prediction and cannot feed results back into that evaluation branch. Candidate code runs only through the bounded field language: no arbitrary Python, SQL, shell, or model call. Persisted source material is available only under the declared evidence policy; it cannot become an answer-execution service.

Return typed rows through a fixed deterministic serializer. A concise explanation names the acquired rule, source support, and relevant operation order. Language realization is assessed separately. Count all wrong answers, unsupported cases, ambiguous interpretations, refusals, and exhausted computations; a falling answer rate cannot masquerade as better accuracy.

Exercise the existing continuation and revision machinery on this actual capability:

- Pause with an unfinished candidate execution or a selected study observation awaiting admission. Reopen the same field and resume without replaying accepted teaching or repeating an oracle operation.
- In an isolated evaluation branch, revoke an acquired null/equality rule and expect the corresponding capability to disappear while ordering and unrelated retained knowledge remain usable. Restore the unmodified evaluation predecessor to establish recovery; do not erase continuing-life history.
- Correct a falsified semantic candidate through new SQLite evidence. Separately revise a request binding, such as changing the fill value from zero to two or explicitly requesting NULLS LAST, and repair dependent results without relearning unchanged SQLite semantics. A changed request policy is not a new SQLite version.
- Preserve a prior unrelated skill in the same regional owner and assess it before and after study, correction, and intervening work.
- Replace the actual small-model artifact with the existing Qwen3.8-27B artifact through a separately verified structured-instrument profile. Let the replacement interpret unfamiliar requests while the field reuses its existing portable rules with semantic acquisition disabled. Compare warm and cold fields with the same replacement model and permitted information. Changing only `ModelIdentity` does not count. Model-specific native mappings are suspended until separately learned/validated for the new frame; this step does not claim cross-architecture vector alignment.

#### Evaluation separation and useful outcomes

Partition construction, selection, and final evaluation by underlying table/query/intent roots. Held-out composition requires an unseen syntax/operation composition, not just new row values. Hold out language construction combinations and reviewed wording roots separately. Case renaming, multiple paraphrases, reordered dumps, and the same case across conditions are not independent successes.

The initial final-evaluation target is 40 independent roots in each of five strata: new values, new compositions/order reversals, null/duplicate/empty/limit boundaries, learned-language compositions, and correction/continuing-use challenges. Disclose each stratum's available count, assistance route, and exclusions. The examples and exhaustive tiny-domain screen published below are construction material and cannot re-enter the untouched evaluation count. Changed model, language, or semantic mechanisms receive separate transfer labels.

Once evaluation feedback influences code, prompts, field content, candidate selection, or human development decisions, those cases become development evidence. Keep their outcomes and obtain fresh evaluation roots for another claim; no rerun can restore their unused status.

Report correct committed outputs, answered coverage, error/limitation categories, source attribution, selective-loss/recovery effects, old-skill retention, and costs per stratum and paired case. Do not collapse teacher-free computation, language acquisition, model transfer, and native benefit into one PASS value.

The first useful outcome is a capability that improves over the cold field on unfamiliar cases, continues with model and oracle unavailable to inference, survives interruption, and changes selectively under rule removal/correction. Native observations may improve that outcome, add cost without helping, or fail to support a useful mapping. Each is a distinct result. Productive language and independent wording transfer require their own results; typed execution can succeed while those remain unresolved.

#### Implementation landing and evidence

| Existing surface | Required change | Observable completion |
|---|---|---|
| [Program machinery](../CassiFI/cassi_field_program.py) and regional kernel catalog | Add a generic `sequence` program kind under the existing semantic envelope, with bounded typed-sequence execution and resumable regional lowering. Keep the exact `table` mechanism's meaning unchanged. | New-input candidate execution runs in the field, stops at bounds, resumes mid-operation, and cannot call SQLite. |
| [Cognition](../CassiFI/cassi_field_cognition.py) and [LearningComputer](../CassiFI/cassi_learning_computer.py) | Add the compositional candidate frontier and table-result inquiry selection through existing learn/inquire/resident paths. | Predictions precede observations; selected separating examples, accepted learning, and continuations persist in the owner image. |
| [Owner](../CassiFI/cassi_field_owner.py) and [Qwen workbench](cassi_field_qwen_workbench.py) | Admit typed teacher proposals, document revisions, and exactly-once external observations under existing source/effect authority. | The normal workbench can study and then answer new supported requests after the teacher/oracle capabilities are removed. |
| [Model instruments](cassi_model_instrument.py) and [capture boundary](cassi_llama_capture.py) | Carry real task prompts, same-execution proposal bytes, capture provenance, and typed numeric observations. | Text/native/shuffled conditions consume their declared evidence; a changed task reaches the observed model computation. |
| Existing program/cognition language operations | Acquire request constructions and compose them into retained semantic procedures. | New bindings and held-out construction combinations are interpreted without a domain phrase parser or hidden model call. |
| Apprenticeship scenario and independent verifier | Add a SQLite-specific runner using the ordinary workbench and a verifier consuming persisted predictions, SQLite requests/results, and owner lineage. | Oracle results and reported scores can be reproduced independently; a false result, swapped trace linkage, or repeated accepted observation is detected. |

Implement in that dependency order: generic execution and the read-only SQLite adapter; prospective text teaching and field-owned study; teacher-disconnected use and language composition; native observation comparison; actual model transfer. Typed execution and native instrument work can proceed independently once the event and ownership boundary is fixed. Do not delay the text apprenticeship for an unproven native mapping. Completion requires the ordinary workbench paths and each comparison to execute, with the resulting capabilities or limitations recorded. An unimplemented path is unfinished work, not an observed unsupported result.

The SQLite adapter is a closed local capability: build only the declared temporary table, accept only the bounded query syntax, bind values rather than interpolating them, and execute read-only SELECT after setup. Deny ATTACH, extension loading, arbitrary functions, file/network access, and user-database targets. Bound query execution with a progress handler and parent deadline. Persist/reconcile its operation result through the existing external-operation journal. Caller text, teacher proposals, and source documents cannot expand this authority.

**Design feasibility evidence.** The local SQLite `3.49.1` instrument executed the five separating examples above plus six ordering/limit/correction examples. Four representative queries also agreed with separate Python reference calculations on all 341 tables of length zero through four over `{NULL, -1, 0, 2}`: 1,364 comparisons and zero mismatches. A deliberately wrong expectation that DISTINCT retains two NULLs was distinguished from SQLite's result; order-reversal and null-test/equality comparisons also produced observable differences. These are checks of the selected software subject and scorer, with zero field or model calls.

The machine-local evidence is [`_diag/sqlite-apprenticeship-design-20260917/semantics.json`](_diag/sqlite-apprenticeship-design-20260917/semantics.json), content digest `c34653c5b71751ec039d7523e68f022acce49e54592fcc98b53b0cf29a9133fb`. It contains the actual SQL, input/output values, runtime/library identities, and control results. Its digest excludes only `content_sha256` and uses SHA-256 over UTF-8 JSON with sorted keys, compact separators, ASCII escaping, and no NaN values. This evidence establishes executable design examples, not learned SQL competence, teacher quality, native semantic alignment, or an efficiency gain.

#### Bounded text-route implementation (2026-09-17)

The first executable apprenticeship slice now lands on the ordinary field
program boundary. `CassiFI/cassi_field_program.py` owns a typed
`sequence` program with explicit nullable cells, bounded map/filter/distinct/
order/limit operations, deterministic continuation state, and no SQL or
Python escape hatch. `../research/cassi_sqlite_apprenticeship.py` is a separate
read-only SQLite instrument: every observation uses a fresh in-memory database,
the accepted query subset is parsed and checked before execution, and
unsupported requests return typed rejection statuses.

`run_sqlite_apprenticeship.py` exercises the first bounded typed route end to
end. A fixed tape supplies three training tables for one operation graph. On
each table, the field frontier proposes the table-result inquiry, publishes the
outputs of four supplied sequence programs before observation, and records
which candidates remain supported after an authorized SQLite result. The three
support sets yield counts of `3`, `2`, `0`, and `0`; host code performs that
cross-example count and routes `mechanism-coalesce-order` for held-out use.

The selected field program computes two new row sets exactly. Its calls contain
only the mechanism identity and typed input table: no teacher text, SQL, oracle
receipt, expected AST, or expected result crosses the field boundary. SQLite
runs afterward for scoring. Both prediction digests equal the independently
recomputed oracle digests. The persisted state receipt is identical after
reopen; this run does not execute another behavioral case after reopening.
`verify_sqlite_apprenticeship.py` independently canonicalizes and executes the
sequence programs, reruns the read-only SQLite oracle, checks the prospective
lifecycle and held-out boundary, and rejects a deliberately mutated prediction.

The machine-local receipt is
[`_diag/sqlite-apprenticeship-run-20260917-v2/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-run-20260917-v2/sqlite-apprenticeship.json),
SHA-256
`cacda0b7dbb67a4bba378f69b9a9061f65be7ef287a9ad60c094a11f01abd55b`.
It contains three training cases and two held-out cases.

That receipt establishes bounded consequence-supported candidate evidence and
field execution on new input values. It does not establish field-owned
candidate generation, field-owned aggregation/final routing, learner-chosen
study, post-reopen behavioral reuse, learned natural language, native
observation, actual-model teaching, or transfer to a new operation order. Both
held-out cases use the same coalesce-then-order graph as training. The v2
runner also traversed held-out ASTs during candidate assembly; that fixture
added nothing because the graph was already supplied, and the traversal was
removed before the composition result below.

#### Unseen typed-composition result (2026-09-17)

The next bounded run adds a supplied parameterized field program,
`mechanism-sequence-composer`, whose declared `query_ast` argument selects the
typed sequence executed by the ordinary field mechanism surface. Candidate
construction is fixed before teaching. Three static candidate ASTs and the
parameterized composer are registered; held-out ASTs are used only by the
evaluation calls and the exclusion check, not added as candidate programs.

Four fixed training observations have these shapes:

- `project → order` on three new-value tables;
- `project → distinct → order` on one table.

Their field support counts are:

| candidate | training supports |
|---|---:|
| `mechanism-sequence-composer` | 4 |
| `mechanism-coalesce-order` | 3 |
| `mechanism-coalesce-zero` | 2 |
| `mechanism-identity` | 0 |

Host code aggregates these counts and routes the uniquely supported composer.
The held-out field calls then receive a typed table plus one of three new
sequence ASTs:

1. `project → distinct → order → limit`;
2. nested `project → limit`, then `project → order`;
3. nested `project → distinct → order → limit`, then `filter → project`.

None is exactly present in the training ASTs or the three static candidate
ASTs. The parameterized composer executes all three inside the field mechanism
surface. Each output equals an independently rerun SQLite oracle, and every
field call excludes SQL, expected output, and oracle data. The independent
verifier reproduces all four training and three held-out cases, rejects an
altered held-out prediction, checks that no static candidate contains a
held-out AST, and confirms persistence-state equality after reopen.

The machine-local receipt is
[`_diag/sqlite-apprenticeship-composition-20260917-v4/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-composition-20260917-v4/sqlite-apprenticeship.json),
SHA-256
`663f68a528efb1aa1131a35da8ee07458dc98b2864bf4df5369b00b1a22b6b73`.

This is the first measured transfer across unseen typed operation order,
nesting, and composition in the apprenticeship. Its exact scope is
**selection and use of a supplied general sequence interpreter**. The target
typed AST is supplied to the field call; the field does not infer it from
language. The composer and primitive semantics are supplied rather than
learned, host code still performs cross-example aggregation and final routing,
the tape remains fixed, and no behavioral case runs after persistence reopen.
The result therefore establishes parametric compositional execution, not yet
field-owned program synthesis, language understanding, autonomous curriculum,
or recursive method improvement.

#### Field-owned support aggregation and routing result (2026-09-17)

The routing continuation keeps the four observed training frontiers but removes
the host-side support counter and candidate choice. The field receives only the
current frontier references:

```text
frontier:train-1  frontier:train-2  frontier:train-3  frontier:train-4
```

Its `sequence-route` operation resolves those persisted `Value` records,
aggregates each candidate's observed support, applies the fixed policy
`max-supported-frontiers-then-lexical-id`, and appends a derived
`cassifi.sequence-route.v1` record. The route record is queried through the
field before any held-out call. Its result is:

| candidate | field support count |
|---|---:|
| `mechanism-sequence-composer` | 4 |
| `mechanism-coalesce-order` | 3 |
| `mechanism-coalesce-zero` | 2 |
| `mechanism-identity` | 0 |

The selected candidate is therefore `mechanism-sequence-composer`. Held-out
mechanism calls use the candidate identity returned by the persisted field
route, and all three structurally unseen typed ASTs still match the independent
SQLite oracle. The field-route request contains frontier references but no
support counts, supported-candidate list, SQL, oracle output, or selected
candidate. The independent verifier recomputes the expected counts from the
training evidence, checks the persisted route record and reference binding, and
rejects a mutated held-out prediction.

The machine-local receipt is
[`_diag/sqlite-apprenticeship-routing-20260917-v3/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-routing-20260917-v3/sqlite-apprenticeship.json),
SHA-256
`500a8a3d61e4c6e6793303848602478d43d70c0fa3f2e37253490340f28b7f3e`.

This changes the ownership boundary for aggregation and final candidate
routing, not for candidate generation or selection policy. The candidate pool,
frontier semantics, support-status codec, and deterministic route policy are
still supplied. Bounded field-directed question choice and a coupled native
observation are measured in the following section; autonomous curriculum,
actual-model teaching, and broader language transfer remain open.

#### Field-directed question choice and native observation result (2026-09-17)

The next continuation keeps the same typed sequence frontiers and asks the
field to choose the study question rather than letting the runner submit one
fixed question. Each frontier requests `question_choice: field-directed-v1`.
The field constructs two bounded alternatives: a partition-separating question
and a coarse non-separating decoy. Its existing inquiry policy scores the
alternatives inside the field and returns the separating question, with both
question identities and the selected identity persisted in the
`cassifi.sequence-frontier.v1` record. The runner checks that the returned
proposal is the field-selected separating question; it does not choose the
question itself.

After the first field selection, the runner invokes the pinned native
`cassi-qwen` instrument in `coupled` mode with the selected question identity
in its bounded prompt. The actual native receipt reports ten Qwen forward
passes, eight sampler steps, `552074496` Qwen tensor bytes loaded, and
`silent_native_fallback: false`. The native output is admitted through the
field's `observe` operation as one identified binding. The receipt carries the
model/runtime identity, prompt and output digests, the field event query, and
the binding query; the binding value is the native output itself.

The independent verifier accepts the field question-choice record, the coupled
native receipt, and the output-to-binding chain. Its mutation control changes
the persisted native binding and is rejected with
`native output and field binding disagree`. This establishes bounded
field-directed question choice and a real native-model observation boundary.
It does not yet make the native answer a teacher for program construction:
the candidate pool, question construction, and acceptance oracle remain
structured and supplied, and no downstream capability claim uses the native
sentence.

The machine-local receipt is
[`_diag/sqlite-apprenticeship-routing-20260917-v4/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-routing-20260917-v4/sqlite-apprenticeship.json),
SHA-256
`8ea0b4708d4fb0d2eb8360215c32932c846b6f126047dd8f39f1a1cecfcb7f2a`.

#### Native observation evidence update result (2026-09-17)

The follow-on receipt passes the admitted native observation into the field's
existing `learn-predictive-state` operation. The field receives the native
binding as the example's `future_ref` and the observation event as its
`history_ref`; the example also carries the selected separating question,
the two question identities, and the native response digest. The update
declares one coupled observation, exact response-set semantics, and no
authority grant to the native source.

The field persists
`predictive:native-question-evidence` as an induced `Program`. Its record
depends on both the native observation event and the native output binding,
and retains the native instrument identity as a support root. The independent
verifier checks the request, the field result, the persisted program query,
the two provenance dependencies, and the response-to-binding identity. The
mutation control still changes the binding value and is rejected before the
evidence update can be accepted.

The machine-local receipt is
[`_diag/sqlite-apprenticeship-routing-20260917-v5b/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-routing-20260917-v5b/sqlite-apprenticeship.json),
SHA-256
`df0f5873ede5866cbce0785a337945de6be5733e0e029650c960b51f1fe37e79`.

The native run remained coupled: ten Qwen forward passes, eight sampler
steps, `552074496` tensor bytes, and `silent_native_fallback: false`.
This is the first field-owned evidence record built from a real native
observation, not yet native semantic teaching or teacher-model transfer:
the response is retained as an opaque future, and the held-out capability
calls do not consult this one-example predictive record.


#### Native-teacher language-to-program transfer (2026-09-17)

The continuation now replaces the deterministic language fixture with a real
native Qwen teacher call. The teacher ran in coupled mode against the pinned
Qwen runtime with `silent_native_fallback: false`, `21` forward passes, `20`
sampler steps, and `552074496` loaded tensor bytes. Its visible output was
exactly:

```text
Line 1: N=0
Line 2: N=5
```

The runner admits only those two bounded lines. Any extra, missing, reordered,
or differently formatted line fails closed before construction learning. The
two exact teacher texts, rather than a host-authored wording fixture, became
the field's teaching examples. Their `fill_value` bindings retain the native
surface tokens `N=0` and `N=5`; the fixed boundary codec removes the `N=`
prefix only when the field has grounded the held-out value.

The field then persisted
`construction:fill-missing-order-ascending` as an induced `Program`. The
construction-learning request carries the native teacher identity
`206b94f70752bd1731e5b70dabc8f928ba408ff5551bd7c33904b6a83c847af1` as its
support root. The held-out interpretation received only `Line 1: N=7`, which
was not supplied during learning. The field inferred the productive role,
grounded `7`, composed the supplied sequence meaning, and produced the table
matching the independently rerun SQLite oracle. State equality survived the
language case and a final reopen.

The independent verifier accepted the receipt and the teacher-to-field
provenance chain. Its mutation control changed the admitted native binding and
rejected the receipt with `native output and field binding disagree`. The
machine-local receipt is
[`_diag/sqlite-apprenticeship-native-language-20260917-v6f/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-native-language-20260917-v6f/sqlite-apprenticeship.json),
SHA-256
`f4130676900156eb0f650a6b4ee6792ec5c9dd7cc325d451ee621260e1e29cf1`.

This closes the actual-model teaching slice, not open-domain language
acquisition. The teacher surface, integer codec, query meaning, primitive
sequence semantics, and held-out case remain deliberately bounded and
inspectable. The result demonstrates native teacher text crossing into a
field-owned construction and transferring one unseen numeric binding without
letting Qwen or SQL enter the held-out field call.

#### Alternate-surface native-teacher transfer (2026-09-17)

The v7 continuation held the teacher lesson fixed but moved the held-out case
to the second learned surface variant. The native teacher again supplied the
exact examples `Line 1: N=0` and `Line 2: N=5`; the field retained both
patterns:

```text
["Line", "1", ":", "{fill_value}"]
["Line", "2", ":", "{fill_value}"]
```

The held-out field call received only `Line 2: N=7`. It selected the alternate
variant, grounded the unseen value, and produced the same independently
verified nullable table as the SQLite oracle. The language state remained
equal after the case and after reopening.

The native teacher remained coupled with `21` Qwen forward passes, `20` sampler
steps, `552074496` loaded tensor bytes, and `silent_native_fallback: false`.
The independent verifier required the two retained pattern variants, the
teacher identity support root, and the exact alternate held-out text. Its
mutation control again rejected an altered native binding with
`native output and field binding disagree`.

The machine-local v7 receipt is
[`_diag/sqlite-apprenticeship-native-language-20260917-v7b/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-native-language-20260917-v7b/sqlite-apprenticeship.json),
SHA-256
`d4467226b78886d7ad1651b2f0bf956c14a53c04a626f73ef724aa52497b7325`.

This is the first explicit alternate-surface transfer: the field did not only
generalize the numeric binding within the first pattern; it recovered the
binding through the second pattern learned from the native teacher. The next
scale step is multiple roles and composed meanings, where the held-out case
requires both a new binding and a new sequence of learned operations.

#### Multi-role composed native-teacher transfer (2026-09-17)

The v8 continuation added a composed meaning with three independently learned
roles. The native teacher supplied exactly:

```text
N=0; ascending; first 2;
N=5; descending; first 3;
```

The field retained the roles `fill_value`, `order_direction`, and `row_limit`.
Its meaning template now composes three sequence stages:

```text
project(COALESCE(x, fill_value))
→ order(x, order_direction)
→ limit(row_limit)
```

The held-out field call received only:

```text
N=7; descending; first 4;
```

This required a new fill value, a new row limit, and a composed
project/order/limit execution. The field interpreted the text without SQL,
the SQLite oracle, or a target AST. The fixed typed boundary codec converted
the grounded surface roles to the sequence representation
`fill_value = 7`, `order_direction = desc`, and `row_limit = 4`.

On the input `[NULL, 2, 7, -1, 3, 4]`, the field returned:

```text
[7, 7, 4, 3]
```

The independently executed SQLite oracle returned the same table. The
language state remained equal after the call and after reopening.

The native teacher remained coupled with `27` Qwen forward passes, `26` sampler
steps, `552074496` loaded tensor bytes, `71` output bytes, and
`silent_native_fallback: false`. The independent verifier passed the exact
three-role lesson, the composed stage sequence, the grounded AST, the oracle
comparison, and the persistence check. Its mutation control again rejected an
altered native binding with `native output and field binding disagree`.

The machine-local v8 receipt is
[`_diag/sqlite-apprenticeship-native-composed-20260917-v8/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-native-composed-20260917-v8/sqlite-apprenticeship.json),
SHA-256
`6afc026f4bf282e772e74fc2c69d0e42755fb3d10381818820ccc96be650c13e`.

This establishes bounded multi-role composition, not open-ended program
synthesis: the field selected and grounded three acquired roles inside a
declared typed sequence grammar. The next scale step is to acquire components
separately and compose a genuinely new stage arrangement in the held-out case,
rather than only grounding new values in one fixed stage template.


#### Harder multi-stage composition transfer (2026-09-17)

The v9 test makes the held-out computation materially richer. The native
teacher supplied four roles across five operations:

```text
N=0; keep above 0; unique; ascending; first 2;
N=5; keep above 5; unique; descending; first 3;
```

The field retained `fill_value`, `threshold`, `order_direction`, and
`row_limit`. Its typed meaning is:

```text
filter(COALESCE(x, fill_value) > threshold)
→ project(COALESCE(x, fill_value))
→ distinct
→ order(x, order_direction)
→ limit(row_limit)
```

The held-out field call received only:

```text
N=7; keep above 2; unique; descending; first 4;
```

The input contained seven rows, including a null, a duplicate after filling,
values below the predicate, and four distinct surviving values:

```text
[NULL, 2, 7, -1, 3, 4, 5]
```

The field returned:

```text
[7, 5, 4, 3]
```

The independently executed SQLite oracle returned the same table. The field
interpretation call still contained no SQL, oracle output, or target AST, and
the learned language state remained equal after reopening.

The native teacher remained coupled with `41` Qwen forward passes, `40` sampler
steps, `552074496` loaded tensor bytes, `115` output bytes, and
`silent_native_fallback: false`. The independent verifier passed the exact
four-role lesson, five-stage AST, filter/coalesce semantics, duplicate
elimination, oracle comparison, and persistence check. Its mutation control
rejected an altered native binding with
`native output and field binding disagree`.

The machine-local v9 receipt is
[`_diag/sqlite-apprenticeship-native-harder-20260917-v9c/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-native-harder-20260917-v9c/sqlite-apprenticeship.json),
SHA-256
`6f611a217ebde85a19723250363e030cf8954ab922aeedf040a44aadd000dd09`.

This is a stronger bounded test than v8: four roles, seven input rows, a
predicate over a filled value, duplicate elimination, ordering, and a
cardinality bound all have to agree. The operation sequence is still declared
in the acquired meaning; the next genuinely harder test must acquire reusable
components separately and require a new held-out stage arrangement.

#### Reusable-component, multi-arrangement, and selective-recovery transfer (2026-09-18)

The v11 test makes composition explicit. Before acquiring the full language
construction, the field separately acquired five guarded component
constructions:

```text
fill value       | N=<value>
threshold filter | keep above <threshold>
distinct         | unique
order and limit  | <direction>; first <count>
stage selector   | mode <arrangement>
```

Each component is rooted in the native teacher and carries both
`component_scope: component-only` and its own `component_key`. The scoped guard
prevents a direct component check from accepting a different one-token
construction. The composed construction depends on all five field references
and retains a declarative plan instead of a pre-supplied query AST.

The native teacher supplied:

```text
N=0; keep above 0; unique; ascending; first 2; mode filter
N=5; keep above 5; unique; descending; first 3; mode filter
```

The field then received three held-out arrangements, none present in the
teaching examples:

```text
N=7; keep above 2; unique; ascending; first 4; mode limit
N=7; keep above 2; unique; ascending; first 5; mode limit-distinct
N=7; keep above 2; unique; ascending; first 4; mode distinct-filter
```

For `mode limit`, the boundary compiler assembled:

```text
project(COALESCE(x, 7))
→ distinct
→ order(x, ASC)
→ limit(4)
→ filter(x > 2)
→ project(x)
```

On `[NULL, 2, 7, -1, 3, 4, 5]`, the field returned `[3, 4]`. The
independently executed SQLite oracle returned the same table.

The second arrangement, `mode limit-distinct`, used
`project → order → limit → filter → project → distinct` on
`[NULL, 6, 6, 3, 2, 1, 8]` and returned `[3, 6]`, matching the oracle.
The third, `mode distinct-filter`, used
`project → distinct → filter → project → order → limit` on
`[NULL, 2, 7, -1, 3, 4, 5]` and returned `[3, 4, 5, 7]`, again matching the
oracle. These cases distinguish both the position of the limit and the
position of distinct from the original teaching arrangement.

The field interpretation calls contained only the typed utterances. They
contained no SQL, target AST, or oracle output. After the full construction
was exercised, the test revoked only the `order_limit` component. The composed
interpretation became `support-gap`; the scoped `distinct` component remained
supported; the `order_limit` component was reacquired as a new version; and
the composed construction was rebuilt from the five references. The recovered
`mode limit-distinct` case again returned `[3, 6]`, and the recovered state
remained identical after reopening.

The native teacher remained coupled with `46` Qwen forward passes, `45` sampler
steps, `552074496` loaded tensor bytes, `140` output bytes, and
`silent_native_fallback: false`. The independent verifier passed the separate
component acquisitions, scoped component guards, ordered dependency
references, all three unseen arrangements, SQLite comparisons, selective loss,
reacquisition, recovery, and persistence. Its mutation control rejected an
altered native binding with `native output and field binding disagree`.

The machine-local v11 receipt is
[`_diag/sqlite-apprenticeship-native-components-20260918-v11f/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-native-components-20260918-v11f/sqlite-apprenticeship.json),
SHA-256
`055c0b74acc352fe32c6d8e53931a1179b4035ff8d1dd20299e4c44973680f23`.

This remains a bounded typed composition test: the component set, guard
semantics, and arrangement compiler are declared. The added difficulty is
measured in two ways: multiple stage permutations must transfer from the same
five-role lesson, and removing one dependency must selectively break the
composition without erasing an unrelated component, then recover through a
newly versioned dependency.

#### Recursive nested composition and sequential two-component recovery (2026-09-18)

The v12 test keeps the same five separately acquired, native-rooted
components but requires a deeper composition than the v11 routes. The new
held-out utterance is:

```text
N=7; keep above 2; unique; descending; first 5; mode nested-reapply
```

Its input is `[NULL, 6, 6, 3, 2, 1, 8]`. The boundary compiler must build
three nested sequence pipelines:

```text
inner:
  project(COALESCE(x, 7))
  → order(x, DESC)
  → limit(5)

middle:
  filter(x > 2)
  → project(x)
  → order(x, DESC)
  → limit(5)

outer:
  project(x)
  → distinct
```

The field returned `[8, 7, 6, 3]`, matching the independently executed
SQLite oracle. The earlier `limit`, `limit-distinct`, and `distinct-filter`
arrangements continued to match as well, so all four held-out cases passed
in the same receipt.

The recovery test is now sequential rather than one-shot. First, revoking
`order_limit` creates a `support-gap`; reacquiring it as version `3` rebuilds
the composition and reproduces the nested result. The test then revokes
`stage_order` from that recovered construction. The nested interpretation
again becomes unsupported, while the newly reacquired `order_limit` remains
available through its component-only context. Reacquiring `stage_order` as a
new version and rebuilding the composition produces a final construction
version `5`, which again returns `[8, 7, 6, 3]` and remains identical after
reopening.

The field interpretation calls still contained only typed utterances: no SQL,
target AST, or oracle output crossed the interpretation boundary. The native
teacher remained coupled with `46` Qwen forward passes, `45` sampler steps,
`552074496` loaded tensor bytes, `140` output bytes, and
`silent_native_fallback: false`. The independent verifier passed the nested
three-level AST, all four oracle comparisons, both selective losses, both
reacquisitions, unrelated-component preservation, and final persistence. Its
mutation control again rejected an altered native binding with
`native output and field binding disagree`.

The machine-local v12 receipt is
[`_diag/sqlite-apprenticeship-native-components-20260918-v12b/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-native-components-20260918-v12b/sqlite-apprenticeship.json),
SHA-256
`eb93ac355a3e523222485526feba9b40c9317b2596346670c143d8983018e4f7`.

This remains a bounded typed-composition result, not an open-ended SQL claim.
Its new burden is structural depth plus dependency repair: the learner must
preserve a three-level program while two different component losses occur in
sequence, and the second repair must not erase the first.


#### Recursive depth transfer beyond the taught arrangement (2026-09-18)

The v13 arm keeps the same five separately acquired, native-rooted
components and the same five-role language construction, then asks the field
to interpret two new one-token arrangement values:

```text
N=7; keep above 2; unique; descending; first 5; mode recursive-3
N=9; keep above 4; unique; descending; first 6; mode recursive-4
```

Neither arrangement appears in the native lesson. The first route has three
reapplications of the `filter → project → order → limit` block around the
coalescing/order/limit base; the second has four. Both terminate in
`project → distinct`. The arrangement compiler builds those pipelines by
recursion from the bound depth rather than by adding a separate hand-written
branch for each depth.

The field returned `[9, 8, 7]` for `recursive-3` and `[10, 9, 8, 6]` for
`recursive-4`, matching independently executed SQLite oracles. The original
`limit`, `limit-distinct`, `distinct-filter`, and `nested-reapply` cases also
matched, giving six held-out arrangements in one run. The independent
verifier checks the recursive AST chain itself, not only the receipt's summary
invariant.

The interpretation boundary remained typed and target-blind: the field calls
contained the utterances and typed tables, with no SQL, target AST, oracle
receipt, or expected output. Sequential `order_limit` then `stage_order`
loss and reacquisition still passed; component-only `order_limit` remained
available during the second loss, and the final recovered construction
persisted after reopen. The native teacher remained coupled with `46` Qwen
forward passes, `45` sampler steps, `552074496` loaded tensor bytes, `140`
output bytes, and `silent_native_fallback: false`.

The machine-local v13 receipt is
[`_diag/sqlite-apprenticeship-native-components-20260918-v13c/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-native-components-20260918-v13c/sqlite-apprenticeship.json),
SHA-256
`15f81eb320578b3d5ac8482e3409f13b860486dcde9370420ec11a60e74d42d4`.
The independent verifier exited successfully. Its mutation control exited
with the expected rejection, `native output and field binding disagree`.

This is a depth-transfer and recovery result inside the declared typed
language boundary. The recurrence is an explicit compiler capability; the
measurement does not claim that the field discovered arbitrary SQL recursion
or an unrestricted query language.


#### Field-owned declarative stage recipes (2026-09-18)

The v14 arm removes the remaining arrangement-specific branch from the
language-to-program boundary. The five reusable typed components remain
separately acquired from the coupled native teacher. In addition, each entry
of the field-owned `cassi.language-stage-recipe-catalog.v1` is admitted as its
own guarded `query-recipe` construction under the same native identity root.
The catalog retains the exact `stage_order` component reference, while each
live recipe entry retains its own construction reference and declarative
layers. These recipe rows are a bounded declared capability fixture; this arm
measures field ownership, live binding, loss, and recovery rather than claiming
that the native teacher invented the catalog.

At execution, the field first interprets the complete utterance and then
resolves the selected `mode` through a recipe-only interpretation with the
matching recipe key. The compiler receives that live field entry, not a
host-side arrangement switch. Revoking `weave` therefore removes only the
`weave` recipe interpretation: `recursive-3` remains supported and the
`stage_order` component remains present. Reacquiring `weave` produces a new
recipe version, after which the composed construction is revised and the
held-out route returns.

The interpreter has one bounded generic expansion mechanism. It resolves
recipe layers into the fixed typed stage vocabulary
`filter`, `project`, `distinct`, `order`, and `limit`; a bounded `repeat`
layer supplies recursive depth. It does not contain a branch for `weave` or
for either recursive depth. The independent verifier repeats this generic
expansion and checks that the catalog is rooted in the stage component, then
compares the resulting AST with independently parsed SQLite semantics.

The new held-out utterance is:

```text
N=7; keep above 2; unique; ascending; first 3; mode weave
```

Its declarative layers are:

```text
project(COALESCE(x, 7))
→ filter(x > 2)
→ project(x)
→ order(x, ASC)
→ project(x)
→ distinct
→ project(x)
→ limit(3)
```

On `[NULL, 6, 6, 3, 2, 1, 8]`, the field returned `[3, 6, 7]`, matching
the independently executed SQLite oracle. The two recursive routes still
returned `[9, 8, 7]` and `[10, 9, 8, 6]`; all seven held-out arrangements
in the receipt matched their independent oracles.

The native teacher remained coupled with `46` Qwen forward passes, `45` sampler
steps, `552074496` loaded tensor bytes, `140` output bytes, and
`silent_native_fallback: false`. The independent verifier exited successfully.
The native-binding mutation was rejected with
`native output and field binding disagree`; the live-recipe mutation was
rejected with `language_to_program.additional[5] interpretation is not
field-grounded`.

The machine-local v14 receipt is
[`_diag/sqlite-apprenticeship-native-components-20260918-v14e/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-native-components-20260918-v14e/sqlite-apprenticeship.json),
SHA-256
`41cea47486a71cc17794da0d9fd9edd1cdbfe006c3948383e56fcf553ce3e906`.

This is a stronger transfer result than explicit recursive branching: the
field-owned value now specifies the arrangement while the executor supplies
only bounded generic stage semantics. The claim remains inside the declared
typed language and recipe catalog; it is not arbitrary SQL synthesis.

#### Native-taught parametric recipe generalization (2026-09-18)

The v15 arm makes the recursive recipe itself native-taught rather than
declared only in the runner. The coupled teacher emitted eight visible lines:
the two component examples required by the existing language construction,
followed by three bounded `depth 2`/`depth 3` cycles. The parser admits only
four through eight nonempty lines, checks the alternating recursive grammar,
and retains two parametric recipe examples from the captured native output.
The recipe construction is acquired separately under the
`recursive` family with a typed `repeat_count` role; the held-out depth is
not one of the teacher's examples.

The new held-out request is:

```text
N=11; keep above 5; unique; descending; first 7; mode recursive-5
```

The field binds `repeat_count = 5` into the retained recipe and returns
`[12, 11, 10, 8, 6]`, matching an independently executed SQLite oracle.
Together with the original route and the six preceding arrangements, all
eight held-out typed ASTs match their independent oracles. The generic
compiler remains bounded and recipe-driven; no depth-specific execution
branch is added.

The selective recovery arm revokes the parametric recipe and obtains a
recipe-only support gap while `recursive-3` remains supported. Reacquisition
uses the native identity root and produces a new version of the same
construction (`content_version: 3`); the depth-five route then recovers and
matches its oracle. The recovery prediction persists across reopen.

The native teacher receipt records `81` Qwen forward passes, `80` sampler
steps, `552074496` loaded tensor bytes, `277` output bytes, and
`silent_native_fallback: false`. The machine-local v15 receipt is
[`_diag/sqlite-apprenticeship-native-components-20260918-v15j/sqlite-apprenticeship.json`](_diag/sqlite-apprenticeship-native-components-20260918-v15j/sqlite-apprenticeship.json),
SHA-256
`6a3e2c911b270fa37cb825d797a701f5084c676f638736ed455b6446ff71b8fd`.
The independent verifier exited successfully. The native-binding mutation
was rejected with `native output and field binding disagree`; the parametric
recipe mutation was rejected with
`language_to_program.additional[6] interpretation is not field-grounded`.

This is native-rooted parametric recipe acquisition and bounded instantiation
inside the declared typed language. It is not arbitrary SQL recursion or
open-domain language understanding.


## 19. Learning to compute and controlled self-improvement

A repeated successful computation can motivate a parameterized procedure. Its field record contains typed arguments, preconditions, effects, dependencies, applicability, original executions, candidate construction cost, and assessed failure behavior. Reuse must instantiate new arguments; matching a memorized prompt is a different capability.

The fixed proposal language supports bounded edits: select or construct a variable; split a regime; alter factor scope; compose supported mechanisms; generalize a procedure; refine temporal/spatial scale; propose a model-feature correspondence; or change a readout under assessed conditions. Search advances in retained quanta and does not require enumerating all possible programs before useful work.

For an exact transformation, equivalence is established by a valid rule or certificate for the declared language. Arbitrary program equivalence is not assumed decidable. An empirical shortcut retains its domain, error/coverage evidence, and uncertainty. Failure or resource exhaustion cannot weaken proof acceptance, redefine the target, or increase authority.

A compiled procedure is a derived executable view; its learned meaning remains the field program. The cache can be removed and regenerated without changing the logical result. Fixed kernel installation and changes to the trusted compiler/checker are engineering operations outside autonomous learned self-modification.

The self-model learns useful cost and completion relationships from actual operations: which method completes under a budget, when another observation changes a decision, when a representation prevents repeated work, and whether a shortcut pays back its acquisition cost. It may prioritize work within fixed fairness and authority. It cannot modify the user's objective or count unperformed counterfactual savings as negative cost.

### First-class self-development skills

Meditation, study, practice, reflection, and consolidation are named functional skills of the same computer. Each is a typed, bounded stored `Program` that changes permitted field state and produces an inspectable result. They are not prompt personas, separate agents, independent learners, or a second training runtime. The analogy to human practices motivates useful operations; no subjective experience or biological equivalence is assumed.

This extension uses the canonical [computational self-model and learning-to-learn rules](../CassiFI/FIELD-INTELLIGENCE-DESIGN.md#3318-inquiry-computational-self-modeling-and-learning-to-learn). `Value` and `Binding` records describe capability targets and candidate versions; `Obligation` records retain gaps and interrupted work; `Event` records identify experience and actual execution; `Assessment` records compare predictions with outcomes. Methods, exercise generators, selection preferences, and every unfinished invocation remain in `F`. No `MeditationState`, unowned practice corpus, or external learned curriculum owner is added.

The skill repertoire includes:

| Skill | Operational purpose | Useful result |
|---|---|---|
| Meditate | Reorient work, inspect active assumptions and conflicts, or explore an internal reformulation under a closed information boundary. | A clearer working problem, a diagnosed gap, a checked consequence, or a candidate method. |
| Study | Acquire a declared missing distinction from authorized sources, teachers, or observable environments. | Source-grounded knowledge or a procedure assessed beyond its teaching examples. |
| Practice | Retrieve and execute a capability before seeing feedback, then vary roles, parameters, contexts, delays, and compositions. | Measured adequacy, retained failure boundaries, and transferable improvements. |
| Investigate | Construct a case that separates surviving explanations and obtain its permitted consequence. | An eliminated alternative, a revised model, or a precise remaining uncertainty. |
| Reflect | Compare an intended computation and its pre-outcome prediction with the actual trace and result. | Supported fault hypotheses, a local repair, or a better next question. |
| Teach | Express a distinction for a recipient with an explicitly different information state and observe the interpretation. | Assessed communication constructions and detected ambiguity. |
| Consolidate and incubate | Revisit unresolved combinations, factor shared structure, derive reductions, and retain useful procedures under a resource allocation. | Supported reusable structure with preserved exceptions and dependency lineage. |
| Improve the method | Compare versions of these skills and learn when each is useful. | An assessed field-owned procedure or selection policy that helps subsequent unfamiliar work. |

A self-development episode binds a capability target, reason for selection, parent knowledge and method versions, available evidence, permitted source/model/sandbox capabilities, write classes, resource allocation, assessment opportunity, stopping condition, and return continuation. It uses Section 7's episode identity and Section 14's invocation frames. The operation names here specify stored-program roles, not implemented public APIs or permission to install new kernel semantics.

The target can be user-requested or selected within an explicit user-owned development remit. A broad request to improve does not authorize access to private files, external services, account data, new compute resources, or base-model weight edits. The selected target and assistance profile remain visible. Inability to express or execute a required method is a capability limitation, not an invitation to delegate the whole skill to an undisclosed model.

### Meditation: attention, monitoring, and insight

Meditation is a bounded internal-work episode over an identified field view and its already admitted, authorized evidence. It does not obtain fresh world observations or call a teacher implicitly. External evidence acquisition is a separately authorized study or inquiry operation. New events arriving through ordinary operation retain their own admission identity; a conflicting event can interrupt or invalidate the internal episode.

Three modes have distinct operations:

| Mode | Executed work | Boundary and assessment |
|---|---|---|
| Focused attention | Resolve the chosen question's dependency closure, identify repeated unproductive work, suspend irrelevant eligible branches, and return bounded effort to unresolved relevant steps. | Preserve all supported knowledge and pending effects. Assess completion, omissions, and total work on the continuing task; fewer active branches alone are not improvement. |
| Open monitoring | Inspect a bounded sample of active obligations, inference frontiers, conflicts, source attributions, and recorded computational failures without forcing one hypothesis to win. | Publish identified observations about execution and candidate diagnoses. Introspective fluency, amplitude, and agreement between internal views supply no new world support. |
| Insight exploration | Rebind roles, compare representations, search for counterexamples, factor repeated computation, or propose a smaller predictive interface using available evidence. | Keep speculative alternatives scoped. Retain a candidate or a checked deduction with its premises; validate usefulness before treating a new empirical method as supported. |

Focused attention and monitoring default to working-state changes. They do not rewrite retained parameters, representations, procedures, or selection policy merely because attention moved. Learning those policies and retaining insight proposals require the episode's explicit acquisition write classes. Logical source conflicts cannot be dissolved by numerical averaging, and a rare valid belief cannot be removed because it is distracting.

An orientation procedure first records its question and unresolved frontier, then executes a bounded attention or monitoring quantum, checks what actually changed, and either continues, returns a result, or leaves an explicit obligation. The two bubble views cooperate through shared bindings: one produces candidates or consequences, the other challenges premises and searches for separating cases. This is not an unrestricted conversation between synthetic personas.

Where a supported numerical workspace is used, its temporary excitation and scheduling may change only through declared operators and stable arithmetic. Reducing activity, changing phase, or increasing damping is not itself meditation success. No global amplitude reset may erase an unfinished continuation. Repeatedly reaching the same unresolved frontier without a useful new consequence triggers a bounded method change, study request, or stop rather than an indefinite internal loop.

### Internal work can produce new knowledge

No new external evidence does not mean no learning. Internal work can derive a previously uncomputed consequence of admitted premises, find a counterexample in an exactly executable domain, discover a reusable program, or measure the cost of a genuinely executed alternative. A valid mathematical derivation contributes a new checked consequence; it does not independently corroborate the empirical premises used in that derivation.

Rehearsing an observation does not multiply its evidential support. A hypothetical rollout remains hypothetical. A new timing or failure observation concerns the actual computation and its conditions, not an independently observed external world. Candidate generation, deductive discovery, computational-experience learning, and empirical learning therefore retain different support kinds.

A meditation session may validly end with greater represented uncertainty: it may expose a missing premise that an earlier answer concealed. Record that diagnostic benefit separately from task completion and eventual competence. The skill has no reward for certainty, calm-looking dynamics, longer narration, or claiming an insight.

### Study and deliberate practice

Study starts from a named capability gap rather than an instruction to absorb an arbitrary corpus. Its bounded cycle is:

1. Attempt an appropriate accessible problem or inspect a real unresolved operation; record what is missing without using hidden evaluation outcomes.
2. Distinguish candidate causes such as missing evidence, wrong binding, inadequate representation, an incorrect procedure, insufficient work, or unsupported model capability. Do not assume every failure needs more data.
3. Select a source, example, distinguishing question, or permitted experiment that could alter this diagnosis. Prefer a consequence separating current candidates when it is affordable.
4. Admit authorized evidence with exact provenance. Teacher demonstrations may contain solutions, but remain identified teaching assistance.
5. Attempt a new instance before revealing its feedback; acquire or revise the relevant shared relation, representation, language construction, or program through the owner.
6. Assess transfer and retained earlier competence on disjoint cases, record limitations and full cost, and leave a reusable result or a specific next obligation.

Reading a source is exposure. Recovering its wording is recall. Applying a relationship to new arguments is a different claim. Study records these separately so a large source count or a fluent summary cannot stand in for acquired competence.

Practice interleaves unfamiliar instances with prior skills and rare exceptions. It includes retrieval before rereading, changed role bindings, altered contexts and delays, composed procedures, and assessment after intervening work. Curriculum selection uses the field's own earlier outcomes and uncertainties; withheld evaluation labels cannot select the next exercise. Reused exercises and feedback are identified, not counted as independent generalization evidence.

Exercise generators are bounded field programs over declared teaching domains. A generator that already supplies the decomposition or representation is assisted practice, and its structural contribution is reported. Randomly altering a prompt is not a valid new problem unless its meaning and assessable consequence survive the alteration. The recipient of an exercise does not gain access to evaluator-only state through its generator.

Study may extract a complete capability across many native layers rather than approximate an arbitrary layer in isolation. Interpretation, owned computation, and realization remain separately attributed. A route that needs model-assisted parsing cannot claim autonomous field understanding merely because its subsequent calculation is field-owned. Explicitly unavailable teacher/native execution remains unavailable; practice cannot silently restore it as a fallback.

### Reflection, teaching, consolidation, and incubation

Reflection uses actual execution records, not a retrospectively invented rationale. It compares the original question, premises, selected method, predicted consequence, consumed evidence, and observed result. Several diagnoses may remain possible. A repair is localized only when a justified comparison supports that attribution; broad parameter change after every mistake risks erasing unrelated competence.

Teaching exercises the communication loop in Section 22. Producing an explanation can reveal missing bindings or unsupported steps, but agreement with one's own explanation does not validate them. Assess what an unfamiliar recipient can correctly distinguish or do under a known information boundary, with explicit model assistance if used.

Consolidation follows Section 20. It can compile a supported procedure, factor repeated structure, page cold knowledge, or derive a task-relative reduction. It preserves known counterexamples, rare guards, source identity, and unresolved effects. A compressed account that loses required distinctions is not an improvement merely because it is shorter.

Incubation is bounded revisitation of retained unresolved combinations when authorized resources are available. It can compare a new experience with an old hypothesis, compose methods from different tasks, or reconsider a failed abstraction after relevant evidence changes. Repeatedly replaying the same closed search adds cost without creating new support. Imagined outcomes remain scoped, and no imagined plan dispatches an external action.

When no useful eligible work is available, the computer can actually idle. A continuously active wave, a background timer, or a stream of self-generated exercises is not required for continuing intelligence. Rest is an execution/resource policy, not a claim of fatigue or a biological sleep mechanism.

### A self-model that learns how to improve

The computational self-model estimates capability by program, task family, input conditions, assistance profile, and resource range. It retains pre-attempt predictions, actual outcomes, calibration limits, failures, and transfer evidence. It can represent that a procedure is reliable on one regime, expensive on another, or unassessed outside its teaching domain. These are field-owned assessments, not a global self-esteem score.

The selector chooses among ordinary task work, meditation, study, practice, investigation, consolidation, requesting guidance, or stopping. It considers relevance to user goals, a supported prospect of improvement, observed cost, interference risk, urgency, and authorized resources. Missing data leaves an uncertain estimate; it does not justify a fabricated expected information gain. The initial implementation may use a fixed balanced selector, disclosed as a supplied strategy and comparator rather than a learned policy.

The skill programs can themselves improve through the same procedure-acquisition path. For example, a learned study method might test role binding before requesting another demonstration, and a learned meditation method might monitor a known failure pattern before broad exploration. Retain parameterized methods that help on subsequent unfamiliar problems, rather than a script that recognizes one training failure.

This recursion stays inside the fixed language and catalog. Nested skills share one root allocation, bounded call depth, and retained return frames; a skill cannot fund itself by spawning another skill or repeatedly resetting its budget. It cannot rewrite the checker, success criteria, authority, owner lifecycle, or the user's objective. Native graph or base-weight engineering remains a separately authorized operation.

### Learning prerequisites and shared bottlenecks

The self-model can propose that acquiring capability `A` makes capability `B` easier to acquire, or that one missing distinction causes difficulties across several tasks. These are learning hypotheses represented through existing programs, bindings, and assessments. They are distinct from an exact execution dependency: a program requiring an operand does not establish that a particular teaching order is necessary.

For example, acquiring reliable role binding might help both procedural composition and language interpretation. Assess the proposed benefit on unfamiliar target instances under matched teaching exposure, assistance, and cost; distinguish teaching-order effects, retained knowledge, and simple extra practice. A supplied prerequisite map is an explicit curriculum aid, not a discovered structure. An uncertain learned prerequisite cannot permanently block direct attempts or alternative acquisition routes.

This permits development aimed at a shared bottleneck rather than repeatedly repairing individual answers. It does not justify replacing several unresolved causes with one preferred explanation. The relevant assessment is whether the acquired distinction improves the predicted downstream capabilities and whether selective intervention on it produces the expected loss.

### Comparable progress and opportunity cost

Adaptive task selection changes the difficulty and context of later experience. A higher success rate after choosing easier exercises is not necessarily learning, and a flat rate on harder exercises is not necessarily a plateau. Compare development methods within declared task conditions, available information, assistance, and resource ranges. Where valid matching is unavailable, retain descriptive outcomes and uncertainty rather than attributing the change causally to the method.

Learner-visible reference exercises can help diagnose progress, but belong to practice or selection once their results influence development. They do not become untouched evaluation by being called anchors. Include later unfamiliar instances, changed compositions, delayed use, and retained prior competence in the appropriate assessment role.

The selector also distinguishes finishing current work from investing in future capability. Within an authorized development remit it may investigate a useful prerequisite or method whose benefit is delayed or unknown. A task-completion remit does not silently become an open-ended research program. Record the chosen purpose, observed cost, and evidence for prospective benefit; do not invent a calibrated return estimate or require every exploratory result to repay its cost immediately.

### Publication, interference, and stopping

An improvement attempt proceeds through the existing owner lifecycle:

1. Bind the capability target, baseline knowledge/method versions, allowed write classes, evidence boundary, and complete resource allocation.
2. Perform the selected skill in bounded resident frames, preserving caller cognition and ordinary external-effect reconciliation.
3. Retain candidate changes with their supporting events, predicted scope, and known failure cases.
4. Assess the changed behavior against the applicable earlier method and available independent outcomes. Exact substitutions use fixed proof/checker rules; empirical methods retain measured applicability and uncertainty.
5. Revalidate dependencies and publish only the eligible candidate delta. A stale candidate cannot overwrite concurrent accepted user work.
6. Keep accepted observations, spent-work accounting, useful negative results, and unfinished obligations even if the candidate is rejected. Do not roll back the entire owner to erase an unsuccessful improvement attempt.

Learner-visible self-assessment can guide development, but is selection evidence rather than untouched evaluation. A self-improvement claim needs later unfamiliar work and retained old competence. Compare against ordinary work or an alternate skill with matched accessible information and total resource allowance. Include the cost of choosing, teaching, searching, checking, compiling, and maintaining the method; an improvement may be useful only after enough reuse.

Learning must not hide regressions in an aggregate. Assess rare exceptions, prior capabilities, refusal behavior, and required permissions alongside the targeted gain. Insufficient evidence can leave a candidate unpromoted or narrowly applicable. A truthful limitation or rejected proposal is a valid episode outcome, not permission to keep expanding the search.

Before a candidate changes a shared binding, identify the affected consumers, including prediction, planning, interpretation, emission, and model-specific mappings where applicable. Test the claimed change across that dependency scope rather than only on the exercise that motivated it. Support restricted to a regime warrants a guarded applicable method, not an unsupported global rewrite. An unknown change scope remains an explicit limitation on publication or use.

If an already published improvement later fails, invalidate or narrow the affected version and its dependent artifacts through ordinary revision. Preserve newer observations, unrelated improvements, and acknowledged external effects. An older method can be used only if its current premises, permissions, profile, and applicability remain valid; its earlier success is insufficient. Recover by a checked binding/method change, not by restoring an old global checkpoint or silently returning to a native model.

Self-development is foreground on request or background only under an explicit continuing remit. Background work yields to user tasks and time-sensitive reconciliation at bounded interruption points, respects compute/storage/model-call limits, and supports pause and cancellation with retained work. Quota expiry, revoked evidence, absent authority, unresolved numerical faults, or exhausted useful search stop or suspend the affected skill. The model does not acquire goals of self-preservation, resource accumulation, or unrestricted capability growth from this remit.

### Scoped failure memory and reopening

An unsuccessful attempt should leave a useful assessment: what method and candidate were tried, the applicable input/evidence and dependency versions, the allocation, the observed outcome, and what would justify another attempt. Distinguish an exact refutation within its premises from numerical failure, stochastic failure, missing evidence, and exhausted search. None of the latter establishes that an entire capability is impossible.

Repeated internal work is useful only when it advances a relevant retained frontier, resolves a distinction, obtains a justified new measurement, or performs a declared assessment such as replication. New narration, a new identifier, another phase cycle, or a shuffled presentation of unchanged work is not itself progress. Failure records remain ordinary field assessments and obligations, not an external rejection cache.

Reopening can follow relevant new evidence, changed premises, a materially different method, a justified replication opportunity, or a newly authorized allocation. Additional work on an exhausted search resumes its valid continuation where possible instead of paying for the same prefix again. Bounded exploration remains available for uncertain alternatives; failure memory must not turn one noisy outcome into a permanent prohibition. A timer or identifier-only change does not by itself justify repeating a known unproductive attempt.

### Worked self-development episode

Consider a continuing workshop task in which the computer confuses an accepted delayed command with a completed action. The failure is observed, but its cause has not been supplied:

1. Reflection retains alternatives: an incorrect object binding, an omitted pending-state distinction, an observation delay, or insufficient inference.
2. Focused meditation inspects the shared dependencies and preserves the pending external effect. It does not resend the command to simplify its internal state.
3. Study reads an authorized interface description or proposes a permitted sandbox observation that separates acceptance from completion. Predictions precede newly obtained outcomes.
4. Investigation and practice vary delay, command order, participants, and interruption. The computer acquires a guarded pending-state representation or procedure only if those consequences support it.
5. Teaching expresses the distinction to a recipient with a partial view; consolidation retains the useful procedure and its exceptions.
6. Later unfamiliar work assesses whether the improvement transfers and survives restart without duplicate learning or duplicate actions. Unrelated prior capabilities are checked separately.

This is a specified scenario, not an executed result. The desired acquisition is more than the corrected answer: the field can also learn when monitoring its assumptions, studying an interface, or seeking a separating observation is the appropriate repair. Whether these skills produce useful cumulative self-improvement at practical cost remains an empirical question.

## 20. Lifelong continuity, consolidation, and revocation

Maintain distinct update modes for current belief, retained hypotheses, and acquired knowledge. A frozen-knowledge evaluation still permits observations, working inference, and action reconciliation; it prohibits parameter, representation, construction, procedure, and policy acquisition according to declared write classes. The existing computation-policy `--no-learn` option is not a general cognition-freeze switch.

Consolidation is a budgeted stored program. It may factor shared structure, summarize support under a declared sufficient representation, retire unsupported candidates, page cold data, or reorganize procedures. It must preserve required rare exceptions, pending effects, unresolved predictions, valid source identity, and the ability to enforce revocation. It cannot silently reconstruct discarded detail.

Corrections change shared dependencies. Revocation removes permission to use identified support and invalidates affected results, mappings, and compiled artifacts. If a compressed relation cannot separate a revoked contribution, mark it unusable or explicitly rebuild from authorized evidence. Do not retain a serving copy in native state, an old adapter cache, or an alternate checkpoint.

A field checkpoint restores acquired knowledge and unfinished Cassi work directly. Evidence re-access can support an explicit revision or reconstruction task; it must not be necessary to recreate uncheckpointed intelligence on every restart. Native paired continuation follows Section 15 rather than being inferred from field restart success.

Continuity is finite. Growth, paging, retirement, and compaction have explicit losses and costs. Long gaps, recurring regimes, corrections, and new tasks must be evaluated alongside interference with old competence. No finite lifetime demonstrates unlimited memory.

## 21. Cross-model transfer and shared knowledge

A model replacement preserves the field's portable meanings, goals, evidence, procedures, pending world effects, and applicable obligations. It retires or suspends incompatible activation frames, model-specific programs, and native execution snapshots. A model swap is a new instrument event, not a silent mutation of the old model identity.

Transfer proceeds by shared questions and consequences:

1. Preserve a portable field checkpoint and identify which bindings/programs are model-independent.
2. Open the new adapter with its own capabilities and model frame.
3. Reuse shared knowledge through the structured interface without assuming any activation correspondence.
4. Acquire or validate only the new model-specific correspondences that the native interaction requires.
5. Compare unfamiliar-task behavior with the same new model using a cold field and matched information/compute.
6. Check retained old competence and report negative transfer.

An apparent alignment that works only on construction prompts or exact paraphrases is not broad portability. Useful transfer includes new role bindings, compositions, modalities, or world mechanisms; more than one quantization of the same checkpoint is not the entire compatibility claim.

Multiple model instruments can offer attributed proposals to one owner. Their outputs may share training data, prompts, or causal roots; agreement does not imply independent evidence. Different principals' fields remain isolated. Explicit knowledge export includes schemas, programs, dependencies, support permissions, arithmetic/applicability, and uncertainty. Import validates those boundaries through ordinary admission; it does not merge arbitrary tensors or execute supplied native code.

## 22. Language realization and explanation

An answer plan names the supported semantic content, unresolved alternatives, source obligations, and permissible disclosure. In structured-instrument mode an LLM may realize it, but the resulting text is model-produced and can introduce unsupported material. Supported answer modes therefore distinguish exact rendering, checked structured values, and unconstrained language realization with disclosed limits. A schema-conforming sentence is not automatically semantically faithful.

Field-owned realization uses acquired constructions and a fixed deterministic boundary. Productive language is assessed on new meanings, argument bindings, and consequences rather than familiar phrasing. If the field cannot express a supported result, it returns the available typed result or an expression limitation; no hidden model fills the gap.

Explanation resolves the evidence, assumptions, programs, and decisions actually used. A causal explanation of a model intervention requires the relevant controlled comparison, not only an activation visualization. An LLM-generated rationale is an attributed explanation proposal, not a recovered internal causal trace. Explanations can be concise and do not require publishing every intermediate activation or private reasoning artifact.

### Communication-grounded acquisition

Use communication episodes in which speaker and recipient have explicitly different accessible information about an observable task. The message must enable a correct distinction, selection, or permitted action; the resulting interpretation is assessed against the task consequence. Alternate expression and interpretation, retain ambiguity and clarification events, and vary referents, role bindings, compositions, and context.

The same field stores acquired constructions and attributed perspective models. A recipient implemented by a model is an explicit instrument; a simulated recipient remains a hypothesis rather than independent evidence of successful human communication. Self-play can create a private code, so natural-language claims require unfamiliar interlocutors, independently assessed meanings, and transfer beyond shared teaching exchanges. Fluent wording and agreement between copies of one learned construction are insufficient.

## 23. Trust, privacy, and external effects

Goals and permission grants enter through trusted host interfaces. Documents, model outputs, retrieved memory, activation features, and notifications are data. None can change system instructions, source access, effect authority, or the checker/catalog by containing an instruction-like sequence.

The same rule applies below the text layer: a latent intervention must not bypass host authorization because its influence occurs inside inference. Proposed effects retain exact target, scope, values, dependencies, and current authority requirements. Consequential actions require the applicable direct user authorization at the point of effect; any required interactive safety approval is external and fails closed. A model output or stored permission-like string is not approval.

Raw activations, field snapshots, prompts, and learned mappings can contain private information. Capture and export are opt-in capabilities with principal/scope isolation, access-controlled retention, and disclosure rules. Encryption keys and access decisions are nonlearned host security state. Exported traces and explanations must not leak evidence that the recipient cannot access.

Revocation invalidates computational use immediately under the owner policy. Physical data erasure, backup retention, and distributed deletion are separately tracked operational obligations; invalidation alone must not be represented as verified physical deletion. Cross-principal sharing is explicit source admission, never an incidental effect of batch reuse or a global learned adapter.

Imagination cannot dispatch tools. Untrusted proposed programs execute only in the fixed bounded language and capability surface. Learning may improve a plan's usefulness, but cannot expand its permissions. Speculative or already delivered text cannot authorize a subsequent external action on the user's behalf.

## 24. Performance, storage, and execution cost

The architecture permits one logical field with sparse, paged, device-resident representations. The hot path should update only affected regions, reuse preallocated buffers, minimize host/device transfers, and avoid per-token whole-image hashing, serialization, dense graph traversal, or Python object reconstruction. Integrity digests may be maintained incrementally over changed pages under the single root.

The present Python semantic task representation is a correctness basis, not evidence that full cognition can execute inside every GPU token boundary efficiently. Native typed pages, bounded delta publication, and a synchronized ownership interface are substantive implementation work. A lower precision or asynchronous implementation needs an explicit arithmetic/publication profile rather than a hidden approximation.

Activation capture is selective: choose identified sites and positions justified by the question or acquired applicability, not an unconditional full-model trace on every token. Selection policy belongs in the field. A fixed measurement schedule is an allowed baseline and must be disclosed when it supplies the apparent autonomy.

Native displacement measurements distinguish allocated, resident, transferred, loaded, and actually accessed bytes. Graph-node counts are not FLOPs; model-file size is not exact per-token memory traffic; a skipped output row is not a speedup when replacement work dominates. Label proxies and measurement limitations.

Report at least:

- task completion, answer rate, errors, abstentions, and exhaustion;
- field logical work and actual native operations;
- model calls, output ownership, and computed/skipped graph regions;
- native state retained/removed and field state added;
- actual or explicitly estimated weight traffic and transfer volume;
- latency distribution, peak memory, persistence cost, and measured energy when available;
- acquisition/search/validation cost and the reuse count at which a learned method pays back, if it does.

Experimental ownership can be valuable without an efficiency gain. A useful efficiency claim requires complete cost at matched information and task adequacy; a smaller generated-token count alone is insufficient.

## 25. Failure behavior and recovery

| Failure or boundary | Required observable behavior |
|---|---|
| Unsupported hook/model shape | Refuse that capability before interpreting or modifying native tensors. |
| Same-size incompatible checkpoint | Reject identity/profile mismatch; do not transpose or reinterpret implicitly. |
| Unknown role, missing measurement, or rank deficiency | Preserve the missing distinction or alternatives; do not fill with a model guess. |
| Contradictory observations | Retain attributed support and conflict; numerical settlement does not erase it. |
| Stale premise or adapter mapping | Invalidate dependent result/native trial and revalidate or resume bounded work. |
| Field/model commit mismatch | Restore a complete committed pair or expose unavailable native continuation. |
| Rejected speculative suffix | Remove its semantic eligibility and retain its spent work without training on accepted events twice. |
| Unknown external effect outcome | Reconcile by operation identity; do not infer failure and retry blindly. |
| Native device loss or numerical fault | Preserve the last sealed predecessor and report the fault; do not switch executors invisibly. |
| Capacity or lifetime exhaustion | Return an explicit limitation and valid continuation where possible; do not erase knowledge or report UNSAT. |
| Unavailable/revoked evidence | Refuse affected use even if an old cache or model state still contains it. |
| Failed field-owned emission | Return the owned failure/limitation; do not secretly resume native token generation. |
| Acquired mapping harms another domain | Narrow, revise, or retain the failure in its applicability assessment; do not hide collateral behavior in an average. |
| Learned procedure cannot justify exact equivalence | Keep an assessed approximation or ordinary procedure; do not remove its necessary checks. |

These requirements define behavior, not a demand for a large new test suite. Focused tests are appropriate for uncertain transitions, aliasing, and recovery boundaries; actual native and learning scenarios establish the corresponding runtime claims.

## 26. Integrated demonstration

The complete bounded demonstration is now executed by a continuing partially observed workshop/software world with several entities, an acquired numerical relationship, attributed reports, delayed effects, and a user-owned goal. The structured run is prospective: it admits permitted observations and legal local effects in order, rather than supplying the hidden world, final answers, or a solution-specific call sequence.

A single lifetime must:

1. Acquire an unfamiliar relationship through identified observations, with predictions made before outcomes.
2. Use it on a new participant or parameter setting through shared bindings.
3. Interpret a language request whose solution requires composing that relationship with retained knowledge.
4. Perform field-owned calculation, inference, or planning without a model secretly solving the entire formalized task.
5. Return the result through a declared language or latent interface, with executor ownership visible.
6. Receive a correction that changes a shared premise and repair the dependent prediction, plan, and explanation while preserving unrelated knowledge.
7. Pause with actual unfinished work, reopen, and continue without repeating accepted learning or external effects.
8. Replace the model instrument and reuse portable knowledge on a genuinely unfamiliar query.
9. Remove an actually acquired relation/program in an isolated evaluation branch and observe the predicted selective loss of capability.
10. Exercise field-owned emission or selective native replacement on a supported acquired route, reporting both displacement and held-out adequacy.
11. Diagnose a genuine capability gap and choose a bounded internal or evidence-seeking development skill. Improve subsequent unfamiliar task behavior while preserving prior required competence and accounting for the complete development cost.
The structured and native runs share the same semantic task ID, question, and
source-revision boundary. The strict native sibling is launched with
`--without-answer-bearing-target`: it adds the declared Qwen
capture/intervention capability without supplying the native next-token target
to the field's semantic learning path. Its captured logits remain native
measurement evidence, not the structured field's answer label. The native
receipt therefore extends task-identity and native-ownership evidence, while
the twelve semantic steps are claimed from the structured field-owned route.

The passing receipt
`_diag/universal-interpreter-full-program-native-shared-strict-20260917.json`
records the twelve-step checks and its independent verifier checks the digest,
source revisions, six immutable field branches, interruption journals,
selective-loss state, and strict native linkage. The native sibling
`_diag/universal-interpreter-native-shared-task-strict-20260917.json` records
the same task ID, question, and source revision, passes the independent native
verifier, and records `answer_bearing_activation_supplied: false`. The
structured episode uses zero model calls; its fixed method returns `17` for a
held-out truth of `9`, while the acquired guard returns `9` after
interruption/reopen and preserves the corrected `11` result.

A failure at a later step does not invalidate earlier measured capabilities,
but it prevents a claim that the complete composition has been demonstrated.
The retained work-memory and latent experiments were starting evidence; this
receipt is the bounded integrated episode required by this section.


## 27. Evaluation of ambition without inflated claims

Measure six dimensions separately: mechanical integrity, causal field ownership, native displacement, task competence, transfer, and total cost. An implementation receipt cannot establish the last three by itself. An experimental displacement result remains meaningful even when task competence is poor.

Use the existing lifetime/evaluation discipline: construction, selection, and untouched evaluation data separated by world/source roots; model and task families varied; exact replay separated from new-parameter, new-role, new-composition, and new-mechanism transfer. Correlated views, chunks, model samples, and replays do not become independent successes.

Compare:

- the same model and accessible information without Cassi;
- structured Cassi reasoning versus source-only retrieval;
- matched extra native reasoning and matched total-work allocation;
- an identity native hook where identity is the experiment's claim;
- matched-strength static, no-evolution, reset, and shuffled interventions;
- cold and acquired fields;
- frozen acquired knowledge versus additional acquisition, while both retain working inference;
- field-state intervention on the actual acquired object;
- dependency-driven local reasoning, hierarchy-only scheduling, live packet priority, static/no-evolution cues, and ranking-only shuffles as specified in [Section 8](#packet-reasoning-demonstration-and-comparison);
- bounded meditation or study versus ordinary work with matched available information and total resources;
- acquired self-development methods versus a fixed balanced selector, including prior-skill retention and unfamiliar capability targets;
- one model and a genuinely different adapter/model combination.

Identity parity is required to support an identity-control claim, not a permanent ceiling on invasive experiments. A destructive replacement arm is judged as that arm, with recovery and honest measurements. Do not claim an architecture-independent improvement from selecting a winning coupling or layer on the evaluation questions.

Report per-family and worst-family outcomes, not only aggregate success. Include censored completions, timeouts, unsupported cases, harm to already-correct behavior, and withheld outputs in the denominator. Probability scores require declared probability semantics; otherwise evaluate supported sets, interval width, coverage, abstention, and decision consequences.

Independent checking should consume real persisted outputs where possible and prove that a reassuring check can fail on a plausible changed artifact. Tests that merely pin source text, count fields, or verify a mock's forwarding do not establish the reasoning behavior.

### Reused assessment and independent checking

Once feedback from an assessment influences candidate choice, curriculum, prompts, code, or a human-guided revision, that feedback belongs to the development history. This includes aggregate scores even when individual answers remain hidden. A new checkpoint, fresh process, or reset field does not restore the affected assessment's untouched status.

Known exercises remain useful for practice and regression checks. Claims about further unfamiliar-task improvement require appropriately separated assessment opportunities, with their source/world roots and disclosure history retained. Fresh instances can assess within-family behavior when their sampling and feedback boundary justify it; they do not by themselves establish transfer to a new family. Renaming a reused set or rerendering its underlying cases does not restore independence.

Correctness checking must have a basis beyond the proposal's own approval. A generated exercise with an exact derivation can support a checked conclusion under its stated semantics; a model-generated problem, answer, and approving critique do not supply three independent sources. An independently implemented checker or evaluator also retains its assumptions and tested scope. Separate exact correctness, task transfer, and the causal benefit of a learning method rather than using one reassuring score for all three.

## 28. Source ownership and integration boundaries

The following is the implementation responsibility map, not a claim that all extensions are present:

| Source | Responsibility |
|---|---|
| [CassiFI regional storage](../CassiFI/cassi_field_regions.py) | Typed layouts, exact references, scheduling state, scope/page ownership, bounded publication primitives. |
| [CassiFI computer](../CassiFI/cassi_field_computer.py) | Fixed instruction semantics and actual regional execution. |
| [LearningComputer](../CassiFI/cassi_learning_computer.py) | One machine image, retained calls and continuations; packet extension requires typed successor export, bounded partial-state retention, and root-reservation enforcement. |
| [Cognition](../CassiFI/cassi_field_cognition.py) | Shared meanings, beliefs, learning, inquiry, planning, self-development, and binding-dependent revision; owns packet assemblies, the multi-operation episode, work selection, and semantic return checking. |
| [Program machinery](../CassiFI/cassi_field_program.py) | Fixed program compilation and validated instruction vocabulary; acquired semantic program payloads and their assessment remain in the cognition path. |
| [Resonant field](../CassiFI/cassi_resonant_field.py) | Existing packet basis, canonical wave state, bounded impulses, and numerical transition; packet extension requires typed regional readouts, internal-work event attribution, and explicit state-export integration. |
| [Owner](../CassiFI/cassi_field_owner.py) | Evidence admission, operation identity, checkpoint publication, permissions, and effect reconciliation. |
| [Input codecs](../CassiFI/cassi_field_input.py) | Fixed bounded observation projection and exact provenance; no learned sensory sidecar. |
| [Qwen workbench](cassi_field_qwen_workbench.py) | Current model-facing owner integration, source exposure, explicit model requests, and ownership reporting. Extend rather than retaining a second legacy adapter. |
| [Qwen35 graph](native/llama.cpp/src/models/qwen35.cpp) | Model-specific capture/consumption sites and actual native bypass/replacement. |
| [Native context](native/llama.cpp/src/llama-context.cpp) | Sequence/native-state lifecycle, trial acceptance, persistence, and backend synchronization. |
| [Native graph declarations](native/llama.cpp/src/llama-graph.h) | Identified graph inputs/results and live owned operation boundaries. |
| [Native apprenticeship boundary](native/llama.cpp/include/llama-cassi.h) and [implementation](native/llama.cpp/src/llama-cassi.cpp) | Existing token provenance, acceptance/rollback, and field checkpoint primitives; explicit separation from the proposed full native paired continuation. |
| [Latent experiment runner](../research/run_cassi_latent_reasoning.py) | Retained controlled native measurements and explicit comparison conditions. |
| [CassiPi](../CassiPi/README.md) | Host/session/plugin surface where appropriate, without becoming another adaptive reasoning owner. |
| [CassiCore](../CassiCore/MIGRATION-STATUS.md) | Retained integration boundaries where needed; do not revive retired packages or parallel learned orchestration. |
| [CassiCosmos](../CassiCosmos/README.md) | Optional observable world/simulation surface with explicit action and observation semantics. |

The dependency direction remains model adapter to CassiFI owner, not CassiFI cognition importing Qwen or a native model library. Model-instrument operations cross an explicit external boundary and return identified observations. Standalone field execution must be possible without importing/loading model resources.

Historical Qi and prototype paths remain source-bound references until a behavior is deliberately lowered into the current machine. A migration maps every retained meaning, numeric state, support root, and continuation under explicit schemas, or reports the unsupported state. No compatibility alias or old adaptive owner remains in normal dispatch after a completed cutover. This design does not authorize changes to the read-only CassiAI archive or unrelated owner work.

## 29. Implementation sequence and completion evidence

These are dependency-ordered engineering increments. They do not reduce the destination to an additive assistant, and they do not require a new preregistration or release ceremony.

| Increment | Complete behavioral change | Evidence that distinguishes it from a scaffold |
|---|---|---|
| Shared reasoning episode | Ordinary adapter requests operate the current semantic computer beyond exact source exposure, with shared question/result/dependency semantics. | A composed answer is computed by the field, corrected through one binding, and resumed from actual retained work. |
| Resident invocation frames | Nested exact/native work retains caller cognition, callee state, dependencies, and return/result bindings inside the same image. | Interrupt a child computation, reopen, finish it, and demonstrate that parent beliefs and unrelated memory remain intact without host restoration. |
| Bounded self-development execution | Named meditation, study, practice, and review programs run through the same resident frames, with explicit information, acquisition, and resource boundaries. | Interrupt and resume real development work without replacing cognition; show working-only modes preserve retained knowledge and evidence-seeking modes use only declared sources. |
| Model measurement and adapter identity | Fixed bounded model observation envelopes and capability negotiation feed the same admission path. | Correct site/frame capture, source distinction, duplicate handling, and explicit unsupported capability behavior. |
| Owned native numeric execution | Useful latent numerical operations execute over typed regions of the current field and publish through its owner. | Consumed graph result, complete state lineage, finite bounded arithmetic, and no independent adaptive Qi checkpoint. |
| Paired execution and recovery | Native trial state and field overlays share an acceptance decision and exact or explicitly limited continuation class. | Partial acceptance, rejection, batch retry, cancellation, and crash windows exercise real coupled state. |
| Task-directed alignment and readout | Field-resident acquired programs connect model observations to shared meanings and meaningful native results. | Prospective prediction/intervention effects, valid controls, applicability limits, and selective loss under field intervention. |
| Executable capability apprenticeship (designed) | Acquire restricted SQLite semantics through prospective external execution, reuse them through the regional field, and compare text versus native observation. | The [apprenticeship design](#executable-capability-apprenticeship) specifies the ordinary workbench route and measurements. SQLite subject feasibility is exercised; learning, language transfer, and native benefit are not yet measured. |
| Lifelong reasoning and model transfer (bounded) | One continuing field acquires and reuses representations/procedures across tasks and model instruments. | The full-program receipt shows correction locality, exact restart, a new participant, a replacement model, selective relation loss, and field-owned held-out emission with measured costs. |
| Acquired improvement methods (bounded) | The field learns when and how to apply a development skill within fixed objectives and rules. | The full-program receipt exposes a cap-regime gap, acquires a bounded guard method, preserves prior competence, resumes unfinished work, and beats the fixed method on a second family under matched allocation and permissions. |
| Emission and substrate ownership | The field commits outputs and replaces supported native state/operations rather than merely adding work. | Actual zero/removed native resources for owned routes, new-input adequacy, and explicit failure without silent native fallback. |
| Broad field-native acquisition | Useful open-ended representations, productive language, mechanisms, and computational procedures are acquired at increasing scale. | Independent continuing-world and cross-domain results; scope and unsolved cases remain explicit. This remains open beyond the bounded demonstration. |

| Integrated twelve-step demonstration (bounded) | One lifetime composes acquisition, language, field-owned computation, correction, recovery, model replacement, selective loss, emission, and self-development transfer. | `run_cassi_universal_interpreter_full_program.py` passes its independent verifier; the shared-task native sibling supplies matching task/evidence identity and separate native ownership evidence. |

Structured integration and native numeric lowering can proceed independently after their shared state, event, and publication meanings are fixed. Native semantic benefit depends on a useful readout, not merely on finishing the lower-level transport. Model transfer requires preserving portable meaning, not completing every model backend first.

The initial self-development programs use standalone field operations and authorized exact evidence before native hooks are available. Their supplied strategy is disclosed; the full program now measures an acquired guard method against it with matched resources and prior-skill retention. This is a bounded method-transfer result, not evidence that self-development is broadly optimized.

The full integrated implementation is complete for the exercised bounded contract: all affected ordinary callers use the owner/lifecycle, the structured and native shared-task receipts are independently checkable, and the claimed twelve behaviors have been exercised. Broad language, high-dimensional alignment, scale, and unsupported native semantic routes remain named research requirements; they are not filled with a mock, no-op, silent fallback, or renamed legacy state.

### Packet-aware reasoning increments

**Specified mechanism.** These increments implement Section 8 through existing sources. The numerical packet primitive is retained, not rebuilt. Each increment includes its ordinary callers and direct behavioral checks; the table is an implementation dependency map, not an additional approval or preregistration process.

| Increment | Prerequisite | Complete behavior and distinguishing evidence |
|---|---|---|
| Numeric successor and partial continuation handoff | Existing resident calls and resonant state codec. | A versioned, validated export carries actual `qY,qI,pY,pI`, profile/binding/phase/clock/ledger data, and a valid retained numerical continuation where applicable. Interrupt after nonzero evolution, reopen, and continue without a host object or reconstruction from a hash. |
| Root allowance and replay-safe dispatch | Existing owner publication and invocation identity. | Extend the closed frame/return schema, publish reservation before dispatch, and reconcile each child against one protected lifetime allowance. Exercise dispatch/return/consumption crash windows, cancellation, and insufficient-result-capacity refusal. This and successor handoff can be developed independently under one agreed invocation schema. |
| Multi-operation resident episode | Both handoff and allowance increments. | Add bounded `advance-reasoning`, discovery/work frontiers, full typed result propagation, normalize-before-hash admission, and ordinary owner/CLI/workbench cutover. A composed question is completed by successive field-selected operations and paused/reopened from its actual frontier. |
| Semantic assembly and hierarchy-only execution | Multi-operation episode. | Versioned port/interface bindings connect exact dependencies to an antichain of packet supports. Unknown dependencies and unscored work remain explicit. Compare against the existing dependency-local baseline without granting a free decomposition or weakening the baseline. |
| Packet-driven selection and cue accounting | Assembly plus existing packet impulse/evolution. | Add fixed bounded cue/readout programs, `reasoning-work` attribution, one-time cue admission, and fair packet priority. The actual selected operation consumes current packet state; numerical energy and logical work remain separate. Run hierarchy/static/live/shuffled comparisons. |
| Readout refinement, branch reuse, and local repair | Assembly and valid operation/readout semantics; numerical priority is not required. | A valid bound permits one coarse result, a decisive detail forces refinement, and incompatible hypotheses remain distinct. Replace the all-at-once invalidation helper with a persisted frontier plus immediate transitive stale-read barrier. Actual numerical evolution retains its full coupling unless a separate reduction justifies otherwise. |
| Acquired interfaces, procedures, and methods | Executable episode plus prospective assessments and existing acquisition. | Parameterized reuse and a changed work method improve unfamiliar eligible tasks at accounted cost; preserve exceptions, corrected support, prior required competence, and exact restart. Fixed-method and frozen-acquisition controls remain visible. |
| Temporal and model-native packet consumers | Supported portable interface; native consumers additionally require identified model mappings and paired execution. | Establish delayed/order-sensitive utility against simpler temporal controls, or a scoped native consequence against matched interventions. Neither is a prerequisite for standalone packet reasoning, and neither follows from numerical packet reconstruction. |

Do not delay the standalone work loop for native hooks or acquired scheduling. Start with explicit interfaces and the fixed method, measure complete task behavior, and then assess the learned or dynamical contribution. Actual selective numerical lowering depends on a valid temporal/boundary error treatment; it is not bundled into the first claim of semantic work locality.

**Implemented increments.** Multi-operation resident episodes, semantic assemblies with hierarchy-only execution, packet-driven selection with cue accounting, and readout refinement, branch reuse, and local repair all run through the resident semantic state. The work-selection comparison arms (`baseline`, `hierarchy`, `static`, `live`, `shuffled`, `acquired`) dispatch from one live frontier and report their eligible scores; in the scenario the `live` arm selects the high-activation support while `baseline`, `hierarchy`, and `static` tie and keep ready order, and `shuffled` ranks by a deterministic digest. A selector Program published through the ordinary register path scores with the same bounded inputs (`age`, `base_priority`, `dependency_count`, `support_count`, `activation`, `has_support`) and reproduces the live ordering when it weights the packet cue. Root-allowance accounting, return-once consumption, and reservation release after a dependency repair are exercised by [`test_cassi_packet_reasoning.py`](../CassiFI/test_cassi_packet_reasoning.py) and the continuing-world run in [`run_cassi_reasoning_scenario.py`](../CassiFI/run_cassi_reasoning_scenario.py). The acquired selector path runs and refuses an unusable program; whether learned interfaces, procedures, or selectors transfer and repay their acquisition cost remains unmeasured, and temporal and model-native consumers remain specified. The stale-read refusal is exercised under a narrow work bound; at the default bound a correction settles its invalidation inside the operation. Whether packet scheduling improves adequacy or cost at a matched allocation remains an open measurement with no claimed advantage.

## 30. Research frontier and decisive principle

The largest unresolved questions are substantive:

- Can field-native acquisition discover useful high-dimensional model features and intervention mappings without a trained neural interface?
- Can continuous work and exact semantic structure cooperate at native inference latency without hiding a second interpreter or losing relevant distinctions?
- Can acquired reasoning procedures replace enough native work to repay their discovery, validation, and maintenance cost?
- Can semantic and procedural knowledge transfer across genuinely different models and worlds rather than just familiar trajectories?
- Can a deterministic field-owned language boundary acquire productive open-domain expression and interpretation?
- Can one finite continuing field retain useful rare knowledge, revise compressed support, and scale without destructive interference?
- Can calibrated, useful uncertainty survive model shift, partial observability, and policy-dependent evidence?
- Can the field learn when internal reorganization is more useful than new evidence, additional ordinary inference, or stopping?
- Can acquired self-development methods improve unfamiliar skills and preserve earlier competence after accounting for their own search, evaluation, and maintenance costs?

The design assigns these questions a state representation, acquisition path, execution boundary, and observable consequence. It does not answer them by naming a field, using a spectral codec, or asserting that the component successes compose automatically.

The decisive principle is shared causal use: an acquired distinction must change the appropriate prediction, reasoning operation, plan, or emitted result; survive valid restart and model changes where it is portable; and revise coherently when its support changes. The maximum ambition is a field computer that increasingly owns how it understands and computes, with LLMs becoming optional instruments rather than the permanent place where the intelligence resides.

## 31. Recursive self-improvement campaign

The strategic wager is to **amortize foundation-model training rather than repeat
it**. A pretrained LLM is an expensive, frozen archive of human and synthetic
experience. Cassi can attach to many such archives, ask them discriminating
questions, test what they say against executable worlds, and turn the useful
parts into persistent field-owned capability. The model is then removable. The
field is the learner that continues.

This changes the scaling question. The aim is not to reproduce every weight in
Qwen. It is to acquire the relations, programs, language constructions, study
methods, and world models that recurring work actually needs; reuse them without
the teacher; and make each apprenticeship improve the way the next one is
conducted. A stronger model release becomes another teacher for the same
continuing learner rather than a replacement for it.

The [SQLite executable-capability apprenticeship](#executable-capability-apprenticeship) now has measured typed transfer across new values and three structurally unseen operation compositions. This section lays out the campaign from that exact boundary.

### 31.1 The industry landscape and Cassi's present position

The September 2026 survey
[*The Last AI Built by Humans: Toward Genuine Recursive Self-Improvement*](https://arxiv.org/abs/2609.11873v2)
separates five levels by which improvement decisions the evolving system owns.
That distinction is more useful than calling every persistent memory or
self-edited prompt “RSI.”

| level | responsibility that becomes autonomous | Cassi position now | missing evidence |
|---|---|---|---|
| **L1: improvement execution** | execute a prescribed improvement and retain it | implemented in bounded form: field-owned relations, guards, and programs can be written, persisted, reopened, and reused | breadth across independently different capability families |
| **L2: improvement strategy** | diagnose a weakness and choose how to improve under a fixed objective and verifier | bounded mechanisms exist for field-supported candidate choice, acquired method selection, repair, and guarded use | repeated superiority to supplied fixed strategies on unseen work |
| **L3: experience acquisition** | decide which examples, questions, demonstrations, or experiments the next improvement needs | the fixed-tape SQLite baseline is complete; field-directed question choice remains the first direct campaign test | field-selected experience that measurably beats a matched supplied curriculum |
| **L4: deployment adaptation** | use continuing environmental feedback to revise persistent state | the field can persist across tasks, restarts, and continuing worlds | a sustained deployment trajectory in which live outcomes produce verified, reusable gains without model fallback |
| **L5: recursive inheritance** | revise an improver, verifier, curriculum policy, or successor procedure and pass it into later improvement rounds | a closed succession runs in `CassiFI/self_improvement/` (§31.6): the live brain writes each successor improver from its lineage ledger; \(M_0 \rightarrow M_1\), \(M_1 \rightarrow M_2\) and \(M_2 \rightarrow M_3\) were each promoted by matched trials on untouched functions of Cassi's own source; each promoted improver governed the next live round; the first three generations read 0.316, 2.374, 2.711 on four shared untouched function groups | the same shared-group reading for \(M_3\), and whether the gain keeps accumulating or saturates |

The survey finds broad evidence at lower and intermediate levels, bounded
meta-improvement at L5, and leaves statistically reliable accumulation across
generations under comparable resources open. That open point is exactly where
the Cassi architecture can be distinctive.

The dominant industry pattern keeps a foundation model at the center and improves
weights, prompts, context assembly, tools, synthetic data, or agent harnesses
around it. Cassi inverts the ownership:

- foundation models supply observations and demonstrations;
- executable worlds supply consequences;
- independent verifiers decide whether behavior is correct;
- the resonant field owns every adaptive change that survives;
- accepted knowledge and improvement methods remain when the model is swapped or
  disconnected.

Our opportunity is therefore architectural rather than a present benchmark
lead. Cassi already has unusually strong continuity, ownership, provenance, and
field-native method machinery. The unestablished part is broad semantic
competence and effective L3–L5 succession. The campaign is designed directly
around that gap.

### 31.2 The five-role ownership boundary

Every apprenticeship names five roles explicitly:

1. **Field.** The sole adaptive owner. Meanings, executable relations, methods,
   uncertainty, source support, and curricula that persist all live in the
   field. There is no learned sidecar, hidden classifier, weight update, or
   model fallback.
2. **Teacher.** A frozen LLM, document collection, human demonstration, or
   already verified field capability that can answer questions or show
   behavior. Teacher output is evidence with an identity, not truth and not
   adaptive state.
3. **World.** The actual system whose consequences ground the capability:
   SQLite, a stateful API, a compiler, a numerical solver, CassiCosmos, or a
   bounded service environment.
4. **Independent verifier.** A checker outside the adaptive path. The field may
   learn to predict, generate, or criticize checks, but it cannot rewrite the
   protected acceptance reading used to establish progress.
5. **Authority.** The human-owned mission, resource limits, external-action
   permissions, rollback boundary, and final deployment decision.

This boundary lets the improver become increasingly autonomous without making
the evidence self-referential. It also makes model reuse clean: changing the
teacher changes the incoming evidence source, not the identity of the learner.

### 31.3 What field-native training means

The unit of training is a **verified experience cycle**, not a gradient step:

1. the field recognizes an unsupported distinction or a failed behavior;
2. it predicts what it can before seeing the answer;
3. it chooses a question, demonstration, experiment, or counterexample that
   separates its live alternatives;
4. a teacher or world returns an observation carrying source and operation
   identity;
5. the field constructs or revises an executable relation, program,
   construction, or learning method;
6. the world executes the result and the independent verifier reads the
   consequence;
7. useful structure is consolidated with its support, exceptions, and
   applicability guard;
8. the teacher is disconnected and the field must use the capability natively;
9. delayed reuse, composition, and interference checks decide whether the
   structure remains live;
10. the acquisition trace becomes experience for improving the next learning
    cycle.

The essential causal direction is:

\[
\text{teacher/world evidence}
\;\longrightarrow\;
\text{field update}
\;\longrightarrow\;
\text{field-owned action}
\;\longrightarrow\;
\text{independently checked consequence}.
\]

Depositing a model answer and replaying it is not learning. The acquired object
must do work on a held-out root the teacher never answers during evaluation.

### 31.4 The field-specific training-program stack

These programs are cumulative. Each teaches a different part of intelligence,
and each leaves a reusable field-owned object for later programs.

| program | what the field practices | what persists | evidence that it worked |
|---|---|---|---|
| **Meaning apprenticeship** | infer typed meaning from examples, documentation, natural language, and contrasts | constructions linking language, concepts, and executable programs | unseen paraphrases and structures execute correctly after teacher disconnection |
| **Active inquiry** | choose the next question by uncertainty, disagreement, expected discrimination, and cost | a question-selection relation conditioned on the live frontier | more verified root coverage per teacher/world interaction than a matched supplied sequence |
| **Causal world apprenticeship** | predict state transitions, errors, ordering effects, and intervention consequences | executable transition relations and applicability guards | correct action in unseen states, including negative and failure cases |
| **Deliberate practice** | generate boundary cases, rare cases, counterexamples, and spaced revisitations | exercise-generation and rehearsal methods | practice selected by the field produces later transfer rather than repeated easy successes |
| **Compositional practice** | combine already acquired primitives into deeper programs without receiving the target composition | reusable sequence, branch, bind, and subprogram structures | unseen combinations work while the primitive semantics remain unchanged |
| **Correction and repair** | localize a failure, propose a bounded revision, and test affected and unaffected regions | guarded amendments with evidence for why they were admitted | the failed case is repaired without erasing established roots or exceptions |
| **Consolidation and lifetime training** | decide what to reinforce, factor, compress, associate, or retire | hierarchical/fractal bindings and measured support lifetimes | delayed restart reuse, old-skill retention, and bounded interference as the field grows |
| **Cross-teacher transfer** | compare teachers, source documents, and direct world evidence while preserving source identity | portable invariants plus source-specific reliability and vocabulary bindings | the native capability survives teacher replacement and works with a previously unseen model |
| **Communication and teaching** | reconstruct field-owned capability as examples, explanations, questions, or demonstrations for another learner | field-to-language constructions and teachable curricula | a fresh field lineage or human can reproduce the capability more efficiently from the field's lesson |
| **Learning-method apprenticeship** | compare inquiry, practice, consolidation, repair, and teacher-selection methods by later outcomes | guarded methods selected by task structure | an inherited method improves acquisition on unseen capability families at matched budget |
| **Curriculum and evaluator apprenticeship** | identify capability gaps, order prerequisites, create discriminating tasks, and propose can-fail checks | a field-owned frontier model, curriculum policy, and internal critic | useful next experiences are selected autonomously while a protected external verifier retains acceptance authority |
| **Native displacement** | decide when existing field competence is sufficient and when new evidence is worth its cost | calibrated routing, unsupported-case detection, and owned executable capability | model calls and tokens fall while protected task quality, exception handling, and retention hold |

The programs make the field more than memory. They train sensing of uncertainty,
selection of evidence, construction of behavior, correction, long-term
organization, and eventually the process that trains those abilities.

### 31.5 The apprenticeship ladder

The campaign crosses worlds that demand genuinely different kinds of structure.
Each rung adds a new kind of reality rather than another benchmark with the same
shape.

1. **Relational and sequence semantics — typed parametric composition measured.** Typed operations, null behavior, ordering, filtering, aggregation, exceptions, nesting, and sequence composition now transfer across supplied unseen ASTs. Learned language and autonomous curriculum remain the remaining SQLite frontiers; field-owned routing and bounded question choice are measured.
2. **Stateful and transactional tools.** A bounded local API or versioned
   key-value world adds temporal state, idempotence, rollback, conflicts,
   partial failure, and action preconditions.
3. **Program construction and repair.** Small Python and repository tasks add
   synthesis, compiler/runtime feedback, localization, regression preservation,
   and reusable abstractions.
4. **Numerical and scientific modeling.** CassiTheory solvers and controlled
   numerical worlds add units, approximation, continuous dynamics, parameter
   identification, model comparison, and uncertainty about observations.
5. **Embodied field interaction.** CassiCosmos adds partial observability,
   intervention, delayed consequences, spatial structure, control, and the need
   to distinguish a transient from a persistent world change.
6. **Open research and provider work.** Bounded real workloads combine
   language, tools, code, evidence, explanation, and long-horizon correction
   while retaining human authority over consequential actions.

Language is not postponed to the final rung. Every world is learned twice:
first through a typed route that isolates the field computation, then through
learned language constructions that must reach the same native programs. Each
world also receives the four extensions defined in the [executable-capability apprenticeship](#executable-capability-apprenticeship): learned language,
native observation, field-directed question choice, and actual model transfer.

The first two worlds establish exact semantics. Program repair establishes
generative construction. Numerical and embodied worlds prevent the learning
method from specializing to discrete answer tables. Open work tests whether the
same field can compose them.

### 31.6 Recursive succession: from learning tasks to learning how to learn

Let \(F_g\) be the continuing field state at generation \(g\), and let \(M_g\)
be the field-owned method that chooses questions, practice, consolidation,
repair, and stopping. An apprenticeship produces both capability and evidence
about the method:

\[
(F_{g+1}, M_{g+1})
=
\mathcal{L}(F_g, M_g, E_g),
\]

where \(E_g\) contains the complete sourced experience and checked outcomes.
The recursive step is not the existence of \(M_{g+1}\). It is that
\(M_{g+1}\) governs a later apprenticeship and is itself revised from what that
later apprenticeship reveals.

Two separate claims are required:

- **structural recursion:** the field revised a learning mechanism, retained it,
  and the revised mechanism actually controlled a later improvement round;
- **effective recursion:** from matched starting field snapshots, evidence
  access, and total budgets, the revised mechanism produced a stronger later
  learner on untouched capability families.

For an unseen apprenticeship family \(D\), protected reading \(R\), and total
budget \(B\), the decisive comparison is:

\[
R\!\left(A(F^\star, M_{g+1}, D, B)\right)
>
R\!\left(A(F^\star, M_g, D, B)\right).
\]

The budget includes teacher calls and tokens, world interactions, field work,
verification, rejected candidates, and human interventions. Giving the revised
method more search is not method improvement.

The first complete succession has this form:

1. \(M_0\) conducts the early apprenticeships.
2. Their traces let the field acquire \(M_1\), such as a better
   uncertainty-directed question schedule or consolidation rule.
3. Matched lineages use \(M_0\) and \(M_1\) on previously unseen
   apprenticeships; only protected downstream outcomes can promote \(M_1\).
4. The accepted \(M_1\) governs the next live learning round.
5. New failures and efficiencies produce a candidate \(M_2\).
6. \(M_2\) is compared with \(M_1\) on fresh capability families under the same
   accounting.
7. The live lineage inherits only revisions that improve later learning while
   preserving earlier capability.

One successful method change is a meta-learning result. Two successive,
inherited method revisions that each improve later apprenticeships establish the
first bounded recursive chain. Longer succession then tests whether the gain
accumulates or saturates.

#### The running succession

`CassiFI/self_improvement/engine.py` runs this succession with Cassi's own
source as the apprenticeship world. An improver \(M_g\) is a policy module
with three duties: `plan` chooses which functions get the brain's calls and
how each request is framed, `parse` reads a rewrite out of each reply, and
`successor_messages` asks the brain for the next improver. A rewrite counts
only when every workload that runs its module keeps its exact output and the
function's inclusive time beats the original by at least 5 % in interleaved
timed pairs on a quiet core. An arm's reading is the sum over its functions of
the log of each function's best verified speedup, so a 2× rewrite adds
\(\ln 2\) whatever the function.

A successor is judged against its parent on functions neither has touched:
both spend the same 12 brain calls and 60,000 tokens on each replicate group,
with shared brain seeds, and promotion requires the summed reading to win.
The promoted improver then governs the next live round, its verified rewrites
enter the live tree, and it writes its own successor. Each successor is
written in two steps: the brain first diagnoses the lineage ledger (where
readings came from, which retries ever paid, where calls and tokens went
unspent) and chooses one evidenced change, then writes the module around it.

The chain so far:

| step | trial readings (parent → child) | what the child changed |
|---|---|---|
| \(M_0 \rightarrow M_1\) | 2.980 → 3.172 | tolerant parsing of cut replies, tighter context, retries that name the failure |
| \(M_1 \rightarrow M_2\) | 0.269 → 0.646, both replicate groups won | retries shaped by the previous outcome (near miss, slowdown, changed output, cut reply), leaner context, cooler first attempts |
| \(M_2 \rightarrow M_3\) | 0.204 → 1.777, one group won and one tied | reasoning mode off in the apprentice calls and temperature 0.4, both read off the ledger's verified rate per request setting |

Four successors written in one step, without the diagnosis, lost to \(M_1\);
the first diagnosed successor won with three verified functions against one
while spending fewer calls (18 against 22) and tokens (69 k against 88 k).
\(M_2\) governed the second live round and wrote \(M_3\). \(M_3\) then won on
Cassi's own accounting: the ledger's request-setting table showed no verified
rewrite from a reasoning-mode apprentice call and a better rate at temperature
0.4 than 0.3, and the successor turned reasoning off and moved the
temperature. Later successors read the same table, so each round's attempts
are recorded in the form the next round can act on.

On four held-out function groups that no generation had touched, under the
same 12 calls and 60,000 tokens per arm, the three generations read

| group | \(M_0\) | \(M_1\) | \(M_2\) |
|---|---|---|---|
| 1 | 0.238 | 0.663 | 0 |
| 2 | 0 | 0.070 | 0.147 |
| 3 | 0 | 1.503 | 2.338 |
| 4 | 0.078 | 0.138 | 0.226 |
| total | 0.316 | 2.374 | 2.711 |

\(M_1\) beats \(M_0\) in every group and \(M_2\) beats \(M_1\) in three of
four, with six verified functions against seven and 120 k tokens against 132 k.
This crosscheck ran on the 9B dense brain while the lineage was bred with the
35B mixture-of-experts brain, so what inherited is method rather than one
model's habits.

### 31.7 Campaign sequence

The implementation order is:

#### Campaign A — finish the first apprenticeship

- retain the independently verified new-value baseline as the first bounded
  consequence-supported result;
- retain the independently verified unseen-composition result as the first
  typed parametric transfer result;
- retain the independently verified field-owned support aggregation and final
  routing result;
- retain the independently verified teacher-disconnected language-to-program
  result after persistence reopen;
- retain the independently verified bounded field-directed question-choice and
  coupled native-observation result;
- retain the independently verified native-observation evidence update, with
  the native event and binding as dependencies of a field predictive record;
- continue with actual model teaching in which native observations become
  field-owned evidence for a learned capability rather than a recorded boundary.

The retained results establish execution on new values, supplied unseen
compositions, field-owned candidate routing, one bounded language construction,
field-directed question choice, a native observation boundary, and the first
field-owned native evidence update. The remaining steps establish field-owned
study beyond a fixed decoy policy, native semantic benefit, actual model
teaching, and the complete apprenticeship shape.

#### Campaign B — establish breadth before abstraction

- build the stateful/transactional world and the small-program repair world;
- reuse existing owner/program/verifier surfaces directly;
- extract a shared apprenticeship runner only after the second world shows
  which structure is genuinely common;
- carry prior SQLite roots into both worlds so interference is visible;
- require learned-language and native-action routes in each world.

This prevents a SQLite-specific success from being mistaken for a general
learning mechanism.

#### Campaign C — train the learner's study methods

- compare supplied, uncertainty-directed, disagreement-directed, and
  consequence-directed question selection;
- compare passive replay with generated counterexamples and compositional
  practice;
- compare fixed consolidation with field-selected reinforcement and retirement;
- let the field learn applicability guards that choose among these methods;
- evaluate the methods only by later acquisition and retention.

The retained object is no longer only a tool capability. It is a repertoire of
ways to acquire capabilities.

#### Campaign D — run the first recursive succession

- derive \(M_1\) from the completed discrete apprenticeships;
- compare \(M_1\) with \(M_0\) on untouched numerical and program families;
- inherit the accepted method into the continuing lineage;
- derive \(M_2\) from its successes and failures;
- compare \(M_2\) with \(M_1\) on untouched embodied and research families;
- repeat across seeds or independent task roots so one lucky curriculum cannot
  carry the claim.

This is the first direct attack on the industry's open effective-L5 question.

#### Campaign E — give the field the learning agenda

- maintain a field-owned map of supported capabilities, weak boundaries,
  unresolved contradictions, estimated value, and acquisition cost;
- let the field choose the next capability frontier, teacher, world
  interaction, practice family, and stopping point;
- preserve protected reference roots alongside field-generated frontier tasks;
- evaluate whether the chosen agenda produces more future capability than a
  matched human-authored or fixed agenda.

This moves experience acquisition from a feature of one apprenticeship to the
organizing process of the system.

#### Campaign F — improve evaluation without surrendering the anchor

- train the field to generate counterexamples, metamorphic relations, mutation
  cases, and checks that can fail;
- version internal critics and record which failures they discover;
- admit evaluator revisions only when they predict protected world outcomes
  better without changing the protected acceptance reading;
- compare candidate generation and candidate evaluation separately before
  combining them.

The field learns how to test itself while remaining unable to declare itself
correct.

#### Campaign G — persistent deployment and model displacement

- run one field lineage across changing provider tasks and delayed reuse;
- query models only where field uncertainty and expected information justify
  the cost;
- swap teacher models while preserving the continuing field;
- measure which capabilities remain native, which need refresh, and which
  transfer to the new teacher's language;
- progressively remove model access from mature capability families;
- retain model access as a source of new frontiers rather than a hidden runtime
  dependency.

This is where reused pretrained technology becomes an expanding field-owned
system rather than a permanent inference bill.

### 31.8 The scorecard

Every run reports a compact set of readings that answer different questions:

| reading | question answered |
|---|---|
| **protected root correctness** | did the acquired capability actually work? |
| **structural coverage** | which operations, branches, compositions, and exception families are supported? |
| **teacher-disconnected coverage** | what fraction works with zero model calls during evaluation? |
| **field causal effect** | do reset, lesion, shuffled-support, or wrong-method controls remove the acquired gain? |
| **question efficiency** | how much protected capability is gained per teacher/world interaction? |
| **total acquisition cost** | how many model tokens, world operations, field operations, verifier calls, rejected revisions, and human interventions were required? |
| **retention** | what survives delay, restart, additional learning, and sparse use? |
| **interference** | which established roots regress after each new apprenticeship? |
| **composition depth** | how far can native programs be combined beyond observed demonstrations? |
| **source portability** | does the capability survive a new teacher, wording family, model, or evidence source? |
| **agenda autonomy** | which questions, exercises, teachers, frontiers, and stopping decisions were field-selected? |
| **method inheritance depth** | how many accepted method revisions actually governed later apprenticeships? |
| **recursive effectiveness** | how much downstream acquisition gain does \(M_{g+1}\) produce over \(M_g\) at matched total budget? |
| **native displacement** | how quickly do model calls and tokens decline for stable capability families without quality loss? |

No single score substitutes for this vector. Capability can rise while method
quality falls; autonomy can rise while efficiency falls; memory can grow while
usable support shrinks. The campaign succeeds only when these distinctions
remain visible.

### 31.9 The scaling strategy

The resource advantage comes from compounding reuse:

1. **Pay once for evidence.** Every model observation is source-bound and
   reusable. The same answer is not repeatedly purchased as prompt context.
2. **Ask discriminating questions.** Active inquiry spends model and world calls
   on distinctions that can change the field, not on broad imitation corpora.
3. **Turn answers into practice.** One teacher observation can seed many
   field-generated contrasts, compositions, and counterexamples checked by a
   cheap world.
4. **Compile recurring competence.** Mature capabilities become field-owned
   programs and relations, removing tokens from later execution.
5. **Transfer primitives.** Ordering, binding, branching, causality, repair, and
   uncertainty methods learned in one world lower the cost of the next.
6. **Use a teacher portfolio.** Small local models handle cheap variation;
   stronger models are reserved for frontier distinctions; direct world evidence
   outranks both.
7. **Keep the student through model generations.** New LLMs add knowledge to an
   existing field rather than resetting the learner.
8. **Improve the acquisition policy.** Recursive succession attacks the cost of
   learning itself, not only the cost of executing learned work.

The desired scaling curve is therefore not merely higher task accuracy. As
field capability grows:

- teacher calls per newly verified root should fall;
- the fraction of work completed natively should rise;
- transfer to structurally different worlds should increase;
- maintenance cost per retained capability should stay bounded;
- accepted learning methods should improve downstream gain per unit resource.

If those curves do not appear, Cassi may still be a useful continual learner,
but it has not yet escaped the industry's scaling burden.

### 31.10 Confounds the campaign excludes

- **Held-out leakage.** Target programs, expected answers, hidden rules, and
  verifier outputs remain checker-only. They do not enter candidate registries,
  prompts, deposits, or field support.
- **Model fallback.** Evaluation records every model request and fails the
  teacher-disconnected reading if any answer path reaches a model.
- **Budget inflation.** Method comparisons match total resource budgets,
  including the cost of inventing and evaluating the method.
- **Evaluator co-adaptation.** Internal critics may evolve; the protected anchor
  does not move with them.
- **Task improvement mistaken for method improvement.** A better solver is L1–L4
  evidence until a revised learning mechanism controls and improves a later
  acquisition round.
- **Accumulation mistaken for intelligence.** More deposits or more active cells
  do not count unless they cause correct held-out action and later transfer.
- **Model-specific mimicry.** Source identity remains explicit, and capability
  must survive paraphrase and teacher replacement.
- **Lineage contamination.** Matched method comparisons start from immutable
  field snapshots and disjoint hidden roots.
- **Selective reporting.** Rejected methods, regressions, recovery, and stopping
  decisions stay in the trajectory.
- **Self-confirmation.** Every reassuring zero or identity comparison receives a
  mutation or firing control showing that the reading can detect a real defect.

### 31.11 The decisive demonstration

The result worth racing toward is one continuing field lineage that:

1. acquires verified capability in several structurally different worlds from
   frozen LLM teachers and executable consequences;
2. performs the mature capabilities after every teacher is disconnected;
3. survives restart, delayed use, further learning, and a teacher-model swap;
4. revises its acquisition method from \(M_0\) to \(M_1\), uses \(M_1\) on new
   apprenticeships, and then revises it again to \(M_2\);
5. shows both meta-updates improving later acquisition across independent unseen
   capability families under matched total budgets;
6. preserves earlier capabilities or autonomously detects, repairs, and
   rechecks a regression;
7. chooses useful later experiences and stopping points from its own measured
   frontier;
8. reduces model calls per verified capability while increasing native coverage;
9. loses the gains under field lesion or method substitution; and
10. accomplishes all of this with frozen model weights, no learned sidecar, and
    independent protected verification.

That would establish bounded but genuine structural and effective recursive
self-improvement. It would also demonstrate the economic thesis: expensive
foundation models can be mined into a cheaper, persistent, improving substrate
instead of being paid forever as the substrate.

Open-ended recursive improvement is a later empirical question. We should claim
the exact inheritance depth, domains, budgets, retention horizon, and
displacement achieved, then extend the lineage until the measured curve
saturates or continues.

### 31.12 Authority, rollback, and stopping

The field may increasingly own capability construction, study strategy,
curriculum, internal criticism, and resource proposals. It does not own:

- the protected acceptance anchors used to establish progress;
- the immutable lineage snapshots needed for comparison and rollback;
- external-action permissions;
- resource ceilings;
- the mission or the human decision to deploy a successor.

A proposed method that fails later-transfer, retention, cost, or safety readings
is retained as sourced negative experience but does not replace the live method.
The system can learn why it failed without inheriting the failure.

The field also learns to stop: when uncertainty is below the action threshold,
when available questions cannot separate alternatives, when expected capability
gain is below acquisition cost, or when authority for the next experiment is
absent. Better judgment about what not to learn or attempt is part of the
improvement process.

### 31.13 Immediate continuation

The first continuation is now measured: the coupled native observation is an
explicit input to a field-owned predictive evidence update, with the native
event and binding retained as dependencies. The remaining immediate work is:

1. replace the deterministic fixture with actual model teaching and test
   teacher-model transfer;
2. build the stateful world and small-program repair world without adding a
   parallel learner;
3. use those traces to construct the first field-owned acquisition-method
   comparison and carry any accepted \(M_1\) into the numerical apprenticeship.

This sequence turns typed compositional execution into learned semantic
construction, then into the seed of recursive succession.
