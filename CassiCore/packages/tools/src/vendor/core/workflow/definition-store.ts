/**
 * VENDOR TYPE STUB — `core/workflow/definition-store.ts` (`WorkflowDefinitionStore`).
 *
 * Type-placeholder for the workflow definition store surface referenced as an
 * inline `import(...).WorkflowDefinitionStore` type in registerCoreTools deps.
 * Aliased to foundation's canonical `IWorkflowDefinitionStore` so the tools'
 * makeWorkflowHandler deps typecheck. Owned by `@cassicore/workflow` (P6);
 * re-pointed there.
 */
import type { IWorkflowDefinitionStore } from '@cassicore/foundation'

/** Persists and queries workflow definitions. */
export type WorkflowDefinitionStore = IWorkflowDefinitionStore
