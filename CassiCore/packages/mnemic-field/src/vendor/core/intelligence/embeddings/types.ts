/**
 * TYPE STUB — embeddings/types.ts (core/intelligence/embeddings/types.ts).
 *
 * Faithful type surface for the symbol mnemic-field consumes: `RerankerMode`.
 * Re-point to the owning package at P5 via the repoint log.
 */
export type RerankerMode = 'local' | 'llm' | 'off'
