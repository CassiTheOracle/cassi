/**
 * VENDORED — temporary type surface of `core/intelligence/cortex/index.ts`.
 * Consumed by @cassicore/thalamus index.ts as `CorticalField` (type-only).
 *
 * `CorticalField` is the six-region cortical field surface
 * (`sensory/association/executive/motor/limbic/monitor`). Thalamus holds it
 * as a bare class type (field + setter), so only the class declaration is
 * needed here. Re-point to `@cassicore/cortex-pineal-dialectic` when that
 * package lands (P5-A turn 2).
 */
export declare class CorticalField {}
