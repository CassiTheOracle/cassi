{
  "name": "@cassicore/plugins",
  "version": "0.1.0",
  "description": "Host-side plugin system: PluginAPI, PluginRegistry, CassiCoreClient SDK, PluginHost (fork-based worker manager) and the external-client curator. Extracted from CassiCore core/plugins/* + core/plugin-host.ts. History-preserved import splice; depends on @cassicore/foundation (protocol types), @cassicore/events (bus), @cassicore/thalamus (external-clients).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@cassicore/events": "*",
    "@cassicore/foundation": "*",
    "@cassicore/thalamus": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
