{
  "name": "@cassicore/workflow",
  "version": "0.1.0",
  "description": "Agent workflow system: engine, builder, steps, templates, persistence, registry, scheduler, state machine, adapters, trigger store. Extracted from CassiCore. History-preserved import splice; depends on @cassicore/foundation (workflow/interfaces types) and @cassicore/{helix,constellation,tools} (adapters), better-sqlite3.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@cassicore/constellation": "*",
    "@cassicore/foundation": "*",
    "@cassicore/helix": "*",
    "@cassicore/tools": "*",
    "better-sqlite3": "^11.10.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
