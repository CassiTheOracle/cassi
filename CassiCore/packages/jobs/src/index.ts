/**
 * @cassicore/jobs — public API.
 *
 * Background job manager extracted from CassiCore core/jobs. Packager-written
 * barrel (source dir had none).
 */
export { JobManager } from './job-manager.js'
export { Job, JobConfig, JobResult, JobStatus, RingBuffer } from './types.js'
