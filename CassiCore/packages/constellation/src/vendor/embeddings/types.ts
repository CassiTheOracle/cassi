/**
 * VENDORED TYPE STUB — mirrors `embeddings/types.js` (CassiCore) type surface
 * re-exported by the vendored `mnemic-field/types.ts` (RerankerMode).
 */
export type RerankerMode =
  | 'hybrid'
  | 'semantic'
  | 'keyword'
  | 'default'
  | string
