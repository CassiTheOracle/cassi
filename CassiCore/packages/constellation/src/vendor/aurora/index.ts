/** VENDORED TYPE STUB — mirrors `aurora/index.js`. Surface: Aurora + auto-scheduling. */
export interface Aurora {
  collectAutoScheduledTopics(sessionCount: number, directedMeditationCount: number): string[]
  [key: string]: unknown
}
