/** VENDORED TYPE STUB — mirrors `mnemic-field/edge-relators.js`. Surface: PhrasePrototypeSet. */
export interface PhrasePrototypeSet {
  phrases: Record<string, string[]>
  labels: string[]
  [key: string]: unknown
}
