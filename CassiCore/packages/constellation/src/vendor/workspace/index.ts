/** VENDORED TYPE STUB — mirrors `workspace/index.js`. Surface: GlobalWorkspace, CognitiveSignal, SignalType, WorkspaceResponse. */
import type { GlobalWorkspace, CognitiveSignal, SignalType } from './global-workspace.js'
export type { GlobalWorkspace, CognitiveSignal, SignalType }

export interface WorkspaceResponse {
  content: string
  [key: string]: unknown
}
