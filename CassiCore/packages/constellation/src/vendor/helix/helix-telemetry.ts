/**
 * VENDORED TYPE STUB — mirrors `helix/helix-telemetry.js` (CassiCore) type surface
 * referenced inline by the vendored `helix/types.ts` (HelixProjectOpts.telemetry).
 */
export interface HelixTelemetry {
  [key: string]: unknown
}
