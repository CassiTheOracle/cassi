/**
 * VENDORED TYPE STUB — mirrors `helix/work-stream.js` (CassiCore) type surface
 * referenced by the vendored `helix/types.ts` (HelixProjectOpts.unityStatusThresholds).
 */
export interface UnityStatusThresholds {
  maxIterations?: number
  maxElapsedMs?: number
  maxRepeatedToolCalls?: number
  [key: string]: unknown
}
