/**
 * VENDORED TYPE STUB — mirrors `helix/helix-metrics.js` (CassiCore) type surface
 * referenced inline by the vendored `helix/types.ts` (HelixResult.metrics).
 */
export interface HelixMetricsSnapshot {
  [key: string]: unknown
}
