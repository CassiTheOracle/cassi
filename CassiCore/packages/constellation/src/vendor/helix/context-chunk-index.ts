/**
 * VENDORED TYPE STUB — mirrors `helix/context-chunk-index.js` (CassiCore) type surface
 * referenced inline by the vendored `helix/brainstem-types.ts` BrainstemDeps.unityChunkIndex.
 */
export interface ContextChunkIndex {
  [key: string]: unknown
}
