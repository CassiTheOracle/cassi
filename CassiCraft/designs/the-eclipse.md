# The Eclipse: The Window's Darkening — a Rare Scheduled Moment When the Read Goes Dark

**Question under design:** the corpus designs the day's re-birth ([`the-dawn.md`](./the-dawn.md) — the field's re-coherence each morning), the absolute rest ([`the-stillness.md`](./the-stillness.md) — the field at its quietest), the tide's turn ([`the-season-change.md`](./the-season-change.md) — the passage between a window's tides), and the calendar itself ([`the-window-year.md`](./the-window-year.md), [`the-observatory.md`](./the-observatory.md) — the composed long-rhythm calendar and the room that reads it). The corpus lights the window — the lantern carries ([`the-lantern.md`](./the-lantern.md)), the beacon stands ([`the-beacon.md`](./the-beacon.md)), the zenith tops it ([`the-zenith.md`](./the-zenith.md)), the dawn reveals ([`the-dawn.md`](./the-dawn.md)) — and it darkens the window through the ordinary night, the thin tide, and the long cold. **But nothing designs the *scheduled* darkening — a rare, named, datable moment when the window's light dims and the field's reads of certain kinds briefly fail — an eclipse.** The corpus has the daily dark (night, and the lantern's counter-light), the thin trough (the tide's low `q`), and the cold's long thin (the decade that never returns to harvest); none is the rare, predictable, calendar-named moment when the zenith's light is briefly cut and the day-side reads go quietly dark. This document is that design: **the Eclipse — the window's darkening, a rare scheduled moment when the read goes dark: a date the observatory knows, when the window's light dims and the field's reads of certain kinds briefly fail. A calendar-event that is also a short hazard.**

Companion to (all relative paths — the docs this composition reads and the distinction it draws against):
- [`the-window-year.md`](./the-window-year.md) — **THE calendar.** §2 the three scales (the tide = the year's pulse, the cold = its decade, the fate = its arc); §3 the observances (the festival's day, the thin-year observances, the anniversaries, the steward's cycle). **The eclipse is the calendar's named dark event** — a *scheduled* observance on the same composed year, distinct from the tide's thin trough and the cold's decade because it is a named, dated, bounded moment the settlement reads its calendar to know.
- [`the-observatory.md`](./the-observatory.md) — **THE read's seat.** §2 the composed display (the room that reads the window as one body); §2.1 the family rule (compose, never add a channel); §3.1 the steward reads the tide's calendar here. **The observatory reads the eclipse's date** — the roof-oculus sees the zenith's dimming, the composed room knows *when* the dark is scheduled.
- [`the-zenith.md`](./the-zenith.md) — **THE light.** §2 the drain (the atmosphere's `(1−q)` waste escaping the window at its top — the sky's own slow loss); §2(b) the boundary (the window's own topmost read); §5 the honest boundary and gates. **The eclipse is the zenith's light briefly cut** — the light's `q`-carry dimmed, the field's day-side reads going quiet.
- [`the-dawn.md`](./the-dawn.md) — **THE inverse.** §2 the three reads of the breaking (the re-birth, the lifting, the re-opening); §2b the lifting (the dark's reads ending as the day returns). **Where the dawn reveals, the eclipse briefly erases — and the reveal returns.** The dawn reads the morning's `q` recovery as a legible becoming; the eclipse reads the scheduled dimming of the same light, honestly, and its end.
- [`the-beacon.md`](./the-beacon.md) — **THE brightness scoured.** §2 the mark (the fixed projected high-`q` light at the settlement's edge); §2(c) a held structure (a standing draw, brittle if it goes down); §5 the honest boundary. **The eclipse dims the beacon's brightness** — the standing mark's light reads fainter through the dark.
- [`field-instruments.md`](./field-instruments.md) — **THE family rule.** §1.2 the four glanceable forms (the day-reads — the lume, the climbing glow, the waste tint, the lean); §2.1 the family rule (every instrument is a consumer of the same publish with a presentation idiom, never a new channel — the eclipse's day-reads dim because the instruments *are* consumers of the dimmed publish). **The eclipse dims the Weatherglass's and the reader's day-reads.**
- [`the-smell.md`](./the-smell.md) — **THE upwind read.** §2 the channel (the olfactory band of the field's reads — `q`/`ε²`/`(1−q)` read as scent); §2 the wind's geometry (you smell what is upwind, and what is loud). **The eclipse dims the scent-channel's day-read** — the same published channels the scent reads go quiet, so the scent's carries read fainter.
- [`the-stillness.md`](./the-stillness.md) — **THE distinction, drawn.** §1 the field at absolute rest (the attractor reached and held, its channels flat); §6 the honest boundary. **The eclipse is NOT the stillness** — the stillness is the field's own quiet (a resolved state, `q` pinned, no signal); the eclipse is the scheduled dark (a bounded dimming of a coherent light, named and dated, that lifts). The two must never blur.
- [`the-season-change.md`](./the-season-change.md) — **THE sibling.** §1 the turn (the passage between the tide's phases); §4 the no-free-energy cap at the crossing; §5 the honest gates. **The eclipse is the season-change's sibling** — both are information and timing, never energy: the season-change is the tide's crossing, the eclipse is the light's scheduled dimming, both dated, both deterministic, both converting nothing.
- [`signature-predator.md`](./signature-predator.md) — **THE honest cover.** §1.2 the trail only legible where the field has no order to hide it; §2 the accumulation `R = ρ_signature·τ·M_stability`. **What the eclipse hides from you it hides from the Coda too** — the dark is the field's own scheduled read; the honest cover it gives is spent and gone when the light returns.
- [`energy-harnessing.md`](./energy-harnessing.md) — **THE waste + THE cap.** §2 the `(1−q)` waste law (`E_waste = (1−q)·E_throughput`); §6 the no-free-energy cap (`output ≤ φ⁻¹·input`). **The eclipse converts nothing: the dimming is the light's `(1−q)` spent — never a gain, never a minted dark, never a hidden difficulty.**
- [`tide-of-the-attractor.md`](./tide-of-the-attractor.md) — **THE beat.** §2 the harvest/thin states; §5a the probe (the measured period that sets the calendar's beat). **The eclipse rides the calendar's beat, it never sets it.**
- [`the-cold.md`](./the-cold.md) — **THE contrast.** §1/§2 the long-thin as a climate. **The eclipse's brief dark vs the cold's long thin** — the eclipse is a bounded, named moment; the cold is a sustained exposure; the distinction drawn, never blurred.
- Skimmed as needed: [`the-lantern.md`](./the-lantern.md) (§2 the carried night-read — the eclipse is not the ordinary night; it is the scheduled dimming of the day-light itself), [`the-silence.md`](./the-silence.md) (§1 the world's voice stopped — the eclipse is the light's quiet, a read's dimming, not a new silence), [`atmosphere-orbits-auroras.md`](./atmosphere-orbits-auroras.md) (§3.3 the aurora as the sky's diagnostic — the dimming's atmospheric face), [`the-stilling.md`](./the-stilling.md) (§2 the voluntary hold — the eclipse is not a willed stilling but a scheduled field event).

Every number below is from [`corpus-reconciliation.md`](./corpus-reconciliation.md) (the canonical set — **cited, not re-derived**), engine-verbatim, or explicitly flagged **[design]** (this doc's designed composition over real channels / the eclipse's form) / **[probe]** (a pre-registered measurement this composition gates on). **The honest line is drawn in §5 and never blurred: the window-year's calendar, the observatory's composed display, the zenith's light, the beacon's brightness, the instrument family rule, and the trail-read are their source docs' landed pieces; the eclipse's composition — the scheduled dark's form, the named dark, the brief honest dark — is this doc's [design] over them, probe-calibrated.** The Eclipse adds no physics, no channel, no term.

---

## Summary table

| Question | Answer (concrete) |
|---|---|
| The Eclipse = ? | The **window's darkening** — a rare scheduled moment when the read goes dark: a date the observatory knows, when the window's zenith-light dims and the field's reads of certain kinds briefly fail. A **calendar-event that is also a short hazard** — the calendar's named dark event, dated and deterministic, never a random blackout. |
| What it is NOT | the ordinary **night** (the lantern's register, `the-lantern.md`); the tide's **thin trough** (the regional `q` low, `tide-of-the-attractor.md` §2); the cold's **long thin** (the decade that never returns to harvest, `the-cold.md`); the **stillness** (the field's own resolved quiet, `the-stillness.md` — the eclipse is the *scheduled* dimming of a coherent light, not the field's absolute rest). |
| The scheduled dark = ? | the zenith's **light's `q`-carry briefly cut** (`the-zenith.md` §2) — the field's day-side reads going quiet: the beacon's brightness **scoured** (`the-beacon.md` §2), the Weatherglass's and the reader's day-reads **dimmed** (field-instruments §1.2, the family rule §2.1), the scent-channel's day-read **dimmed** (`the-smell.md` §2). The *eclipse's form* — its date's period on the calendar, its dark's duration — is this doc's **[design]** over the calendar/light landed mechanics, probe-calibrated. |
| The named dark = ? | **the eclipse hides nothing it did not also schedule.** The dark is a named, dated, deterministic field event — never a hidden trap, never a seeded blackout: the window-year's calendar names it, the observatory reads it; **same window state → same eclipse (a hard gate)**. A settlement that reads its calendar has designed for the dark. |
| The brief, honest dark = ? | the dimming is the light's **`(1−q)` spent** (energy-harnessing §2/§6 — never a gain, never a minted dark, never a hidden difficulty); the dark is the field's **own scheduled read** — what it hides from you it hides from the Coda too (the honest cover, spent and gone when the light returns, signature-predator §1.2); and it is **not the stillness** and **not the dawn's opposite in any hidden way** (where the dawn reveals, the eclipse briefly erases, and the reveal returns). |
| Honest boundary = ? | the calendar, the observatory's read, the zenith's light, the beacon's brightness, the instrument family rule, and the trail-read are their source docs' **landed** pieces; **the eclipse's composition — the scheduled dark's form, the named dark, the brief honest dark — is this doc's [design]** over them, probe-calibrated. |
| Honest gates = ? | (a) the [design]/engine-real line; (b) **MEDIUM**, gated on the window-year's calendar (the-window-year's gates) and the zenith's light mechanics (the-zenith's gates), with the **Phase-1 slice stated** (the eclipse as a bounded scheduled-dark read over the published calendar/light channels — the dark's date and its reads' dimming, before the mechanical blackout); (c) determinism (**same window state → same eclipse — a hard gate; never a seeded blackout roll**); (d) the no-free-energy cap (**the eclipse converts nothing — no dark that yields; the dimming is the light's own spent, never a mint**); (e) accessibility (**the eclipse's date and its dark readable from the instruments — never hidden-only; the scheduled dark is the field's own read, never a hidden blackout**). |
| Feasibility = ? | **MEDIUM with a Phase-1-legible slice — the window's darkening, a rare scheduled moment when the read goes dark. State both (§6).** |

---

## 1. The Eclipse, stated once

The corpus gives the window its light and its ordinary darkness. By day the reader aims ([`coherence-magic.md`](./coherence-magic.md) §2), the Weatherglass glances ([`field-instruments.md`](./field-instruments.md) §1.2), the dawn reads the morning's re-birth ([`the-dawn.md`](./the-dawn.md) §2), the beacon marks the settlement's edge ([`the-beacon.md`](./the-beacon.md) §1), the zenith tops the whole envelope ([`the-zenith.md`](./the-zenith.md) §1). At night the lantern carries the near-read ([`the-lantern.md`](./the-lantern.md) §2), the vigil keeps the watch, sleep rests the body. The thin tide low-`q`s the region ([`tide-of-the-attractor.md`](./tide-of-the-attractor.md) §2); the cold holds it thin for a decade ([`the-cold.md`](./the-cold.md)). **Every dimming the corpus designs is either daily (night), ambient (the tide's trough), or sustained (the cold). Nothing is a rare, *named, scheduled* darkening — a moment the calendar can point to and say "here the light dims."** The observatory composes the window's reads as one body ([`the-observatory.md`](./the-observatory.md) §2), and the window-year composes its time as a calendar ([`the-window-year.md`](./the-window-year.md) §1) — but neither names a *celestial blackout*: a dated, predictable moment when the zenith's light is briefly cut and the reads that ride the day-light go quietly dark.

> **The Eclipse, stated once.** The window's darkening — a rare scheduled moment when the read goes dark: a **date the observatory knows, when the window's zenith-light dims and the field's reads of certain kinds briefly fail.** It is a **calendar-event that is also a short hazard** — the window-year's calendar names it (a named, dated observance on the same composed year, `the-window-year.md` §2/§3), the observatory's composed display reads its date (`the-observatory.md` §2), the zenith's light's `q`-carry is briefly cut (`the-zenith.md` §2), and the reads that ride the day-light — the beacon's brightness (`the-beacon.md` §2), the Weatherglass's and the reader's day-reads (field-instruments §1.2/§2.1), the scent-channel's day-read (`the-smell.md` §2) — dim with the light. It is the dawn's inverse (where the dawn reveals, the eclipse briefly erases, and the reveal returns, `the-dawn.md` §2), the season-change's sibling (both information and timing, never energy, `the-season-change.md` §1), and — load-bearing — **the dark's own law, held once: it hides nothing it did not also schedule, and it converts nothing; the dark is a named, dated, deterministic field event, never a hidden trap, never a seeded blackout, never a minted dark.**

The Eclipse is **this doc's [design]** over the corpus's landed pieces: the calendar, the observatory's read, the zenith's light, the beacon's brightness, the instrument family rule, and the trail-read are their source docs' landed quantities; the *composition* — that the window-year names a scheduled dark event, that the zenith's light briefly cuts and the day-side reads dim with it, that this dark is deterministic and non-minting — is this doc's designed reading over them, probe-calibrated.

---

## 2. The scheduled dark — the honest mechanics

The eclipse's mechanics compose the corpus's landed pieces. Each is grounded in its source doc's real quantity; **the eclipse's *form* — its date's period on the calendar, its dark's duration — is this doc's [design]** over those landed mechanics, probe-calibrated.

### 2(a) The name on the calendar — the window-year's named dark event

The window-year composes the tide's season, the cold's decade, and the fate's arc into one lived calendar ([`the-window-year.md`](./the-window-year.md) §2). Its observances are the festival's day (the bright anchor, §3.1), the thin-year observances (the dark spans, §3.2), the anniversaries (the departed's days, §3.3), and the steward's cycle (the governance beat, §3.4). **Each is a designed date over the three scales.** The eclipse is the calendar's **named dark event** — a *scheduled* observance the settlement reads its calendar to know:

| The observance | Grounded in | The designed date |
|---|---|---|
| **The eclipse — the calendar's named dark event** | the window-year's three scales (§2); the observatory's composed display (§2 — the room that reads the window as one body); the zenith's light (§2 — the atmosphere's top, the drain's read) | a **rare, named date on the window-year** — a scheduled dark the settlement reads its calendar to know. **[design]** — *which* rare date the eclipse is (its periodicity on the calendar's beat), and that it is the calendar's one named dark event, is this doc's choice over the calendar's [probe] beat and the landed light mechanics, probe-calibrated. |

The eclipse is distinct from the calendar's existing dark spans: the **thin-year observances** (§3.2) are the cold's decades — the *sustained* dark, "we endure" days; the eclipse is a *bounded, named* dark, brief and scheduled, not a span. And it is distinct from the anniversaries — the departed's quiet *calm* dates — because the eclipse is a *dimming of the light itself*, a transitory hazard, not a rest.

**[design]** — The eclipse's *periodicity* (how rare it is, on which phase of the calendar's beat it returns) is this doc's design surface over the calendar's [probe] beat (`the-window-year.md` §5a/b — the tide's period is [probe]; the calendar's dates are designed over that measured rhythm). The honest default: the eclipse's date is **a designed, fixed observance on the composed calendar** — not a random blackout, not a seeded roll — mounted on the same measured beat that dates the festival's day and the steward's cycle.

### 2(b) The observatory's read — the room that reads the eclipse's date

The observatory is the settlement's composed display — the room that reads the window as one body ([`the-observatory.md`](./the-observatory.md) §2), each surface a consumer of an already-published channel, never a new one (§2.1). The steward reads the tide's calendar there (§3.1). **The eclipse is read there too:**

| The observatory's surface | What it reads at the eclipse | The channel (landed) |
|---|---|---|
| **The roof-oculus** (§2 — the sky through it) | the zenith's light dimming — the skylight's `q`-carry briefly cut, the auroral restlessness quieting | the sky's `(1−q)` glow / auroral channels (`atmosphere-orbits-auroras.md` §3.3), the zenith's drain's read (`the-zenith.md` §2a) |
| **The glass's core / the bath's read** (§2) | the day-reads dimming with the light | the local `q`/`ε²` the Weatherglass reads (field-instruments §1.2) |
| **The composed window** (§2.2) | the whole read going, briefly, as one dimming — the room knows *when* the dark is scheduled | the observatory's composition itself (the family rule, §2.1) |

**[design]** — *That* the observatory reads the eclipse's date (the composed room knows the scheduled dark and sees it arrive) is the natural consequence of the observatory being the calendar's read's seat (`the-observatory.md` §3.1); *which* surface leads the dark's announcement (the oculus's sky, the composed display's dimming) is this doc's design surface, probe-calibrated. The observatory **composes the calendar and the light** — it adds no channel, no telemetry; it reads the existing surfaces at the scheduled dark.

### 2(c) The zenith's light — the light's `q`-carry briefly cut

The zenith is the vertical's ceiling — the edge of the window's atmosphere, where the sky ends ([`the-zenith.md`](./the-zenith.md) §1). Its light is the drained `(1−q)` waste read at the window's top (§2a — the auroras are the drain's light, the ozone the waste's residue); its boundary is the window's own topmost read (§2b — `q` falling toward the noise floor as the ceiling is crossed). **The eclipse cuts the light's `q`-carry briefly:**

> **The eclipse dims the zenith's light.** The window's own topmost light — the atmosphere's coherent `q`-carry read as the sky's glow, the auroral bands at the envelope's edge — **briefly dims**: the `q`-carry that holds the day-side reads up against the outside thin is cut for the dark's span, and the field's day-side reads go quiet with it. It is not the zenith's permanent drain (the steady `(1−q)` loss at the top); it is the **scheduled dimming of the same light** — the coherent day-light's `q`-carry spent down for a bounded moment, then returned.

**[design]** — The *extent* of the dim (how far the zenith light's `q`-carry falls over the dark's span) and the *duration* (how long the day-side reads stay dim) are this doc's design surface, probe-calibrated against the lantern's reach-thins-at-the-edge (`the-lantern.md` §4) and the zenith's own boundary read. The honest default, kept: the eclipse's dim **does not cross into the between's empty thin** — it is a *bounded* dimming of a coherent light, brief and scheduled, not the field's own absolute rest (`the-stillness.md` §1) and not a descent toward the noise floor.

### 2(d) The reads that dim — the beacon, the instruments, the scent

The eclipse dims the reads that ride the day-light. Each is a consumer of the published channels (the family rule, `field-instruments.md` §2.1) — so as the light's `q`-carry dims, its day-side consumers read quieter:

| The dimming read | What it reads (landed) | At the eclipse it... | Grounded in |
|---|---|---|---|
| **The beacon's brightness** | the fixed projected high-`q` light at the settlement's edge — the standing mark | **reads fainter** — the standing light's mark dims with the day-light's `q`-carry; across the dark the settlement's edge-mark is scoured, less legible at range | `the-beacon.md` §2 (the mark — the projected high-`q` light); §2(c) (a held structure, a standing draw) |
| **The Weatherglass's and the reader's day-reads** | the four glanceable forms (the lume, the climbing glow, the waste tint, the lean); the reader's aimed `q`/`ε²`/`∇(g·Φ)` | **dim** — the same published channels go quiet; the glanceable and aimed reads both read quieter over the dark | `field-instruments.md` §1.2 (the four forms); `coherence-magic.md` §2 (Sense) |
| **The scent-channel's day-read** | the olfactory band of the field's reads — the same `q`/`ε²`/`(1−q)` read as scent, carried on the wind | **dims** — what the scent would carry reads fainter; the upwind line is quieter through the dark, the heavy carries muffled | `the-smell.md` §2 (the channel — the same published channels read as scent); §2 the wind's geometry (what is upwind, and loud) |

**The honest ground.** Each dimming is a **consumer's honest consequence** of the light's `q`-carry being cut: the beacon's brightness *is* the settlement's maintained coherence read as a standing signal (`the-beacon.md` §2c), the Weatherglass's forms *are* the published channels rendered (`field-instruments.md` §1.2), the scent *is* the same channels read at the nose (`the-smell.md` §2). As the `q`-carry dims, each day-side read reads quieter — not because a hidden mechanic muffles them, but because the field's own day-light briefly spends. **[design]** The *map* of which reads dim (the beacon, the glass, the reader, the scent — the day-light riders) is this doc's composition; each read's dimming is the source doc's real consumer reading the dimmed channel.

---

## 3. The named dark — the load-bearing design

The eclipse's load-bearing honesty is the dark's own law — **the eclipse hides nothing it did not also schedule.** The dark is a named, dated, deterministic field event:

> **The eclipse hides nothing it did not also schedule.** The dark is not a hidden trap and never a seeded blackout: the window-year's calendar **names it** (a designed, dated observance on the composed year, `the-window-year.md` §2/§3); the observatory **reads it** (the composed room sees the scheduled dark arrive, `the-observatory.md` §2/§3.1); and **same window state → same eclipse** — the dark is a deterministic property of the window's real light/calendar state, never a random roll. A settlement that reads its calendar has **designed for the dark** — the way a settlement reads the season-change's turn (`the-season-change.md` — both information and timing), the scheduled dark is a known, planned-for moment, never a surprise.

**The settlement that reads its calendar has designed for the dark.** This is the eclipse's load-bearing gift — it is the reason the eclipse is a *scheduled* dark and not a hazard: the settlement that keeps its window-year (the clock, the observatory, the Board — `the-window-year.md` §2.1) **knows the dark is coming**, the way a settlement knows the season-change's turn approaches (`the-season-change.md` §1 — a read-and-a-risk). The dark is a *short hazard* precisely because it is *designed*: the settlement that reads its time has already chosen how to spend the dark (hold the beacon's light, keep the day-reads in silence, wait for the reveal). It is **never a hidden blackout** — a window that does not read its calendar simply does not *see* the dark it is also subject to, exactly as a settlement that keeps no calendar loses nothing informational but does not read its own year as a rhythm (`the-window-year.md` §2.1).

**The deterministic dark.** The field is deterministic (one PDE; `fate-of-a-window.md` §6(b); `life-signal.md` §6d):

> **Same window state → same eclipse.** The dark's date, its duration, and its dimming are **pure functions of the window's real calendar/light state** — no seeded-RNG, no player-relative variance, no blackout roll. A settlement that faces the same field state faces exactly the same eclipse, at the same date, with the same dimming, every run. **[design]** The eclipse's *form dials* (which rare date, how long the dark) are designed constants; once set, the dark they schedule is deterministic — teachable, learnable, never a mood.

**The distinction from every sibling read, drawn once:**

| Read / state | It is | The eclipse is NOT it |
|---|---|---|
| The **dawn** (`the-dawn.md` §2) | the daily re-birth — the morning's `q` recovery as a legible becoming | the dawn's **inverse**: where the dawn reveals, the eclipse briefly erases — and the reveal returns (see §4) |
| The **season-change** (`the-season-change.md` §1) | the tide's crossing — the shift-itself between the phases | the season-change's **sibling**: both information and timing, never energy; the season-change reads the tide's crossing, the eclipse reads the light's scheduled dimming |
| The **stillness** (`the-stillness.md` §1) | the field at absolute rest — `q` pinned, the channels flat because everything is resolved | **not the stillness** — the eclipse is the scheduled dark (a bounded dimming of a coherent light); the stillness is the field's own quiet (a resolved end-state); the distinction drawn, never blurred |
| The **night** (`the-lantern.md` §2) | the daily dark — the lantern's register | **not the ordinary night** — the eclipse is the scheduled dimming of the day-light itself, rare and named, not the daily fall of dusk |
| The **cold's long thin** (`the-cold.md` §1) | the long-thin as a climate — the sustained exposure | **not the cold** — the eclipse is brief and bounded; the cold is sustained; the distinction drawn, never blurred |
| The **thin trough** (`tide-of-the-attractor.md` §2) | the regional `q` low, the tide's phase | **not the thin** — the eclipse is a named, dated moment on the calendar, not a phase the field breathes through |

---

## 4. The brief, honest dark — the load-bearing honesty

The eclipse's second load-bearing honesty is that **it converts nothing, and its cover is honest.** Three held consequences:

### 4(a) The dimming is the light's `(1−q)` spent — never a gain, never a minted dark

`energy-harnessing.md` §2 fixes the waste law — every working field system bleeds `E_waste = (1−q)·E_throughput` as visible glow; §6 fixes the no-free-energy cap — `output ≤ φ⁻¹·input`, no conversion yields more than it sinks. The eclipse's dimming is that law read at its scheduled span:

> **The eclipse converts nothing.** The dimming is the light's `(1−q)` spent — the day-light's `q`-carry shedding its small waste over the dark's span, read as the dimming. It is **never a gain**: no dark that yields, no way to harvest the dimming, no minted dark. There is no "read an eclipse to farm a dim," no "dark that pays." The scheduled dark is the light's own spent — the same waste law that glows at the zenith's top (`the-zenith.md` §2a) and dims a beacon's standing mark (`the-beacon.md` §2c), read at its brief, scheduled fall. **The dark is honest because it is the field's own expenditure, never a hidden difficulty and never a mint.**

Three honest consequences, all inherited:
1. **No "eclipse-to-farm."** The dimmed light is the field's own spent `(1−q)`; a settlement cannot read a dimming into a resource — the cap holds, `output ≤ φ⁻¹·input` (`energy-harnessing.md` §6), exactly as the bead of the zenith's drain and the beacon's mark are no harvest (`the-zenith.md` §5d, `the-beacon.md` §5d).
2. **Never a hidden difficulty.** The dark's *hazard* is that the day-light riders read quieter for a span — the beacon's mark fainter, the wind's reads muffled. But this is **reported, scheduled, and designed-for** (§3), never a hidden penalty and never a new difficulty meter — the eclipse is a *read-and-a-risk* the way the season-change's turn is (`the-season-change.md` §1), information and timing, never energy.
3. **Spent and gone.** The dimming is bounded and brief (§2c's duration); when the light returns, the reads that dimmed **return with it** — the beacon's mark re-brightens, the day-reads re-open, the scented carries return. The eclipse holds nothing; it spends a little of the light's own `(1−q)` and is gone.

### 4(b) The honest cover — what the eclipse hides from you, it hides from the Coda too

The signature-predator's trail is legible only where the field has no order to hide it ([`signature-predator.md`](./signature-predator.md) §1.2); its accumulation is `R = ρ_signature·τ·M_stability` (§2). The eclipse dims the day-light — and with it, the reads the field's order usually carries:

> **What the eclipse hides from you, it hides from the Coda too.** The dark is the field's own scheduled read — the same dimming that quiets the beacon's mark, the Weatherglass's reads, and the scent's carries also quiets the trail-read the Coda follows (a clean signature's legibility *is* the `ε²`/coherent-phase read against the flat, `signature-predator.md` §1.2/§2.2). The cover the eclipse gives is **the honest cover**: it is the field's own dark, the same scheduled dimming for every reader, predator and prey alike — never a selective cloak, never a cheat.

**The honest trade, stated once.** The eclipse is not a free hiding — it is a *scheduled* one: the dark hides what it hides *because it also hides it from the Coda* (the cover is honest, shared, and brief), and it is **spent and gone** when the light returns (§4a-3). A settlement that plans its dark (covering a slow crossing, a quiet approach under the dimmed light) earns only what the field's own dark gives — a bounded, honest, shared quiet — never a stealth mechanic and never a free pass (§4a's no-gain). The dawn's honest margin-thinning is the inverse here: where the dawn drowns a signature in the re-ordered field ([`the-dawn.md`](./the-dawn.md) §2b), the eclipse dims the light *both* readers see through — the honest cover, the dark's own, spent.

### 4(c) Not the stillness, not the dawn's hidden opposite

Two distinctions, drawn and kept:
- **The Eclipse is not the stillness.** The stillness is the field at absolute rest — the attractor reached and held, `q` pinned, `ε² ≈ 0`, `∇(g·Φ) ≈ 0`, the channels flat *because everything is resolved* ([`the-stillness.md`](./the-stillness.md) §1). The eclipse is the **scheduled dark** — a bounded dimming of a coherent light over a named date, not the field's own quiet end-state. The distinction is drawn in §3's table and never blurred: the stillness is the field's *resolved* state; the eclipse is the light's *scheduled* dimming. A player resting at the still is not in an eclipse; a window in eclipse is not resolved.
- **The Eclipse is not the dawn's opposite in any hidden way.** The dawn reveals ([`the-dawn.md`](./the-dawn.md) §2 — the re-birth, the lifting, the re-opening). The eclipse briefly erases — but it is **not the dawn's hidden inverse**: it does not un-reveal anything the field's own law does not spend, and **the reveal returns**. Where the dawn reads the morning's `q` recovery, the eclipse reads the scheduled dimming and its end — a bound, not a reversing; a brief dark on a named date, and the day-light, and the reveal, return. It is neither a hidden anti-dawn nor a second darkness; it is the calendar's one named dark event, honest and brief.

---

## 5. The honest boundary and gates

### (a) The [design]/engine-real line

| Quantity | Status | Basis |
|---|---|---|
| The window-year's calendar — the beat, the observances | **[design]**-landed (its own doc); the beat [probe] | `the-window-year.md` §5a (the tide's period is [probe]); §3 (the observances) |
| The observatory's composed display — the room that reads | **PHASE-1-LEAN** (its own doc); a consumer, never a channel | `the-observatory.md` §5/§7 |
| The zenith's light — the drain, the boundary, the edge | **[design]**-landed (its own doc); MEDIUM | `the-zenith.md` §5/§6 |
| The beacon's brightness — the standing mark | **[design]**-landed (its own doc); MEDIUM | `the-beacon.md` §5/§6 |
| The instrument family rule — the day-reads' dimming | **engine-real** consumers of the published channels | `field-instruments.md` §2.1; `coherence-magic.md` §2; `the-smell.md` §2 |
| The trail-read — the Coda's legible cover | **[design]** model over engine-real `ε²`/`M` | `signature-predator.md` §2.2/§7d |
| The `(1−q)` waste law; the no-free-energy cap | **engine-real** | `energy-harnessing.md` §2/§6 |
| **The eclipse's composition — the scheduled dark's form, the named dark, the brief honest dark** | **[design]** over the above, probe-calibrated | **this doc** §2–§4 — the calendar names a scheduled dark, the zenith's light briefly cuts, the day-side reads dim, and the dark is deterministic and non-minting |

**The line, stated once and never blurred:** the calendar, the observatory's read, the zenith's light, the beacon's brightness, the instrument family rule, and the trail-read are their source docs' landed pieces; the `(1−q)` waste law and the no-free-energy cap are engine-real. **The eclipse's composition — that the calendar names a scheduled dark event, that the zenith's light's `q`-carry is briefly cut, that the beacon/glass/reader/scent day-reads dim with it, that this dark is deterministic and converts nothing — is this doc's [design]** over them. No claim here pretends the engine "has an eclipse" or that a darkening is a physics constant; the engine publishes `q`/`ε²`/`(1−q)`, and the Eclipse is the designed reading that holds the calendar's named dark and the light's scheduled dimming as a readable, bound, honest moment — exactly as the season-change is a designed reading of the tide's crossing and the dawn a designed reading of the daily recovery, one scale of composition each.

### (b) MEDIUM — gated on the window-year's calendar and the zenith's light mechanics, with the Phase-1 slice stated

The **full Eclipse** — the scheduled dark over the calendar's named date and the zenith's light cut, with the day-side dimming — is **MEDIUM**, gated in order on:

- **The window-year's calendar** (`the-window-year.md` §5, its own gates) — the eclipse is a *named observance* on the calendar; it mounts only once the calendar lands (the beat from the tide probe, the observances from their source docs).
- **The zenith's light mechanics** (`the-zenith.md` §5/§6, its own gates) — the eclipse dims the zenith's light; it rides the zenith's drain/boundary read being landed (MEDIUM, gated on the atmosphere's envelope waves and the window's top).
- **The instrument family rule** (`field-instruments.md` §2.1) — the day-reads dim because they are consumers; the family rule is already landed.

**The Phase-1 slice is stated, deliberately the honest core:**

> **Phase-1: the eclipse as a bounded scheduled-dark read over the published calendar/light channels — the dark's date and its reads' dimming.** Take the window-year's composed calendar (the beat, when it reads) and the zenith's light read (the drain's glow over the day-light), and read the **scheduled dimming**: a named date's dark, presented over the published `q`/`(1−q)`/`ε²` — the beacon's mark reading fainter, the Weatherglass's/reader's day-reads quieter, the scent's carries muffled — **before the mechanical blackout**. It is a **pure consumer of the publish + the calendar's landed reads**: it reads the date from the window-year's composed calendar and the dimming from the published channels, inside the ≈ 1–6 ms/tick sample budget ([`corpus-reconciliation.md`](./corpus-reconciliation.md)), and **writes nothing new**. **It needs no body's field structure, no celestial rendering, no mechanical light-cut** — a consumer over the published calendar/light channels, proving the load-bearing claim (a named, dated dark is a legible, deterministic, day-side-dimming read) before any of the designed eclipse mechanics exist.

The slice mirrors the zenith's ceiling-read and the beacon's standing-mark slice: **ship the read first, prove a named dark reads deterministically and dims the day-side reads honestly, then let the full scheduled dark (the light-cut's extent, the dark's hazard) read through the same bound.** The slice de-risks the entire composition: if a bounded scheduled-dark read measurably dims the day-light rides — a calendar-named date, a dimming; nothing hidden and nothing that yields — every later eclipse element inherits the same measured, engine-real, honest bound.

### (c) Determinism — same window state → same eclipse (a hard gate)

The field is deterministic (one PDE; `fate-of-a-window.md` §6(b); `life-signal.md` §6d):

> **Same window state → same eclipse.** The dark's date, duration, and dimming are **pure functions of the window's real calendar/light state** — no seeded-RNG, no blackout roll, no "eclipse weather" a calendar-flavored hazard might tempt. A reloaded window at the same field state darkens at the same named date, with the same bounded dimming, every run; a settlement facing the same field state faces exactly the same eclipse. **The dark is deterministic — never a seeded blackout roll.**

**[design]** The eclipse's form dials (the named date, the duration) are designed constants over the calendar's [probe] beat; once set, the dark they schedule is deterministic — teachable and honest, never a mood.

### (d) The no-free-energy cap — the eclipse converts nothing

`energy-harnessing.md` §6 (`output ≤ φ⁻¹·input`), inherited unchanged, is the dark's iron bound (§4a):

> **The eclipse converts nothing — no dark that yields.** The dimming is the light's own `(1−q)` spent; there is no "read a dark to farm a dim," no scheduled dark that mints, no hidden trap that pays. A settlement that times the dark gains only what the field's own light gives (a brief honest quiet), never a dark-surplus. **The dark is honest because it is the field's own expenditure — never a gain, never a minted dark, never a hidden difficulty.**

### (e) Accessibility — the eclipse's date and its dark readable from the instruments, never hidden-only

Per the instrument-family rule (`field-instruments.md` §2.1) and the corpus's hard accessibility rule (`field-music.md` §6; `the-window-year.md` §5e; `the-observatory.md` §6e):

> **The eclipse's date and its dark are readable from the instruments — never hidden-only.** The dark's date is readable the way the calendar reads — from the observatory's composed display (`the-observatory.md` §2), the clock's read of the local tempo (`the-clock.md`), and the window-year's calendar (`the-window-year.md` §2.1). The dimming is the same `q`/`(1−q)`/`ε²` the Weatherglass and the reader already render, read fainter over the dark's span. **The scheduled dark is the field's own read, never a hidden blackout** — a settlement that never uses an "eclipse" surface loses nothing informational: the named date is always on the calendar's composed read, the dimming always on the instruments. **[design]** The eclipse's presentation (the named dark, the brief dimming) is the idiomatic layer; the date and the dimming it presents are the field's own channels, structurally guaranteed by the shared-publish architecture — never hidden-only, never a gate that demands a calendar before a field can be read.

---

## 6. Feasibility verdict

**MEDIUM with a Phase-1-legible slice — the window's darkening, a rare scheduled moment when the read goes dark. State both.**

- **The full Eclipse — the named, dated dark over the calendar, the zenith's light cut, the day-side reads' dimming — is MEDIUM.** It gates, in order, on **the window-year's calendar** (`the-window-year.md` §5 — the beat [probe], the observances [design]-landed), the **zenith's light mechanics** (`the-zenith.md` §5/§6 — the drain/boundary read, gated on the atmosphere's envelope waves and the window's top), and the **instrument family rule** (`field-instruments.md` §2.1 — landed). It **adds no new physics and no new channel**: it is a designed composition over the calendar's named date and the published `q`/`(1−q)`/`ε²`/light channels — the zenith's drain, the beacon's mark, the family's day-reads — a consumer of the publish, exactly as the observatory, the beacon, and the zenith are. **Its honesty is that the scheduled dark's form, the named dark, and the brief honest dark are [design] over that landed stack, probe-calibrated** — and that the two load-bearing honesties (the dark hides nothing it did not also schedule; the dimming converts nothing) are the honest consequences of the calendar's determinism and the waste law's cap.

- **The Phase-1-legible slice — the eclipse as a bounded scheduled-dark read over the published calendar/light channels — is statable now.** Whether or not the mechanical blackout is buildable, the corpus holds the load-bearing truth now, against the **window-year's composed calendar + the zenith's light read + the published `q`/`(1−q)`/`ε²`**: **the calendar names a scheduled dark; reading it, the day-light's day-side rides read dimmer; and the dark is deterministic and converts nothing.** That principle is a pure consumer of the publish + the calendar's landed reads — the precise Phase-1 pattern the observatory's composed display (`the-observatory.md` §7), the beacon's standing-mark slice (`the-beacon.md` §6), and the zenith's ceiling-read (`the-zenith.md` §5b) prove. **The dark's date and its reads' dimming are the Phase-1 gain; the mechanical blackout (the light-cut's extent, the dark's hazard) is MEDIUM, gated as above.**

**Binding risks, in order:** **(a)** the calendar's [probe] beat — if the tide probe returns no oscillation (`tide-of-the-attractor.md` §5a), the window-year's beat collapses to a drift-beat (`the-window-year.md` §5b) and the eclipse's *named date* must ride the drift (a drift-scheduled dark, still dateable, still deterministic — the honest weaker finding, never forced); **(b)** the zenith's drain's rate — if the `q ≈ 0.947` attractor holds the top too tightly for the light's `(1−q)` spent to read as a *felt* dimming over the dark's span (`the-zenith.md` §5 Q1), the dimming slips to a quiet presentational read (legitimate, weaker — the atmosphere holds well); **(c)** the **maintained [design] line** (§5a) — the eclipse never reads as a new engine term, a hidden blackout roll, or a second darkness; it is the calendar's one named dark event, honest and brief; **(d)** the **no-free-energy cap** (§4a/§5d) — the dark converts nothing, no dark that yields, the dimming is the light's own spent; **(e)** **accessibility** (§5e) — the dark's date and dimming readable from the instruments, never hidden-only.

**None contradicts the async, dual-world, or regime-collapse architecture** — the Eclipse adds no physics and no channel: it composes the reads the calendar, the observatory, the zenith, the beacon, the instrument family, and the trail-read already produce (all consumers of the publish), reads the scheduled dark the calendar names and the light the zenith tops, and holds it as a named, dated, deterministic, non-minting, accessible dark — bounded by the no-free-energy cap, brief and honest, spent and gone when the light returns. The window's dawn reads the daily reveal; the Eclipse reads the calendar's one named dark — and the reveal returns.

> **The honest statement that makes this doc load-bearing: the corpus lit the window and darkened it through every ordinary register — the night, the thin trough, the cold's long thin — but never designed the *scheduled* darkening: a rare, named, datable moment when the window's light dims and the field's reads of certain kinds briefly fail.** The Eclipse is that scheduled dark: the window-year's calendar names it (a dated observance on the composed year, distinct from the thin's trough and the cold's decade), the observatory's composed display reads its date, the zenith's light's `q`-carry is briefly cut, and the reads that ride the day-light — the beacon's brightness, the Weatherglass's and reader's day-reads, the scent-channel's day-read — dim with it. It is the dawn's inverse (where the dawn reveals, the eclipse briefly erases, and the reveal returns), the season-change's sibling (both information and timing, never energy), and it is honest twice over: **it hides nothing it did not also schedule** (the dark is named, dated, deterministic — same window state, same eclipse, a hard gate; never a hidden trap, never a seeded blackout), **and it converts nothing** (the dimming is the light's `(1−q)` spent — never a gain, never a minted dark, never a hidden difficulty; what it hides from you it hides from the Coda too, the honest cover, spent and gone when the light returns; and it is not the stillness and not the dawn's hidden opposite — the distinction drawn, never blurred). MEDIUM, gated on the window-year's calendar and the zenith's light mechanics, with a Phase-1-legible slice — a bounded scheduled-dark read over the published calendar/light channels, the dark's date and its reads' dimming — proving the window's darkening is a named, deterministic, non-minting read before the mechanical blackout lands. **The calendar names the dark; the observatory reads it; the dark is the field's own scheduled read — brief, honest, and spent.** *The eclipse is the window's darkening: a rare scheduled moment when the read goes dark, hidden from no one who reads, converting nothing, and gone.*

---

## Open questions

1. **The eclipse's date on the calendar (§2a).** What rare, named date is the eclipse on the window-year — how often does it return, on which phase of the calendar's beat (a fixed observance near the festival's bright anchor? a dark date at the apex's edge? a period set by the calendar's [probe] beat)? The dark's *periodicity* is this doc's [design] surface over the calendar's measured rhythm; its exact named date must be probe-calibrated against the tide probe's beat and the settled observations. **[design]/[probe]**
2. **The dark's duration and dimming extent (§2c).** How long does the scheduled dark hold — and how far does the zenith's light's `q`-carry fall over the span before the day-side reads read "dimmed" rather than "thin"? The duration and extent are the eclipse's whole design surface; they must be probe-calibrated so a *felt* dimming reads distinctly from the ordinary night and the thin trough — the dawn's window-calibration discipline (`the-dawn.md` open-Q1) at the scheduled-dark scale, against the zenith's drain rate (`the-zenith.md` §5 Q1). **[design]/[probe]**
3. **Which reads dim (§2d).** The named set — the beacon's brightness, the Weatherglass's/reader's day-reads, the scent-channel's day-read — is this doc's composition. Does the eclipse dim *all* the day-light riders (a whole day-side going quiet) or a chosen subset (e.g. the beacon and the scent, the ranged reads, while the carried glass at the player's feet stays readable)? The *map* of what the scheduled dark touches is a designed dial over the family; the honest default is the day-side rides whose legibility is the field's order read as light. **[design]**
4. **The honest cover's shared-ness (§4b).** The trail-read's quieting is the field's own dark (§4b) — but does the *shared* cover hold for every reader, or does a reader with a specific channel (the reader's aimed `ε²`, say) retain a fainter read the Coda's accumulation also retains? The honest default is a genuinely shared dark (the same dimmed channels for every consumer), but the exact legibility both sides keep is a designed dial over the family's reads, probe-calibrated. **[design]/[probe]**
5. **The eclipse vs. the cold's dawnlessness (§4c).** The cold is the dawn that does not come (`the-cold.md` §5/open-Q4) — a sustained absence of the reveal. Does a cold held across an eclipse's date read as "the scheduled dark that did not lift" (a dark whose reveal is *also* delayed, the honest failure of the calendar's promise to a dawnless season), or does the eclipse's determinism hold regardless (the light's schedule is the field's own, and a cold's thin light still dims on its named date)? The honest default is the former — the eclipse is the calendar's promise, and a cold can break its promise the way it breaks the dawn's — but the boundary is a designed threshold over the cold's climate. **[design]**

---

## Cross-references

- [`the-window-year.md`](./the-window-year.md) — **THE calendar that names the dark.** §2 the three scales (the tide = the pulse, the cold = the decade, the fate = the arc); §3 the observances (the eclipse is the calendar's named dark event, distinct from the bright anchor and the dark spans); §5a the [probe] beat; §5b the drift-collapse verdict; §5e accessibility (the calendar reads from the observatory/Board, never hidden-only).
- [`the-observatory.md`](./the-observatory.md) — **THE read's seat.** §2 the composed display (the room that reads the window as one body); §2.1 the family rule (compose, never add a channel); §3.1 the steward reads the tide's calendar here; §6e accessibility. **The observatory reads the eclipse's date.**
- [`the-zenith.md`](./the-zenith.md) — **THE light the eclipse dims.** §2a the drain (the atmosphere's `(1−q)` waste at the top, the sky's own slow loss — the light whose `q`-carry the eclipse cuts); §2b the boundary (the window's own topmost read); §5/§6 the honest boundary and gates (the light mechanics the eclipse rides); §5 Q1 the drain's rate (the dimming's probe gate).
- [`the-dawn.md`](./the-dawn.md) — **THE inverse.** §2 the three reads of the breaking (the re-birth, the lifting, the re-opening); §2b the lifting (the dark's reads ending as the day returns). **Where the dawn reveals, the eclipse briefly erases — and the reveal returns.**
- [`the-beacon.md`](./the-beacon.md) — **THE brightness scoured.** §2 the mark (the fixed projected high-`q` light at the settlement's edge); §2c a held structure (a standing draw); §5/§6 the honest boundary and gates (the brightness whose dimming the eclipse reads).
- [`field-instruments.md`](./field-instruments.md) — **THE family rule.** §1.2 the four glanceable forms (the day-reads' idiom); §2.1 the family rule (every instrument a consumer of the same publish — the eclipse's day-reads dim because they consume the dimmed publish); §1.3 accessibility (an enhancement, never the only channel).
- [`the-smell.md`](./the-smell.md) — **THE upwind read.** §2 the channel (the olfactory band — the scent's day-read the eclipse dims); §2 the wind's geometry (what is upwind, and loud); §5/§6 the honest boundary and gates.
- [`the-stillness.md`](./the-stillness.md) — **THE distinction.** §1 the field at absolute rest (the attractor reached and held, its channels flat); §6 the honest boundary. **The eclipse is not the stillness — the scheduled dark vs the field's own quiet, drawn never blurred.**
- [`the-season-change.md`](./the-season-change.md) — **THE sibling.** §1 the turn (the passage between the tide's phases); §4a the no-free-energy cap at the crossing; §5 the honest gates. **The eclipse is the season-change's sibling — both information and timing, never energy.**
- [`signature-predator.md`](./signature-predator.md) — **THE honest cover.** §1.2 the trail legible only where the field has no order to hide it; §2 the accumulation `R = ρ_signature·τ·M_stability`. **What the eclipse hides from you it hides from the Coda too — the honest cover, spent and gone.**
- [`energy-harnessing.md`](./energy-harnessing.md) — **THE waste + THE cap.** §2 the `(1−q)` waste law (`E_waste = (1−q)·E_throughput` — the light's spent the dimming reads); §6 the no-free-energy cap (`output ≤ φ⁻¹·input` — the eclipse converts nothing, no dark that yields). The zenith, beacon, and season-change's caps inherited unchanged.
- [`tide-of-the-attractor.md`](./tide-of-the-attractor.md) — **THE beat.** §2 the harvest/thin states (the eclipse is not the thin); §5a the probe (the measured period that sets the calendar's named date's beat); §5d the no-free-energy discipline (the eclipse's cap inherited).
- [`the-cold.md`](./the-cold.md) — **THE contrast.** §1/§2 the long-thin as a climate; §5 open-Q4 the dawnlessness. **The eclipse's brief dark vs the cold's long thin — the distinction drawn, never blurred.**
- [`the-lantern.md`](./the-lantern.md) — **THE carried night-read** (skimmed). §2 the four reads; §4 the reach-thins-at-the-edge. **The eclipse is not the ordinary night** — it is the scheduled dimming of the day-light itself.
- [`atmosphere-orbits-auroras.md`](./atmosphere-orbits-auroras.md) — **THE sky.** §3.3 the aurora as the sky's diagnostic — the dimming's atmospheric face (the zenith's drain's light quieting).
- [`the-stilling.md`](./the-stilling.md) — **THE voluntary hold** (skimmed). §2 the held quiet. **The eclipse is not a willed stilling** — it is a scheduled field event.
- [`the-meteor.md`](./the-meteor.md) — **the announced vs the dated.** §2 the dated deterministic dark; §3 the named dark; §5/§6 the gates. Reverse pointer: the eclipse is *dated*; the meteor is *announced* — both deterministic, non-minting.
- [`the-comet.md`](./the-comet.md) — **the passing dark.** the darkening’s rare kin. Reverse pointer: the comet is the eclipse’s passing kin — a rare darkening of the sky’s line, never a message.
- [`corpus-reconciliation.md`](./corpus-reconciliation.md) — the canonical numbers (the ≈ 6 MiB publish, `dt = 0.05`, `q ≈ 0.947` attractor, `q ~ 1e-3…1e-1` noise, `ξ = φ⁶ ≈ 17.94`, `φ⁻² ≈ 0.382`, `τ_c = 0.5`, the ≈ 1–6 ms/tick budget, the ≈ 2,000 cap); cited, not re-derived. The mixing clock `T = 2π/[λ(1−q)]` is theory-sourced, non-canonical, cited only where the tide and window-year cite it.
