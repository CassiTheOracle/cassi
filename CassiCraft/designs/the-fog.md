# The Fog: The Held Blur — the Weather That Hides What It Wraps

**Question under design:** the weather stack has the **front** (the `c_s` storm — the field's own ordered `ε²` front, a hazard that arrives and passes, [`field-hazards.md`](./field-hazards.md) §2), the **rain** (a nourishing-but-wet fall — a weather that gives, [`the-rain.md`](./the-rain.md) §3), the **wind** (a directional flow — a current that carries, [`the-wind.md`](./the-wind.md) §1), the **cold** (a long-thin climate — a winter that wears, [`the-cold.md`](./the-cold.md) §1), the **flood** (a surfeit event — a harvest that drowns, [`the-flood.md`](./the-flood.md)), the **blizzard** (a white driving storm — the cold's thin carried into a moving obscuring white-out, [`the-blizzard.md`](./the-blizzard.md)), the **season-change** (a tide's turn), the **eclipse** (a scheduled dim), the **meteor** (a falling arrival). **But the static blur was never written**: no weather is a *held blur* — a slow-held region of low-`q` obscurity that hides what it wraps. Every weather above either *arrives* (the front, the flood, the meteor), *departs* (the rain's fall, the cold's passing), *moves* (the wind, the blizzard), or *turns* (the season, the eclipse). **None *holds*.** This document is that design: **the Fog is the held blur — the weather that hides what it wraps**: a slow-held region of low-`q` obscurity (the field's own held thin — a bounded region where the `q`-carry is spent low and the reads go soft), the one weather that is **neither arrival nor departure but hesitation made visible**.

The load-bearing honesty, stated once and kept: **the fog hides what it wraps, and what it hides it hides from you and the Coda alike — the held blur is the field's own read, never a hidden difficulty and never a free cover.** The fog's obscurity is the field's own low-`q` state — the same channels the instruments show (`q`, `ε²`, `(1−q)`, `∇(g·Φ)`, the medium velocity `FieldVel`), read at their soft register: the smell-channel swamped (the upwind-only read drowned, [`the-smell.md`](./the-smell.md) §2), the life-signal blurred (the maintenance read softened, [`life-signal.md`](./life-signal.md) §3), the walk's footing landmarks erased (the three reads buried, [`the-walk.md`](./the-walk.md) §2), and where the signature-predator and the mimic hunt with impunity — the trail-read's legibility and the waited shape's cover in the blur ([`signature-predator.md`](./signature-predator.md) §1.2), ([`the-mimic.md`](./the-mimic.md) §2). The held blur is a weather-read of the same channels ([`weather-not-storm.md`](./weather-not-storm.md) §2 — a field's own held state, never a punishment); its approach and its blur are readable the same way the hazards are ([`field-hazards.md`](./field-hazards.md) §5.1 — may not arrive, but its obscurity reads before the wrap); and it converts nothing — the held blur is the field's own `(1−q)` spent, never a gain, never a minted fog, never a hidden difficulty ([`energy-harnessing.md`](./energy-harnessing.md) §2/§6).

Companion to (all relative paths — the docs this held blur composes and erases):
- [`the-blizzard.md`](./the-blizzard.md) — **THE white-out's static twin.** §2/§4 the white-out — **the Fog is the blizzard's static twin**: the blizzard erases with *driven white*, the fog hides with *held blur*; the honest-cover precedent — **what the white hides from you it hides from the Coda too, spent and gone when the field re-coheres** — is inherited at the static register.
- [`the-marsh.md`](./the-marsh.md) — **THE air's marsh.** §2/§3 the tessellated-`q` hiding — **the fog is the air's marsh**: the static hiding texture at the air's register, where the pattern does not carry.
- [`the-mirage.md`](./the-mirage.md) — **THE deceitful read's opposite.** §2 the composed bright — **the fog is not the mirage**: the mirage *composes a false bright* (channels reading as an absent source), the fog *blurs what is truly there* (channels read soft at a real locality).
- [`the-smell.md`](./the-smell.md) — **THE upwind-only read.** §2 the wind's geometry and the mask — **the fog swamps the smell-channel**: the nose drowned in the field's own soft register.
- [`life-signal.md`](./life-signal.md) — **THE maintenance read.** §3 the four classes — **the fog blurs them**: the soft register softens the maintained-live read.
- [`the-walk.md`](./the-walk.md) — **THE three reads.** §2 the gradient at foot-pace, the thin's cost, the faint trail — **the fog buries them all**: the footing landmarks erased under the blur.
- [`signature-predator.md`](./signature-predator.md) — **THE trail-read.** §1.2 the trail legible only where the field has no order to hide it — **the fog's honest cover**: what the blur hides from you it hides from the trail-read too.
- [`the-mimic.md`](./the-mimic.md) — **THE waited shape.** §2 the waited shape — **the fog as the mimic's cover**: the waited shape hunts where the blur hides the wary's read.
- [`weather-not-storm.md`](./weather-not-storm.md) — **THE provenance.** §2 weather vs punishment — **the fog as a weather-read of the same channels**: the field's own held state, never a punishment.
- [`field-hazards.md`](./field-hazards.md) — **THE hazard layer.** §2 the `c_s` front's approach; §5.1 readable-before-it-arrives — the fog's approach (its obscurity) readable the same way.
- [`energy-harnessing.md`](./energy-harnessing.md) — **THE waste + THE cap.** §2 the `(1−q)` waste law `E_waste = (1−q)·E_throughput` (the held blur is the field's own spent thin); §6 the no-free-energy cap (`output ≤ φ⁻¹·input` — the fog converts nothing, never a mint, never a free cover).
- [`field-instruments.md`](./field-instruments.md) — **THE family rule.** §1.4/§2.1 the instruments are consumers of the publish — the fog's reads are consumers of the publish, never hidden-only, the soft register read the same way the Weatherglass's dead-grey reads the desert.

Every number below is from [`corpus-reconciliation.md`](./corpus-reconciliation.md) (the canonical set — **cited, not re-derived**), engine-verbatim, or explicitly flagged **[design]** (a designed surface / the held blur's form / the hesitation-made-visible frame / the honest cover over real channels, probe-calibrated) / **[probe]** (a pre-registered Phase-1 measurement). **The honest boundary is drawn in §5 and never blurred: the smell's upwind read, the maintenance read, the walk's three reads, the trail-read, the waited shape, the white-out's honest-cover precedent, and the provenance classifier are their source docs' landed pieces; the fog's composition — the held blur's form, the hesitation-made-visible frame, the honest cover — is this doc's [design] over them, probe-calibrated.** No claim here pretends the engine "has fog" or "raises a blur"; the engine publishes the `q`/`ε²`/`(1−q)`/`∇(g·Φ)` the low-`q` register is read through, and the Fog is the designed reading that composes them into the held blur — a consumer of the publish exactly as the Wind, the Cold, the Blizzard, and the Marsh are, never a new channel, never a write.

---

## Summary table

| Question | Answer (concrete) |
|---|---|
| The Fog = ? | The **held blur — the weather that hides what it wraps**: a slow-held region of low-`q` obscurity (the field's own held thin — a bounded region where the `q`-carry is spent low and the reads go soft). The one weather that is **neither arrival nor departure but hesitation made visible**: it does not arrive like the storm's front and does not depart like the rain's fall; it *holds* — a region that stays, a slow blur the settlement must read and move through. |
| The composed blur = ? | **a slow-held region of low-`q` obscurity, composed over landed mechanics, never a new weather system**: the field's own held thin register — `q` carried low (the cold's quiet thin, `the-cold.md` §2), the reads going soft the way the marsh's tessellated `q` diffuses a signature (`the-marsh.md` §3). The fog's *form* — its blur's bound, its hold's duration — is this doc's [design] over the landed low-`q`/read channels, probe-calibrated. |
| The hidden reads = ? | the smell-channel **swamped** (the upwind-only read drowned in the field's own soft register, `the-smell.md` §2), the life-signal **blurred** (the maintenance read softened, `life-signal.md` §3), the walk's footing landmarks **erased** (the gradient, the thin's cost, the faint trail — three reads buried, `the-walk.md` §2), the predator's and the mimic's **cover** (the trail-read's legibility and the waited shape's cover in the blur, `signature-predator.md` §1.2 / `the-mimic.md` §2). |
| The honest cover = ? | **the fog hides what it wraps, and what it hides it hides from you and the Coda alike**: the held blur is the field's own read — the same channels the instruments show, read at their soft register, never a hidden weather and never a hidden trap (what the blur hides from you it hides from the trail-read too, `signature-predator.md` §1.2 — the honest cover, the blur's own, spent and gone when the field re-coheres, the blizzard's honest-cover precedent at the static register, `the-blizzard.md` §4). |
| The honest boundary = ? | the smell's upwind read, the maintenance read, the walk's three reads, the trail-read, the waited shape, the white-out's honest-cover precedent, the provenance classifier are their source docs' **landed** pieces; **the fog's composition — the held blur's form, the hesitation-made-visible frame, the honest cover — is this doc's [design]** over them, probe-calibrated. |
| Honest gates = ? | (a) the [design]/engine-real line; (b) **MEDIUM** — gated on the field's held-thin mechanics (the low-`q` register's softness, the cold's quiet at the weather's register) and the reads' softening (the smell's/walk's/life-signal's gates), with the **Phase-1 slice stated** (the fog as a bounded held-blur read over the published low-`q`/read channels — the blur's obscurity, before the mechanical fog); (c) determinism (same field state → same fog — a hard gate; never a seeded fog roll); (d) the no-free-energy cap (the fog converts nothing — no blur that yields; the held obscurity is the field's own spent, never a mint, never a hidden difficulty, never a free cover); (e) accessibility (the fog's approach and its blur readable from the instruments — never hidden-only; the held blur is the field's own read, never a hidden weather). |
| Feasibility | **MEDIUM with a Phase-1-legible slice — the held blur, the weather that hides what it wraps. State both** (§6). |

---

## 1. The Fog, stated once

The corpus's weather stack is all *arrivals, departures, flows, and turns* — and the fog is the register none of them occupies. The **front** is the storm's `c_s`-traveling `ε²` wave — it *arrives*, wounds, and *passes* ([`field-hazards.md`](./field-hazards.md) §2). The **rain** is a nourishing-but-wet fall — it *gives* and *recedes* ([`the-rain.md`](./the-rain.md) §3). The **wind** is a directional current — it *flows* and *carries* ([`the-wind.md`](./the-wind.md)). The **cold** is a long-thin climate — it *wears* ([`the-cold.md`](./the-cold.md)). The **blizzard** is the cold's thin carried into a *moving* white-out — it *drives* and *erases* ([`the-blizzard.md`](./the-blizzard.md)). The **season-change** *turns*, the **eclipse** *dims*, the **meteor** *arrives* and falls. Every one is defined by a *motion* — it arrives, it moves, it passes, it turns. **None *holds*.** The static blur is the one weather the corpus never asked for: a region that **stays**.

> **The Fog, stated once.** It is the **held blur — the weather that hides what it wraps**: a slow-held region of low-`q` obscurity, the field's own held thin — a bounded region where the `q`-carry is spent low and the reads go soft. It hides what it wraps honestly: the smell-channel swamped (the upwind-only read drowned in the field's own soft register, [`the-smell.md`](./the-smell.md) §2), the life-signal blurred (the maintenance read softened, [`life-signal.md`](./life-signal.md) §3), the walk's footing landmarks erased (the gradient, the thin's cost, the faint trail — three reads buried, [`the-walk.md`](./the-walk.md) §2), and where the signature-predator and the mimic hunt with impunity — the trail-read's legibility and the waited shape's cover in the blur ([`signature-predator.md`](./signature-predator.md) §1.2, [`the-mimic.md`](./the-mimic.md) §2). It is the one weather that is **neither arrival nor departure but hesitation made visible**: it does not arrive like the storm's front and it does not depart like the rain's fall — it *holds*, a region that stays, a slow blur the settlement must read and move through. And it is honest to the field's own law: **the fog hides what it wraps, and what it hides it hides from you and the Coda alike — what the blur hides from you it hides from the trail-read too; the held blur is the field's own read, never a hidden difficulty and never a free cover, spent and gone when the field re-coheres.**

**Why this is the weather stack's missing register, honestly.** Every existing weather is a weather of *motion* — the storm's approach, the rain's fall, the wind's current, the cold's wearing, the blizzard's driving. The corpus's danger layer already names that the storm is "the one hazard that can arrive with no player-caused trigger — it is the field's own extreme, not a punishment" ([`field-hazards.md`](./field-hazards.md) §6.2); the weather doc's own provenance classifier (§2) reads whether an extreme is the field's own weather or an aimed punishment. **But no weather asks the question that is neither arrival nor departure: what the field does when it does not move.** The cold holds its thin as a *season*; the blizzard carries that thin as a *storm*. The fog is the thin *held in place* — the quiet register taken not as a season's wearing or a storm's driving, but as a **bounded place that stays**, where the reads go soft because the field's own `q`-carry is spent low. It is the weather of the field's own hesitation — the blur that neither comes nor goes, but holds.

**The honest scope, stated once: the smell's upwind read, the maintenance read, the walk's three reads, the trail-read, the waited shape, the white-out's honest-cover precedent, and the provenance classifier are their source docs' landed pieces; the fog's composition — the held blur's form, the hesitation-made-visible frame, the honest cover — is this doc's [design] over them, probe-calibrated.** The Fog never claims the engine "has fog" or "raises a blur"; it reads the low-`q`/`(1−q)`/`ε²`/`∇(g·Φ)` the sampler already computes (the ≈ 6 MiB publish, [`corpus-reconciliation.md`](./corpus-reconciliation.md)) and presents them as the held blur — a consumer of the publish exactly as the Wind, the Cold, the Blizzard, and the Marsh are, never a new channel, never a write.

---

## 2. The held blur — the honest mechanics

The fog is **not a new weather system**. It is the field's own held-thin register *composed into a bounded place* — the quiet low-`q` state that the cold wears as a season and the blizzard drives as a storm, taken instead as a **region that stays**. Each grounded in its source doc's real quantities; **the fog's form — its blur's bound, its hold's duration — is this doc's [design]** over the landed held-thin mechanics, probe-calibrated.

### (a) The field's own held thin — the low-`q` register that is the fog's blur (the sources: `the-cold.md`, `energy-harnessing.md`)

**The landed piece.** The cold is the long-thin as a lived season ([`the-cold.md`](./the-cold.md) §1): regional `q` held near the noise floor (`q ~ 1e-3…1e-1` vs. the attractor `q ≈ 0.947`, [`corpus-reconciliation.md`](./corpus-reconciliation.md)), the harvest never fully returns, every machine wastes `E_waste = (1−q)·E_throughput` ([`energy-harnessing.md`](./energy-harnessing.md) §2). **[design]** The cold is the *still* winter — the long-thin held quiet. The Fog takes that same low-`q` register and **holds it in a bounded region**: the same `q`-low, waste-large field that is a still cold is, held in a locality rather than worn over a season, a ***held thin*** — where the `q`-carry is spent low and the reads go soft. **[design]** — whether a bounded locality holds its thin as a still blur (the fog) rather than re-cohering or being driven by a current is this doc's dial over the regional-`q`/`(1−q)` state, probe-calibrated against the cold's thin-regime gate ([`the-cold.md`](./the-cold.md) §6(b)) and the blizzard's driving-strength line ([`the-blizzard.md`](./the-blizzard.md) §6 risk b).

### (b) The reads going soft — the soft register that is the blur's effect (the source: `the-marsh.md`)

**The landed piece.** The marsh's tessellated `q` diffuses a signature — the trail-read, the watcher's clean signature, the walker's `ε²` wake all blur into the texture, the pattern does not carry ([`the-marsh.md`](./the-marsh.md) §3). **[design]** The Fog is **the marsh's soft-register hiding at the air's register**: where the marsh's *tessellated* `q` blurs a signature into a fine-grained texture, the fog's *held-low* `q` blurs every read into the field's own soft register — not a texture of hummocks and pools, but a uniform softness where the reads read *less distinctly* because the field's own `q`-carry is low. The fog is the air's marsh: the static hiding texture at the air's register, the same low-`q` read made a place. **[design]** — the fog's *softness* (how low the `q` must be, how much the reads soften) is this doc's dial over the marsh's hiding mechanics, probe-calibrated on the Phase-1 slice.

### The composed form — the [design] over the landed stack

| The fog's element | What it reads | Grounding |
|---|---|---|
| **The held blur** | the field's own low-`q` state held in a bounded region — the `q`-carry spent low, the reads soft (not driven, not worn — *held*) | `the-cold.md` §2 (the quiet thin's `q`-low register); `energy-harnessing.md` §2 (the `(1−q)` waste spent in the low-`q` field) |
| **The blur's bound** | how far the soft register extends — a bounded region the settlement reads and moves through, set so the fog reads as a *held place of obscurity*, not the whole season | [design] — the bound is this doc's dial over the low-`q`/`(1−q)` distribution, probe-calibrated; the blur's reach is legible (§5e) |
| **The hold's duration** | how long the region stays a blur — a *slow-held* region, not a transient event and not a permanent climate | [design] — the duration is this doc's dial, probe-calibrated; the fog holds and, when the field re-coheres, is *gone* (the honest cover, §4) |
| **The weather-read provenance** | the fog happens *where the field is* — a held low-`q` state of the shared channels, self-limiting, following the law, never aimed at a player | `weather-not-storm.md` §2 (the weather verdict); the provenance classifier's read applied to the held blur |

**[design]** — the *held blur's form* (the blur's bound, the hold's duration, the obscurity's softness) is this doc's designed surface over the published `q`/`ε²`/`(1−q)`, probe-calibrated exactly as the blizzard's white-out bound and the marsh's tessellation scale are over their landed stacks. What is landed is the field's low-`q` thin state, the reads' soft-register mechanics, and the weather-read provenance; **the Fog is the designed reading that holds that thin in a bounded place as the blur.** It adds no physics; it composes what the corpus already publishes, at the held register.

---

## 3. The hesitation made visible — the load-bearing design

This is the Fog's whole point: **the fog neither arrives nor departs; it *holds* — and that holding is the weather of the field's own hesitation made a place.** The storm arrives as a `c_s` front ([`field-hazards.md`](./field-hazards.md) §2) — you read it before it arrives, shelter, and let it pass. The rain falls and recedes ([`the-rain.md`](./the-rain.md) §3) — it gives and is gone. **The fog does neither.** It is not the storm's *approach* (there is no front to read before it arrives — the held blur is already there, and its obscurity itself is what you read, [`field-hazards.md`](./field-hazards.md) §5.1's readable-before-it-arrives held at the static register) and not the rain's *departure* (there is no fall to pass — the blur holds until the field re-coheres, [`the-walk.md`](./the-walk.md)'s craft at the blurred footing, the crossing that reads and moves through).

| The weather it is not | It is | The honest contrast |
|---|---|---|
| The **front** ([`field-hazards.md`](./field-hazards.md) §2) | a `c_s`-traveling `ε²` wave — it **arrives** | the fog does **not arrive**: there is no front, no approach — the held blur is a *place that is*, readable before it *wraps* (its obscurity's reach, §5e), not before it arrives |
| The **rain** ([`the-rain.md`](./the-rain.md) §3) | a nourishing-but-wet fall — it **departs** | the fog does **not depart**: it is not a passing precipitation — it *holds*, and its leaving is the field *re-cohering*, not the fall's recession |
| The **blizzard** ([`the-blizzard.md`](./the-blizzard.md) §1) | the cold's thin carried into a **driven white** — it moves and erases | the fog is the blizzard's **static twin**: the same obscuring blur, held in place rather than driven — *hesitation made visible*, whereas the blizzard is the field's urgency made visible |

**The hesitation made visible, stated once.** The corpus's weather is the field in *motion* — arriving, departing, flowing, turning. **The fog is the field's own hesitation made a place**: a bounded region where the field neither organizes toward the attractor (it holds its thin low) nor dissolves into an extreme (it is not a scar, not a desert-that-takes) — it simply *holds*, a slow blur the settlement must **read and move through** (the walk's craft at the blurred footing, [`the-walk.md`](./the-walk.md) §2: you read where the gradient is soft, you time the crossing, you carry little, and you leave a faint trail that is itself blurred). It is the weather of the field pausing — and the settlement's honest answer to a weather that holds is not to *shelter until it passes* (it does not pass like a storm) but to *read it, cross it, and live through its holding*.

**[design]** — that the fog *holds* (rather than arrives or departs) is this doc's designed frame over the landed low-`q`/read mechanics: the hesitation-made-visible is the design's framing claim, and its honesty is that it names a *real* field register (the low-`q` region that neither drives nor wears but stays) that the read-mechanics of §2 already compose.

---

## 4. The honest cover — the load-bearing honesty

The fog hides what it wraps, and the hiding is honest in the corpus's exact, *symmetric* sense: **what the fog hides from you, it hides from the Coda's trail-read too.** This is the load-bearing honesty of the whole design.

### (a) What the blur hides, it hides from the Coda too — the honest cover

The signature-predator's trail-read is legible only where the field has no order to hide it ([`signature-predator.md`](./signature-predator.md) §1.2): a clean trail through the desert is the only organized thing in flat disorder, and the Coda accumulates on it (`R = ρ_signature · τ · M_stability`, §2). **A fog buries every trail — yours and the predator's read alike.** The held blur is the field's own low-`q` state at its softest: not the desert's flat orderlessness (where one clean signature stands out) and not the blizzard's driven white (where the field's own order buries every trail in motion) — it is the *soft* register where a clean signature reads neither bright-against-flat (the trail-read's condition, [`signature-predator.md`](./signature-predator.md) §1.2) nor clean-against-plain (the watcher's exposure, [`the-vigil.md`](./the-vigil.md) §3). The blur softens every read the same way. **What the fog hides from you, it hides from the trail-read too: the held blur is the field's own soft read, and it erases every read the same way.**

> **The held blur is the field's own soft register, hiding every trail alike.** The trail the Coda hunts is the single organized thing against flat disorder ([`signature-predator.md`](./signature-predator.md) §1.2). Under the held blur, there is no flat disorder to contrast and no bright-against-plain to read — the field's own low-`q` softness makes a clean signature *soft* exactly as it makes every read soft ([`the-marsh.md`](./the-marsh.md) §3 — the pattern does not carry). **What the Fog hides from you, it hides from the trail-read too: the blur is the field's own held softness, and it softens every read the same way.**

### (b) The cover is the blur's own, spent and gone when the field re-coheres — never a free cloak

The white-out's honest-cover precedent ([`the-blizzard.md`](./the-blizzard.md) §4) is the fog's direct precedent, inherited at the static register: the blizzard's white hides because the field's own `(1−q)` waste is loud — it is *spent*, never collected, and when the white clears the cover is gone with the spent waste. **The fog is the same law held in place.** The held blur is the field's own low-`q` `(1−q)` spent — the `q`-carry held low, the waste fraction large — and that spent state is what softens the reads. The fog's cover is **the blur's own, spent and gone when the field re-coheres**:

> **The fog's cover is never a free cloak.** The blur hides because the field's own low-`q` state is soft — the held thin is the field's own `(1−q)` spent, and the field *will* re-cohere toward the attractor ([`energy-harnessing.md`](./energy-harnessing.md) §2 — the `−ω₀²(EY−φ·EI)` re-lock pulls it back; `the-cold.md` §1 — the season turns). **There is no blur that yields: no concealment a player can farm, no held-softness that pays, no fog that mints a hidden shield.** The fog's own cover (what it hides from you and the Coda alike) is spent when the field re-coheres — the `(1−q)` is the field's own spent, gone with the re-organization, and the trails the blur softened are softened *by the field's own soft register*, never by a designed cloak the settlement owns. **This is the blizzard's honest-cover precedent at the static register ([`the-blizzard.md`](./the-blizzard.md) §4): where the blizzard's is spent and gone when the white clears, the fog's is spent and gone when the field re-coheres.**

### (c) The no-free-energy cap — the fog converts nothing

`energy-harnessing.md` §6 (`output ≤ φ⁻¹·input`), inherited unchanged through the corpus's weather and read docs, is the Fog's iron bound:

> **A fog converts nothing — no held blur that yields.** The held blur is the field's own `(1−q)` spent — the low-`q` register's waste fraction held in a bounded region, never collected, never banked. The obscurity is information-loss, never a resource; the cover is the field's own soft register, gone when the field re-coheres. **There is no "read a fog to farm a cover," no "held-softness that mints a hidden shield," no fog that yields coherence or concealment the settlement did not already have in the field.** The fog's softness is the field's own `(1−q)` spent, never a gain, never a minted fog, never a hidden difficulty, never a free cover.

Three concrete guards, all inherited from the corpus's no-free-energy discipline ([`energy-harnessing.md`](./energy-harnessing.md) §6; [`the-blizzard.md`](./the-blizzard.md) §4c; [`the-marsh.md`](./the-marsh.md) §7d; [`signature-predator.md`](./signature-predator.md) §7e):
1. **No held-blur into a concealment-mint.** The softness cannot be farmed into a standing shield — the cover is the field's own spent `(1−q)`, `output ≤ φ⁻¹·input` held through it, gone when the field re-coheres.
2. **The cover is the field's own, never a cloak you own.** The blur hides the same ways for you and the Coda (the field's soft register softening every read); a settlement cannot store the fog's concealment any more than it can mint the wind's carry or bank a tailwind ([`the-marsh.md`](./the-marsh.md) §7d — the symmetric hiding is itself the no-stealth-yield guard).
3. **The softness is honest information-loss, never a hidden difficulty.** The buried reads are buried by the field's own low-`q` soft register, never by a designed opacity — a fog that "erases" reads the field does not show, or that hides a difficulty the field is not itself soft about, is a false blur the honesty forbids.

---

## 5. The honest boundary and gates

### (a) The [design]/engine-real line

Drawn once and never blurred — the line every weather/composition doc draws, held at the held blur:

| Quantity | Status | Basis |
|---|---|---|
| The field's low-`q` thin register — regional `q ~ 1e-3…1e-1` held near the floor; the `(1−q)` waste law; the re-lock toward the attractor | **engine-real** | `the-cold.md` §2; `energy-harnessing.md` §2; `corpus-reconciliation.md` |
| The reads' soft-register mechanics — the marsh's tessellated `q` diffusing a signature, a low-`q` register making reads read less | **landed** | `the-marsh.md` §2/§3; `the-sea.md` §2b (a uniform low-`q` hides the ridges) |
| The smell's upwind read (the wind's geometry + the mask) | **landed** | `the-smell.md` §2/§5 |
| The maintenance read (the life-signal's four classes) | **landed** | `life-signal.md` §3/§6 |
| The walk's three reads (the gradient, the thin's cost, the faint trail) and the stride-cost mapping | **landed** | `the-walk.md` §2/§4a |
| The trail-read — the signature-predator's legibility only where the field has no order | **landed** | `signature-predator.md` §1.2/§2 |
| The waited shape — the mimic's cover | **landed** | `the-mimic.md` §2/§3 |
| The white-out's honest-cover precedent — the field's own obscuring, spent and gone | **landed** (precedent) | `the-blizzard.md` §4 |
| The provenance — the weather-vs-punishment classifier over the published channels | **landed** (design-over-real, per its own doc) | `weather-not-storm.md` §2/§6 |
| **The fog's composition** — the held blur's form (the blur's bound, the hold's duration), the hesitation-made-visible frame, the honest cover (the field's own soft register softening every read, spent and gone when the field re-coheres) | **[design]**, probe-calibrated | **this doc** §2/§3/§4 |

**The line, stated once:** the smell's upwind read, the maintenance read, the walk's three reads, the trail-read, the waited shape, the white-out's honest-cover precedent, and the provenance classifier are their source docs' **landed** pieces. **The fog's composition — the held blur's form, the hesitation-made-visible frame, the honest cover — is this doc's [design]** over them, probe-calibrated exactly as the blizzard's white-out composition and the marsh's tessellation are over their landed stacks. No claim here pretends the engine "has fog" or "raises a blur"; the engine publishes the low `q`, the `(1−q)` waste, and the read channels, and the Fog is the designed reading that composes them into the held blur — a consumer of the publish, never a new channel, never a write.

### (b) MEDIUM, gated on the held-thin mechanics and the reads' softening — with the Phase-1 slice stated

The **full Fog — the composed held blur mechanically softening the reads, with its bound, its hold's duration, and its spent-cover honesty — is MEDIUM**, gated in order on:

- **The field's held-thin mechanics** (`the-cold.md` §6(b) — the thin-trough being real; `the-marsh.md` §8 — the patchy/low-`q` register being a real hiding texture, Phase-1.5): the fog's blur only reads as a *held softness* once the low-`q` register measurably softens the reads. This is the shared thin/hiding gate; the Fog inherits it unchanged.
- **The reads' softening** (the smell's mask, `the-smell.md` §5; the walk's stride-cost and faint trail, `the-walk.md` §4; the life-signal's classes, `life-signal.md` §6b — each landed, each mechanically softening under the held blur only at Phase-1.5+).

**The Phase-1 slice is stated, deliberately built as the honest core:**

> **Phase-1: the fog as a bounded held-blur read over the published low-`q`/read channels — the blur's obscurity, before the mechanical fog.** Take the Phase-1 box's low-`q` state (`q` toward `1e-3…1e-1`, the `(1−q)` waste large) and **read the blur's obscurity honestly**: that under a bounded low-`q` register, the published `q`/`ε²`/`(1−q)` read at their soft register *obscures* the reads — the smell-channel's faint signals, the walker's stride-cost and wake, the life-signal's classes, the trail-read's legibility all read *soft*. It is a **pure consumer of the publish** — it reads `q`/`ε²`/`(1−q)`/`∇(g·Φ)` from the ≈ 6 MiB snapshot inside the ≈ 1–6 ms/tick sample budget (the Weatherglass's "one extra sample at the player's position" pattern, [`field-instruments.md`](./field-instruments.md) §1.4), computes the soft register's obscurity, and presents the fog as the bounded held blur.

The slice de-risks the whole vocabulary: if a bounded low-`q` register measurably *obscures* the reads (and reads the field's own soft register honestly), every later fog element (the mechanical blur's bound, the hold's duration, the spent-cover honesty) inherits the same measured, engine-real channels — exactly the de-risking pattern of the blizzard's white-out slice ([`the-blizzard.md`](./the-blizzard.md) §5b), the marsh's patchy-`q` slice ([`the-marsh.md`](./the-marsh.md) §8), and the walk's ridge→scar→margin slice ([`the-walk.md`](./the-walk.md) §4b).

### (c) Determinism — a hard gate

The field is deterministic (one PDE; `the-walk.md` §4c; `the-blizzard.md` §5c; `the-marsh.md` §7c; `life-signal.md` §6d):

> **Same field state → same fog.** The held blur's bound, its hold's duration, its obscurity of each read, and its spent-cover honesty are each a **pure function of the region's real `q`/`ε²`/`(1−q)` trajectory** — no seeded-RNG, no player-relative variance, **never a seeded fog roll**. A player who re-enters the same window at the same field state reads the same held blur, the same soft register, the same spent cover, every time. **This is a hard gate: the Fog is a *true, learnable* fact about the low-`q` field — reproducible and teachable, never a mood.**

This is what makes the Fog honest as a weather: a settlement can *learn* its region's held blur — where the low-`q` field holds soft, how long it holds, what the blur softens (the same reads, for the same field). No rolled fog, no random heap of obscurity.

### (d) The no-free-energy cap — a fog converts nothing

Held without exception, per `energy-harnessing.md` §6 and §4 this doc. **A fog converts nothing — no blur that yields; the held obscurity is the field's own `(1−q)` spent, never a mint, never a hidden difficulty, never a free cover; the cover is the field's own, spent and gone when the field re-coheres.** A fog cannot be farmed into a concealment-mint any more than a marsh can be farmed into a stealth-yield or a blizzard into a white-out cloak ([`the-marsh.md`](./the-marsh.md) §7d; [`the-blizzard.md`](./the-blizzard.md) §5d) — the corollary of the same law: **the blur softens the field's own way, and provides nothing.**

### (e) Accessibility — the fog's approach and its blur are readable from the instruments, never hidden-only

Per the instrument-family rule ([`field-instruments.md`](./field-instruments.md) §2.1) and the corpus's never-hidden-only discipline ([`field-music.md`](./field-music.md) §6; `the-walk.md` §4e; `the-marsh.md` §7e):

> **The fog's approach and its blur are readable from the instruments — never hidden-only.** The coming blur reads the way the cold and the thin already read: the regional-`q` trend toward the low-`q` register (`the-cold.md` §6(e) — the `(1−q)` glow floor), the reads' soft register on the Weatherglass's dead-flat-grey/lume (`field-instruments.md` §1.2 — the same grey the desert reads, at the held register). **And the blur itself is the field's own read** — a player who walks into the fog reads the same `q`/`ε²`/`(1−q)` the instruments always show, at the register where it softens; the buried reads are buried by the field's own published soft signal, never by a hidden opacity. **A player who never uses a "fog" overlay loses nothing informational** — the blur is the same low-`q`/waste-tint/soft-register the instruments already render, never a hidden weather, never a hidden trap. **The held blur is the field's own read, structurally guaranteed by the shared-publish architecture ([`field-instruments.md`](./field-instruments.md) §1.3).**

---

## 6. Feasibility verdict

**MEDIUM with a Phase-1-legible slice — the held blur, the weather that hides what it wraps. State both.**

- **The full Fog — the composed held blur mechanically softening the reads, with its bound, its hold's duration, and its spent-cover honesty — is MEDIUM.** It gates, in order, on the **field's held-thin mechanics** (`the-cold.md` §6(b) — the thin-trough being real; `the-marsh.md` §8 — the low-`q`/patchy register being a real hiding texture, Phase-1.5) and the **reads' softening** (the landed smell/walk/life-signal/trail-read reads each mechanically softening under the held blur, Phase-1.5+). It **adds no new physics and no new channel**: it is a designed composition over the published low-`q`/read channels, the landed read-machinery, the trail-read, the waited shape, the white-out's honest-cover precedent, the provenance classifier, and the cap — a consumer of the publish, exactly as the Wind, the Cold, the Blizzard, and the Marsh are. **Its honesty is that the held blur's form, the hesitation-made-visible frame, and the honest cover are [design] over that landed stack, probe-calibrated.**

- **The Phase-1-legible slice — the fog as a bounded held-blur read over the published low-`q`/read channels (the blur's obscurity, before the mechanical fog) — is statable now.** Whether or not the mechanical blur is physically presented, the corpus holds the load-bearing truth now, against **the field's low-`q` thin + the reads' soft-register mechanics + the landed read-machinery + the trail-read + the white-out's honest-cover precedent**: **at a bounded low-`q` register, the field's own `q`/`ε²`/`(1−q)` read as a held blur that softens the reads — the smell swamped, the life-signal blurred, the footing buried, the trail softened — and that obscurity is the field's own read, the same for you and the Coda's trail-read, the field's own spent cover gone when the field re-coheres.** That principle is a pure consumer of the publish — the precise Phase-1 pattern the blizzard's white-out slice, the marsh's patchy-`q` slice, and the walk's ridge→scar→margin slice prove ([`the-blizzard.md`](./the-blizzard.md) §5b; [`the-marsh.md`](./the-marsh.md) §8; [`the-walk.md`](./the-walk.md) §4b). **The held blur — the weather that hides what it wraps, neither arrival nor departure but hesitation made visible — is the Phase-1 gain; the mechanical fog (the blur's bound, the hold's duration, the mechanical softening) is MEDIUM, gated as above.**

**Binding risks, in order:** **(a)** the **held-thin gate** (`the-cold.md` §6(b), `the-marsh.md` §8) — does a bounded locality actually *hold* its low-`q` register as a still blur (the fog) rather than re-cohering quickly or being driven by a current into a blizzard-like event? A re-cohering locality pushes the fog to *pure presentation* (a legitimate soft-register read with no mechanical hold), still honest. **(b)** the **reads' softening magnitude** (§2/§5b) — does the low-`q` register actually soften the reads enough to feel as *obscurity*, or is the contrast between the soft register and the reads too small at Phase-1 to feel? The §5b slice measures it; a weak softening collapses the hide to *pure soft-presentation* (honest reads going soft, no mechanical invisibility), still legitimate and still symmetric. **(c)** the **honest cover's symmetry** (§4) — the fog must never read as one-sided concealment (a free cloak for the player against the Coda); the cover is the field's own soft register hiding everyone, and the moment it reads as "hide in the fog from the predator while the predator cannot read you in it," the honest cover collapses into the very free-cloak the cap forbids. The blizzard's honest-cover precedent (§4 there) and the marsh's no-asymmetric-cloak guard ([`the-marsh.md`](./the-marsh.md) §7d) are the walls that hold it. **None contradicts the async, dual-world, or regime-collapse architecture** — the Fog is a consumer of the publish, the same way the Wind, the Cold, the Blizzard, and the Marsh are, and it reads the soft register the field's determinism already guarantees.

> **The honest statement that makes this doc load-bearing: the weather stack has the front (a hazard that arrives), the rain (a weather that gives and recedes), the wind (a flow that carries), the cold (a climate that wears), the blizzard (a white storm that drives and erases), the season-change (a turn), the eclipse (a scheduled dim), the meteor (a falling arrival) — but no static blur: no weather that neither arrives nor departs but *holds*.** The Fog is that blur — the held blur, the weather that hides what it wraps: a slow-held region of low-`q` obscurity, the field's own held thin — a bounded region where the `q`-carry is spent low and the reads go soft. It hides what it wraps honestly ([`the-smell.md`](./the-smell.md) §2 — the smell-channel swamped; [`life-signal.md`](./life-signal.md) §3 — the maintenance read blurred; [`the-walk.md`](./the-walk.md) §2 — the walk's three reads buried under the blur; [`signature-predator.md`](./signature-predator.md) §1.2 / [`the-mimic.md`](./the-mimic.md) §2 — the predator's and the mimic's cover). It is the one weather that is **neither arrival nor departure but hesitation made visible** — it does not arrive like the storm's front ([`field-hazards.md`](./field-hazards.md) §2) and does not depart like the rain's fall ([`the-rain.md`](./the-rain.md) §3); it *holds*, a region that stays, a slow blur the settlement must read and move through. And it is honest to the field's own law: **the fog hides what it wraps, and what it hides it hides from you and the Coda alike** ([`signature-predator.md`](./signature-predator.md) §1.2 — the honest cover, the blur's own, spent and gone when the field re-coheres, the blizzard's honest-cover precedent at the static register, [`the-blizzard.md`](./the-blizzard.md) §4); and the fog converts nothing ([`energy-harnessing.md`](./energy-harnessing.md) §6 — the held blur is the field's own `(1−q)` spent, never a gain, never a minted fog, never a hidden difficulty, never a free cover). It is the air's marsh ([`the-marsh.md`](./the-marsh.md) §2/§3 — the static hiding texture at the air's register) and not the mirage ([`the-mirage.md`](./the-mirage.md) §2 — it blurs what is truly there, never composes a false bright), a weather-read of the same channels, never a punishment ([`weather-not-storm.md`](./weather-not-storm.md) §2), its approach and its blur readable from the instruments, never hidden-only ([`field-instruments.md`](./field-instruments.md) §2.1/§1.2). The corpus read the weather's arrival, departure, flow, and turn; **the Fog is the held register the stack never wrote — the weather that is neither coming nor going, but standing: the field's own hesitation made a place, hiding what it wraps, and hiding it from the hider and the hidden alike, spent and gone when the field re-coheres.** MEDIUM with a Phase-1-legible slice — the held blur, the weather that hides what it wraps.

---

## Open questions

1. **The hold's stability (§2/§3/§6).** Does a bounded locality actually *hold* its low-`q` register as a still blur — or does the field's `−ω₀²(EY−φ·EI)` re-lock pull it back toward the attractor too quickly, collapsing the fog to a *transient* soft register (or pushing it, when a current picks the thin up, into a blizzard-like driven white-out, [`the-blizzard.md`](./the-blizzard.md) §6)? The fog's defining "holds" is the detail most at risk from the field's determinism; the Phase-1 slice's held-region read (does a bounded low-`q` locality stay soft?) is the deciding probe. **[probe]**
2. **The reads' softening magnitude (§2/§5b).** Does the low-`q`/`(1−q)` register actually soften the reads enough to *feel* as obscurity — a walker's faint trail genuinely illegible, a life-signal's classes blurred — or is the contrast between a soft register and the reads too small at Phase-1, collapsing the hide to *pure soft-presentation* (honest reads going soft, no mechanical invisibility)? The §5b slice measures it; both outcomes are honest, and the doc states what a weak blur means (§6 risk b). **[probe]**
3. **The fog's relation to the blizzard's onset (§3).** The rain reads whether a rising water is still rain or has become the flood's beginning ([`the-rain.md`](./the-rain.md) §4). Does the fog carry an analogous edge — the moment a held low-`q` blur, caught by a rising `FieldVel`, becomes a *driven* white-out (the blizzard) rather than a held blur (the fog), and is that read the fog's honest equivalent of the rain→flood boundary? The static-vs-driven line between the fog and its blizzard-twin is a [design] dial over the `FieldVel`/`q` trend, inheriting the blizzard's driving-strength gate ([`the-blizzard.md`](./the-blizzard.md) §6 risk a). **[design]/[probe]**
4. **The fog's boundary with the marsh and the desert (§2).** The fog is the air's marsh ([`the-marsh.md`](./the-marsh.md)) and a low-`q` regional state like the desert's ([`field-hazards.md`](./field-hazards.md) §3). What separates a *fog* (a held blur that softens the reads and then clears) from a *trending desert* (a `q` collapse that creeps and persists)? The honest read is the same trend-vs-level that separates the season from the storm's weather ([`weather-not-storm.md`](./weather-not-storm.md) §6e): the fog's softness is bounded and re-coheres; the desert's collapse is a sustained drain. Whether a player reads the two as distinct is a [design] dial over the regional-`q`/`(1−q)` trend, probe-calibrated. **[design]/[probe]**
5. **The mimic's cover depth (§4/the-mimic).** The waited shape hunts where the blur hides the wary's read ([`the-mimic.md`](./the-mimic.md) §2). Does a mimic's cover in the fog read as *it is hidden too* (the symmetric honest cover — the blur softens the mimic's worn shape's drift as much as the wary's read), or is the mimic naturally at home in the soft register (more legible in the fog than its prey is)? The honest default is the former (the fog's cover is symmetric — what hides the wary hides the mimic's drift, [`the-marsh.md`](./the-marsh.md) §7d's no-asymmetric-cloak); whether a mimic knowingly hunts *better* in the blur is a designed dial over the waited shape's habitat, not an engine claim. **[design]**
6. **The fog's duration and the season's turn (§3).** The cold is a season that turns ([`the-cold.md`](./the-cold.md) §1; [`the-season-change.md`](./the-season-change.md) §2). Is the fog's hold keyed to the thin's duration (a fog that endures for the thin trough and clears as the field re-coheres toward harvest), or can a fog persist *into* a harvest-region boundary (a held blur independent of the season's turn)? The honest default is the former (the fog's hold is the field's own thin-hold, bounded by the re-coherence, [`energy-harnessing.md`](./energy-harnessing.md) §2); whether a fog ever outlasts its thin is a [design] dial over the regional-`q` trajectory, probe-calibrated. **[design]**

---

## Cross-references

- [`the-blizzard.md`](./the-blizzard.md) — **THE white-out's static twin.** §2 the composed storm (the fog's composition mirrors); §3 the erasure (the fog's softening of the reads is the static register of the blizzard's erasure); §4 the honest cover (the fog inherits it at the static register — what the blur hides from you it hides from the Coda too, spent and gone when the field re-coheres); §5b/§6 the MEDIUM + Phase-1-slice precedent the fog's slice mirrors; §5c/d/e determinism, no-mint, accessibility. **The Fog is the blizzard's static twin — blur held, not driven.**
- [`the-marsh.md`](./the-marsh.md) — **THE air's marsh.** §2 the tessellated `q` (the low-`q` register the fog's blur reads); §3 the hiding (the fog's softening of the reads is the marsh's hiding at the air's register); §4 the symmetric hiding (the fog's honest cover inherits it — what hides you hides what hunts you); §7c/d/e determinism, no-mint, accessibility; §8 the patchy-`q` Phase-1 slice (the fog's slice's sibling). **The Fog is the air's marsh — the static hiding texture at the air's register.**
- [`the-mirage.md`](./the-mirage.md) — **THE deceitful read's opposite.** §2 the composed bright (the field's channels composing an absent source — the fog's inverse); §3 the field-cannot-lie frame (the fog's honesty is the same law, applied to blurring not composing). **The Fog is not the mirage: the mirage composes a false bright, the fog blurs what is truly there.** Reverse pointer: where the mirage reads an absent thing, the fog softens a present one.
- [`the-smell.md`](./the-smell.md) — **THE upwind-only read.** §2 the wind's geometry + the mask (the fog's soft register swamps the channel — you read the loud stink and miss the faint one, and in the fog the whole register is soft); §5 the landed read; §6 the no-free-energy. **A fog swamps the smell-channel — the nose drowned in the field's own soft register.**
- [`life-signal.md`](./life-signal.md) — **THE maintenance read.** §3 the four classes (the fog's soft register blurs them — the maintained-live read softened); §6a the [design]/engine-real line; §6b the noise-floor probe (the blur's legibility gate); §6c accessibility; §6d determinism. **The fog blurs the life-signal — the maintenance read goes soft.**
- [`the-walk.md`](./the-walk.md) — **THE three reads.** §2/§2a/§2b/§2c the gradient at foot-pace, the thin's cost, the faint trail; §4a the stride-cost mapping. **The fog buries them all under the soft register — the footing landmarks erased**; §4b the ridge→scar→margin Phase-1 slice (the fog's slice's sibling); §4c/d/e determinism, no-mint, accessibility.
- [`signature-predator.md`](./signature-predator.md) — **THE trail-read.** §1.2 the trail legible only where the field has no order to hide it (the fog's low-`q` softness denies the legibility condition); §2 the accumulation `R = ρ_signature·τ·M_stability`; §7e the no-farming gate. **The fog's honest cover — what the blur hides from you it hides from the Coda too, spent and gone when the field re-coheres.**
- [`the-mimic.md`](./the-mimic.md) — **THE waited shape.** §2 the waited shape (the fog as the mimic's cover — the waited shape hunts where the blur hides the wary's read); §3 the drift (the fog's soft register blurs the maintenance-read tell it would otherwise follow); §5e accessibility. **The fog is the mimic's cover — the waited shape hunts where the blur hides the wary's read.**
- [`the-blizzard.md`](./the-blizzard.md) §4 — **THE honest-cover precedent**, inherited at the static register. See above.
- [`weather-not-storm.md`](./weather-not-storm.md) — **THE provenance.** §2 weather-vs-punishment (the fog is a weather-read of the same channels — the field's own held state, never a punishment); §3 the storm-provenance probe; §6 the gates. **The fog is the field's own held weather, never a punishment.**
- [`field-hazards.md`](./field-hazards.md) — **THE hazard layer.** §2 the storm's `c_s` front (what the fog is not — it does not arrive); §5.1 readable-before-it-arrives (the fog's approach — its obscurity — readable the same way); §1 the no-monster-table house style (a fog is a presented reading, never a hidden spawn) §5.3 the no-free-energy gate. **The fog's blur is readable, never hidden-only.**
- [`the-cold.md`](./the-cold.md) — **THE long-thin.** §2 the quiet thin (`q ~ 1e-3…1e-1` held, the harvest never fully returns); §3 the husbandry through the trough; §6(b) the thin-regime gate; §4/§7 the drift-collapse honesty. **The fog rides the field's held-thin — the quiet register held in a bounded region.**
- [`energy-harnessing.md`](./energy-harnessing.md) — **THE waste + THE cap.** §2 the `(1−q)` waste law `E_waste = (1−q)·E_throughput` (the held blur is the field's own spent thin); §6 the no-free-energy cap (`output ≤ φ⁻¹·input` — a fog converts nothing, the held obscurity is the field's own spent, never a mint, never a free cover).
- [`field-instruments.md`](./field-instruments.md) — **THE family rule.** §1.2 the four glanceable forms (the soft register's lume/grey — the fog's blur reads through the instruments, never hidden-only); §1.4 the sample-at-position cost profile (the fog's read cost — one extra sample at the player's position); §2.1 the family rule — the fog's reads are consumers of the publish, never a new channel.
- [`the-dawn.md`](./the-dawn.md) — **the clearing.** §2 the breaking (the day's re-birth); §2b the margin's thinning. **The fog's re-coherence is the dawn's clearing at the held register — where the dawn reveals, the fog's clearing reveals.** Skimmed.
- [`the-rain.md`](./the-rain.md) — **the fog's non-departure.** §3 the nourishing-but-wet (a fall that gives and recedes). **The fog does not depart like the rain's fall — it holds.** Reference only.
- [`the-dune.md`](./the-dune.md) — **the ground-twin.** §2 the held blur; §4 the honest cover. Reverse pointer: the dune is the fog’s ground-twin — the land’s slow-held covering that softens the reads, spent and gone when the field re-coheres.
- [`the-lightning.md`](./the-lightning.md) — **the static’s inverse.** §2 the held blur. Reverse pointer: the lightning is the fog’s inverse — where the fog blurs slowly, the flash reveals at once.
- [`world-difficulty.md`](./world-difficulty.md) — **the scaled blur.** §2 the held blur. Reverse pointer: a wild world’s weather scales the fog’s held blur.
- [`corpus-reconciliation.md`](./corpus-reconciliation.md) — the canonical numbers (the ≈ 6 MiB publish = `q` 1 + pot 1 + `∇(g·Φ)` 3 + ρ 1, plus the atmosphere's fifth engine-real `FieldVel` channel; the 64³ grid / 192³ box; `dt = 0.05`; `q ≈ 0.947` attractor; `q ~ 1e-3…1e-1` noise; `ξ = φ⁶ ≈ 17.94`; `φ⁻² ≈ 0.382`; `τ_c = 0.5`; the ≈ 1–6 ms/tick sample budget; the ≈ 2,000-entity cap; the ≈ 40 ns/entity river-law steer `a = −G_N·(π/ρ)·∇(g·Φ)`; `c_s = h₀/dt`); **cited, not re-derived.**
