# The Comet: The Passing Bright — the Sky's Visitor, the Returning Body, the Meteor's Periodic Cousin

**Question under design:** the corpus designs the sky's arrivals and its
readings. The Meteor is the **falling bright** — the rarest sky-event, a body
that arrives once, its coming announced before it lands (`the-meteor.md` §1/§2a).
The sky stack — atmosphere, orbits, auroras — is the field's own sky
(`atmosphere-orbits-auroras.md` §1/§2), and its bodies are the merge lineage
condensed into orbit (`atmosphere-orbits-auroras.md` §2.2). The Observatory is
the **reading room** — where a settlement reads the sky together
(`the-observatory.md` §1). The Window's Year mounts the long-rhythm calendar
(`the-window-year.md` §1). The Tide is the long pulse the living world shares
(`tide-of-the-attractor.md` §1). The Dawn and the Eclipse are the sky's daily
turn and its rare darkening (`the-dawn.md` §1, `the-eclipse.md` §1), and the
Zenith is the sky's crown (`the-zenith.md` §1). Every one of these reads a body
that arrives once, an orbit that holds, or a sky that turns. **But nothing
designs the body that arrives *again* — the returning bright, the sky's visitor
that comes, goes, and comes back on a readable period.** The meteor falls once
and is gone; the corpus has no designed **returning** body. **This document
designs the passing bright — the Comet, the sky's visitor, the returning body,
the meteor's periodic cousin.**

The load-bearing composition, stated once:

> **The Comet is the passing bright — the sky's visitor, the returning body, the
> meteor's periodic cousin: a body that arrives, passes, and returns on a readable
> long period, its orbit read on the instruments the way the sky's other bodies
> are read** (`atmosphere-orbits-auroras.md` §2 — bodies are the merge lineage
> condensed; §2.3 — orbit dynamics). Where the Meteor falls **once** and is gone
> (`the-meteor.md` §1 — a body that arrives), the Comet **returns** — the same
> body, the same visited line, back on the long period the windows year counts
> (`tide-of-the-attractor.md` §1 — the long pulse). **The load-bearing honesty is
> the return's own law: a comet is never a portent — it is NOT a message** (the
> field cannot lie, cannot telegraph — `the-dispute.md` §2); **it is a
> deterministic returning body** (its orbit reads on the instruments; same field
> state → same return, never a seeded "omen roll"); **and it never mints** (it
> yields nothing, it is a body that passes; a "comet that granted wishes" is a
> false reading the field sheds, `energy-harnessing.md` §6 — `output ≤ φ⁻¹·input`
> held hard). **Its spectacle is the field's own — legible, accessible, never
> hidden-only.**

This document is the corpus's **returning-body doc** — the face of the sky that
completes the sky's *time*: the dawn is the daily turn, the eclipse is the rare
darkening, the meteor is the one-time arrival, the orbit is the held line, and
**the comet is the long return**. It is the one body whose whole point is that it
comes *back* — the sky's visitor on a readable period, the meteor's cousin that
did not fall once but orbits again. And its honesty is that the return carries no
message: the field cannot telegraph (`the-dispute.md` §2), so the comet is never
an omen, never a wish-granter, never a hidden sign — it is a deterministic body
that passes, its spectacle the field's own, its period a read, its meaning none.

Companion to (all relative paths):
- [`the-meteor.md`](./the-meteor.md) — **THE falling bright — the Comet's
  cousin.** §1 the rarest sky-event, a body that arrives; §2(a) the herald's
  announced read; §5b MEDIUM-LATE; §5c determinism (same sky state → same
  meteor). **The meteor falls once and is gone; the comet returns — the same
  arrived body, on a readable period.**
- [`atmosphere-orbits-auroras.md`](./atmosphere-orbits-auroras.md) — **THE sky
  stack — the Comet's home.** §2 orbits as open-boundary field phenomenology;
  §2.2 bodies = the merge lineage condensed; §2.3 orbit dynamics (softened,
  damped). **The comet is one more body of the sky's own orbit — a condensed
  lineage on a long return, read like the others.**
- [`the-window-year.md`](./the-window-year.md) — **THE long-rhythm calendar —
  the Comet's period.** §1 the composed long-rhythm calendar; §3 the observances;
  §2 the three scales. **The comet's return is a date on the window's long
  calendar — the visiting bright named in the year's rhythm.**
- [`tide-of-the-attractor.md`](./tide-of-the-attractor.md) — **THE long pulse —
  the Comet's tempo-companion.** §1 the tide as the large-scale regional q
  oscillation; §2 the regime table; §5a the probe. **The comet rides the same
  long time as the tide — the sky's visitor on the window's own breath.**
- [`the-observatory.md`](./the-observatory.md) — **THE reading room — the
  Comet's read.** §1 where a settlement reads together; §2 the room's body (each
  surface a consumer of an already-published channel); §2.2 the whole window as
  one body. **The comet's orbit and its return are read in the observatory, the
  sky's line made a page.**
- [`the-dawn.md`](./the-dawn.md) — **THE sky's turn — the Comet's even-keel
  cousin.** §1 the field's re-birth each day, the transition read. **Where the
  dawn turns the sky daily, the comet turns it rarely — both the field's own
  motion, neither a message.**
- [`the-eclipse.md`](./the-eclipse.md) — **THE darkening — the Comet's
  rarity-kin.** §1 the window's darkening, a rare scheduled moment when the read
  goes dark; §2(a) the name on the calendar. **The eclipse and the comet are both
  rare scheduled sky-events — the one a darkening, the other a passing, neither a
  portent.**
- [`the-zenith.md`](./the-zenith.md) — **THE sky's crown — the Comet's height.**
  §1 the vertical's ceiling; §2(b) the boundary, the seam between the window's
  air and the between's thin. **A comet crosses the sky's crown on its return —
  the passing bright at the zenith's edge.**
- [`field-instruments.md`](./field-instruments.md) — **THE instrument family
  rule.** §1.4 the Weatherglass's single-sample economics; **§2.1 the family
  rule — every instrument is a consumer of the same publish, never a new
  channel.** **The comet's reads are consumers of the publish.**
- [`energy-harnessing.md`](./energy-harnessing.md) — **THE cap.** §6 the
  no-free-energy cap — `output ≤ φ⁻¹·input` — a comet yields nothing, a passing
  body, never a mint.
- [`the-dispute.md`](./the-dispute.md) — **THE field-true judgment — the Comet's
  non-message.** §2 the field cannot lie the way a witness can; the field is
  deterministic and legible. **A comet is not a portent — the field cannot
  telegraph, so the returning bright carries no hidden sign.**
- [`corpus-reconciliation.md`](./corpus-reconciliation.md) — the canonical numbers,
  cited, not re-derived (§2).

Every number below is from [`corpus-reconciliation.md`](./corpus-reconciliation.md)
(§2 the canonical set — cited, not re-derived), engine-verbatim, or explicitly
flagged **[design]** (this doc's composition over landed pieces) / **[probe]** (a
measurement the design gates on). The honest boundary is drawn in §5 and never
blurred: **the meteor's one-time arrival, the sky stack's orbits, the window-year's
calendar, the tide's long pulse, the observatory's reads, the dawn's turn, the
eclipse's darkening, and the zenith's crown are their source docs' landed pieces;
the comet's composition — the return's form, the passing-bright frame, the
non-portent — is this document's [design] over them.** No claim here pretends the
engine writes "comet" or "return" records beyond the bodies it already condenses
and the publish the instruments already read; a comet is a *designed return* of
the sky's own condensed lineage, over the meteor's announced-arrival and the
orbit-dynamics' landed mechanics.

---

## Summary table

| Question | Answer (concrete) |
|---|---|
| The Comet = ? | **the passing bright — the sky's visitor, the returning body, the meteor's periodic cousin**: a body that arrives, passes, and returns on a readable long period, its orbit read on the instruments the way the sky's other bodies are read. |
| The return = ? | a body of the sky's own condensed lineage (atmosphere §2.2 — bodies are the merge lineage condensed) on a **long readable period**: it arrives announced like the meteor (the-meteor §2a), passes, and comes back on the long return the window's year counts (the-window-year §1), its orbit softened and damped like the sky's other orbits (atmosphere §2.3). Same field state → same return (the-dispute §2). The return's period and its line are this doc's [design] over the landed orbit-dynamics, probe-calibrated. |
| The load-bearing design | the Comet is the **sky's returning body — the meteor's periodic cousin**: where the meteor falls once and is gone, the comet returns on a readable period, the same body, the same visited line, back on the long time the tide and the year share. |
| The load-bearing honesty | a comet is **never a portent — it is NOT a message** (the field cannot lie, cannot telegraph, the-dispute §2); it is a **deterministic returning body** (its orbit reads on the instruments; same field state → same return, never a seeded "omen roll"); **it never mints** (it yields nothing, a body that passes; a "comet that granted wishes" is a false reading the field sheds, energy-harnessing §6); **its spectacle is the field's own — legible, accessible, never hidden-only**. |
| The honest boundary = ? | the meteor's one-time arrival, the sky stack's orbits, the window-year's calendar, the tide's long pulse, the observatory's reads, the dawn's turn, the eclipse's darkening, and the zenith's crown are their source docs' **landed pieces**; **the comet's composition — the return's form, the passing-bright frame, the non-portent — is this doc's [design]** over them. |
| Honest gates = ? | (a) the [design]/engine-real line (§5a); (b) **MEDIUM-LATE — gated on the sky's announced-momentum and the long-period orbit dynamics, with the framing Phase-1-legible** (§5b); (c) determinism (same field state → same return — a hard gate; never a seeded "omen roll"); (d) the no-free-energy cap (a comet yields nothing — a body that passes, never a mint, never a granted wish); (e) accessibility (the comet's period and its return readable from the observatory and the instruments — never hidden-only). |
| Feasibility | **MEDIUM-LATE — gated on the sky's announced-momentum and the long-period orbit dynamics, with the framing Phase-1-legible. State both.** |

---

## 1. The Comet, stated once

The corpus's sky is complete in every arrival *except the return*. The Meteor
falls — the rarest sky-event, a body that arrives once, announced before it lands
(`the-meteor.md` §1/§2a). The sky stack holds — the atmosphere is the field's
envelope, its bodies the merge lineage condensed into orbit
(`atmosphere-orbits-auroras.md` §1/§2.2). The Observatory reads — the reading
room where a settlement reads the sky together (`the-observatory.md` §1). The
Year mounts — the long-rhythm calendar onto which the sky's events are dated
(`the-window-year.md` §1). The Tide pulses — the long breath the living world
shares (`tide-of-the-attractor.md` §1). The Dawn turns and the Eclipse darkens
(`the-dawn.md` §1, `the-eclipse.md` §1), and the Zenith crowns the sky
(`the-zenith.md` §1).

None of these is a body that **comes back**. The meteor arrives and is gone; the
orbit holds a condensed lineage in place; the dawn and the eclipse are the sky's
own motion, not a visitor. **No designed body returns on a readable period.**

> **The Comet, stated once.** The **passing bright — the sky's visitor, the
> returning body, the meteor's periodic cousin**: a body that arrives, passes,
> and returns on a readable long period, its orbit read on the instruments the
> way the sky's other bodies are read. Where the meteor falls once and is gone,
> the comet returns — the same body, the same visited line, back on the long time
> the windows year counts.

**The honest status, once.** Every piece the Comet composes is its source doc's
landed piece — the meteor's announced-arrival and its determinism (`the-meteor.md`
§2a/§2d/§5c), the sky stack's orbit dynamics and its condensed bodies
(`atmosphere-orbits-auroras.md` §2.1/§2.2/§2.3), the window-year's long calendar
(`the-window-year.md` §1), the tide's long pulse (`tide-of-the-attractor.md`
§1/§5a), the observatory's reading room (`the-observatory.md` §1/§2), the dawn's
turn and the eclipse's scheduled dark (`the-dawn.md` §1, `the-eclipse.md` §1/§2a),
the zenith's crown (`the-zenith.md` §1), the field-true non-deception
(`the-dispute.md` §2), and the no-free-energy cap (`energy-harnessing.md` §6).
**The comet's composition — that a body *can* return on a readable period,
*how* the return is formed (§2), what the passing bright *means* (§3), and why
the non-portent is the return's own law (§4) — is this document's [design] over
those landed pieces, probe-calibrated.** No new physics, no new channel: the Comet
is a *designed return* of the sky's own condensed lineage, read through the
publish a settlement already reads, composed over the meteor's announced-arrival
and the orbit-dynamics' landed mechanics.

---

## 2. The returning body — the honest mechanics

### (a) The body — the merge lineage condensed, the sky's own

The sky's bodies are not separate arrivals; they are the merge lineage condensed —
the field's own matter gathered into a body the open-boundary arm holds in orbit
(`atmosphere-orbits-auroras.md` §2.1 — why orbits exist at all; §2.2 — bodies
are the merge lineage condensed). **The Comet is one more body of that lineage —
a condensed mass on an orbit whose period is long.** It is not a separate sky; it
is the same field that condenses every body, gathered into a visitor whose return
its orbit reads. The comet's mass, its trail, its line — all facts of the field's
own condensation, the same way a body's orbit is a fact of the open-boundary arm
it rides.

### (b) The long orbit — the returning line, its period readable

A comet's defining feature is its **long period** — an orbit so extended that the
body leaves the window's near sky and returns on a rhythm the settlement counts
in years, not days. **That period is a read** — the same orbit-dynamics
(softened, damped — `atmosphere-orbits-auroras.md` §2.3) the settlement reads for
its other bodies, read at the longer arm: the comet's line, its aphelion and
perihelion, its return-date all on the instrumentation the Observatory composes
(`the-observatory.md` §2 — each surface a consumer of an already-published
channel). The return is deterministic and pre-readable: same field state → same
orbit → same return, every time (`the-dispute.md` §2). **The comet's period is
the window-long read that makes the return legible instead of surprising.**

### (c) The arrival, announced like the meteor — the passing bright, read before it comes

The Meteor's honesty is that its coming is **announced before it lands** — the
herald's announced read, the bell's toll, the observatory's page
(`the-meteor.md` §2a/§2b/§2c). **The Comet's return is announced the same way —
the passing bright read on the observatory's page well before it brightens the
sky.** Because the period is a read, the return is never a surprise: the
settlement that reads its sky knows the visitor is coming, dates it on the
window-year's calendar (`the-window-year.md` §1), and watches it brighten on the
line it read. The announced return is the comet's accessibility — the passing
bright never arrives in the dark, because its orbit was always legible.

### (d) The trail — the field's own wake shown, never a message

The comet's trail is the field's own display — the condensed body's
`(1−q)` and its interaction with the sky's thin shown as a brightness the whole
window can read. **The trail is not a message.** The field cannot telegraph
(`the-dispute.md` §2), so the comet's bright line says nothing, portends nothing,
warns nothing — it is the body's own orbit made visible, the way the aurora is a
discharge made visible and the storm is weather made visible
(`field-music.md` §2.1/§2.2). A settlement that "read a meaning" in the trail has
misread the field's indifference: the trail is the orbit's display, and its only
content is the body that made it.

### (e) The return and the calendar — the long period dated on the year

The Window's Year mounts the long-rhythm calendar — the observances, the three
scales of a settlement's time (`the-window-year.md` §1/§3). **The comet's return
is a date on that calendar — the visiting bright named in the year's rhythm**,
the way the eclipse is a named dark event (`the-eclipse.md` §2a). The return is
not a portent that arrives; it is a *scheduled* spectacle the settlement's own
year already holds, read and dated like the dawn's daily turn and the eclipse's
rare darkening. The calendar's honesty is that it dates the comet without
meaning it — the visitor's day is a read, not a sign.

### (f) The return's form — this doc's [design] over the landed primitives

The Comet's mechanics are entirely composed from the landed primitives — the
meteor's announced-arrival (`the-meteor.md` §2a), the orbit-dynamics
(`atmosphere-orbits-auroras.md` §2.2/§2.3), the year's calendar
(`the-window-year.md` §1), the tide's long pulse (`tide-of-the-attractor.md` §1),
the observatory's read (`the-observatory.md` §1/§2), the eclipse's scheduling
(`the-eclipse.md` §2a) — and the return's own form remains **this doc's [design],
probe-calibrated**: the comet's period (its length in years, itself a probe
riding the tide's long time, `tide-of-the-attractor.md` §5a), its visited line,
its trail's brightness, its return's date on the calendar. These are dials over
grounded landed quantities — the orbit's dynamics, the year's scales, the tide's
breath — never a new physics. The Comet is the meteor's cousin made returning; the
return's form is the one designed thing this doc adds, and it is a period, never a
message.

---

## 3. The load-bearing design — why the returning body is honest to design

### (a) The comet is the meteor's periodic cousin — same arrived body, made returning

The Meteor is the corpus's one-time arrival — the falling bright, a body that
arrives, announced, and is gone (`the-meteor.md` §1). **The Comet is that same
arrived body made returning — the meteor's periodic cousin.** Where the meteor
falls once, the comet orbits; where the meteor is a landing, the comet is a
passing; where the meteor's crater marks a fresh scar (`the-meteor.md` §2d), the
comet's return marks a repeated line. The kinship is load-bearing because it
grounds the comet in the meteor's landed honesty: the same announced arrival, the
same sky's own body, read the same way — only the *time* differs, the fall
become an orbit, the one-arrival become a return.

### (b) The return is honest to design because it completes the sky's time — the visitor on the long breath

The sky's time is already designed in layers: the dawn turns daily
(`the-dawn.md` §1), the year mounts the seasons (`the-window-year.md` §1), the
tide pulses on the long breath (`tide-of-the-attractor.md` §1), the eclipse
darkens rarely (`the-eclipse.md` §1). **The comet completes that time with the
period the sky's own orbit-dynamics can hold — the visitor's return mounted on
the same long breath the tide and the year share.** It is honest to design
because it is the sky's own orbit written at its longest arm: a body that returns
on a period the settlement's calendar can date, the way its other bodies are
orbits it can read. The return is not invented; it is the orbit-dynamics
(`atmosphere-orbits-auroras.md` §2.3) read at the window's long scale.

### (c) The spectacle it shows is never a gift — a passing, never a yield

Because the comet is a body that *passes*, it is the value of a passing and never
a yield. **Nothing a comet appears to give is real: it grants no wish, portends
no fortune, carries no order it bestows.** The field's indifference
(`the-dispute.md` §2) gives nothing away for the watching; `output ≤ φ⁻¹·input`
(`energy-harnessing.md` §6) holds through the comet exactly as through any body —
it condenses, it orbits, it passes, and the window's order is unchanged. **The
comet's value is not that it mints but that it *shows* — a spectacle the sky's
own body makes, read and dated, yielding nothing and meaning nothing beyond the
return itself.**

---

## 4. The load-bearing honesty

### Never a portent — the field cannot telegraph

The sharpest honesty the Comet owns is the one that answers the obvious
misreading: **a comet is never a portent.** The corpus's field cannot lie the way
a witness can — the same channels that adjudicate live-vs-residue adjudicate
whose claim is true, because the field is deterministic and legible
(`the-dispute.md` §2). **A deterministic field cannot telegraph a future it is
not itself — the comet's return is the orbit's read, never a sign, never a
warning, never a prophecy.** "The comet foretells the flood" is a false reading:
the field has no will to foretell, and the comet's passage is a body's orbit, its
only content the matter that made it. **A comet that 'spoke' — that portended, as
an omen-telegraph — would be a hidden sign the field's indifference sheds.**

### A deterministic returning body — same field state, same return, never a seeded "omen roll"

The Comet's return is the opposite of an omen **roll**: it is a deterministic fact
of the body's orbit. Same field state → same orbit → same return, every run,
never a seeded RNG deciding "this year the comet is auspicious / ill-fated." **The
orbit reads on the instruments** — the sky stack's own orbit-dynamics
(`atmosphere-orbits-auroras.md` §2.3), the observatory's page
(`the-observatory.md` §2), the year's calendar (`the-window-year.md` §1) — and
the return is a fact of that read, not a judgment of it. **There is no hidden
roll behind the passing bright** — a settlement that reads its sky knows the
return's date, its line, its passing, all deterministically, and nothing the
comet "means" is set by chance.

### It never mints — a body that passes, never a granted wish

The no-free-energy cap (`energy-harnessing.md` §6 — `output ≤ φ⁻¹·input`) held
hard:

> **A comet yields nothing — it is a body that passes, never a mint, never a
> granted wish.** The condensing, the orbit, the passing are all the field's own
> motion, and the window's order is unchanged by the visitor. **A "comet that
> granted wishes" or "a passing that bestowed order" is a false reading the field
> sheds** — the field is indifferent (`the-dispute.md` §2), `output ≤ φ⁻¹·input`
> holds through the return exactly as through any body, and nothing the bright
> shows is a gift to be taken.

### Its spectacle is the field's own — legible, accessible, never hidden-only

Per the instrument-family rule (`field-instruments.md` §2.1 — every instrument is
a consumer of the same publish, never a new channel) and the corpus's
never-hidden-only discipline:

> **The comet's period and its return are the published channels' own, never a
> hidden sign.** The orbit reads on the sky stack's own dynamics
> (`atmosphere-orbits-auroras.md` §2.2/§2.3), the return's date on the year's
> calendar (`the-window-year.md` §1), the passing bright on the observatory's
> read (`the-observatory.md` §2). **The passing bright is the field's own
> spectacle, never a hidden omen.** A settlement that never frames a "comet"
> surface loses nothing the underlying channels do not already show — the
> returning body is legible from the instruments, more laboriously, with or
> without the comet's frame. **A comet that hid its meaning or arrived as a
> secret sign would be a hidden portent the design's own honesty forbids**
> (§4).

---

## 5. The honest boundary and gates

### (a) The [design]/engine-real line

The line, drawn once and never blurred: **the meteor's one-time arrival, the sky
stack's orbits, the window-year's calendar, the tide's long pulse, the
observatory's reads, the dawn's turn, the eclipse's darkening, and the zenith's
crown are their source docs' landed pieces** — each a designed composition over
engine-real channels, exactly as their own docs draw it (`the-meteor.md` §5a,
`atmosphere-orbits-auroras.md` §5, `the-window-year.md` §5a, `tide-of-the-attractor.md`
§5, `the-observatory.md` §6a, `the-dawn.md` §5a, `the-eclipse.md` §5a,
`the-zenith.md` §4a). **The comet's composition — the return's form (§2f), the
passing-bright frame (§3), the non-portent (§4) — is this doc's [design] over
them.** No claim here pretends the engine writes "comet" or "return" records; the
engine condenses the sky's bodies and publishes the channels a return is read
through; a comet is the deliberate *return* composed out of the orbit-dynamics'
landed mechanics, exactly as a meteor is a deliberate *arrival* composed out of
the same. The comet adds no new physics, no new channel, no new record.

### (b) MEDIUM-LATE — gated on the sky's announced-momentum and the long-period orbit dynamics, with the framing Phase-1-legible

**MEDIUM-LATE.** The Comet gates on the sky's announced-momentum mechanics
(`the-meteor.md` §5b — MEDIUM-LATE, gated on the announced arrival, Phase-1.5+)
and on the **long-period orbit dynamics** (a body whose orbit leaves the near sky
and returns on a readable period gates on the open-boundary orbit arm at its long
scale — `atmosphere-orbits-auroras.md` §2.3, gated on the orbit dynamics and the
window-relative box, Phase-1.5+/Phase-2). Until those land, the *mechanical*
comet — a returning body whose period is a readable orbit — cannot resolve.
**MEDIUM-LATE, not LATER**, because it rides *landed* pieces — the meteor's
announced-arrival honesty, the orbit-dynamics' condensed bodies, the year's
calendar, the tide's long pulse, the observatory's reads — and adds no physics
and no channel.

**The Phase-1-legible framing, stated:** **the comet as a designed returning-body
read over the published sky channels — the visitor's period and its return as a
legible, dated, non-portentous read, before any mechanical returning orbit.**
Whether or not the long-period orbit is built, the corpus can hold now, as a
designed read over the sky stack's orbits and the year's calendar: **a body of
the sky's own lineage returns on a readable period — its return read on the
instruments, dated on the year, deterministic (§5c), non-minting (§5d), and
accessible (§5e) — and that return is never a portent (§4)**. Every one is a
designed statement statable now against the meteor's announced arrival, the
orbit-dynamics, the year's calendar, and the field's determinism. **The returning
body's legible period is the Phase-1 framing; the mechanical comet is
MEDIUM-LATE, gated as above.**

### (c) Determinism — a hard gate

Same field state → same return, same orbit, same passing, every run. **A hard
gate, never a seeded "omen roll."** The comet adds no randomness: whether the
body returns and when is a deterministic fact of its orbit, exactly as the meteor
falls deterministically (`the-meteor.md` §5c) and the field's judgment is
deterministic (`the-dispute.md` §2). **There is no seeded-RNG judgment about a
comet's auspice, its fortune, or its meaning** — a run of the field gives the
same body, the same return, the same bright, every time, and nothing the comet
"means" is set by chance. Same field state, same orbit → the same return, every
run.

### (d) The no-free-energy cap — a comet yields nothing, never a mint

`energy-harnessing.md` §6 (`output ≤ φ⁻¹·input`) inherited unchanged as the
return's hard bound:

> **A comet yields nothing — no mint, no granted wish, no bestowed order.** The
> body condenses, orbits, and passes by the field's own motion, and the window's
> order is unchanged. **`output ≤ φ⁻¹·input` holds through the return exactly as
> through any body** — the passing bright is a spectacle, never a yield. **A
> "comet that granted wishes" or "a passing that bestowed fortune" is a false
> reading the field's indifference sheds** (`the-dispute.md` §2). The comet's
> only gift is its return itself, and a return is not a reward.

### (e) Accessibility — the comet's period and its return readable from the instruments, never hidden-only

Per the instrument-family rule (`field-instruments.md` §2.1 — every instrument is
a consumer of the same publish, never a new channel) and the corpus's
never-hidden-only discipline:

> **The comet's period and its return are readable from the instruments — never
> hidden-only.** The orbit reads on the sky stack's dynamics
> (`atmosphere-orbits-auroras.md` §2.2/§2.3), the return's date on the year's
> calendar (`the-window-year.md` §1), the passing bright on the observatory's
> page (`the-observatory.md` §2). **The passing bright is the field's own
> spectacle, never a hidden omen.** A settlement that never frames a "comet"
> surface loses nothing the underlying channels do not already show. **A comet
> that hid its meaning or arrived as a secret sign would be a hidden portent the
> design's own honesty forbids** (§4). The Comet is *returning*, never *hidden*.

---

## 6. Feasibility verdict

**MEDIUM-LATE — gated on the sky's announced-momentum and the long-period orbit
dynamics, with the framing Phase-1-legible. State both.**

**The full Comet — a mechanical returning body whose long-period orbit is a
readable read — is MEDIUM-LATE.** It gates on the sky's announced-momentum
mechanics (`the-meteor.md` §5b — MEDIUM-LATE, Phase-1.5+) and on the long-period
orbit dynamics (a body that leaves the near sky and returns gates on the
open-boundary orbit arm at its long scale — `atmosphere-orbits-auroras.md` §2.3,
Phase-1.5+/Phase-2, the window-relative box and the seam). It adds no physics and
no new channel: a comet is a *designed return* of the sky's own condensed
lineage, read through the publish, bounded by the §6 cap, and composed over the
meteor's announced arrival and the orbit-dynamics.

**The Phase-1-legible framing — the comet as a designed returning-body read over
the published sky channels — is designable and demoable now.** Whether or not the
long-period orbit is built, the corpus can hold the load-bearing truths now,
against the meteor + the sky stack + the year + the tide + the observatory + the
dispute: **a comet is the meteor's periodic cousin — the same arrived body, made
returning (§1/§3a)**; **it is never a portent — the field cannot telegraph, so
the returning bright carries no hidden sign (§4)**; **it is a deterministic
returning body — same field state → same return, its orbit read on the
instruments, never a seeded 'omen roll' (§2b/§4/§5c)**; **it never mints — a
body that passes, never a granted wish, `output ≤ φ⁻¹·input` held hard
(§4/§5d)**; and **its spectacle is the field's own — legible, accessible, never
hidden-only (§5e)**. Every one is a designed statement statable now against the
meteor's announced arrival, the sky stack's orbits, the year's calendar, and the
field's determinism. **The returning body's legible period is the Phase-1
framing; the mechanical comet is MEDIUM-LATE, gated as above.**

**What it fills.** The corpus designs the sky's every arrival and its time: the
meteor that falls once (`the-meteor.md` §1), the orbit that holds
(`atmosphere-orbits-auroras.md` §2.2), the dawn that turns, the eclipse that
darkens, the year that mounts — but **never the body that returns on a readable
period**. Every sky-event is an arrival, a hold, a turn, or a darkening; none is a
*visitor that comes back*. **The Comet is that gap closed: the passing bright, the
sky's visitor, the returning body, the meteor's periodic cousin — a body that
returns on the long breath, never a portent, never a mint, its period a read and
its spectacle the field's own.**

**Binding risks, in order:**

1. **The "portent" slide (§4).** The sharpest failure is the comet reading as an
   omen — "the comet foretells the famine." The guard holds: the field cannot
   telegraph (`the-dispute.md` §2 — a deterministic field cannot foretell a
   future it is not itself), so the returning bright carries no sign. **A comet
   that portends is a hidden sign the field's indifference sheds.**
2. **The "omen roll" blur (§4/§5c).** The comet's return must never read as a
   seeded judgment — "this return is auspicious, that return is ill-fated." The
   determinism gate holds: same field state → same return, every time, with no
   RNG deciding the pass's "meaning." The return is a fact of the orbit, never a
   roll.
3. **The "granted wish" mint (§4/§5d).** A comet must never read as a bestower —
   "the passing bright brought us fortune." The no-free-energy cap holds: the
   field yields nothing (`the-dispute.md` §2 — it is indifferent),
   `output ≤ φ⁻¹·input` through the return, and a "comet that granted wishes" is
   a false reading the field sheds.
4. **The "meteor blurred" slide (§3a).** The Comet must not blur into a Meteor
   with a tail. The line holds: the meteor *falls* once and its crater marks a
   scar (`the-meteor.md` §2d); the comet *returns* on a readable period, its
   trail re-shown, its line re-visited. A "comet" that lands once has missed the
   return that defines it.
5. **The [design] line (§5a).** Keeping the return's form (its period, its line,
   its calendar-date) strictly [design] over the landed meteor arrival, orbit
   dynamics, year calendar, and tide pulse — so the corpus never reads a Comet as
   an engine term, a prophecy, or a new-physics claim.

**None contradicts the async, dual-world, or regime-collapse architecture** — the
Comet adds no physics and no channel: it is the *designed return* of the sky's
own condensed lineage, read through the publish, bounded by the §6 cap, honest
through the meteor's announced arrival, the orbit-dynamics, and the field's
determinism, and dated through the year's calendar and the tide's long pulse.

> **The honest statement that makes this doc load-bearing: the corpus designs the
> sky's every arrival and its time — the meteor that falls, the orbit that holds,
> the dawn that turns, the eclipse that darkens, the year that mounts — but never
> the body that returns on a readable period.** The Comet is that design: the
> passing bright, the sky's visitor, the returning body, the meteor's periodic
> cousin — the same arrived body, made returning, its orbit read on the
> instruments the way the sky's other bodies are read. It is never a portent: the
> field cannot lie, cannot telegraph, so the returning bright carries no hidden
> sign. It is a deterministic returning body — same field state → same return,
> never a seeded "omen roll." And it never mints: it yields nothing, a body that
> passes, a "comet that granted wishes" a false reading the field sheds,
> `output ≤ φ⁻¹·input` held hard. Its spectacle is the field's own — legible,
> accessible, never hidden-only. **The returning body's legible period is the
> Phase-1 framing; the mechanical comet is MEDIUM-LATE, gated on the sky's
> announced-momentum and the long-period orbit dynamics.** *The meteor falls and
> is gone; the comet returns — the same visited line, back on the long breath, a
> spectacle that says nothing and takes nothing, and is no less bright for it.*

---

## Open questions

1. **The comet's period (§2f/§3b).** How long is a designed return — one window-year,
   several, a full tide? The honest default is a period long enough to be a
   *visitor* (rare, not daily) and short enough to be *read* (a settlement
   catches several returns across its life), probe-calibrated against the tide's
   long pulse (`tide-of-the-attractor.md` §5a) and the year's scales
   (`the-window-year.md` §2). **[design]/[probe]**
2. **The return's relation to the meteor (§2a/§3a).** Is the comet a distinct
   body *class* (a long-orbit visitor, the meteor's recurring cousin) or a *re-used*
   meteor-line (the same condensed lineage whose one arrival the crater marked,
   re-viewed on a later pass)? The honest kinship leans the former — the meteor
   lands and scars, the comet passes and returns — with the shared announced-read
   ground. **[design]/[probe]**
3. **The trail's legibility (§2d).** Is the comet's trail a *bright display* (the
   body's `(1−q)` and its thin-interaction shown as a glow the whole window
   reads, the way the aurora glows) or a *thin measurable* (the trail's density
   read on the instruments, its brightness a bounded field-fact)? The corpus's
   accessibility pulls toward both-legible; the exact display is [design]. **[design]**
4. **The portent's honest neighbor.** The corpus designs omens honestly: the
   reading-ahead reads the field's *momentum* (`the-reading-ahead.md` §1), never
   a hidden sign. Does the comet's period risk being misread as a momentum-omen
   (a settlement that "reads" the return as a forecast), and does the honest
   design foreclose that by making the return's only content the orbit itself?
   Inherits the reading-ahead's non-prophecy discipline. **[design]**
5. **The calendar's date (§2e).** Is the comet's return a *named* observance on
   the window-year (the visiting bright's day, dated like the eclipse's named
   dark, `the-eclipse.md` §2a) or an *undated* passing (a read that comes, shown
   on the page, without a festival)? The honest default leans the named-but-empty
   date — dated like the eclipse, meaningful only as the spectacle it is,
   never a portent. **[design]**
6. **The comet and the seam (§2b/§5b).** A comet whose orbit leaves the window's
   near sky crosses the seam to the between's thin (`the-zenith.md` §2b, the
   window-relative box). Is its return an *internal* orbit (the body stays within
   the window's box, the period is just long) or does it *leave and return*
   through the boundary (the passing bright that departs to the between and comes
   back)? The honest default leans the internal long orbit first (the visitor
   never leaves the field it is made of), with the seam-crossing a LATER
   extension. **[design]**

---

## Cross-references

- [`the-meteor.md`](./the-meteor.md) — **THE falling bright — the Comet's
  cousin.** §1 the rarest sky-event, a body that arrives; §2(a) the herald's
  announced read; §2(b) the bell's toll; §2(d) the crater (a fresh scar);
  §5b MEDIUM-LATE; §5c determinism (same sky state → same meteor). **The meteor
  falls once; the comet returns — the same arrived body, made periodic** (§1/§3a
  this doc).
- [`atmosphere-orbits-auroras.md`](./atmosphere-orbits-auroras.md) — **THE sky
  stack — the Comet's home.** §2 orbits as open-boundary field phenomenology;
  §2.1 why orbits exist at all; §2.2 bodies = the merge lineage condensed; §2.3
  orbit dynamics (softened, damped); §5 gates. **The comet is one more body of
  the sky's own orbit — a condensed lineage on a long return, read like the
  others** (§2a/§2b this doc).
- [`the-window-year.md`](./the-window-year.md) — **THE long-rhythm calendar —
  the Comet's period.** §1 the composed long-rhythm calendar; §2 the three
  scales; §3 the observances. **The comet's return is a date on the window's long
  calendar — the visiting bright named in the year's rhythm** (§2e this doc).
- [`tide-of-the-attractor.md`](./tide-of-the-attractor.md) — **THE long pulse —
  the Comet's tempo-companion.** §1 the tide as the large-scale regional q
  oscillation; §2 the regime table; §5a the probe. **The comet rides the same
  long time as the tide — the sky's visitor on the window's own breath** (§2f/§3b
  this doc).
- [`the-observatory.md`](./the-observatory.md) — **THE reading room — the
  Comet's read.** §1 where a settlement reads together; §2 the room's body (each
  surface a consumer of an already-published channel); §2.2 the whole window as
  one body. **The comet's orbit and its return are read in the observatory, the
  sky's line made a page** (§2c/§3 this doc).
- [`the-dawn.md`](./the-dawn.md) — **THE sky's turn — the Comet's even-keel
  cousin.** §1 the field's re-birth each day, the transition read. **Where the
  dawn turns the sky daily, the comet turns it rarely — both the field's own
  motion, neither a message** (§1/§3 this doc).
- [`the-eclipse.md`](./the-eclipse.md) — **THE darkening — the Comet's
  rarity-kin.** §1 the window's darkening, a rare scheduled moment when the read
  goes dark; §2(a) the name on the calendar. **The eclipse and the comet are both
  rare scheduled sky-events — the one a darkening, the other a passing, neither a
  portent** (§1/§2e this doc).
- [`the-zenith.md`](./the-zenith.md) — **THE sky's crown — the Comet's height.**
  §1 the vertical's ceiling; §2(b) the boundary, the seam between the window's
  air and the between's thin. **A comet crosses the sky's crown on its return —
  the passing bright at the zenith's edge** (§1/§2b this doc).
- [`the-reading-ahead.md`](./the-reading-ahead.md) — **THE non-prophecy — the
  Comet's honest neighbor.** §1 the oracle reads the field's momentum, never a
  hidden sign. **The comet's period is a read, never a forecast — the returning
  bright carries no message the field did not already show** (§4/open-Q4 this
  doc).
- [`field-instruments.md`](./field-instruments.md) — **THE instrument family
  rule.** §1.4 the Weatherglass's single-sample economics (the return's reads'
  cost profile); **§2.1 the family rule — every instrument is a consumer of the
  same publish, never a new channel. The comet's reads are consumers of the
  publish** (§2c/§5e this doc).
- [`energy-harnessing.md`](./energy-harnessing.md) — **THE cap.** §6 the
  no-free-energy cap — `output ≤ φ⁻¹·input` — a comet yields nothing, a passing
  body, never a mint, never a granted wish (§4/§5d this doc).
- [`the-dispute.md`](./the-dispute.md) — **THE field-true judgment — the Comet's
  non-message.** §2 the field cannot lie the way a witness can; the field is
  deterministic and legible. **A comet is not a portent — the field cannot
  telegraph, so the returning bright carries no hidden sign** (§4 this doc).
- [`corpus-reconciliation.md`](./corpus-reconciliation.md) — the canonical numbers
  (window = 192³/64³/12³ box, dt = 0.05, τ_c = 0.5, φ⁻² ≈ 0.382, ξ = φ⁶ ≈ 17.94,
  ω₀² = 20.0, π/ρ clamp 0.72, attractor q ≈ 0.947, q ~ 1e-3…1e-1 noise, ≈ 6 MiB
  publish = q 1 + pot 1 + ∇(g·Φ) 3 + ρ 1, ≈ 1–6 ms/tick, ≈ 2,000 cap, 8,192 sites,
  ≈ 40 ns/entity, `output ≤ φ⁻¹·input`); cited, not re-derived.
